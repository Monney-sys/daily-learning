# 2026-07-31 中间件解析漏洞

> 解析漏洞的核心：**文件后缀不是可执行脚本后缀，但中间件按照脚本来执行它。** 本篇覆盖 Apache / Nginx / IIS。

---

## 一、Apache 解析漏洞

### 1.1 多后缀解析（Apache 1.x / 2.0.x）

Apache 的 `mod_mime` 从右向左识别扩展名，遇到不认识的扩展名继续向左匹配：

```
文件名：shell.php.xxx
  → .xxx 不认识 → 继续向左
  → .php  认识！→ 按 PHP 执行
```

**利用**：上传 `shell.php.xxx`（`.xxx` 为任意不在黑名单的后缀），Apache 仍按 PHP 执行。

**防御**：Apache 2.2+ 默认不开启此行为，且建议用 SetHandler 精确控制。

### 1.2 .htaccess 文件利用

```apache
# 让 .jpg 文件当 PHP 执行
AddType application/x-httpd-php .jpg

# 让特定文件名当 PHP 执行
<FilesMatch "shell">
    SetHandler application/x-httpd-php
</FilesMatch>
```

前提条件：
- `AllowOverride All`（在 Apache 配置中开启）
- 上传黑名单没有封 `.htaccess`

### 1.3 CVE-2017-15715 — Apache HTTPD 换行解析

- **影响**：Apache HTTPD 2.4.0 ~ 2.4.29
- **原理**：Apache 匹配文件扩展名时，正则 `$` 锚点没有正确处理换行符 `\x0a`，导致 `shell.php\x0a.jpg` 的扩展名被识别为 `.php`
- **利用**：Burp 截获上传请求 → 文件名改为 `shell.php\x0a.jpg`（Hex 模式在 `.php` 和 `.jpg` 之间插入 `0a`）→ 上传 → 访问时 Apache 按 PHP 执行
- **复现**：
  ```bash
  docker pull httpd:2.4.28
  # 配置 PHP-FPM + FilesMatch，上传 shell.php\x0a.jpg
  ```
- **修复**：升级 Apache HTTPD ≥ 2.4.30

---

## 二、Nginx 解析漏洞

### 2.1 cgi.fix_pathinfo 漏洞（最常见）

- **条件**：Nginx + FastCGI + PHP，`cgi.fix_pathinfo=1`（PHP 默认开启）
- **原理**：
  ```
  访问 URL：http://target/upload/shell.jpg/1.php
  ├── shell.jpg  → 真实存在的文件（图片马）
  └── /1.php    → 被 PHP 当作 PATH_INFO，Nginx 看到 .php 就交给 FastCGI
  ```
  PHP 收到后把 `shell.jpg` 当主脚本执行，`/1.php` 为路径信息。

- **利用**：
  1. 上传图片马 `shell.jpg`（内容 `<?php phpinfo(); ?>`）
  2. 访问 `http://target/upload/shell.jpg/1.php`
  3. Nginx 因为 URL 以 `.php` 结尾，转发给 PHP-FPM
  4. PHP 执行 `shell.jpg` 的内容

- **复现**：
  ```bash
  # Docker 搭建 Nginx + PHP-FPM
  # php.ini 中 cgi.fix_pathinfo=1（默认就是 1）
  # 上传图片马 shell.jpg
  curl http://target/upload/shell.jpg/1.php
  ```

- **防御**：`php.ini` 设置 `cgi.fix_pathinfo=0`

### 2.2 CVE-2013-4547 — Nginx 空字节解析

- **影响**：Nginx 0.8.41 ~ 1.4.3 / 1.5.0 ~ 1.5.7
- **原理**：Nginx 解析 URL 时，空字节 `\x00` 导致后缀检测被截断
- **利用**：
  1. 上传图片马 `shell.jpg`
  2. 访问 `http://target/shell.jpg\x00.php`
  3. Nginx 把 `\x00.php` 当作后缀部分处理后截断，最终执行 `shell.jpg` 作为 PHP

- **复现**：Docker 搭建受影响版本的 Nginx，Burp 在 URL 中插入 `%00.php`

### 2.3 Nginx 配置不当 — 目录解析

错误配置示例：
```nginx
location ~ \.php$ {
    fastcgi_pass 127.0.0.1:9000;
}
location /upload/ {
    # 未对 upload 目录禁止 PHP 执行
    # 攻击者直接上传 .php 访问即可
}
```

**防御**：上传目录单独配置：
```nginx
location /upload/ {
    location ~ \.php$ { deny all; }
}
```

---

## 三、IIS 解析漏洞

### 3.1 IIS 6.0 文件夹解析

- **原理**：IIS 6.0 会执行以 `.asp` / `.asa` 命名的**文件夹**中的所有文件
- **利用方式 1**：
  1. 创建文件夹 `1.asp/`（可通过 WebDAV 或上传接口间接创建）
  2. 在文件夹内上传 `1.jpg`（内容是 ASP 木马）
  3. 访问 `http://target/1.asp/1.jpg` → 按 ASP 执行

- **利用方式 2**（分号截断）：
  1. 上传 `1.asp;.jpg`
  2. IIS 6.0 截断分号后的内容
  3. 文件存为 `1.asp`，按 ASP 执行

### 3.2 IIS 7.0 / 7.5 FastCGI 解析

- **原理**：同 Nginx `cgi.fix_pathinfo`，`1.jpg/1.php` 被 PHP 解析
- **利用**：上传图片马，访问 `http://target/upload/1.jpg/1.php`

---

## 四、总结对比

| 中间件 | 漏洞类型 | 利用文件名 | 前提 |
|--------|----------|-----------|------|
| Apache | 多后缀 | `shell.php.xxx` | mod_mime 从右向左匹配 |
| Apache | .htaccess | `.htaccess` | AllowOverride All |
| Apache | CVE-2017-15715 | `shell.php\x0a.jpg` | Apache < 2.4.30 |
| Nginx | fix_pathinfo | `shell.jpg/1.php` | cgi.fix_pathinfo=1 |
| Nginx | CVE-2013-4547 | `shell.jpg\x00.php` | Nginx < 1.4.4 / 1.5.8 |
| IIS 6.0 | 文件夹解析 | `1.asp/1.jpg` | IIS 6.0 |
| IIS 6.0 | 分号截断 | `1.asp;.jpg` | IIS 6.0 |
| IIS 7.x | FastCGI | `1.jpg/1.php` | fix_pathinfo=1 |

> 关联笔记：[文件上传漏洞体系总览](./2026-07-31-file-upload-vulnerability.md) | [编辑器与 CMS 上传漏洞](./2026-07-31-file-upload-editor-cms.md) | [近年文件上传 CVE](./2026-07-31-file-upload-recent-cve.md)

> ⚠️ 声明：以上技术仅用于授权靶场和渗透测试授权目标。
