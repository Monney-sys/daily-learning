# 2026-08-18(续) 代码审计②：PHP 框架 MVC 项目代码审计

> 状态：🧪 理论未验证（Xdebug 断点环境已配好 ✅，demo 项目已跑通，还没拿真实框架项目实操）
> 代码审计板块第二课：**知道源码用了框架**的情况下怎么审。新技能点：**断点测试看函数调用走向**（Xdebug + PHPStorm）。

---

## 一、是什么（我的理解）

### 1.1 一句话

框架审计 = 先画**框架的审计地图**（入口→路由→控制器→模型），再找**绕过框架自带防护**的写法 + **框架版本本身的已知漏洞**。和无框架审计最大的区别：**入口从"每个 .php"变成了"唯一入口 index.php"**。

### 1.2 无框架 vs 框架（对比表，复习用）

| 维度 | 无框架（昨天） | 框架 MVC（今天） |
|------|--------------|-----------------|
| 入口 | 每个 .php 都是入口 | 唯一入口 index.php → 前端控制器 |
| 审计地图 | 打开目录 = 页面地图 | 路由文件 → 控制器列表 → 模型层 |
| 信任边界 | 所有输入裸奔 | 框架自带过滤（TP `I()`/Laravel ORM），**漏洞 = 绕过防护的写法** |
| 危险函数 | 直接搜就行 | 命中点要先判断：框架防护覆盖了没？ |
| 版本因素 | 影响小 | **影响大**：老框架版本 = 已知 RCE（TP 3.x/5.x 经典链） |
| 新工具 | Seay / semgrep / grep | + **Xdebug 断点**看调用走向 + 框架 debug/日志 |

### 1.3 类比（快递盒系列延续）

```
无框架 = 没有安检门的小仓库，漏洞到处都是
框架   = 有安检门的正规仓库：
          安检门本身有漏洞 → 框架版本 CVE（先查版本！）
          员工不走安检门   → 绕过框架防护的原生写法（审计重点）
          监控摄像头       → 框架 debug 模式 / runtime 日志（辅助确认）
断点测试 = 跟着监控回放每一件货的流转路线（调用栈 = 谁经手了谁）
```

---

## 二、框架审计地图（从哪进）

按这个顺序看，别一头扎进代码：

1. **入口 `index.php`**：加载了什么（自动加载、路由注册、全局配置）——快速确认框架类型和版本
2. **路由文件**（TP `route/` 或 `config/route.php`；Laravel `routes/web.php`）：URL → 控制器方法的映射，**攻击面清单**（相当于 Java 的 `@GetMapping` 列表）
3. **控制器列表**：每个方法 = 一个攻击面；重点看**收参数的写法**（见下方高危清单）
4. **模型层**：SQL 集中地，查**原生拼接**
5. **composer.json / 版本号**：查已知 CVE（thinkphp 3.x/5.x RCE、Laravel 反序列化等）——框架审计**先对版本**，这步经常直接出结果
6. **config 里的调试开关**：`APP_DEBUG` / `debug=true` 开着 = 报错页泄露路径/配置/堆栈

---

## 三、怎么找漏洞点（工具 + 断点）

### 3.1 高危写法清单（绕过框架防护 = 洞）

**ThinkPHP 系（课程大概率是 TP）：**
| 写法 | 问题 |
|------|------|
| `Db::query("SELECT ... WHERE id=" . $id)` / `M()->query()` | 原生拼接，绕过 ORM 预编译 |
| `request()->get()` / `param()` 裸收参数 | 绕过 `input()` 的默认过滤 |
| `where('id', $id)` 传数组/表达式可控 | 条件注入（TP 5.1 后收紧了，老版本中招） |
| `unserialize()` 处理 Cookie/参数 | TP5 反序列化链（经典 RCE） |
| 模板 `{volist}` / 动态函数调用 `$func($param)` | 模板注入/命令执行面 |

**Laravel 系（通用参考）：**
| 写法 | 问题 |
|------|------|
| `DB::raw()` / `whereRaw()` / `orderByRaw()` 拼用户输入 | 绕过 Eloquent 预编译 |
| `request()` 全局函数 vs `$request->validate()` | 少了校验层 |
| 模型 `$guarded=[]` / 不写 `$fillable` | Mass Assignment 越权写库 |

**通用（框架无关）：**
- `APP_DEBUG=true` 上线没关、日志文件可访问（`runtime/log`、`storage/logs`）
- composer 依赖版本老旧（**先查 CVE**）
- 文件上传没走框架的校验组件，自己手写校验

### 3.2 断点测试（新技能点：看函数调用走向）

**原理**：Xdebug 让 PHP 进程在断点处暂停，IDE 通过 DBGp 协议（端口 9003）接管 —— 单步执行、看变量、看**调用栈**（谁调了谁、参数从哪来）。等于把"数据流追踪"变成可视化回放。

**我的环境（已配好，2026-08-18）**：
- XAMPP `D:\xmp`（PHP 8.2.12 ZTS）+ **Xdebug 3.4.0**（`D:\xmp\php\ext\php_xdebug.dll`，php.ini 已加配置，**备份在 `php.ini.bak-20260818`**）
- IDE：PHPStorm `D:\PhpStorm 2025.3.3`（项目 `D:\php\`）
- 验证过：9003 端口 DBGp 握手收到 init 包 ✅；demo 项目跑通 ✅

**PHPStorm 三步上手**：
1. 打开项目 → 行号右侧点一下设断点（红点）
2. 右上角 **Debug** 按钮运行（选 CLI Interpreter `D:\xmp\php\php.exe`）
3. 第一次弹 "Incoming connection from Xdebug" → Accept，然后就能：`F7 步入 / F8 跳过 / Shift+F8 步出`，看底部 **Variables 变量面板 + Frames 调用栈面板**

**断点下在哪**（审计场景）：
- 入口 `index.php`（看整体流向）
- 路由分发处（看 URL 怎么映射到控制器方法）
- **危险函数调用行**（看参数一路怎么来的——这是审计核心）
- 过滤函数前后各一个（确认过滤是否存在、是否可绕）

**触发方式**：CLI `php -d xdebug.start_with_request=yes 脚本.php` / `XDEBUG_SESSION=1 php 脚本.php`；浏览器用 Xdebug helper 扩展或 `?XDEBUG_SESSION_START=1`

**练手骨架**：`D:\php\demo-mvc\`（index→router→controller→model 迷你 MVC，vuln() 拼接 SQL 对照 safe() 预处理，README 里标了 3 个关键断点位置）

### 3.3 辅助手段

- **框架 debug 页**：报错页直接泄露框架版本/路径/配置 → 版本确认 + 信息收集
- **runtime/log 日志**：TP 的 `runtime/log`、Laravel `storage/logs` —— 看真实执行过的 SQL/请求
- **Seay/semgrep 沿用**：命中点语义变了——扫到 `query()` 要结合框架判断：是 ORM 的（安全）还是原生拼接（洞）

---

## 四、怎么防御（审计的镜像）

- 框架层防护用全：ORM 预编译、`input()` 过滤、模板转义、CSRF —— **禁止绕过防护的原生写法**
- 收参数统一走框架入口（`input()`/`validate()`），不裸用 `request()->get()`
- **框架/依赖勤升级**（TP 老版本 RCE 都是公开的）
- 上线关 debug、日志不暴露在 web 目录、生产环境禁用反序列化不可信数据

---

## 五、总结 / 疑问

- 核心收获：框架审计 = **版本 CVE 排查 + 绕过框架防护的写法 + 断点可视化数据流**；工具（Seay/semgrep）继续用，但判断标准变了
- 疑问（已登记 OPEN-QUESTIONS）：
  1. TP 的 `input()`/`I()` 默认过滤规则到底是什么？（哪些注入类型默认被拦、哪些拦不住）——待查框架源码
  2. PHPStorm 断点 F7 步入会进框架内部函数吗？（要不要给框架源码也装断点？）
  3. 实际审计里框架日志（runtime/log）和 Xdebug 断点哪个用得多？
  4. 已知 TP5 反序列化 RCE 链，审计时怎么快速判断目标版本能不能用？（版本→链的对应关系表？）

---

## 工具部署（续）：Xdebug 配置记录

```
Xdebug 3.4.0 for PHP 8.2 (ZTS x64) → D:\xmp\php\ext\php_xdebug.dll
php.ini 追加:
  zend_extension = php_xdebug.dll
  xdebug.mode = debug
  xdebug.start_with_request = trigger   (IDE 触发, 不拖慢日常请求)
  xdebug.client_host = 127.0.0.1 / client_port = 9003
备份: php.ini.bak-20260818
验证: php -m 有 xdebug ✅ | DBGp 握手 9003 收到 init 包 ✅
踩坑: xdebug.org 直连下载页空响应, 直接按文件名规律探测:
      https://xdebug.org/files/php_xdebug-<版本>-8.2-vs16-x86_64.dll (3.3.2/3.4.0 可用)
```

## 方法论沉淀

```
框架审计 = 版本CVE排查(先) + 找绕过框架防护的原生写法 + 断点可视化数据流
断点测试 = 危险函数行设断点 → 看调用栈回放参数来源 → 确认过滤
```

## 待实践

- [ ] PHPStorm 打开 `D:\php\demo-mvc`，按 README 三个断点走一遍 F7/F8，亲眼看到 `$_GET['id']` 走到 `$sql` 拼接
- [ ] 下个真实 TP5 老项目源码，用 Seay/semgrep 扫 + 断点验证一个 SQL 拼接点
- [ ] 查 TP 的 `input()` 过滤规则源码，把疑问 1 解决掉

## 关联笔记

- [2026-08-18-PHP无框架代码审计入门.md](./2026-08-18-PHP无框架代码审计入门.md) — 昨天/前课：无框架审计（审计两主线、危险函数速查表）
- [2026-08-12-Java安全代码审计.md](./2026-08-12-Java安全代码审计.md) — 框架注解入口（`@GetMapping`）那套，和路由文件同理
- [2026-07-22-中间件安全与Web源码分析.md](./2026-07-22-中间件安全与Web源码分析.md)

> ⚠️ 声明：仅限授权环境（靶场/CTF/授权渗透）中测试，所有 Payload 只用于验证漏洞存在。
