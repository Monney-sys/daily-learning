# 2026-08-08 越权漏洞详解

> 水平越权 + 垂直越权 — 利用业务逻辑缺失，而不是攻击输入

## 知识框架速览

> 🧠 **[打开交互式思维导图](https://monney-sys.github.io/daily-learning/mindmaps/%E8%B6%8A%E6%9D%83%E6%BC%8F%E6%B4%9E.html)**（滚轮缩放 · 拖拽平移 · 点击折叠分支，新标签页打开）

下方正文按「产生原因 → 原理 → 水平越权 → 垂直越权 → 检测 → 绕过 → 防御 → 组合利用」展开详述。

---

## 一、产生原因

### 1.1 为什么越权漏洞如此普遍？

越权漏洞和其他漏洞有一个根本区别：**它不是代码写错了，而是逻辑没写全。**

```python
# 这段代码没有任何语法错误，运行完全正常——
# 但它就是越权漏洞：
@app.route('/api/order/<order_id>')
@login_required                    # ← 只做了登录校验
def get_order(order_id):
    order = Order.query.get(order_id)  # ← 根据用户传入的 ID 直接查
    return jsonify(order.to_dict())    # ← 没判断 order.user_id == current_user.id
```

SQL 注入你会看到数据库报错，XSS 你会看到弹窗——越权什么都不会发生，接口正常返回 200，数据正常显示，只是**数据不是你的**。

### 1.2 五大根因

| 根因 | 典型代码 | 问题出在哪 |
|------|---------|-----------|
| **只校验登录，不校验权限** | `@login_required` + 无角色判断 | 登录了不等于有权限做所有事 |
| **信任前端传参** | `userId = request.form['userId']` | 攻击者能改 userId，后端不应信任 |
| **前端隐藏代替后端校验** | 普通用户页面不显示管理按钮 | 接口 URL 仍然可以直接访问 |
| **自增 ID 可预测** | 订单号 1001 → 1002 → 1003 | 改个数字就能遍历所有数据 |
| **开发者的惯性思维** | "普通用户怎么会知道管理接口的 URL？" | 攻击者一定会枚举 |

### 1.3 一句话记住

> **登录 ≠ 授权。登录验证了"你是谁"，授权验证了"你能做什么"。越权漏洞发生的根本原因就是只做了前半句。**

---

## 二、原理

### 2.1 两张图看懂

```
水平越权（Horizontal）：
┌──────────┐     ┌──────────┐     ┌──────────┐
│  用户 A   │ ──→ │  用户 B   │ ──→ │  用户 C   │
│ 普通用户  │     │ 普通用户  │     │ 普通用户  │
└──────────┘     └──────────┘     └──────────┘
      ↑ 权限等级相同，但数据应该互不可见
      ↑ 越权：A 访问了 B 的订单 / 个人信息

垂直越权（Vertical）：
            ┌──────────────┐
            │   管理员      │  ← 你没权限访问
            └──────┬───────┘
            ┌──────┴───────┐
            │   普通用户    │  ← 你在这
            └──────────────┘
      ↑ 越权：从下面往上面跳，执行管理员操作
```

### 2.2 水平越权 vs 垂直越权

| | 水平越权（IDOR） | 垂直越权 |
|---|---|---|
| **本质** | 绕过"数据归属"校验 | 绕过"角色权限"校验 |
| **攻击者身份** | 已登录的普通用户 | 已登录的普通用户（或未登录） |
| **攻击方式** | 篡改资源 ID（订单号 / 用户ID / 手机号） | 直接访问高权限接口 / URL |
| **关键词** | 同级别 → 横向跨越 | 低权限 → 向上跨越 |
| **典型结果** | A 看到 B 的数据 | 普通用户干管理员的事 |

### 2.3 与 SQL 注入 / XSS 的本质区别

```
SQL 注入 / XSS → 恶意输入 → 触发异常行为 → 往往有报错或异常输出
越权漏洞      → 合法请求 → 正常执行        → 返回 200，数据正常显示
```

> 越权是一种**逻辑漏洞**。代码语法正确、逻辑通顺、接口正常工作——但业务规则上不允许的操作被执行了。

---

## 三、水平越权（IDOR — Insecure Direct Object Reference）

### 3.1 核心攻击模式

**修改请求中的"资源标识符"，访问不属于自己的数据。**

```bash
# 场景 1：查看订单
# 你是用户 A（userId=10），登录后查看自己的订单：
GET /order/detail?id=1001    # 返回订单 1001（属于用户A）✅

# 把 id 改成别人的订单号：
GET /order/detail?id=1002    # 返回订单 1002（属于用户B）💥
# 接口返回 200，数据正常显示——但不是你的订单

# 场景 2：修改密码
# 正常流程：用户修改自己的密码
POST /user/updatePassword
Cookie: session=abc123
Body: userId=10&newPassword=123456     # userId 来自前端，后端直接用了

# 攻击者篡改 userId：
Body: userId=11&newPassword=hacked     # 把用户 11 的密码改了 💥
# 后端应该从 session 中取 userId，不应信任前端传来的值
```

### 3.2 可被篡改的参数类型

```bash
# 1. 数字 ID（最常见）
?orderId=1001          → 1002, 1003, 1004...
?userId=10             → 1, 2, 3...（管理员通常是低 ID）

# 2. 用户名
?username=zhangsan     → lisi, wangwu, admin

# 3. 手机号 / 邮箱
?phone=13800138000     → 13800138001, 13800138002
?email=user@test.com   → admin@test.com

# 4. UUID（也不绝对安全）
?id=550e8400-e29b-...  → 可能在其他接口响应中泄露，或被爬取收集

# 5. 文件名
?file=user_10_report.pdf  → user_11_report.pdf
```

### 3.3 典型漏洞场景

| 场景 | 漏洞点 | 危害 |
|------|--------|------|
| **订单详情** | 修改 orderId | 看到别人的收货地址、手机号、购买记录 |
| **个人信息** | 修改 userId / profileId | 看到别人的身份证、银行卡号 |
| **修改密码** | 篡改请求中的 userId | 重置任意用户的密码 |
| **文件下载** | 修改文件名中的用户标识 | 下载别人的合同、证件照 |
| **聊天记录** | 修改 conversationId | 看到别人的私信 |
| **API 接口** | 修改 RESTful 资源 ID | `DELETE /api/users/11` 删掉别人 |

### 3.4 关键特征（一条都不少）

- ✅ 接口返回 HTTP 200
- ✅ 数据正常显示，格式和结构完全正确
- ✅ 没有报错信息，服务器日志看不出异常
- ❌ **数据不属于当前登录用户**

---

## 四、垂直越权

### 4.1 核心攻击模式

**低权限用户（或未登录用户）直接访问本应对其隐藏的高权限功能。**

```bash
# 场景 1：未登录访问管理后台
# 网站登录后才显示"用户管理"菜单，但——
GET /admin/user/list       # 直接输入 URL → 返回了所有用户列表 💥
# 前端隐藏了菜单 ≠ 后端做了权限校验

# 场景 2：普通用户调用管理接口
# 后端代码：
@app.route('/api/admin/user/delete', methods=['POST'])
@login_required    # ← 只校验了登录！
def delete_user():
    user_id = request.form['userId']
    User.query.filter_by(id=user_id).delete()
    return 'ok'

# 你是普通用户 role=user，但成功登录后直接 POST：
POST /api/admin/user/delete
Cookie: session=abc123    # 这是普通用户的 Cookie
Body: userId=10           # 删除了用户 10 💥
```

### 4.2 常见入口

| 入口 | 怎么发现 | 举例 |
|------|---------|------|
| **管理后台 URL** | 猜路径 / 爬虫枚举 | `/admin/` `/manage/` `/backend/` `/console/` |
| **Cookie 角色字段** | 抓包看 Cookie | `role=user` 改成 `role=admin` |
| **JWT 中的角色声明** | 解码 JWT 看 payload | `{"role":"user"}` 改成 `{"role":"admin"}` |
| **API 接口路径** | 从 JS 源码中提取 | `/api/admin/` 路径写在前端 JS 里 |
| **未登录访问** | 不登录直接打 | 部分接口后端根本没校验登录态 |

### 4.3 典型场景

| 场景 | 普通用户能做什么... | 应该只有管理员能做 |
|------|-------------------|-------------------|
| 用户管理 | 查看所有用户列表 | ✅ 管理员 |
| 删除用户 | POST 删除请求 | ✅ 管理员 |
| 修改配置 | 修改系统设置 | ✅ 管理员 |
| 查看全站订单 | 看到所有用户的订单 | ✅ 管理员 |
| 发布公告 | 发布全站通知 | ✅ 管理员 |

### 4.4 Pikachu 实战踩坑记录

在 Pikachu 垂直越权模块遇到的一个坑：

**现象**：登录 admin 账号时经常失败，只发 1 个 POST 包就返回登录失败，排除账号密码错误。

**根因**：PHP 的 Session 机制——登录依赖 Session 上下文，而 Session 在**第一次 GET 请求时建立**。

```
错误流程（失败）：
  POST /vul/overpermission/op2/op2_admin.php   → 直接发 POST
  → 没有 PHPSESSID → PHP 无法维护 Session 状态 → 登录失败 ❌
  （只发 1 个包）

正确流程（成功）：
  ① GET /vul/overpermission/op2/op2_admin.php   → 拿 PHPSESSID + 页面
  ② POST /vul/overpermission/op2/op2_admin.php  → 带 Cookie + submit=登录
  → 登录成功 ✅
  （发 2 个包：先 GET 建立 Session，再 POST 提交登录）
```

**排查方法**：

```bash
# curl 模拟完整流程
# Step 1: GET 拿 Cookie
curl -v -c /tmp/cookie.txt http://靶场/vul/overpermission/op2/op2_admin.php

# Step 2: 带 Cookie POST 登录
curl -v -b /tmp/cookie.txt \
  -d "username=admin&password=123456&submit=登录" \
  http://靶场/vul/overpermission/op2/op2_admin.php
```

---

## 五、检测方法

### 5.1 白盒审计

搜索以下模式：

```python
# 模式 1：有登录校验但无角色校验
@app.route('/admin/xxx')
@login_required     # 有这行
# @admin_required   # 缺这行 ← 垂直越权入口

# 模式 2：从请求参数中取 userId / role
userId = request.args.get('userId')     # ← 应该从 session 取
role = request.cookies.get('role')      # ← 不可信，用户能改

# 模式 3：根据用户输入查数据，但没校验归属
order = Order.query.get(request.args.get('id'))
# ↑ 缺了：if order.user_id != current_user.id: abort(403)
```

**关注的关键字**：

| 语言 | 搜索关键词 |
|------|-----------|
| Python Flask | `@login_required` 但无角色装饰器 / `request.args.get` 取 userId |
| Java Spring | `@PreAuthorize` 缺失 / `@RequestMapping("/admin/")` 无权限注解 |
| PHP | `$_GET['id']` / `$_POST['user_id']` / `$_COOKIE['role']` |

### 5.2 黑盒测试

#### 准备工作

```
注册至少 3 个账号：
  - 账号 A：普通用户（测试水平越权用）
  - 账号 B：普通用户（和 A 同级，A 的攻击目标）
  - 账号 C：低权限用户（测试垂直越权用）
```

#### 水平越权测试流程

```
Step 1：账号 A 登录 → 访问 /api/order/1001 → 收到 A 的订单数据
Step 2：同一 Cookie，访问 /api/order/1002 → 收到 B 的订单数据？
  ├── 是 → 💥 水平越权确认
  └── 否 → 继续尝试其他 ID / 参数
Step 3：用 Burp Intruder 批量遍历（id=1 ~ id=200），看响应差异
```

#### 垂直越权测试流程

```
Step 1：账号 C（普通用户）登录 → 拿到 Cookie
Step 2：用 C 的 Cookie 直接访问管理接口：
  GET /admin/user/list       → 返回用户列表？→ 💥 垂直越权确认
  POST /admin/user/delete    → 成功删除？  → 💥 垂直越权确认
Step 3：尝试未登录访问：
  清除 Cookie → 访问 /admin/ → 能进去？   → 💥 未授权访问确认
```

### 5.3 工具

| 工具 | 用途 | 一句话用法 |
|------|------|-----------|
| **Burp Repeater** | 单个请求改参数重放 | 改 ID 看响应 |
| **Burp Intruder** | 批量遍历 ID | Payload 设置 1~1000，看哪些 ID 返回数据 |
| **Autorize**（Burp 插件） | 自动对比高低权限响应 | 配置低权限 Cookie，自动重放所有请求并标记差异 |
| **AuthMatrix**（Burp 插件） | 多角色权限矩阵 | 同时管理多个角色的 Cookie，批量测试接口权限 |

---

## 六、绕过手法

### 6.1 参数篡改

```bash
# 水平越权 — 改资源 ID
GET /api/user/profile?userId=10      → userId=11
POST /api/order/detail               → Body: orderId=1001 → 1002

# 垂直越权 — 改角色标识
Cookie: role=user                    → role=admin
Cookie: isAdmin=false                → isAdmin=true
Cookie: userLevel=1                  → userLevel=0（管理员）
# JWT payload: {"role":"user"}       → {"role":"admin"}（需配合签名绕过或密钥泄露）
```

### 6.2 ID 枚举（水平越权核心手法）

```bash
# 方法 1：顺序遍历
# /api/user/1, /api/user/2, /api/user/3 ...
# 用 Burp Intruder，Payload 类型选 Numbers，From 1 To 1000

# 方法 2：从其他接口泄露中收集 ID
# 比如 /api/user/search?keyword= 返回了用户列表，其中包含 userId
# 收集到 userId 列表 → 逐个越权访问

# 方法 3：从业务场景推测
# 新注册用户 → 看自己的 ID → 推测其他用户的 ID 范围
# 如果注册时返回 userId=1088，那大概率 1~1087 都是已有用户
```

### 6.3 接口重放

```bash
# 用低权限账号正常操作一遍，Burp 记录所有请求
# 然后换成高权限账号 Cookie，重放同样请求
# 对比响应，找出"低权限被拒绝但高权限通过"的接口
#   → 再换回低权限 Cookie 尝试这些接口 → 有些可能根本没做权限校验
```

### 6.4 未授权访问

```bash
# 不登录直接打 — 有些接口后端完全裸奔
GET /admin/export/user_data.csv     # 没登录也能导出 💥
GET /api/debug/config               # 调试接口没关 💥

# 方法：用目录扫描工具发现隐藏路径
dirsearch -u https://target.com -w common.txt
ffuf -u 'https://target.com/FUZZ' -w directory-list.txt
```

---

## 七、防御方案

### 7.1 水平越权防御

**核心原则**：从服务端会话（Session / Token）中获取当前用户身份，**不信任前端传来的 userId**。查询数据后校验这条数据是否属于当前用户。

```python
# ❌ 错误：信任前端传来的 userId
@app.route('/api/order/<order_id>')
@login_required
def get_order(order_id):
    order = Order.query.get(order_id)   # 直接查
    return jsonify(order.to_dict())     # 不校验归属 💥


# ✅ 正确：从 Session 取 userId + 校验归属
@app.route('/api/order/<order_id>')
@login_required
def get_order(order_id):
    order = Order.query.get(order_id)

    # 第 1 道：订单不存在
    if order is None:
        abort(404)

    # 第 2 道：订单不属于当前用户（核心防线）
    if order.user_id != current_user.id:   # current_user 从 Session 来
        abort(403)                          # 不是你的 → 拒绝

    return jsonify(order.to_dict())
```

```java
// ✅ Java Spring Security 版本
@GetMapping("/api/order/{orderId}")
public ResponseEntity<?> getOrder(@PathVariable Long orderId,
                                   @AuthenticationPrincipal User currentUser) {
    Order order = orderService.findById(orderId);
    if (order == null) {
        return ResponseEntity.notFound().build();
    }
    // 核心：校验归属
    if (!order.getUserId().equals(currentUser.getId())) {
        return ResponseEntity.status(403).build();  // 不是你的 → 拒绝
    }
    return ResponseEntity.ok(order);
}
```

```php
// ✅ PHP 版本
session_start();
$order_id = $_GET['id'];
$current_user_id = $_SESSION['user_id'];  // 从 Session 取，不信任前端

$order = $db->query("SELECT * FROM orders WHERE id = " . intval($order_id));
//                                                           ↑ 至少做类型转换

if (!$order || $order['user_id'] != $current_user_id) {
    // 订单不存在 或 不属于当前用户 → 拒绝
    http_response_code(403);
    die('Forbidden');
}
// 安全：返回订单数据
```

**辅助手段**（不能替代归属校验）：

| 手段 | 效果 | 局限性 |
|------|------|--------|
| UUID 替代自增 ID | 增加猜测难度 | 如果 UUID 在其他接口泄露，依然能被利用 |
| 请求频率限制 | 减缓 ID 遍历速度 | 不能阻止定向攻击 |
| 响应数据脱敏 | 减少泄露影响 | 没解决根本问题 |

### 7.2 垂直越权防御

**核心原则**：每个接口都需要做角色校验，不仅校验"是否登录"，还要校验"是否有权限"。

```python
# ✅ 方法 1：装饰器校验角色
from functools import wraps

def admin_required(f):
    @wraps(f)
    def decorated(*args, **kwargs):
        if not current_user.is_authenticated:
            abort(401)          # 未登录
        if current_user.role != 'admin':
            abort(403)          # 非管理员 → 拒绝
        return f(*args, **kwargs)
    return decorated

@app.route('/admin/user/delete', methods=['POST'])
@login_required
@admin_required     # ← 关键：角色校验
def delete_user():
    ...


# ✅ 方法 2：RBAC 权限模型（更灵活）
# 不直接判断"是不是 admin"，而是判断"有没有这个权限"
@app.route('/api/user/delete', methods=['POST'])
@login_required
@require_permission('user:delete')   # 检查当前角色是否有 user:delete 权限
def delete_user():
    ...
# RBAC 结构：用户 → 角色 → 权限
# 用户 Tony → 角色 admin → 权限 {user:read, user:write, user:delete, ...}
# 用户 Bob  → 角色 user  → 权限 {user:read}
```

```java
// ✅ Java Spring Security 注解方式
@RestController
@RequestMapping("/admin")
@PreAuthorize("hasRole('ADMIN')")   // 整个 Controller 都需要 ADMIN 角色
public class AdminController {

    @PostMapping("/user/delete")
    public ResponseEntity<?> deleteUser(@RequestParam Long userId) {
        // 已通过 @PreAuthorize 校验，只允许 ADMIN 角色进入
        userService.delete(userId);
        return ResponseEntity.ok().build();
    }
}
```

### 7.3 防御分层总览

```
第 1 层：代码层
  ├── 从 Session/Token 取用户身份，不信任前端传参
  ├── 数据查询后校验归属（order.user_id == current_user.id）
  └── 每个接口做角色校验（不止校验登录态）

第 2 层：中间件层
  ├── 统一鉴权拦截器（Spring Interceptor / Flask before_request）
  ├── RBAC / ABAC 权限模型
  └── 默认拒绝所有 /admin/*，按需白名单放开

第 3 层：运行时
  ├── WAF / RASP：检测异常 ID 遍历（同一接口短时间内大量不同 ID 请求）
  └── 审计日志：记录敏感操作，异常模式告警

第 4 层：CI/CD 门禁
  └── SAST 扫描：检查 @login_required 后是否有角色校验
```

### 7.4 全局原则

```
1. 默认拒绝（Deny by Default）
   新接口先要求最高权限，再按需向下放开
   不要让"忘了加权限校验"成为漏洞

2. 权限最小化（Least Privilege）
   用户只拥有完成任务所需的最小权限
   普通用户不需要"查看所有用户列表"的权限

3. 服务端校验（Server-Side Validation）
   前端隐藏按钮不是安全措施，只是 UX 优化
   权限校验必须在后端完成，前端可为辅不可为主

4. 不相信前端
   userId、role、isAdmin 等身份/权限字段不做为后端判断依据
   唯一信任源：服务端 Session / Token
```

---

## 八、越权与其他漏洞的组合利用

越权通常不是终点，而是攻击链的起点或中间环节：

```
链 1：越权 + 文件下载
  水平越权访问 A 用户的文件下载链接
  → 替换文件 ID → 下载 B 用户的合同 / 证件 💥

链 2：越权 + XSS（权限提升经典链）
  低权限用户越权给管理员发消息（消息功能没做角色校验）
  → 消息内容包含 XSS Payload
  → 管理员查看消息 → XSS 触发 → 攻击者拿到管理员 Cookie
  → 用管理员 Cookie 登录后台 💥

链 3：越权 + 文件上传（GetShell 链）
  低权限用户越权访问文件上传接口（接口没校验角色）
  → 上传 webshell → RCE 💥

链 4：越权 + SQL 注入
  越权访问管理后台 → 后台功能没做输入过滤
  → 后台搜索框存在 SQL 注入 → 拖库 💥
```

---

## 九、总结

| 维度 | 核心认知 |
|------|---------|
| **产生原因** | 只校验登录不校验权限 / 信任前端传参 / 前端隐藏代替后端校验 / 自增 ID 可预测 |
| **水平越权** | 篡改资源 ID 访问同级别他人的数据 — 接口返回 200，数据正常，但不是你的 |
| **垂直越权** | 低权限用户调用高权限接口 — 管理后台 URL 直接可访问 / Cookie 角色字段可篡改 |
| **检测** | 白盒搜权限校验缺失 + 黑盒用多账号交叉对比响应 + Autorize 插件辅助 |
| **绕过** | 参数篡改 / ID 枚举 / Cookie 改角色字段 / 接口重放 / 未登录直接打 |
| **防御** | 从 Session 取 userId + 校验数据归属 + 每个接口做角色校验 + RBAC 模型 |
| **组合利用** | 越权 + XSS 提权 / + 文件上传 GetShell / + SQL 注入拖库 |

### 关键认知转变

> 学 SQL 注入、XSS 时你关注的是 **"输入了什么"** 。学越权时你要关注的是 **"少了什么"** — 少了那行 `if order.user_id != current_user.id`，少了一个 `@admin_required` 装饰器。越权漏洞是"缺失"的艺术。

---

### 关联笔记

- [文件下载漏洞详解](./2026-08-07-文件下载漏洞详解.md) — 越权 + 文件下载的经典组合
- [CSRF 与 SSRF 漏洞详解](./2026-08-04-CSRF与SSRF漏洞详解.md) — CSRF 也是一种"越权"（借用用户身份执行操作）
- [XSS 跨站脚本攻击入门](./2026-08-02-XSS跨站脚本攻击入门.md) — 越权 + XSS 权限提升链
- [文件包含漏洞详解](./2026-08-05-文件包含漏洞详解.md) — 逻辑漏洞家族（越权/逻辑绕过）的横向对照
- [RCE 远程代码/命令执行详解](./2026-08-05-RCE远程代码命令执行详解.md) — 越权 + 文件上传 GetShell 的终点

### 待实践

- [ ] Pikachu 水平越权关卡：注册两个账号，验证能否互相查看个人信息
- [ ] Pikachu 垂直越权关卡：普通用户登录后，尝试直接访问管理接口 URL
- [ ] 用 Autorize（Burp 插件）对 Pikachu 做一次自动化越权扫描
- [ ] 自己写一个有越权漏洞的简单 Flask Demo，然后一步步加上防御代码

> ⚠️ 声明：以上技术仅用于授权靶场、CTF 竞赛和渗透测试授权目标，未经授权对他人系统进行越权访问属于违法行为。
