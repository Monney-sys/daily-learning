# 2026-09-04 应急响应·数据库应急与漏洞口令检索（系列收官：案例1 LogonTracer · 案例2 数据库日志 · 案例3 自查漏洞模拟渗透 · 案例4 ir-rescue 工具箱）

> 状态：🧪 理论（课程截图），待实操——MySQL general_log 三连命令本机就能练；LogonTracer / 知攻善防应急靶场已列入待实践
> 承接 09-02 引入课 + 09-03 WEB 排查篇/OS 应急篇：本篇是应急响应系列**最后一部分**——把引入课「追查」分支里剩下的内容落地：日志分析（工具化：LogonTracer）+ 数据源扩展（数据库层日志）+ 模拟渗透分析（自查漏洞）+ 脚本软件分析（ir-rescue），并用「必须知识点」给整个系列收尾

## 一、本课定位：追查分支的最后一公里

| 案例 | 课程内容 | 补全的「追查」分支 |
|------|---------|-------------------|
| 案例1 LogonTracer | Win 登录日志自动可视化（neo4j 图数据库） | 日志分析（工具化） |
| 案例2 数据库日志 | MySQL/MSSQL/Oracle 日志启用 · 爆破/注入分析 | 日志分析（数据源扩展：数据库层） |
| 案例3 漏洞口令检索 | 自查漏洞 · 模拟渗透测试寻找攻击源头 | 模拟渗透分析 + 漏洞检查 |
| 案例4 ir-rescue 工具箱 | 应急取证自动化脚本（win+nix） | 脚本软件分析 |

一句话：前两篇解决「日志在哪、怎么人工查」，本篇解决「**日志没有/没思路时怎么反推攻击源（模拟渗透）+ 把分析过程工具化/自动化**」。

## 二、案例1 - Win 日志自动神器 LogonTracer（外网内网）

- **是什么**：JPCERTCC（日本 JPCERT）开源工具。把 Windows 安全日志的登录事件导入 **Neo4j 图数据库**，自动画出「谁 · 在哪台 · 什么时间 · 登录」的关系图，浏览器 Web 界面交互查看——适合爆破/横向登录分析（图形化比翻事件 ID 快得多）。
- **架构**：日志源（Windows 事件日志 / evtx 文件）→ LogonTracer 解析 → Neo4j 存图 → Web GUI 展示
- **Linux 侧部署**（课程步骤；阿里云主机记得开放端口及关闭防火墙）：
  1. 下载并解压 neo4j：`tar -zxvf neo4j-community-4.2.1-unix.tar`
  2. 装 Java11：`sudo yum install java-11-openjdk -y`
  3. 改 neo4j 配置允许外部访问并启动：
     ```
     dbms.connector.bolt.listen_address=0.0.0.0:7687
     dbms.connector.http.listen_address=0.0.0.0:7474
     ./bin/neo4j console &
     ```
  4. 拉代码装依赖：`git clone https://github.com/JPCERTCC/LogonTracer.git` + `pip3 install -r requirements.txt`
- **启动/导入两种姿势**（参数已对照官方 README 查证）：
  - 远程模式（neo4j 在服务器，起 Web 看结果）：`python3 logontracer.py -r -o 8080 -u neo4j -p <密码> -s <neo4j服务器IP>`（`-r/--run`=启动 web 应用；`-o`=web 端口默认 8080；`-s`=neo4j 地址；`-u/-p`=neo4j 账号密码）
  - 导入本地 evtx（单机分析）：`python3 logontracer.py -e Security.evtx -z <UTC时区偏移> -u neo4j -p <密码> -s 127.0.0.1`（课程示例 `-z -13`；官方 README 示例 `-z 9`=JST）
- **结果**：浏览器访问 `http://<IP>:8080` 的 web_gui 看分析图
- 外网/内网：外网=云主机部署一套（放行 7474/7687/8080）；内网=本机/内网服务器部署，把 evtx 拷过去导入即可

## 三、案例2 - 数据库日志分析：爆破 · 注入（MySQL / MSSQL / Oracle）

- **为什么要查**：常见数据库攻击 = 弱口令、SQL 注入、提升权限、窃取备份。分析数据库日志 → 发现攻击行为 → 还原攻击场景 → 追溯攻击源。
- **MySQL：启用 → 记录 → 分析**（重点 general log 通用查询日志：记录所有到达的 SQL 语句，注入 payload 和登录尝试都会留下）：
  ```
  show variables like '%general%';   -- ①看当前状态：general_log 开关 + general_log_file 位置
  SET GLOBAL general_log = 'On';     -- ②开启通用查询日志
  SET GLOBAL general_log_file = '/var/lib/mysql/mysql.log';  -- ③指定日志文件
  ```
  分析看什么：爆破 = 海量 Access denied / 失败连接记录；注入 = union/select 拼接、报错回显等异常语句。
  ⭐ 补充（标准知识）：general log 是全量记录、文件涨得快，**分析完记得 `SET GLOBAL general_log='Off'` 关掉**（防止拖垮业务 + 日志被持续写脏）；生产常开的是 binlog/慢查询，general log 多为排查期临时开。
- **MSSQL：查看 → 跟踪 → 分析**：查看日志可分析登录等（错误日志/登录审核）；配置跟踪（Profiler / 服务器端跟踪）可分析具体操作（SQL 语句、对象变更、登录）。→ 落地操作细节 ❓ 待对照视频
- **Oracle**：课程标题含 Oracle，本课截图未展开 → ❓ 待对照（猜测走审计日志 + 告警日志路线，待验证）

## 四、案例3 - 自查漏洞 · 模拟渗透测试寻找攻击源头（漏洞口令检索）

- **什么时候用**：① 日志被删除或没价值信息 ② 没有思路分析 → 换成**模拟渗透**思路：假设自己是攻击者，从攻击面重打一遍，找到能进来的洞 = 还原攻击入口/攻击源。
- **1. 系统漏洞自查**：
  - Windows：WindowsVulnScan（`D:\Myproject\venv\Scripts\python.exe cve-check.py -C -f KB.json`）——读本机已装补丁 KB 列表比对 CVE 库，得出可被利用的漏洞清单（和 08-26 Windows 提权补丁筛选同理：缺哪个补丁 ≈ 可能从哪个洞进）
  - Linux：`./linux-exploit-suggester.sh`——读内核版本/发行版，提示可尝试的内核 EXP
- **2. 应用/服务漏洞自查**：Windows `Get-WmiObject -class Win32_Product`（列已装软件清单）；Linux `LinEnum.sh`（枚举：内核/服务/SUID/计划任务/敏感文件…）；再配合前期信息收集 + `searchsploit <服务名>`（例：`searchsploit weblogic`）按应用服务协议检索公开 EXP——知道环境跑什么，就能猜攻击者用什么洞。
- **3. 协议弱口令自查**：SNetCracker（SNETCracker「超级弱口令检查工具」，Windows 平台弱口令审计，已查证）——工具探针或人工获取判断：自查 SSH/RDP/SMB/数据库等有无弱口令（口令也是攻击者常走的路，呼应 09-03 爆破案例）。
- 口诀：**日志没了 → 把自己当攻击者 → 系统补丁 / 已装服务 / 弱口令 三条线自查 → 找到洞 = 找到攻击源**。

## 五、案例4 - 自动化 ir-rescue 应急响应工具箱（实时为您提供服务）

- **是什么**（已查证官方仓库 github.com/diogo-fernan/ir-rescue）：两个姐妹脚本——`ir-rescue-win.bat`（Windows：纯批处理 + 内置命令 + Sysinternals/NirSoft 三方工具，刻意不用 PowerShell/WMI 保横向兼容）+ `ir-rescue-nix.sh`（Unix：bash4+）。功能：按**数据易失性顺序**批量采集取证数据（网络连接/进程/账户/计划任务/日志/自启动项…），按数据类型分门别类落盘供后续分析。
- **课程意图（重点）**：不是背工具，而是「**分析脚本工具原理，尝试自己编写修改，成为自己的工具箱杀器**」——读懂每段在收集什么 → 改成适合自己的（加命令/改输出/接自己的分析流程）。
- 呼应 09-03 案例4 Gscan：同类思想。Gscan（Linux 一键八项）vs ir-rescue（win+nix 更全的取证采集），按平台和采集深度选型。

## 六、必须知识点（课程总结，系列收官）

1. 第三方应用是**选择性安装**的——如何做好信息收集和漏洞探针，也是获取攻击者思路的重要操作；除了应用自身漏洞，**提前预知 / 口令相关攻击**也要筛选（→ 案例3）
2. 排除三方应用攻击行为后，**自查漏洞、分析攻击者思路**——人工配合工具脚本（→ 案例3 模拟渗透 + 案例4 工具化）
3. 工具/脚本更新迭代快、分类复杂——**打造自己的工具箱迫在眉睫**（整系列落脚点：把 收集-分析-溯源 各环节沉淀成自己的脚本库）

## 七、总结：应急响应系列收官全景

| 案例 | 内容 | 对应追查分支 | 一句话 |
|------|------|-------------|--------|
| 案例1 LogonTracer | Win 登录事件 → neo4j 图 | 日志分析（可视化） | 爆破/横向登录一眼看穿 |
| 案例2 数据库日志 | MySQL general log / MSSQL 查看跟踪 | 日志分析（数据库源） | 注入/爆破语句都在 general log |
| 案例3 漏洞口令检索 | 自查漏洞 + 模拟渗透 | 模拟渗透分析 + 漏洞检查 | 日志没了就当攻击者重打 |
| 案例4 ir-rescue | 取证数据批量采集 | 脚本软件分析 | 读原理改成自己的 |

一句话记住整个系列：应急响应 = 「表现 → 收集 → 攻击 → 追查」；追查四板斧 = **日志**（Win 事件 ID / Linux grep / LogonTracer 图 / 数据库 general log）、**后门**（查杀 + 首次写入时间反查）、**模拟渗透**（补丁/服务/弱口令自查）、**工具化**（Gscan / ir-rescue / 自己的脚本）；最后把每步沉淀成自己的工具箱。

## 疑问

1. ❓ Oracle 数据库日志分析的具体手法？（课程标题含 Oracle 但没展开；审计日志/告警日志路线待验证）MSSQL「查看/跟踪/分析」的落地操作细节（Profiler 服务器端跟踪怎么配、登录日志在哪看）？
2. ❓ WindowsVulnScan 用法细节：KB.json（本机补丁 KB 列表）怎么生成？`cve-check.py -C/-f` 参数含义？和 08-26 wmic qfe 补丁筛选的关系？
3. ❓ LogonTracer 的 `-z` 时区怎么定（课程示例 -13 的来历？中国机器一般填几）？导入后的登录关系图怎么读——哪些图形特征 = 异常/爆破？（与人工查 4624/4625 如何互相印证）

## 关联笔记

- [2026-09-02-应急响应-总体框架与溯源.md](./2026-09-02-应急响应-总体框架与溯源.md) — 引入课「追查」五分支：本篇案例1/3/4 把日志分析、模拟渗透分析、脚本软件分析全部落地，系列闭环
- [2026-09-03-应急响应-WEB入侵排查展开.md](./2026-09-03-应急响应-WEB入侵排查展开.md) / [2026-09-03-应急响应-操作系统应急响应WinLinux.md](./2026-09-03-应急响应-操作系统应急响应WinLinux.md) — 前两篇：WEB 应用层 + OS 主机层排查；本篇案例3 弱口令自查呼应 OS 篇爆破案例1
- [2026-08-26-Windows提权-补丁筛选与at-sc-ps提权.md](./2026-08-26-Windows提权-补丁筛选与at-sc-ps提权.md) — 补丁→CVE 的映射思路（案例3 WindowsVulnScan 同款逻辑）
- [2026-08-14-服务协议与接口漏洞发现.md](./2026-08-14-服务协议与接口漏洞发现.md) — searchsploit 按服务协议检索 EXP 的前置思路

## 待实践

- ⭐ **【待做·全流程应急靶场】知攻善防实验室应急响应靶场系列**（护网蓝队训练标配，可把整个应急响应系列知识全流程用上：主机信息收集 → 日志（含数据库/phpmyadmin）→ 后门追查 → 流量 → 弱口令/漏洞自查 → 写报告）：
  - 场景示例（linux2，已有博客园 writeup）：webshell 告警上机排查，题目要求提交 ①攻击者 IP ②被改的管理员密码 ③首次 webshell 连接 URL ④连接密码 ⑤数据包 flag ⑥后续上传木马文件名 ⑦⑧隐藏 flag——纯应急溯源题目，无攻击环节
  - 获取：原版来自知攻善防实验室（B站 opus 968899052437504041）；镜像下载见博客园 viphhs《[网络安全]知攻善防应急响应靶场-linux2》（cnblogs.com/viphhs/p/18915200，OVF 镜像双击导入 VMware 即可，root/Inch@957821）；另有 Windows/web1/web2 等场景（CSDN 分卷镜像）；配套学习仓库 Bypass007/Emergency-Response-Notes
  - 计划：实操当天让 Hermes 先帮把镜像下好 → 导入 VMware → 自己先打，卡住再看手册
- 案例1 LogonTracer 实操：本机 `wevtutil epl Security C:/temp/Security.evtx` 导出一份真实登录日志 → Kali VM 或云主机装 neo4j 4.x + Java11 → 导入看图（neo4j 吃内存，Kali 2G 可能紧，可考虑云主机）
- 案例2 MySQL 实操：XAMPP/本机 MySQL 跑通三连命令 → 随便执行几条 SQL + 制造一次失败登录 → 看 mysql.log 记录了什么 → 记得 `general_log='Off'` 关掉
- 案例3 工具实操：Kali 上跑 `./linux-exploit-suggester.sh`、`searchsploit weblogic` 看输出长什么样；SNetCracker 仅授权环境自查弱口令
- 案例4 ir-rescue：读源码挑 3 段（网络/进程/账户采集）理解收集逻辑，仿写一个自己的 mini 收集脚本（呼应「打造自己的工具箱」）

> ⚠️ 声明：仅限授权环境（靶场/CTF/授权渗透）中测试，所有 Payload 只用于验证漏洞存在。