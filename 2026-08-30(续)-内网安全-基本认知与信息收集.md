# 2026-08-30 内网安全①：基本认知 + 信息收集 + 后续探针（内网渗透开课第一课）

> 状态：🧪 理论（课程导图 + 案例截图），待实操
> 今天开内网渗透板块。第一课是地基：**基本认知**（域/AD/DC 概念 + 网络拓扑 + 实验环境）、**信息收集**（四类：基本/网络/用户/凭据，案例1~4）、**后续探针**（存活主机/域控制器/网络架构/服务接口，案例5）。核心思路：先搞清楚「我在哪、环境长什么样、谁是老大」，再想怎么打——跟 Web 渗透先信息收集一个道理，只是目标从「站点」变成「内网/域」。

## 一、一句话认知

内网渗透 = 拿到边界机器后，**在内网里认路（信息收集）→ 找目标（域控/核心业务机）→ 横向移动 → 拿下域控** 的过程。第一课全在为「认路」打基础：基本认知回答「内网是什么结构」，信息收集回答「这台机器上有什么」，后续探针回答「网络里还有谁、谁说了算」。

## 二、基本认知

### 1. 名词（导图「名词」分支）

| 名词 | 我的理解 |
|------|----------|
| 局域网 LAN | 小范围组网（一个办公室/楼层），物理层面的概念 |
| 工作组 Workgroup | 对等网络，每台机器自己管自己的账号，没有集中认证，「人人平等」 |
| 域环境 Domain | 集中管理的网络，账号/策略统一由域控下发 |
| 活动目录 AD | 存域内所有对象（用户/计算机/组/组策略）的数据库 + 服务，是域的「户口本」 |
| 域控制器 DC | 跑 AD 的那台服务器，域内说了算的「老大」 |

（导图还有省略号「......」，后续应该还有 DNS/GPO/信任关系等，待课程展开）

### 2. 域模型（导图「域」分支）

- **单域**：最简单的形态，一个域一个 DC
- **父域和子域**：树状层级，子域是父域的分支（子域域名是父域域名的子域）
- **域树和域森林**：多个有父子关系的域组成域树，多棵域树共享配置组成域森林（森林是最大的安全边界）

我的理解：**单域 → 域树 → 域森林** 是从小到大的三层结构，实际打域控要分清「我在哪个域、目标 DC 在哪个域、域间有没有信任关系」。

### 3. 认知（导图「认知」分支——老师留的几个问题）

- **Linux 域渗透问题**：域环境基本是 Windows 的东西（AD），Linux 也能加域（Samba/SSSD），但内网渗透主战场是 Windows AD
- **局域网技术适用问题**：工作组/纯局域网和域环境手法不同——没域就不用打域控，直接横向
- **大概内网安全流程问题**：信息收集 → 后续探针 → 横向移动 → 提权 → 拿下域控（后续课程逐步展开）

### 4. 网络拓扑认知（两张拓扑图）

**标准边界拓扑（概念图）**：

```
互联网 → 外部路由器 → 防火墙① → DMZ → 防火墙② → 内网
                                  │                    │
                     proxy服务器/IDS/抗病毒      办公电脑/企业服务器
                     电商/邮件/Web服务器         文件服务器/数据库服务器
```

- DMZ = 放对外服务的区（Web/邮件/电商），被两道防火墙夹住；内网才是核心资产区

**实验环境拓扑（域控靶场，后续实操会用到）**：

| 主机 | 系统 | IP |
|------|------|-----|
| 域控 DC | Win2008R2 | 192.168.3.21 |
| fileserver | win2003 | 192.168.3.30 |
| sqlserver | win2012 | 192.168.3.32 |
| webserver | win2008 | 192.168.3.31 + 192.168.230.131 |
| mary-pc | win7 | 192.168.3.25 |
| jack-pc | win8.1 | 192.168.3.29 |
| Kali 攻击机 | Kali | 192.168.230.133 |

- 注意 webserver 有两个 IP（3.31 + 230.131）——像是双网卡，一头在域网段、一头通向攻击机网段（230），Kali 从 230.131 这个口进来（❓ 待验证）

## 三、信息收集（四类，案例1~4）

> 目标：了解当前服务器的**计算机基本信息**，为后续判断服务器角色、网络环境做准备（案例1 引言）

### 1. 基本信息（版本/补丁/服务/任务/防护）—— 案例1

| 命令 | 作用 |
|------|------|
| `systeminfo` | 详细信息（系统版本、补丁、主机名、域、网卡等全量信息） |
| `net start` | 启动的服务 |
| `tasklist` | 进程列表 |
| `schtasks` | 计划任务 |

### 2. 网络信息（开放端口/网络环境/出口代理）—— 案例2

| 命令 | 作用 |
|------|------|
| `ipconfig /all` | 判断存在域（看 DNS 后缀/域名） |
| `net view /domain` | 判断存在域（列出域列表） |
| `net time /domain` | 判断主域（返回的域名就是域控） |
| `netstat -ano` | 当前网络端口开放情况 |
| `nslookup 域名` | 追踪来源地址（DNS 解析） |

### 3. 用户信息（域用户/本地用户/用户权限/对应组信息）—— 案例3

**系统默认常见用户身份**：

| 身份 | 说明 |
|------|------|
| Domain Admins | 域管理员，默认对域控制器有完全控制权 |
| Domain Computers | 域内机器 |
| Domain Controllers | 域控制器 |
| Domain Guest | 域访客，权限低 |
| Domain Users | 域用户 |
| Enterprise Admins | 企业系统管理员用户，默认对域控制器有完全控制权 |

**收集命令**：

| 命令 | 作用 |
|------|------|
| `whoami /all` | 用户权限 |
| `net config workstation` | 登录信息 |
| `net user` | 本地用户 |
| `net localgroup` | 本地用户组 |
| `net user /domain` | 获取域用户信息 |
| `net group /domain` | 获取域用户组信息 |
| `wmic useraccount get /all` | 域用户详细信息 |
| `net group "Domain Admins" /domain` | 查询域管理员账户 |
| `net group "Enterprise Admins" /domain` | 查询企业管理员账户 |
| `net group "Domain Controllers" /domain` | 查询域控制器账户 |

### 4. 凭据信息（明文/hash）—— 案例4

**工具**：

| 工具 | 平台 | 作用 |
|------|------|------|
| mimikatz | win | 计算机用户 HASH / 明文获取 |
| mimipenguin | linux | Linux 版凭据获取 |
| LaZagne | all | 各种协议服务口令获取 |
| XenArmor | win | 各种协议服务口令获取 |
| `netsh wlan show profiles` + `netsh wlan show profile name="无线名称" key=clear` | win | Windows 保存的 WIFI 密码 |

**凭据来源清单（导图编号列表）**：

1. 站点源码备份文件、数据库备份文件等
2. 各类数据库 Web 管理入口，如 PHPMyAdmin
3. 浏览器保存密码、浏览器 Cookies
4. 其他用户会话、3389 和 ipc$ 连接记录、回收站内容
5. Windows 保存的 WIFI 密码
6. 网络内部的各种账号和密码：Email、VPN、FTP、OA 等

## 四、后续探针（案例5）

> 目标：为后续横向思路做准备，针对应用、协议等各类攻击手法（案例5 引言）
> 探针四对象：**存活主机 / 域控制器 / 网络架构 / 服务接口**

### 1. 探针域控制器名及地址信息

```
net time /domain   nslookup   ping
```

（net time /domain 返回的域名 → nslookup 解析 → ping 拿 IP）

### 2. 探针域内存活主机及地址信息

| 工具/命令 | 类型 |
|-----------|------|
| `nbtscan 192.168.3.0/24` | 第三方工具 |
| `for /L %I in (1,1,254) DO @ping -w 1 -n 1 192.168.3.%I \| findstr "TTL="` | 自带内部命令（循环 ping + findstr 过滤存活） |
| `nmap` / `masscan` | 第三方工具 |
| nishang / empire 等 | 第三方 PowerShell 脚本 |

### 3. nishang 使用演示（PowerShell 脚本集）

```powershell
Import-Module .\nishang.psm1        # 导入模块
Set-ExecutionPolicy RemoteSigned    # 设置执行策略
Get-Command -Module nishang         # 获取模块的命令函数
Get-Information                     # 获取常规计算机信息
Invoke-PortScan -StartAddress 192.168.3.0 -EndAddress 192.168.3.100 -ResolveHost -ScanPort...  # 端口扫描
```

其他功能：删除补丁、反弹 Shell、凭据获取等（目录对应文件有演示语法）

### 4. 探针域内主机角色及服务信息（利用开放端口服务及计算机名判断）

**核心业务机器清单（10 类）**：

1. 高级管理人员、系统管理员、财务/人事/业务人员的个人计算机
2. 产品管理系统服务器
3. 办公系统服务器
4. 财务应用系统服务器
5. 核心产品源码服务器（自建 SVN、GIT）
6. 数据库服务器
7. 文件或网盘服务器、共享服务器
8. 电子邮件服务器
9. 网络监控系统服务器
10. 其他服务器（内部技术文档服务器、其他监控服务器等）

思路：**开放端口 + 计算机名**双管齐下判断机器角色——数据库服务器常开 1433/3306，源码服务器有 80/443 或 SVN/GIT 端口，邮件服务器 25/110/143 等。

## 五、总结：内网渗透第一课地图

| 板块 | 内容 | 案例 |
|------|------|------|
| 基本认知 | 名词（LAN/工作组/域/AD/DC）、域模型（单域/父子域/域树森林）、网络拓扑（边界/DMZ/内网 + 实验环境） | — |
| 信息收集 | 四类：基本信息（版本/补丁/服务/任务/防护）、网络信息（端口/环境/代理）、用户信息（域/本地/权限/组）、凭据信息（明文/hash） | 案例1~4 |
| 后续探针 | 探什么：存活主机/域控制器/网络架构/服务接口；工具：net time+nslookup+ping / nbtscan / for+ping+findstr / nmap / masscan / nishang；核心业务机识别（端口+计算机名） | 案例5 |

**一句话串起来**：内网渗透第一步 = 认识环境（域模型 + 拓扑）→ 收集信息（四类，案例1~4）→ 探针定位（谁是域控、有哪些主机、跑什么服务，案例5），为横向移动和最终拿下域控做准备。

## 疑问

1. ❓ 域/域树/域森林的完整层级关系（导图「域」分支还有省略号，应该还有信任关系/DNS 后缀等没展开）
2. ❓ Linux 加域（Samba/SSSD）在内网渗透里的角色——课程「认知」分支只列了「Linux 域渗透问题」这个标题没展开
3. ❓ `net time /domain` 的各种报错分别说明什么（非域环境 / 找不到域控 / 权限不足）？
4. ❓ 实验环境 webserver 为什么有两个 IP（3.31 + 230.131）——双网卡连攻击机网段？Kali 如何进域？
5. ❓ nishang 的 Invoke-PortScan 完整参数语法 + 演示文件在哪？

## 关联笔记

- [2026-08-14-服务协议与接口漏洞发现.md](./2026-08-14-服务协议与接口漏洞发现.md) — 资产收集/服务协议字典，和「信息收集」「探针服务接口」思路相通
- [2026-08-28(续2)-Kioptrix1靶机渗透实战.md](./2026-08-28(续2)-Kioptrix1靶机渗透实战.md) — nbtscan/SMB 枚举实操（探针工具实战）
- [2026-08-25(续3)-权限提升概述.md](./2026-08-25(续3)-权限提升概述.md) — 提权板块（内网渗透后半段：拿域控权限）
- [2026-08-15-DC1靶机渗透实战.md](./2026-08-15-DC1靶机渗透实战.md) — 完整渗透流程实操（外网→拿shell→提权）

## 待实践

- 在实验环境（域控靶场）实操案例1~5 的命令，逐个记录真实输出
- 跑一遍 `nbtscan 192.168.3.0/24` + nmap 扫存活
- 搭 nishang（PowerShell 执行策略 + Import-Module + Get-Command）
- 用 `net time /domain`、`net view /domain`、`ipconfig /all` 判断域环境

> ⚠️ 声明：仅限授权环境（靶场/CTF/授权渗透）中测试，所有 Payload 只用于验证漏洞存在。
