# 2026-08-14 APP 应用漏洞发现（小迪安全 · 漏洞发现系列 ③）

> 状态：🧪 理论未验证（刚学完，抓包/逆向实操待补）

> 漏洞发现系列第三课：**对 APP 应用的漏洞发现**。和昨天两课连着看：①操作系统是"扫端口 → 比对漏洞库"，②Web 应用是"认框架 → 选工具 → 扫+人测+审计"，今天 APP 的核心是"**先抓包看它走什么协议，再按协议分流，最后逆向兜底**"。这张图的分支是：抓包、协议、逆向。

---

## 一、是什么（我的理解）

### 1.1 一句话

APP 漏洞发现 = 先**抓包识别 APP 流量走什么协议**（http/https 还是自定义协议），WEB 协议类**直接复用昨天②的 Web 应用思路**，其他协议类**按协议专项分析**，扫不到的靠**逆向（反编译 APK）**兜底。

### 1.2 系列对比（延续快递盒系列）

```
① 操作系统 = 进小区先看"楼型"（Windows/Linux），再查这栋楼的通病
② Web 应用 = 进小区先看"装修风格"（PHP/Java/Python 框架），再查这种风格已知的毛病
③ APP 应用 = 先看手机壳里装的 APP"跟外面怎么说话"（http 还是自定义协议），
             说话方式决定用哪套工具、哪套思路
```

| 维度 | ① 操作系统 | ② Web 应用 | ③ APP 应用 |
|------|-----------|-----------|-----------|
| 先摸什么 | 端口 / 服务 / 版本 | 语言 / 框架 / CMS 指纹 | 流量协议（抓包） |
| 主要工具 | Nmap / Nessus / OpenVAS | WPScan / xray / AppScan | Burp / Charles / Fiddler + Wireshark |
| 漏洞来源 | OS 内核、系统服务 CVE | 框架 / CMS 组件 CVE + 业务逻辑 | 服务端接口漏洞 + APP 客户端自身逻辑 |
| 兜底手段 | 补丁核对 | 代码审计 + 人工测试 | 逆向（反编译 APK） |

**核心心法：协议决定工具。** APP 本质是"客户端壳 + 网络通信 + 服务端接口"三层。只要流量是 http/https，**服务端接口就是 Web 接口**，昨天学的那套方法论直接搬过来用；只有非 http 协议才需要另起炉灶。

---

## 二、抓包（先看 APP 说什么话）

导图上的抓包工具，按协议分两类：

### 2.1 http/https 类

| 工具 | 定位 | 我的理解 |
|------|------|----------|
| Burpsuite | Web 抓包/改包/重放一体 | 渗透标配。APP 手机设置代理指向 Burp，https 需安装 CA 证书（和 Web 端一样） |
| Charles | 图形化抓包工具 | 看请求树/响应体非常直观，调试 API 常用 |
| Fiddler | 老牌抓包工具 | Windows 上经典，脚本扩展能力强 |
| 抓包精灵 | 国内手机端抓包 App | 手机上直接抓包/改包，方便排查手机应用流量 |
| 其他 | HTTP Toolkit / Reqable / whistle 等 | 各有侧重，够用就行 |

### 2.2 其他协议类

- **Wireshark**：通用协议分析神器，抓 TCP/UDP/自定义协议流量，看报文结构、字段含义

**我的理解**：抓包这一步的目标不是"抓到就行"，而是**判断协议类型** —— 抓到的包决定了后面走哪条路（三、协议分流）。

> 注意点：APP 抓 https 流量要先装证书信任；如果 APP 做了 **SSL Pinning（证书固定）**，普通代理抓不到包，得靠逆向手段绕过（见四），这也是"抓包"和"逆向"连在一起的原因。

---

## 三、协议（按协议分流，决定测试思路）

导图上把协议分成两类，处理思路完全不同：

### 3.1 WEB 协议类 → 按上述 WEB 应用思路

http/https 流量，**直接复用昨天②的整套 Web 漏洞发现方法论**：

- 指纹识别 → 工具扫（xray 挂代理逛一遍）→ 人工测参数/功能
- APP 接口本质还是 REST API / JSON 接口，漏洞类型完全一样：SQLi、越权（IDOR）、认证缺陷、业务逻辑漏洞、未授权访问
- 特点：**APP 接口往往比网页端暴露更多** —— 未授权接口、调试接口、硬编码的密钥/token 都可能在 APP 里找到

### 3.2 其他协议类 → 按下述其他协议思路

非 http 协议（游戏协议、即时通讯、IoT 等自定义协议）：

1. **分析协议格式**：Wireshark 抓包 → 看报文结构 → 猜字段含义（长度/类型/校验和）
2. **找服务端组件**：协议最终打到的服务端/中间件是什么
3. **测协议层漏洞**：协议解析漏洞、逻辑漏洞、重放攻击、篡改字段
4. 可能需要写脚本/模拟客户端发包（比 Web 的 curl 麻烦一档）

---

## 四、逆向（拆开 APP 看逻辑，兜底手段）

导图上的两个点：

### 4.1 一键提取 APK 涉及 URL

把 APK 反编译，**提取代码里硬编码的所有 URL**（接口地址、CDN、第三方服务、回调地址）→ 得到一份"接口资产清单" → 逐个测这些接口（未授权、越权、敏感信息泄露）。

- 工具：jadx（反编译看代码）、APKLeaks（专门提 URL/密钥）、MobSF（自动化分析）、apktool

### 4.2 反编译重写代码段编译测试

**把 APK 反编译 → 修改关键代码段（去校验、改逻辑、加日志）→ 重新编译打包签名 → 安装测试**。典型场景：

- 绕过 **SSL Pinning**（去掉证书校验，让抓包工具能看到 https 明文）
- 绕过 root 检测 / 模拟器检测 / 加固壳
- 分析加密算法 / 本地校验逻辑，找硬编码密钥

工具链：`apktool`（解包 + 改 smali）→ 改代码 → `apktool b` 重打包 → 签名（apksigner）→ 安装；`jadx` 负责把 dex 还原成可读的 Java 代码辅助理解。

---

## 五、我的总结 / 疑问

### 总结

- APP 漏洞发现闭环：**抓包（识别协议）→ 分流（WEB 协议复用 Web 思路 / 其他协议专项分析）→ 逆向兜底（提取 URL 资产 + 反编译改逻辑）**
- 和昨天②的本质联系：**APP 的 Web 流量 = Web 漏洞发现的移动端入口**，方法论完全复用，不用重复学一遍
- 新增的两个能力点是「**抓包环境搭建**（手机挂代理 + 证书信任）」和「**逆向**（反编译 / 重打包 / 签名）」
- 心法：先抓包定协议，再决定工具 —— 协议不对，工具全废

### 疑问

- SSL Pinning 具体怎么绕过？用 Frida / objection 这类动态注入工具吗？（反编译重打包 vs 动态注入，哪条路更常用？）
- "一键提取 APK 涉及 URL"具体用什么工具实现？jadx + 正则，还是专门的 APKLeaks？
- 反编译重打包后签名会失效，怎么处理签名校验？（重签名 / 去校验？）
- 抓包精灵这类国内工具和 Burp/Charles 比，优势是什么？（手机端直抓，不用配代理？）

---

## 关联笔记

- [Web 应用漏洞发现](./2026-08-13-Web应用漏洞发现.md) — 系列②，WEB 协议类直接复用它
- [操作系统漏洞发现](./2026-08-13-操作系统漏洞发现.md) — 系列①，同系列对比着看
- [数据包与抓包实战](./2026-07-21-数据包与抓包实战.md) — 抓包基础、协议分析
- [操作系统与第三方安全测试](./2026-07-23-操作系统与第三方安全测试.md) — CDN 绕过、第三方组件思路

## 待实践

- [ ] 手机连 Burp 代理抓一次 https APP 流量（装 CA 证书全流程）
- [ ] 用 jadx 反编译一个 APK，提取所有硬编码 URL，整理成接口清单
- [ ] Wireshark 抓一次手机 APP 流量，练习判断协议类型（http vs 其他）
- [ ] 找一个做 SSL Pinning 的 APP，实操绕过（动态注入或重打包）

> ⚠️ 声明：以上技术仅用于 CTF 竞赛和授权靶场环境。
