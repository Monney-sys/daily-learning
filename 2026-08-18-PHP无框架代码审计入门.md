# 2026-08-18 代码审计入门：PHP 无框架项目漏洞挖掘（代码审计板块第一课）

> 状态：🧪 理论未验证（刚学完思路，工具已装好，还没拿真实源码实操）
> 今天开始一个新板块：**代码审计**。第一课是 PHP 无框架项目怎么挖漏洞。顺带把审计工具装齐了：**Seay 源代码审计系统（GUI）+ semgrep（命令行）**，都验证过能用。

---

## 一、是什么（我的理解）

### 1.1 一句话

**代码审计 = 不靠黑盒猜，直接读源码找漏洞。** 核心就一件事：找到**用户可控的输入**（`$_GET` / `$_POST` / `$_REQUEST` / Cookie / 上传文件 / 请求头）是怎么流进**危险操作**（执行命令 / 拼 SQL / 包含文件 / 直接输出 / 反序列化）的，中间有没有过滤、过滤能不能绕。

### 1.2 "无框架项目"意味着什么（信任边界视角）

原生 PHP 项目没有 MVC、没有统一路由、没有框架自带的过滤器，**每个 .php 页面自己收参数、自己拼 SQL、自己输出**。信任边界 = 所有用户可控输入；漏洞 = 输入直接进了危险函数。审计的目标就是把这些"输入 → 危险函数"的通路找出来。

### 1.3 类比（快递盒系列延续）

```
无框架 PHP 项目 = 一家没有安检门的小仓库
每个 .php 文件  = 一个自己收货的小货架（没人统一管）
用户输入       = 快递盒（$_GET/$_POST/Cookie…）
危险函数       = 贴着"危险品"标签的货架（system/query/include…）
代码审计       = 查每个货架：盒子从哪进来、经没经过安检（过滤）、最后到哪
```

---

## 二、课程导图（截图整理）

> 老师课程的思维导图，我按截图整理（个别字看不清，待对照视频补充）。中心主题是**代码审计**，分了「方法 / 功能 / SQL注入案例」几个分支：

**审计切入点（方法/功能合并整理）：**
| 切入点 | 关注什么 |
|--------|---------|
| 可控变量 | 哪些变量能被用户控制（`$_GET/$_POST/$_REQUEST/Cookie/上传`） |
| 特定函数 | 危险函数、敏感函数（见下面的速查表） |
| 验证绕过 | 过滤规则能不能绕（黑名单/大小写/编码/二次注入） |
| 数据交互 | SQL 拼接、数据库查询语句 |
| 文件上传 | 上传功能的后缀校验、路径拼接 |
| 输入输出 | 输出有没有转义（XSS）、输入有没有过滤 |
| 应用功能 | 业务功能点：登录、找回密码、支付、下载等 |
| 数据库监控 | 开 SQL 日志看实际执行的语句（辅助定位注入点）|
| 文件监控 | 监控文件被改/新增（找 webshell 落盘）|
| 抓包获取 | 黑盒抓包拿到真实参数名，回源码里找对应处理 |
| 黑盒扫描 | 先扫出疑似漏洞点，再回源码确认（黑白结合）|

**SQL注入（导图里的案例分支）**：可控变量 → 特定函数 → 应用功能 → 数据库监控。

---

## 三、怎么审计（核心方法论）

### 3.1 两条主线（正向 + 反向）

```
正向（可控变量追踪）：输入出发 → 跟到危险函数 → 看沿途过滤
       $_GET['id'] ──→ $id ──→ "SELECT ... WHERE id=".$id ──→ query()
       （每一跳都问：能不能控制？过滤了吗？）

反向（敏感函数回溯）：危险函数出发 → 往上查参数来源
       system($cmd) ←── $cmd ←── $_POST['cmd']
       （grep 到危险函数后，看它的参数是不是用户可控）
```

实战中**两条线配合**：反向线先圈出所有危险函数（工具扫），正向线再确认哪些真被用户输入触达。

### 3.2 审计流程（拿到源码后按这个走）

1. **搭环境看入口**：本地把项目跑起来（XAMPP / php -S），先看 `index.php`、`config.php` / `conn.php`（数据库账号）、目录结构 —— 无框架项目入口 = 各页面直接 include，入口文件暴露了页面地图
2. **工具扫一遍**（圈可疑点）：
   - **Seay 源代码审计系统**（GUI）：一键扫描，正则匹配危险函数，双击看代码上下文
   - **semgrep**（命令行）：自定义规则扫，适合批量和自动化
   - 人肉 grep：`grep -rn "system\|eval\|unserialize" 源码目录`
3. **反向确认**：对每个命中的危险函数，看参数来源是否用户可控
4. **正向追踪**：对每个可控输入，看它最终流向哪里（可能一个输入进多个危险点）
5. **验证绕过**：有过滤就试绕（黑名单大小写/双写、编码、二次注入、宽字节）
6. **落地验证**：本地实打一次（带 Payload），配合**数据库监控**（开 SQL 日志看执行语句）和**文件监控**（看有没有 webshell 落盘）确认漏洞真实存在

### 3.3 PHP 危险函数速查表（看到就警觉，参考 Java 笔记那套）

| 漏洞类型 | PHP 里的危险写法 | 安全写法 |
|---------|-----------------|---------|
| 命令/代码执行 | `system()` `exec()` `shell_exec()` `passthru()` `popen()` `proc_open()` `eval()` `assert()`、反引号 `` `cmd` `` | 禁用以降级/白名单命令 |
| SQL 注入 | 字符串拼接进 `mysqli_query()` / `$conn->query()` / `PDO::query()`（无占位符） | 预处理语句 `prepare` + 绑定参数 |
| 文件包含 | `include` / `require` + 用户可控路径（LFI/RFI） | 白名单映射，禁止用户传路径 |
| 文件读写/下载 | `file_get_contents()` `fopen()` `readfile()` `unlink()` `move_uploaded_file()` + 用户路径 | 路径规范化 + 白名单目录 |
| 反序列化 | `unserialize()` 处理用户可控数据 | 禁止反序列化不可信数据 |
| XSS | `echo` / `print` 直接输出用户输入 | `htmlspecialchars()` 输出编码 |
| SSRF | `file_get_contents($url)` `curl_exec()` 请求用户可控 URL | URL 白名单 + 禁内网段 |
| 上传 | `move_uploaded_file()` + 黑名单后缀 + 路径拼接 | 白名单 + 随机文件名 |

### 3.4 工具部署记录（2026-08-18，都验证过）

| 工具 | 类型 | 位置 | 用法 |
|------|------|------|------|
| **Seay 源代码审计系统 2.1** | GUI 一键扫描 | `C:\Users\huoyuchen\SecurityTools\Seay\Seay源代码审计系统.exe`（双击 `Seay.bat` 启动）| 选源码目录 → 点"自动审计" → 按漏洞类型看结果；SHA-1 已校验 `2B41D46A...9056` 一致 |
| **semgrep 1.173.0** | CLI 规则扫描 | `C:\Users\huoyuchen\SecurityTools\semgrep-venv`（`semgrep.bat` 启动，bat 里已清 PYTHONPATH 防污染）| 自定义规则示例在 `SecurityTools\semgrep_test\`，扫描命令：`semgrep scan --config 规则.yaml 目标目录` |

> ⚠️ 踩坑记录：semgrep 装完报 `ModuleNotFoundError: mcp/anyio/colorama...` —— 清华镜像首次安装依赖解析没走全，**卸载重装一次**就好了；Hermes 环境设了 PYTHONPATH 会导致 semgrep 误加载别的 mcp 包，启动前 `set PYTHONPATH=` 清掉。

---

## 四、怎么防御（审计的镜像）

- 无框架项目**补上统一入口**：所有请求过公共过滤层，禁止每个页面自己裸收参数
- 数据库层：**预处理语句**（PDO prepare + bindParam），杜绝字符串拼 SQL
- 命令执行：危险函数禁用（`disable_functions`）/ 白名单命令 + 参数严格校验
- 输出统一编码（`htmlspecialchars`）；文件操作路径规范化 + 白名单目录
- 反序列化数据不可信；上传白名单后缀 + 随机文件名
- 上线前过一遍审计工具（Seay/semgrep）+ 黑盒扫描兜底

---

## 五、总结 / 疑问

- 核心收获：审计 = **可控输入 × 危险函数 × 过滤可绕** 三要素；工具只是加速器，真正的活是顺着数据流人肉确认
- 疑问（已登记 OPEN-QUESTIONS）：
  1. 导图里"数据库监控 / 文件监控"具体指什么工具手段？（开 SQL 日志？文件完整性监控？）
  2. 工具扫出的"疑似漏洞"怎么快速确认可利用？（黑白结合的具体流程？）
  3. 无框架 vs 框架项目（MVC）审计的切入点差异？MVC 是不是从路由注解进、无框架从入口文件进？
  4. 想实操练手，老 CMS 源码（DedeCMS/PHPCMS 早期版本）去哪下干净的历史版本？

---

## 方法论沉淀

```
代码审计 = 输入可控性 × 危险函数可达性 × 过滤可绕过性
工具扫可疑点（反向）→ 数据流确认（正向）→ 绕过滤 → 落地验证
```

## 待实践

- [ ] 拿一个真实无框架 PHP 源码（老 CMS 或靶场源码），用 Seay + semgrep 各扫一遍，挑一个 SQLi 完整走通"可控变量 → 危险函数 → 绕过 → 验证"
- [ ] 学一下 semgrep 的 PHP 官方规则集（`p/php`）怎么用，和自定义规则对比
- [ ] 对比审计一个框架项目（如 ThinkPHP 老版本），体会切入点差异

## 六、课程实战案例补充（2026-08-23 第三课）

> 第三课（文件操作审计）课程里给了两个无框架实战案例，补在这。案例原理细节见 [2026-08-23-文件操作代码审计.md](./2026-08-23-文件操作代码审计.md)。

### 案例 1：Metinfo（米拓建站）— 变量覆盖

- 漏洞类型：**变量覆盖**（`extract()` / `parse_str()` / `$$` 动态赋值）
- 一句话本质：变量覆盖本身不是漏洞，**被覆盖的变量流进危险函数（文件删除/写入/上传）才是洞**
- 审计方式：自动审计（Seay）或搜索特征 `extract(` / `parse_str(` / `foreach ($_xxx as $k=>$v) $$k=$v`
- 利用思路：覆盖框架内部变量（上传目录、文件名、操作对象）→ 影响文件操作参数 → 任意文件删除/写入
- 状态：🧪 待实操（源码已下 `D:\php\audit-targets\metinfo-master.zip`）

### 案例 2：phpmydmin — 反序列化

- 漏洞类型：**反序列化**（`unserialize()` 处理用户可控数据）
- 一句话本质：可控反序列化入口 + 类魔术方法链（`__destruct` / `__wakeup` / `__toString`…）→ RCE
- 审计方式：自动审计或搜索 `unserialize(`，找入口参数来源 + POP 链
- 状态：🧪 课程素材不在公开源，待从课程网盘获取

---

## 关联笔记

- [2026-07-22-中间件安全与Web源码分析.md](./2026-07-22-中间件安全与Web源码分析.md) — 第一次接触 Web 源码分析
- [2026-08-12-Java安全代码审计.md](./2026-08-12-Java安全代码审计.md) — Java 版高危 API 速查 + 审计五步法（思路同源）
- [2026-08-13(续)-Web应用漏洞发现.md](./2026-08-13(续)-Web应用漏洞发现.md) — 漏洞发现系列里提过的代码审计
- [2026-08-12-JWT安全与攻击.md](./2026-08-12-JWT安全与攻击.md) — 之前实操过的 JWT 审计点

> ⚠️ 声明：仅限授权环境（靶场/CTF/授权渗透）中测试，所有 Payload 只用于验证漏洞存在。
