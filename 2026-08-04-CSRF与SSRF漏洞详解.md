# 2026-08-04 CSRF 与 SSRF 漏洞详解

## 一、两种"请求伪造"先搞清楚谁是谁

| | CSRF | SSRF |
|------|------|------|
| **全称** | Cross-Site Request Forgery | Server-Side Request Forgery |
| **中文名** | 跨站请求伪造 | 服务器端请求伪造 |
| **借谁的手** | **受害者浏览器** | **Web 服务器** |
| **请求从哪发出** | 受害者电脑 | 服务器自己 |
| **攻击目标** | 受害者已登录的其他网站 | **内网 / 本机 / 云元数据** |
| **一句话** | 你点链接，浏览器帮你转账 | 你传 URL，服务器帮你探测内网 |

> 共同点：都不是注入攻击，都是**伪造请求**。不同的是谁在发这个请求。


## 二、CSRF（跨站请求伪造）

### 2.1 原理

```
① 你登录了 bank.com → Cookie 存在浏览器
② 你在另一个标签页打开论坛帖子
③ 帖子里有一张"图片"：
    <img src="http://bank.com/transfer?to=hacker&amount=10000" />
④ 浏览器自动发 GET 请求去 bank.com → 自动带上 Cookie
⑤ 银行服务器：Cookie 有效 → "是本人操作" → 转账成功
⑥ 你什么都没做，钱没了
```

**攻击不需要你知道受害者的密码——浏览器会自动把 Cookie 带上。**

### 2.2 三个必要条件（缺一不可）

```
条件 ①：受害者已登录目标网站（Cookie 有效）
条件 ②：目标网站存在有"副作用"的操作（转账、改密码、删数据...）
条件 ③：目标网站对这个操作的验证**只依赖 Cookie**（无 Token / 无 Referer 校验）
```

### 2.3 应用场景

| 场景 | 攻击手法 |
|------|---------|
| **银行转账** | `<img>` 标签触发 GET 转账请求 |
| **修改密码** | 自动提交 POST 表单，把密码改成攻击者知道的 |
| **后台删用户** | 管理员逛论坛时触发删除操作 |
| **发帖/发私信** | 以受害者名义发垃圾广告 |
| **关注/点赞** | 刷粉、刷赞 |
| **添加管理员** | 如果后台有"添加用户"接口且无 CSRF Token |

### 2.4 如何探测 CSRF

**第一步：找一个有"副作用"的操作**

```
- 修改密码
- 修改邮箱/手机号
- 转账/支付
- 添加/删除用户
- 修改权限
- 发帖/私信/评论
```

**第二步：检查这个请求的校验机制**

```
用 Burp 抓包看：

① 有没有 csrf_token / _token / authenticity_token 等随机参数？
   → 没有 → 大概率有 CSRF ⚠️

② 有没有 Referer / Origin 校验？
   → 删除 Referer 头发送 → 还能成功 → CSRF ⚠️

③ GET 请求就能完成修改操作？
   → 一个 <img> 标签就能触发 → 高危 CSRF 🚨

④ 验证码/二次确认？
   → 没有 → CSRF 风险
```

**第三步：构造 PoC（Proof of Concept，概念验证）**

```html
<!-- 最简单的 GET 型 PoC -->
<img src="http://target.com/change_password?new_pass=hacked123">

<!-- POST 型自动提交 PoC -->
<form action="http://target.com/change_password" method="POST" id="csrf">
  <input type="hidden" name="new_password" value="hacked123">
  <input type="hidden" name="confirm_password" value="hacked123">
</form>
<script>document.getElementById('csrf').submit();</script>
```

> ⚠️ 构造 PoC 后自己打开测试——如果能成功，说明 CSRF 漏洞存在。**不要用这个链接去钓别人。**

### 2.5 CSRF 防御

| 防御方式 | 做法 | 推荐度 |
|----------|------|:---:|
| **Anti-CSRF Token** | 每次操作前，服务端生成随机 Token 藏在表单里 | ⭐⭐⭐ |
| **SameSite Cookie** | `Set-Cookie: session=xxx; SameSite=Strict` → 跨站请求不带 Cookie | ⭐⭐⭐ |
| **Referer / Origin 校验** | 检查请求来源是否为本域名 | ⭐⭐ |
| **二次验证** | 改密码/转账要求再输一次密码或短信验证码 | ⭐⭐ |
| **POST 替代 GET** | 有副作用的操作用 POST/PUT/DELETE，不用 GET | ⭐ |
| **验证码** | 敏感操作加验证码 | ⭐⭐ |

**Anti-CSRF Token 工作原理**：

```
① 用户打开修改密码页面 → 服务器生成 Token: "a8f3b9..."
   表单里藏着：<input type="hidden" name="csrf_token" value="a8f3b9...">
   同时服务器 Session 里存了：token = "a8f3b9..."

② 用户提交表单 → 服务器比较：
   表单里的 token vs Session 里的 token → 一致 → 通过 ✅

③ 攻击者构造的 CSRF 表单 → 没有 token / token 是猜的
   → 服务器比较 → 不匹配 → 拒绝 ❌
```

**SameSite Cookie 详解**：

| SameSite 值 | 效果 |
|:---:|------|
| `Strict` | **跨站请求完全不发 Cookie** → CSRF 根本没法触发（最严格） |
| `Lax` | `<img>` `<form>` `<iframe>` AJAX 都不带 Cookie；只有从别的网站**点链接跳过来**时才带 |
| `None` | 无限制（必须同时设 `Secure`，只允许 HTTPS） |

```http
# 推荐生产配置
Set-Cookie: session=xxx; HttpOnly; Secure; SameSite=Lax
```

### 2.6 CSRF 和 XSS 的关系

| 情况 | 后果 |
|------|------|
| **有 XSS，有 CSRF** | XSS 能从页面里直接读到 Token → Token 防御形同虚设 |
| **有 XSS，无 CSRF** | XSS 本身就能做 CSRF 做不到的事（操控页面、读数据） |
| **无 XSS，有 CSRF** | Token + SameSite 能防住，单独 Cookie 认证的旧系统易受攻击 |


## 三、SSRF（服务器端请求伪造）

### 3.1 内外网前置知识

```
互联网（外网）
    │
    │  公网 IP：人人都能访问
    │
┌───┴───────────────┐
│   路由器 / NAT     │
│   192.168.1.1      │
└───┬───────┬───────┘
    │       │
  你的电脑   数据库服务器
  .1.5       .1.100:3306
    ↑           ↑
  外面的人访问不到 192.168.1.x
  但你家内部所有设备可以互相访问
```

**内网专用 IP 段**（公网不存在，路由器丢弃）：

| IP 段 | 典型用途 |
|-------|---------|
| `10.0.0.0/8` | 公司/学校大型内网 |
| `172.16.0.0/12` | Docker 容器网络 |
| `192.168.0.0/16` | 家用路由器 |
| `127.0.0.1` | 本机回环（自己访问自己） |

### 3.2 原理

```
正常使用：
  网站有"抓取网页内容"功能 → 用户传 URL → 服务器 fetch 回来展示

攻击利用：
  用户传 http://127.0.0.1:3306 → 服务器从本机访问 MySQL 端口 → 信息泄露
  用户传 http://192.168.1.100/admin → 服务器访问内网管理后台 → 内网穿透
```

**一句话：你让服务器以它的身份（内网视角）去发请求，访问你从外网摸不到的东西。**

### 3.3 攻击链路

```
你（外网） ──→ 目标 Web 服务器（有外网 IP）
                     │
                     ├── 127.0.0.1:3306  （MySQL，外网访问不了）
                     ├── 127.0.0.1:6379  （Redis）
                     ├── 127.0.0.1:22    （SSH）
                     ├── 192.168.1.100:8080（内网其他应用）
                     ├── 169.254.169.254   （☁️ 云元数据 API）
                     └── file:///etc/passwd（本地文件）
```

### 3.4 常见攻击目标与危害

| 目标 | Payload | 能得到什么 |
|------|---------|-----------|
| **云元数据 API** | `http://169.254.169.254/latest/meta-data/` | ⭐ AWS/阿里云/腾讯云的临时凭证、AccessKey、私钥 |
| **本地文件读取** | `file:///etc/passwd`、`file:///C:/Windows/win.ini` | 系统账号、配置文件源码 |
| **内网端口扫描** | `http://192.168.1.1:22`、`:3306`、`:6379`、`:8080` | 内网存活主机和服务 |
| **Redis 未授权** | `gopher://127.0.0.1:6379/_[写文件命令]` | 写入 webshell 或 SSH 公钥 → 服务器沦陷 |
| **FastCGI** | `gopher://127.0.0.1:9000/_[FastCGI 包]` | 代码执行 |
| **内网敏感页面** | `http://192.168.1.1/admin/index.php` | 内网管理后台 |
| **Server 信息泄露** | `dict://127.0.0.1:22` | SSH banner、中间件版本 |
| **DOS** | 请求超大文件 / 请求内网慢服务 | 耗尽服务器资源 |

### 3.5 不只 HTTP——SSRF 支持的协议

```
http://  → 内网 Web 探测、云元数据访问
https:// → 同上（加密版）
file://  → 读取服务器本地文件
gopher://→ 构造任意 TCP 包（打 Redis / FastCGI / MySQL）
dict://  → 探测端口、看 banner
ftp://   → FTP 端口探测
```

### 3.6 如何探测 SSRF

**去哪找 SSRF？——所有让服务器"替你访问一个地址"的功能：**

| 功能 | 位置示例 | 参数 |
|------|---------|------|
| 网页抓取/预览 | URL 输入框 | `?url=` `?fetch=` `?src=` |
| 图片加载/代理 | 图片 URL | `?img=` `?image_url=` `?path=` |
| 文件上传（远程） | 远程文件 URL | `?file=` `?remote=` |
| PDF 生成 | HTML→PDF 含外部资源 | WKHTMLTOPDF |
| API 聚合 | 转发请求 | `?target=` `?redirect_uri=` |
| Webhook | 回调 URL 配置 | `?callback=` `?notify_url=` |

**探测步骤**：

```
Step 1：找个能让服务器发请求的功能，把 URL 参数改成自己的服务器
    http://target.com/fetch?url=http://你的IP:端口/test

Step 2：看你的服务器有没有收到请求
    收到了 → 功能确实会发请求 → SSRF 可能 ✅
    没收到 → 可能被过滤 / 功能不生效

Step 3：把 URL 换成内网地址
    http://target.com/fetch?url=http://127.0.0.1:3306
    http://target.com/fetch?url=http://192.168.1.1
    http://target.com/fetch?url=file:///etc/passwd

Step 4：根据响应判断
    返回 MySQL 错误信息 → MySQL 端口开了 ✅
    返回了文件内容 → file:// 协议可用 ✅
    返回超时/连接拒绝 → 端口关了
    返回"URL 不合法" → 被过滤了
```

**绕过 IP 黑名单**：

```
过滤了 127.0.0.1？试试这些：
→ http://localhost/
→ http://0.0.0.0/
→ http://2130706433/       ← 127.0.0.1 十进制写法
→ http://0x7f000001/       ← 十六进制
→ http://0177.0.0.1/       ← 八进制
→ http://127.0.0.1.nip.io  ← DNS 解析到 127.0.0.1
→ http://[::1]/             ← IPv6 回环
→ http://127.0.0.1:80@evil.com/  ← 利用 URL 解析歧义
→ 302 跳转（你自己的服务器收到请求后 302 到 127.0.0.1）
```

### 3.7 SSRF 防御

| 防御方式 | 做法 | 推荐度 |
|----------|------|:---:|
| **白名单** | 只允许请求特定域名/IP | ⭐⭐⭐ |
| **禁用危险协议** | 只允许 `http://` 和 `https://` | ⭐⭐⭐ |
| **过滤内网 IP** | 拒绝私有 IP 段 + `127.0.0.1` + `169.254.169.254` | ⭐⭐ |
| **不返回原始内容** | 只返回处理后的数据，不把服务端请求的响应直接返回给用户 | ⭐⭐ |
| **验证返回类型** | 只接受 `image/*` 等预期 Content-Type | ⭐ |
| **内网服务鉴权** | Redis / MySQL 等加上密码认证 | ⭐⭐⭐ |
| **最小权限** | 服务器以低权限用户运行，限制能访问的内网资源 | ⭐⭐ |

**为什么白名单优于黑名单**：

```
❌ 黑名单：拒绝 127.0.0.1 → 攻击者用 2130706433 绕过
✅ 白名单：只允许 https://api.trusted.com/* → 所有内网地址都不是 api.trusted.com，一律拒绝
```

**安全配置示例（PHP）**：
```php
// ✅ 白名单域名
$allowed_hosts = ['api.example.com', 'cdn.example.com'];
$host = parse_url($url, PHP_URL_HOST);
if (!in_array($host, $allowed_hosts)) {
    die('不允许的目标');
}

// ✅ 解析域名后检查 IP 不是内网
$ip = gethostbyname($host);
if (filter_var($ip, FILTER_VALIDATE_IP, FILTER_FLAG_NO_PRIV_RANGE | FILTER_FLAG_NO_RES_RANGE) === false) {
    die('不允许的内网地址');
}

// ✅ 只允许 http/https
$scheme = parse_url($url, PHP_URL_SCHEME);
if (!in_array($scheme, ['http', 'https'])) {
    die('不允许的协议');
}

// ✅ 最后才发请求
$response = file_get_contents($url);
```


## 四、XSS / CSRF / SSRF 横向对比

| | XSS | CSRF | SSRF |
|------|------|------|------|
| **攻击方式** | 注入恶意脚本 | 伪造跨站请求 | 伪造服务器端请求 |
| **执行位置** | 受害者浏览器 | 受害者浏览器 | **Web 服务器** |
| **请求的源 IP** | 受害者的 IP | 受害者的 IP | **服务器的 IP（内网视角）** |
| **需要登录态吗** | 不需要（但能利用） | ✅ 受害者必须已登录 | 不需要 |
| **能做什么** | 任意 JS 操作（偷数据、钓鱼、操控） | 以受害者身份发请求（转账、改密码） | 探测内网、读文件、打内部服务 |
| **核心防御** | 输出编码 + CSP | Anti-CSRF Token + SameSite | URL 白名单 + 禁危险协议 |
| **谁更难防** | ⭐⭐⭐⭐ 最灵活 | ⭐⭐⭐ Token 可堵 | ⭐⭐⭐⭐ 绕过手法多 |

### 互相利用关系

```
XSS 可以做到 CSRF 做不到的事
    └── XSS 可以从页面里读到 CSRF Token → 绕过 CSRF 防御
    └── XSS 可以直接操控页面，不需要 CSRF

CSRF 能做 XSS 做不到的事吗？
    └── 不能。XSS 的能力是 CSRF 的严格超集

SSRF 独立于 XSS 和 CSRF
    └── 不需要用户交互，不需要受害者
    └── 纯粹的服务端漏洞
```

## 五、总结

| 漏洞 | 一句话 | 核心危害 |
|------|--------|---------|
| **CSRF** | 你点链接，浏览器帮你转账 | 以你身份做你不想做的事 |
| **SSRF** | 你给 URL，服务器帮你访问内网 | 打到内网、拿到凭证、穿透隔离 |

> **CSRF 借别人的手，SSRF 借服务器的手。** 前者需要社工（诱导点击），后者可能只需要一个 URL 参数。两种都是"请求伪造"，但攻击目标完全不同。

### 关联笔记

- [XSS 入门认知框架](./2026-08-02-XSS跨站脚本攻击入门.md) — XSS + CSRF 组合利用
- [XSS WAF 绕过与安全修复](./2026-08-04-XSS-WAF绕过与安全修复.md) — SSRF 绕过 IP 黑名单思路同源
- [XSS 平台工具与 HttpOnly 绕过](./2026-08-03-XSS平台工具与HttpOnly绕过.md) — HttpOnly 对 CSRF 的影响
- [文件上传漏洞体系总览](./2026-07-31-文件上传漏洞.md) — Webshell 拿到后内网横向移动（SSRF 和 Webshell 都是内网跳板）

> ⚠️ 声明：以上技术仅用于授权靶场和渗透测试授权目标，未经授权使用属于违法行为。
