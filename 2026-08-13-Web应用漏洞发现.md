# 2026-08-13 Web 应用漏洞发现（小迪安全 · 漏洞发现系列 ②）

> 漏洞发现系列第二课：**对 Web 应用的漏洞发现**。和昨天（①操作系统）对比着学：OS 是"扫端口 → 比对漏洞库"，Web 是"**认框架 → 选对工具 → 工具扫 + 人测 + 审计**"。这张图的分支是：工具框架、开发框架、代码审计、人工测试。

---

## 一、是什么（我的理解）

### 1.1 一句话

Web 漏洞发现 = 先**识别 Web 应用用的什么语言/框架/CMS**，再**选对应的工具去扫已知漏洞**，扫不到的靠**代码审计 + 人工测试**兜底。

### 1.2 和 OS 漏洞发现对比（延续快递盒系列）

```
操作系统漏洞发现 = 进小区前先看"楼型"（Windows/Linux），再查这栋楼的通病
Web 漏洞发现     = 进小区前先看"装修风格"（PHP/Java/Python、用了哪个框架），
                   再查这种风格（框架/CMS）已知的毛病
```

| 维度 | ① 操作系统 | ② Web 应用 |
|------|-----------|-----------|
| 先摸什么 | 端口 / 服务 / 版本 | 语言 / 框架 / CMS（指纹识别） |
| 主要工具 | Nmap / Nessus / OpenVAS | WPScan / xray / AppScan 等 |
| 漏洞来源 | OS 内核、系统服务 CVE | 框架 / CMS 组件 CVE + 业务逻辑 |
| 兜底手段 | 补丁核对 | 代码审计 + 人工测试 |

**核心心法：框架决定工具。** WordPress 的洞用 WPScan 扫，ThinkPHP 的洞要去 ThinkPHP 漏洞库查——拿错工具等于白扫。

---

## 二、工具框架（用什么找）

### 2.1 漏洞平台（查"这个框架/CMS 有没有已知漏洞和利用"）

| 平台 | 定位 |
|------|------|
| CNVD | 国内官方漏洞库（国家信息安全漏洞共享平台） |
| Seebug | 知道创宇漏洞平台，含利用情报 |
| 1337day | 国外漏洞利用发布站 |
| Exploit-DB | 全球最全利用代码库 |
| Packetstorm Security | 老牌安全资料站 |

（和系列①的漏洞平台完全一致 —— 平台是通用的，区别在于查什么关键词。）

### 2.2 CMS 扫描工具（按 CMS 类型对口扫）

| 工具 | 对口 CMS |
|------|----------|
| WPScan | WordPress（最流行，漏洞最多，第一优先） |
| Joomscan | Joomla |
| Drupalscan | Drupal |
| CMSscan（导图显示 cmaScan） | 通用 CMS 扫描器（类似 CMSeek / CMSmap 这类） |

### 2.3 综合漏洞扫描器（不管什么 CMS，全站扫一遍）

- **xray**：国产被动/主动扫描器，代理模式挂浏览器上，日常神器
- **OWASP ZAP**：免费开源的 Web 扫描器（导图上写 OWASP）
- **AppScan**：IBM 商业扫描器，企业标配
- **企业公司内部产品**：厂商自研扫描平台（红队/乙方内部资产）

我的理解：**先 CMS 对口工具精扫，再综合扫描器全扫**，两边结果交叉验证，漏报率最低。

---

## 三、开发框架（先认人，再出招）

| 语言 | 框架 | 我该知道什么 |
|------|------|-------------|
| PHP | Yii、Laravel、ThinkPHP | ThinkPHP 是国产框架，历史漏洞一大堆（RCE 系列）；Laravel 有 debug 模式泄露等经典洞 |
| Java | Struts2、Spring、Maven、Shiro | Struts2 老洞无数（OGNL RCE）；Shiro 反序列化（rememberMe）；Spring 家族看版本 |
| Python | Flask、Django、Tornado | Django 有调试泄露 / 反序列化，Flask 模板注入（SSTI） |

> 注意：Maven 是构建工具不是框架，但导图放在 Java 下——因为 Java 项目依赖都在 `pom.xml` 里，**查依赖版本 = 查已知 CVE**（延续 Java 代码审计笔记的思路）。

**为什么要先认框架**：指纹识别（看响应头 / 报错页 / 路径特征 / 特定文件）→ 确定语言和框架 → 直接去对应漏洞库搜"框架名 + 版本"，已知洞基本就出来了。**90% 的 Web 攻击是打已知漏洞，不是打 0day。**

---

## 四、代码审计（白盒找，工具扫不出的洞）

导图上的三种审计扫描方式：

1. **函数名扫描**：搜代码里的危险函数（`eval` / `exec` / `include` / 反序列化入口），从"危险 API 出发"反查数据流（延续 Java 审计五步法的思路）
2. **功能点扫描**：按功能模块过一遍（登录、上传、下载、查询…），每个功能点看有没有对应的漏洞类型（上传点查上传、查询点查注入）
3. **格式头扫描**：按文件格式/文件头特征找可疑文件（比如上传目录里的非预期文件、备份文件、源码泄露）

---

## 五、人工测试（黑盒兜底）

工具扫完剩下的靠手测：

- **URL 参数**：每个参数逐个测（加引号看报错、拼 payload 看反应）—— 参数就是注入点候选，延续之前"注入点识别"的方法论
- **功能测试**：把整个应用的每个功能点手动过一遍，看逻辑漏洞（越权、支付、验证码 —— 前面学的那些）

**工具 vs 人工的分工**：工具查已知（指纹/漏洞库比对），人工查未知（业务逻辑、组合利用）。逻辑漏洞工具永远扫不出来。

---

## 六、我的总结 / 疑问

### 总结

- Web 漏洞发现闭环：**指纹识别（语言/框架/CMS）→ 对口工具扫（WPScan/xray/AppScan）→ 漏洞库比对（CNVD/Exploit-DB）→ 代码审计 + 人工测试兜底**
- 和系列①的本质区别：OS 靠"端口服务指纹"，Web 靠"**框架指纹**"，指纹对了工具才有效
- 工具平台是通用的，**关键能力是"认框架 + 查对漏洞库 + 读扫描结果"**

### 疑问

- WPScan / Joomscan 这类工具是纯指纹扫描，还是能直接打？（我理解是"指纹 + 已知插件漏洞验证"，利用还是要手动/MSF）
- xray 的被动代理模式具体怎么挂浏览器？（待实践）
- 代码审计里"格式头扫描"具体怎么操作？用工具还是手工翻文件？

---

## 关联笔记

- [操作系统漏洞发现](./2026-08-13-操作系统漏洞发现.md) — 系列①，同一天学的，对比着看
- [中间件安全与Web源码分析](./2026-07-22-中间件安全与Web源码分析.md) — CMS / 源码分析的基础
- [Java 安全代码审计](./2026-08-12-Java安全代码审计.md) — 函数名扫描 / 审计五步法的出处

## 待实践

- [ ] 本地装 WPScan，对着 WordPress 靶机扫一次，看报告结构
- [ ] 学 xray 被动代理模式：浏览器挂代理 → 逛一遍目标站 → 看扫描结果
- [ ] 指纹识别练习：随便开几个网站，判断语言/框架/CMS（看响应头、特征文件）
- [ ] 查 ThinkPHP / Struts2 的历史漏洞列表，各挑一个在 Vulhub 复现

> ⚠️ 声明：以上技术仅用于 CTF 竞赛和授权靶场环境。
