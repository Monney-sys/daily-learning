# 2026-08-12 JWT 安全与攻击（Java 安全第一关）

> 状态：🧪 部分理论未验证（3.2 攻击面清单未全过靶场）· ❓ 有疑问

> 今天开始学 **Java 安全板块**，第一关是 JWT。08-10 的《Token 安全与防护绕过》里粗浅接触过 alg:none，这次从 Java 安全视角系统过一遍，并且上手实操了基础绕过。这篇是深入版：结构拆解 → 实操记录 → 完整攻击面 → 防御。

---

## 一、是什么（我的理解）

### 1.1 一句话

JWT = JSON Web Token，一种**无状态认证令牌**：服务器把用户身份信息"签发"给客户端，客户端每次请求带上，服务器验签通过就信任里面的信息。

### 1.2 三段式结构

一个 JWT 长这样（三段用 `.` 分隔）：

```
eyJhbGciOiJIUzI1NiJ9.eyJzdWIiOiJhZG1pbiJ9.9xZQ2Xj5n0sB4xX9sXyUYw
└─────Header─────┘└───Payload───┘└─────签名─────┘
```

| 段 | 内容 | 说明 |
|----|------|------|
| **Header** | `{"alg":"HS256","typ":"JWT"}` | 声明用什么算法签名 ← **攻击者可控！** |
| **Payload（claims）** | `{"sub":"admin","role":"admin","exp":...}` | 声明：身份/权限/过期时间 ← **攻击者可控！** |
| **Signature** | `HMACSHA256(base64url(header)+"."+base64url(payload), secret)` | 前两段的防篡改印章 |

**关键认知：Header 和 Payload 只是 Base64URL 编码，不是加密** —— 谁拿到都能解码看内容，也能改了重新编码。防篡改的唯一手段是签名。

Base64URL 和普通 Base64 的区别：用 `-` `_` 代替 `+` `/`，且**去掉 `=` 填充**（URL 安全）。

### 1.3 类比（延续快递盒/门禁系列）

```
JWT = 一张盖了印章的通行证

Header  = 通行证上写的"印章类型说明"  ← 攻击者可以涂改
Payload = 通行证上写的身份信息        ← 攻击者可以涂改
签名    = 印章本身（只有持章人 = 服务端密钥能盖）
服务端验签 = 拿印章样本比对，对得上才放行

攻击思路 = 要么让门卫别比对印章（alg:none）
        = 要么自己伪造一枚一样的章（弱密钥/算法混淆/密钥泄露）
```

### 1.4 和 Session 的对比（建立坐标系）

| 维度 | Session | JWT |
|------|---------|-----|
| 状态存哪 | 服务端（内存/DB/Redis） | 客户端（Payload 里） |
| 吊销/登出 | 容易，删掉 session 就行 | 难，无状态 → 只能靠黑名单（jti）+ Redis |
| 分布式扩展 | 要共享存储/粘性会话 | 天然适合，任何服务都能独立验签 |
| 安全边界 | 客户端只拿到一个随机 ID | **全部身份信息都放在客户端手里** |
| 性能 | 每次查存储 | 本地验签，快 |

一句话：**Session 把"你是谁"藏在服务端，JWT 把"你是谁"交到客户端手里，只留一个签名兜底。**

---

## 二、怎么产生的（信任边界错位）

JWT 安全的核心一句话：

> **客户端能看见、能改动一切，防篡改全靠签名；签名安全全靠"算法 + 密钥"两件事。**

漏洞之所以普遍，是因为信任边界天然错位：

1. **签名算法由客户端说了算** —— `alg` 字段写在 Header 里（客户端可改的那段），服务端如果直接信 Header 而不做白名单 → alg:none / 算法混淆
2. **密钥管理是人的问题** —— 弱密钥（`secret`、`123456`）、硬编码在源码/配置文件 → 可爆破/可泄露
3. **验签流程不完整** —— 只验签不验 `alg`、不验 `exp`、不验 `iss/aud`，或干脆不验签
4. **"可解码"被误当成"安全"** —— 开发者以为 Base64 编码就是加密，把手机号/密码哈希直接塞进 Payload

---

## 三、怎么利用

### 3.1 我今天实操的两招（上手记录）

**① 解码看结构**：JWT 三段各自 Base64URL 解码，Header/Payload 明文直接可见。

```python
import base64, json

def b64url_decode(s):
    return base64.urlsafe_b64decode(s + '=' * (-len(s) % 4))

jwt = "eyJhbGciOiJIUzI1NiJ9.eyJhZG1pbiI6ZmFsc2V9.xxxxxx"
h, p, s = jwt.split('.')
print(json.loads(b64url_decode(h)))   # {'alg': 'HS256', 'typ': 'JWT'}
print(json.loads(b64url_decode(p)))   # {'admin': False}
```

**② alg:none 绕过**：把 Header 的 `alg` 改成 `none`，Payload 里 `admin:false → true`，签名段删掉（**但保留最后一个点**）：

```
正常:   eyJhbGciOiJIUzI1NiJ9.eyJhZG1pbiI6ZmFsc2V9.9xZQ2Xj5n0sB4xX...
攻击:   eyJhbGciOiJub25lIn0.eyJhZG1pbiI6dHJ1ZX0.
        └─{"alg":"none"}─┘ └─admin:true─┘ └空签名┘
```

服务端如果没显式禁止 `none` → 跳过签名校验 → 直接信任 Payload → 我就是 admin 了。

（以上是我今天实际操作的；3.2 以下是理论扩展，还没全部过靶场验证。）

### 3.2 完整攻击面清单（按实战频率）

| # | 攻击 | 原理 | 关键操作 |
|---|------|------|---------|
| 1 | **alg:none** | 服务端没白名单，认了 `none` 跳过验签 | 改 header + 删签名留点 |
| 2 | **算法混淆 RS256→HS256** | 服务端同时支持两种算法；RS256 用公钥验签、HS256 用对称密钥验签 —— **拿公钥当 HMAC 密钥签名**，服务端也能验过 | 先拿公钥（`/.well-known/jwks.json`），用公钥做 HS256 密钥重签 |
| 3 | **弱密钥爆破** | HS256 密钥太短/是字典词 | `hashcat -m 16500 jwt.txt rockyou.txt` |
| 4 | **密钥泄露** | `JWT_SECRET` 硬编码在源码/配置/.env | 搜 `secret`/`key`/`jwt` |
| 5 | **kid 注入** | `kid` 字段用来选密钥，可注入路径遍历/SQLi 读任意文件当密钥 | `"kid":"../../../../dev/null"`（用空文件当密钥）或 SQLi |
| 6 | **jku / x5u 注入** | 服务端从 header 里的 URL 拉取公钥，URL 可控 | 指向攻击者自己的 JWKS 服务器 |
| 7 | **不验签** | 改了 payload 直接用 | 纯篡改 |
| 8 | **exp/nbf 不校验** | 过期 token 依然有效 | 把 `exp` 改成未来时间 |
| 9 | **敏感信息泄露** | Payload 只是编码不是加密 | 解码即得手机号/密码哈希 |

### 3.3 Java 侧特有的坑（这关是 Java 板块）

- **常用库**：`jjwt`、`nimbus-jose-jwt`、`spring-security-oauth2-jose`
- **常见误用**：`JwtParserBuilder` 只 `.setSigningKey()` 不 `.requireExpiration()` / `.requireIssuer()`；手写解析 JWT；库版本老
- **CVE-2022-21449（Psychic Signatures）**：Java 15~18 内置 ECDSA 签名校验缺陷，构造特定 `(r,s)` 值的签名**永远验过** → 用 ES256 签名的 JWT 可以无密钥直接伪造。Java 侧特有，升级 JDK 解决（17.0.3+ / 11.0.15+ / 8u333+）
- **工具**：`jwt_tool`（`-M` 扫描、`-X a` alg:none、`-X k` 公钥混淆）、Burp 插件 **JWT Editor**（可视化改 header/payload 并重签）

---

## 四、怎么防御

| 层面 | 措施 |
|------|------|
| 算法 | 服务端**写死白名单**（如只接受 HS256 且密钥 ≥256bit），显式拒绝 `none`；不信任 Header 里的 alg |
| 密钥 | 强随机、不硬编码（环境变量/KMS）、定期轮换；签名密钥与加密密钥分开用 |
| 验证 | 全链验证缺一不可：**签名 → alg 白名单 → exp/nbf → iss/aud** |
| 载荷 | Payload 最小化，不放敏感信息（手机号/密码哈希）；敏感数据走服务端查询 |
| 会话管理 | 短时效（短 exp）+ refresh token 轮换；登出用黑名单（jti）+ Redis |
| 库与运行时 | 用成熟库最新版；升级 JDK 修复 CVE-2022-21449；WAF 特征检测 `alg:none` |

---

## 五、我的总结 / 疑问

### 总结：四层信任链，断一层就能伪造

```
伪造 JWT = 攻破四层中的任意一层：
  ① 服务端不验签        → 直接改 payload 就能用
  ② alg 可控            → none / 算法混淆
  ③ 密钥可获取          → 弱密钥爆破 / 源码泄露
  ④ 校验不完整          → 过期 token、伪造 iss/aud 照样过
```

一句话：**JWT 的所有安全性，最终落在"服务端怎么验、密钥藏多深"上；而这两件事的关键参数（alg）却写在客户端可控的 Header 里 —— 所以算法必须服务端说了算。**

### 疑问

- 为什么很多库默认同时接受 RS256/HS256？→ 历史兼容原因，算法混淆至今有效，待验证
- `alg:none` 是规范里就有的，规范为什么会留这种口子？（规范要求"实现必须拒绝 none"，但很多库没遵守）
- 公钥的标准获取位置是 `/.well-known/jwks.json`，但实际也可能藏在别处，怎么系统性地找？

---

## 关联笔记

- [Token 安全与防护绕过](./2026-08-10-Token安全与防护绕过.md) — 08-10 粗浅版：结构/alg:none/算法混淆/爆破都有，今天这版是 Java 板块深挖 + 实操记录
- [登录脆弱与认证缺陷](./2026-08-09-登录脆弱与认证缺陷.md) — JWT 伪造在认证缺陷中的上下文
- [Java 反序列化入门](./2026-08-11-Java反序列化入门.md) — 同板块；"密钥/库选型"和"攻击面从信任边界出发"的思路可复用
- [加解密算法与编码](./加解密算法与编码.md) — HMAC / RSA 签名原理的地基

## 待实践

- [ ] PortSwigger JWT 分类 Lab：unverified signature → flawed verification → weak HMAC key → algorithm confusion 四关连打
- [ ] WebGoat JWT 关卡：JWT basics → cracking → alg confusion
- [ ] 用 `hashcat -m 16500` 实际爆破一个弱密钥 JWT（准备字典）
- [ ] `jwt_tool` 完整流程跑一遍：`-M` 扫描 → `-X a`（alg:none）→ `-X k`（公钥混淆）
- [ ] 本地 Java 项目（jjwt）复现"只验签不验 alg"的误用，亲眼看到绕过过程

> ⚠️ 声明：以上技术仅用于 CTF 竞赛和授权靶场环境。
