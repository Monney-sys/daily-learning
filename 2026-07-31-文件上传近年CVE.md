# 2026-07-31 文件上传近年 CVE 与趋势（2024–2026）

> 文件上传漏洞并没有过时。近年仍有大量高危 CVE，且手法在不断演进。

---

## 一、手法演进趋势

```
老手法（200x~201x）              新手法（2023~2026）
─────────────────────────      ─────────────────────────
后缀黑名单绕过                  MIME 猜测 + 重命名 API
空字节截断                     参数污染 + 路径穿越
解析漏洞利用                   Cookie/Header 注入路径
编辑器默认路径                  反序列化 + 文件写入链
.htaccess 上传                 云存储签名绕过（AWS S3 / OSS）
```

---

## 二、2024 年

### CVE-2024-53677 — Apache Struts 文件上传 RCE（⭐ 推荐复现）

| 项目 | 内容 |
|------|------|
| CVSS | 9.5（Critical） |
| 影响 | Struts 2.0.0 ~ 2.5.33, 6.0.0 ~ 6.3.0.2 |
| 手法 | **参数污染 + 路径穿越** |

**原理**：在 multipart 请求中用不同大小写重复同一个参数名，Struts 的 `FileUploadInterceptor` 将文件名和路径分离处理时出现不一致，导致 `../` 路径穿越，webshell 写入 web 可访问目录。

**不同于传统绕过**：不是绕过扩展名检测，而是**直接改变文件的落盘路径**——文件存到 web 目录下，不管什么后缀都能访问执行。

**修复**：升级 Struts ≥ 6.4.0，或迁移到 `ActionFileUploadInterceptor`

### CVE-2024-11613 / 11635 — WordPress File Upload 插件 RCE

| 项目 | CVE-2024-11613 | CVE-2024-11635 |
|------|---------------|---------------|
| CVSS | 9.8 | 9.8 |
| 影响 | File Upload ≤ 4.24.15 | File Upload ≤ 4.24.12 |
| 手法 | `source` 参数过滤不严 | `wfu_ABSPATH` Cookie 注入 |

**教训**：CMS 插件生态是文件上传的重灾区。WordPress 60000+ 插件，任何上传功能都可能出问题。这两个漏洞都是**未认证**即可触发。

**复现**：
```bash
# Docker 搭建 WordPress + 受影响版本插件
docker run -d --name wp-upload -p 8085:80 wordpress:6.5
# 安装 wfu-file-upload 4.24.12 版本
# GitHub 搜索 "CVE-2024-11635 PoC" 找到利用脚本
```

### CVE-2024-47823 — Laravel Livewire

| 项目 | 内容 |
|------|------|
| CVSS | High |
| 影响 | livewire/livewire < v2.12.7, < v3.5.2 |
| 手法 | **MIME 猜测扩展名** |

**原理**：Livewire 框架根据上传文件的 MIME type **自行猜测**文件扩展名，而不是从文件名中获取。攻击者上传含 `Content-Type: image/png` 的 PHP 文件 → Livewire 认为是 `.png` → 文件以 `.png` 存储，但内容是 PHP。如果服务器配置了 `.png` 执行 PHP（如 Apache 多后缀），即可 RCE。

**亮点**：MIME 绕过的新形态——不是后端 MIME 校验被绕过，而是**框架自己猜错了后缀**。

### CVE-2024-7772 — Jupiter X Core（WordPress 主题）

| 项目 | 内容 |
|------|------|
| CVSS | 9.8（Critical） |
| 影响 | ≤ 4.6.5 |
| 手法 | `validate` 函数文件类型验证不当 |

**原理**：未认证攻击者可直接上传恶意文件，无需任何权限，实现 RCE。

---

## 三、2025 年

### CVE-2025-56399 — Laravel FileManager 重命名绕过（⭐ 推荐复现）

| 项目 | 内容 |
|------|------|
| CVSS | 8.5（High） |
| 影响 | alexusmai/laravel-file-manager ≤ 3.3.1 |
| 手法 | **上传 + 重命名 API 组合** |

**原理**：
1. 上传一个含 PHP 代码的 `.png` 文件 → 内容校验通过（确实是图片格式）
2. 调用 **rename API** 将其改名为 `.php` → 重命名接口没做扩展名校验
3. 访问改名后的 `.php` 文件 → 执行

**亮点**：**绕过的不上传接口，而是重命名接口**——两个 API 校验粒度不一致。GitHub 上已有完整 PoC。

### CVE-2025-10353 — Melis Platform 未认证上传

| 项目 | 内容 |
|------|------|
| CVSS | Critical |
| 影响 | Melis Platform `melis-cms-slider` 模块 |
| 手法 | **经典"忘了加认证"** |

**原理**：`MelisCmsSliderDetails/saveDetailsForm` 端点未做认证也文件类型校验，直接上传 webshell 到 `media/sliders/`。

### CVE-2025-34100 — BuilderEngine + elFinder

| 项目 | 内容 |
|------|------|
| CVSS | Critical |
| 影响 | BuilderEngine 3.5.0（集成 elFinder 2.0） |
| 手法 | 未认证上传 `.php` 文件 |

---

## 四、2026 年

### CVE-2026-54414 — FileRise 路径穿越 + 管理员接管

| 项目 | 内容 |
|------|------|
| CVSS | 9.8（Critical） |
| 影响 | FileRise < 3.16.0 |
| 手法 | **验证后再解码（TOCTOU）** |

**原理**：共享文件夹上传接口在验证文件名安全性**之后**才对文件名进行 URL 解码。攻击者提交一个百分号编码的 `../` 路径 → 验证时 `%2e%2e%2f` 不是 `../` → 通过 → 解码后变成 `../` → 路径穿越，覆盖 `users/users.txt` 创建管理员。

**亮点**：**验证和解码的顺序错误**——验证阶段文件名"安全"，解码后变危险。

---

## 五、近年手法总结

| 新手法 | 核心原理 | 代表 CVE | 难度 |
|--------|----------|----------|------|
| 参数污染 + 路径穿越 | 文件名与路径分离处理不一致 | CVE-2024-53677 | 中 |
| Cookie/Header 注入路径 | 从非预期来源获取文件路径 | CVE-2024-11635 | 低 |
| 框架猜后缀 | MIME → 扩展名映射逻辑缺陷 | CVE-2024-47823 | 低 |
| 重命名 API 绕过 | 上传校验严格，重命名不校验 | CVE-2025-56399 | 低 |
| 验证后解码 (TOCTOU) | 文件名安全检查和解码的顺序错 | CVE-2026-54414 | 中 |
| 云存储签名绕过 | PUT 到 OSS/S3 时绕过客户端签名 | 各厂商 SRC 高频 | 中-高 |

---

## 六、经典 CVE 回顾

### CVE-2017-12615 — Tomcat PUT 任意文件写入

- **影响**：Apache Tomcat 7.0.0 ~ 7.0.79（Windows）
- **条件**：`web.xml` 中 `readonly=false`（开启 PUT）
- **利用**：
  ```bash
  curl -X PUT http://target:8080/shell.jsp/ \
    -d '<% Runtime.getRuntime().exec("cmd /c whoami"); %>'
  # 文件名末尾加 / 绕过后缀检测
  ```

### CVE-2017-1002024 — KindEditor 目录遍历

见编辑器笔记（`./2026-07-31-file-upload-editor-cms.md`）的 KindEditor 章节。

### CVE-2015-5254 — ActiveMQ 任意文件上传

- **影响**：Apache ActiveMQ 5.0.0 ~ 5.11.1
- **利用**：无需认证，PUT 方法上传 JSP webshell 到 `/fileserver/`
  ```bash
  curl -X PUT http://target:8161/fileserver/shell.jsp \
    -d '<% out.println("test"); %>'
  ```

---

## 七、复现优先级建议

| 优先级 | CVE | 原因 |
|--------|-----|------|
| 1 | CVE-2024-53677 (Struts) | 手法新颖（参数污染），影响面广，技术含量高 |
| 2 | CVE-2025-56399 (Laravel) | GitHub 有完整 PoC，搭建简单，理解 API 组合攻击 |
| 3 | CVE-2024-11635 (WordPress) | 实战最常见场景（CMS 插件），未认证直接 RCE |
| 4 | CVE-2024-47823 (Livewire) | 理解框架"自作聪明"的危害 |
| 5 | CVE-2026-54414 (FileRise) | 学习 TOCTOU / 验证时序漏洞 |

---

> 关联笔记：[文件上传漏洞体系总览](./2026-07-31-file-upload-vulnerability.md) | [解析漏洞](./2026-07-31-file-upload-parser-vulnerability.md) | [编辑器与 CMS 上传漏洞](./2026-07-31-file-upload-editor-cms.md)

> ⚠️ 声明：以上技术仅用于授权靶场和渗透测试授权目标。
