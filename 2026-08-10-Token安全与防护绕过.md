# 2026-08-10 Token 安全与防护绕过

> Token 常见于 CSRF 防御、身份认证、密码重置三个场景。Token 不是银弹，生成、存储、传输、校验、销毁每个环节都可能出问题。

---

## 一、是什么

Token 就是一个"令牌"——一串凭据，服务端生成，客户端拿着它证明自己有权限做某件事。在你学过的漏洞体系里，Token 出现在三个场景：

| 场景 | 作用 | 例子 |
|------|------|------|
| **CSRF Token** | 防跨站请求伪造 | 表单里的隐藏字段 `<input name="csrf_token" value="abc123">` |
| **认证 Token / JWT** | 替代 Session 维持登录态 | `Authorization: Bearer eyJhbGci...` |
| **重置密码 Token** | 证明你拥有这个邮箱/手机号 | `/reset?token=a1b2c3d4...` |

不管哪种，Token 的生命周期是一样的：

```
生成 → 分发 → 传输 → 校验 → 销毁
  │       │      │       │       │
  ▼       ▼      ▼       ▼       ▼
 可预测  泄露   劫持    未绑定   不失效
```

---

## 二、CSRF Token 的绕过

### 2.1 CSRF Token 是用来防什么的

先理清前提——CSRF Token 是正确的防御方案。它的逻辑：

```
请求页面时：
  服务端生成随机 token → 藏在 <form> 里 → 同时也存到服务端 Session

提交表单时：
  服务端比对：用户提交的 token == Session 中存的 token？
  → 是 → 请求来自本站页面，放行
  → 否 → 可能是跨站伪造，拒绝

为什么攻击者拿不到：
  攻击者的恶意页面发起的跨域请求，读不到目标站页面的 DOM，
  所以拿不到藏在 <form> 里的 token 值。
```

但以下几点能让 CSRF Token 失效。

### 2.2 Token 可预测

如果 CSRF Token 的生成算法太弱：

```
生成方式		能否预测
─────────────────────────────
MD5(时间戳)		能，时间戳可以被推测
Base64(用户名+固定盐)	能，自己算
纯自增数字		能，遍历
UUID 随机数		基本不能 ← 这才是正常的
```

测试方法：连续刷新页面，拿几个 token 对比：
```
第一次：csrf_token=1001
第二次：csrf_token=1002
第三次：csrf_token=1003
→ 自增数字，可直接伪造
```

### 2.3 Token 和用户未绑定

有些系统 CSRF Token 是全局共享的——所有用户用同一个池子里的 token。攻击者登录后拿到自己的 token，用在目标用户的请求里：

```
攻击者访问页面 → 拿到 token=xyz
构造 CSRF 页面 → 表单里写 token=xyz
目标用户访问恶意页面 → 表单提交带了 token=xyz
服务端检查：token=xyz 是否有效？是 → 放行
```

服务端只验了"token 是否在有效集合里"，没验"这个 token 是否发给当前用户的"。

### 2.4 Token 未关联操作

同一个 CSRF Token 能用来做任何操作——修改邮箱、修改密码、转账、下单。攻击者拿到任意一个接口的 token，就能用在其他敏感操作上。

### 2.5 Token 校验可绕过

最离谱的情况：

| 绕过方式 | 原理 |
|---------|------|
| 不传 token | 后端写了 `if isset` 而非 `if not isset → reject`，不传就不验 |
| 传空 token | `token=` → 后端 `if empty` 判断有问题 |
| 改请求方法 | POST 有 token 校验 → 改成 GET 同一 URL → 没有 token 校验 |
| 手动附一个任意值 | 后端只检查了 `token 字段是否存在`，没检查值是否正确 |

### 2.6 Token 泄露

```
GET /change_email?new_email=att@x.com&csrf_token=abc123
```

Token 在 URL 参数里，会被记录在：
- 浏览器历史
- Referer 头（当用户从该页面点链接跳转到别的站时）
- 服务器访问日志
- 各种分析/统计工具

这就是为什么 CSRF Token 应该放在 POST Body 或 Header 里，**绝对不应该出现在 URL 里**。

---

## 三、认证 Token / JWT 安全

### 3.1 JWT 是什么

```
JWT 结构：Header.Payload.Signature

Header:  {"alg": "HS256", "typ": "JWT"}        ← 指定签名算法
Payload: {"user_id": 10, "role": "user", "exp": 1700000000}  ← 实际数据
Signature: HMAC-SHA256(Header.Payload, secret)   ← 防篡改

完整 JWT：
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9
.eyJ1c2VyX2lkIjoxMCwicm9sZSI6InVzZXIifQ
.qwX-9Cg7...（签名）
```

**JWT 和传统 Session 的区别**：Session 里"你是谁"存在服务端，JWT 里"你是谁"存在客户端（Payload 里）。JWT 靠签名防止客户端篡改 Payload。

### 3.2 算法混淆攻击（最著名的绕过）

```
alg: "none"
```

JWT 标准支持 `alg: "none"`，即不签名。如果服务端没有显式禁止：

```
正常 JWT：Header 说用 HS256 签名 → 服务端验签名
攻击 JWT：Header 改成 "alg":"none" → Payload 改成 role=admin → 去掉签名部分
→ 服务端看到 alg:none → 跳过签名校验 → 直接接受 Payload
```

操作：在 [jwt.io](https://jwt.io) 上改 Header 的 `alg` 为 `none`，改 Payload 的 role 为 `admin`，去掉签名部分（但保留最后的点号）。

### 3.3 HMAC 密钥混淆

```
alg: "HS256"  ← HMAC SHA-256    → 用服务端密钥签名
alg: "RS256"  ← RSA SHA-256     → 用服务端私钥签名，客户端用公钥验
```

如果服务端同时支持 HS256 和 RS256：

```
1. 拿到网站公钥（通常公开在 /.well-known/jwks.json）
2. 把 JWT Header 改成 "alg":"HS256"
3. 用公钥当 secret 做 HMAC 签名（公钥就是服务端的 HMAC 密钥）
4. 服务端看到 alg:HS256 → 用 HMAC 方式校验
   → 服务端从数据库取出 HMAC 密钥
   → 但实际读出的是 RS256 的公钥
   → 用公钥去 HMAC 校验 → 通过！
```

这是一种密钥重用攻击——把公钥当 HMAC 密钥用。

### 3.4 密钥可爆破

HS256 用 HMAC 签名，如果密钥太弱：

```
# 用 hashcat 爆破 JWT 密钥
hashcat -m 16500 jwt.txt rockyou.txt
```

密钥如果是 `secret`、`password`、`123456` 这种，秒破。拿到密钥就能自己签名，任意修改 Payload。

### 3.5 JWT 敏感信息泄露

JWT 的 Payload 只是 Base64 编码（不是加密），任何人都能解码看内容：

```
eyJ1c2VyX2lkIjoxMCwicm9sZSI6InVzZXIifQ
→ Base64 解码 → {"user_id":10,"role":"user"}
```

如果开发者把敏感信息直接放 Payload 里（手机号、身份证号、密码哈希），任何人拿到 JWT 都能解出来。

### 3.6 JWT 不过期 / 无登出机制

```
"exp": 31536000000  ← 设置了过期时间，但是是一千年后
或者根本没有 exp 字段
```

而且 JWT 是无状态的——服务端不记录"这个 JWT 还在有效期吗"。用户点击"退出登录"只是前端删了 JWT，但这个 JWT 本身仍然有效，攻击者窃取后可以一直用。

缓解方案：JWT 配合黑名单（Redis 存已失效的 JWT ID），每次请求查一次。

---

## 四、重置密码 Token

这部分在找回与重置机制的笔记里已经详细写了，这里做总结性对照：

| 攻击手法 | 对应之前笔记的位置 |
|---------|------------------|
| Token 可预测（时间戳/MD5/自增） | 找回机制 → 4.2 凭证可预测 |
| Token 和用户未绑定 | 找回机制 → 4.3 身份绑定缺陷 |
| Token 在 URL 里泄露 | 本节 2.6 同原理 |
| Token 用完不销毁 | 找回机制 → 4.5 凭证复用 |

---

## 五、Token 防护机制的总览

Token 做得对应该满足这些条件：

| 环节 | 应该怎么做 |
|------|-----------|
| **生成** | 足够随机（UUID/密钥+随机盐）、长度足够（至少 32 字符） |
| **存储** | 服务端存储 Token → 用户 → 操作的绑定关系，不信任客户端只传一个裸 Token |
| **传输** | 只在 POST Body 或 Header 里传，绝对不在 URL 里 |
| **校验** | 校验 Token 存在 + 正确 + 属于当前用户 + 未过期 + 用于正确的操作类型 |
| **销毁** | 使用后立即删除 / 标记已使用，全局唯一，用过即废 |

这些也同时是验证码防护的要求——Token 和验证码本质上是同一件事：**一个有时效性的、一次性的、与服务端状态绑定的凭据**。

---

## 六、Token 和验证码的共性问题

对比你的两篇笔记，验证码和 Token 的漏洞模式高度重合：

```
验证码							Token
────────────────────────────────────────────────
验证码太短 / 可预测				Token 可预测（时间戳、自增）
验证码在响应里返回				Token 在 URL 里泄露
验证码与用户未绑定				Token 与用户未绑定
验证码可复用 / 验证后不销毁		Token 用完不销毁
验证码空值绕过				CSRF Token 不传就跳过
```

根因是完全一致的：**一个凭据从生成到销毁的生命周期中，每一步都只做了"有凭据"的检查，没做"凭据和操作者是绑定且只能使用一次"的检查。**

---

## 关联笔记

- [验证码安全与绕过技术](./2026-08-10-验证码安全与绕过技术.md) — 同天学的，验证码和 Token 漏洞模式高度重合
- [找回与重置机制的逻辑越权](./2026-08-09-找回与重置机制的逻辑越权.md) — Token 在重置密码场景的漏洞
- [CSRF 与 SSRF 漏洞详解](./2026-08-04-CSRF与SSRF漏洞详解.md) — CSRF Token 的原始应用场景
- [登录脆弱与认证缺陷](./2026-08-09-登录脆弱与认证缺陷.md) — JWT 伪造、Cookie 篡改

## 待实践

- [ ] jwt.io 上构造 alg:none 攻击，验证 JWT 绕过
- [ ] PortSwigger JWT 相关 Lab（Authentication 分类下）
- [ ] 回顾之前写的有 CSRF Token 的笔记，验证能不能用上面几种方式绕过
- [ ] 用 hashcat 爆一次弱密钥的 JWT（准备 rockyou.txt 字典）

> ⚠️ 声明：以上技术仅用于授权靶场、CTF 竞赛和渗透测试授权目标。
