# 2026-08-15 信息收集阶段遭遇 WAF 防护如何绕过

> 状态：🧪 理论未验证（老师只讲了思路和测试流程，本地 WAF 环境还没自己实操）

> 今天学的是 WAF 绕过的**第一部分：信息收集阶段**。信息收集（扫描、目录爆破、指纹识别）本身就经常被 WAF 拦，这一课讲的是**怎么判断被拦、怎么绕过去继续收集信息**。

---

## 一、是什么（我的理解）

### 1.1 一句话

信息收集阶段（路径爆破、扫描探测）被 WAF 拦截时，**先建一个本地 WAF 测试环境复现拦截**，抓包分析**被拦的是"数据包内容"还是"请求速度"**，再针对性绕过 —— 数据包问题改特征，速度问题降频率。

### 1.2 为什么信息收集就会遇到 WAF

渗透测试从信息收集就开始发大量请求（路径爆破 webpathbrute、目录扫描、指纹识别），这种高频、特征明显的请求很容易触发 WAF。**绕不过 WAF，后面的漏洞发现/利用根本没法继续** —— 所以这是 WAF 绕过的第一部分。

---

## 二、测试环境：三层 WAF 本地组合

先在本地搭一套和真实目标类似的 WAF 环境，验证绕过手法有效后再上真实目标：

| 层 | WAF | 说明 |
|----|-----|------|
| 1 | **安全狗 Safedog** | 本地先测这一层（免费，本地安装即可） |
| 2 | **阿里云 WAF** | 加一层云 WAF（免费版） |
| 3 | **宝塔（付费插件）** | 再叠一层 Web 面板的 WAF 插件 |

**流程**：先本地测安全狗 → 手法验证有效 → 加阿里云 → 再加宝塔，**层层叠加，逐步逼近真实目标环境**。避免直接在真实站点上反复试探触发封禁。

---

## 三、绕过分析：扫描探针机制

怎么知道被 WAF 拦了、拦的是哪一类？**用进程抓包工具抓取 webpathbrute（路径爆破工具）的数据包来分析**，拦截分两种：

| 现象 | 类型 | 含义 |
|------|------|------|
| **直接误报或无结果** | 数据包拦截 | 请求内容/特征直接命中规则，一上来就被拦 |
| **扫描一段时间后突然误报** | 速度过快拦截 | 请求频率超阈值，触发 CC 防护 |

**核心：先分清被拦的是"内容"还是"速度"，才能选对绕过手法。**

---

## 四、绕过手法：安全狗本地测试四连

| 场景 | 拦截类型 | 绕过手法 |
|------|---------|---------|
| 安全狗默认拦截（未开 CC） | 数据包问题 | **更改提交方式** 或 **模拟用户** |
| 安全狗默认拦截（开了 CC） | 数据包问题 | **延时** 或 **代理池** 或 **爬虫引擎** |

**逻辑**：
- **数据包拦截 → 改内容特征**：更换提交方式（GET/POST/Content-Type 等）、模拟用户（伪装成正常浏览器 UA/请求头）
- **速度过快拦截 → 降请求节奏**：延时（放慢请求间隔）、代理池（分散 IP）、爬虫引擎（让流量看起来像搜索引擎蜘蛛）

---

## 五、多层叠加后的限制（阿里云 / 宝塔）

绕过手法不是万能的，叠了不同 WAF 后效果不一样：

| WAF | 模拟爬虫引擎 | 可用绕过手段 |
|-----|------------|-------------|
| **阿里云** | ❌ 无法模拟搜索引擎爬虫 | 只能 **代理池或延时** |
| **宝塔** | ❓ 爬虫效果未知 | 延迟可以 ✅、代理池可以 ✅ |

**结论**：爬虫引擎这条路在阿里云上行不通；**代理池 + 延时是最通用的降速绕过组合**。

---

## 六、我的总结

- 信息收集阶段就会遇到 WAF，绕不过就没法继续后面的测试
- 遇到拦截**先本地搭环境复现**（安全狗 → 加阿里云 → 加宝塔），别在真实目标上硬试
- **判断拦截类型**：直接误报 = 数据包拦截；扫一会才误报 = 速度过快拦截
- **对症绕过**：数据包问题 → 换提交方式/模拟用户；速度问题 → 延时/代理池/爬虫引擎
- 多层 WAF 下爬虫引擎不一定好用（阿里云模拟不了），**代理池 + 延时最稳**

---

## 关联笔记

- [文件上传WAF绕过与防御](./2026-08-02-文件上传WAF绕过与防御.md) — 之前学的 15 种上传场景 WAF 绕过手法，和这课"改数据包特征"的思路呼应
- [SQL注入WAF绕过](./2026-07-30-SQL注入WAF绕过.md) — 注入场景的 WAF 绕过（双写/大小写/内联注释），同样是让 payload 不像攻击
- [XSS-WAF绕过与安全修复](./2026-08-04-XSS-WAF绕过与安全修复.md) — XSS 场景 12 种绕过，本质都是骗过 WAF 的检测

## 待实践

- [ ] 本地装安全狗，复现"数据包拦截 vs 速度过快拦截"两种现象
- [ ] 用 webpathbrute 抓一次包，对比不同提交方式 / UA / 延时下的拦截差异
- [ ] 实测代理池 + 延时组合对安全狗/宝塔的绕过效果

> ⚠️ 声明：以上技术仅用于 CTF 竞赛和授权靶场环境。
