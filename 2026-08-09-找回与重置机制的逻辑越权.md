# 2026-08-09 找回与重置机制的逻辑越权

> 找回/重置机制不只是密码重置，所有通过凭证证明"你就是这个账号的主人"的流程，每个环节都可能被绕过或越权。

---

## 一、是什么

找回与重置机制覆盖的场景比"忘记密码"更广：

- 找回密码 / 重置密码
- 找回用户名 / 账号
- 修改绑定手机号 / 邮箱
- 密保问题重置
- 账号申诉 / 人工找回

本质都是同一件事：**用户没法用正常方式证明身份了（忘了密码/丢了手机号…），系统需要另一套机制来确认"你就是你"**。这套机制的每个环节如果校验缺失，就构成逻辑越权——攻击者可以冒充别人走完流程，接管账号。

和之前学的越权（查看别人订单/数据）的区别：这里的越权直接导致**账号被接管**，危害更大。

---

## 二、产生原因

这套机制的根因和之前学的越权、支付篡改一脉相承：

1. **客户端传参被直接信任** — 用户身份（user_id/phone/email）、验证码、Token，后端没有和服务端状态强绑定
2. **状态机不完整** — 找回流程的每一步之间，缺失"上一步必须通过才能进下一步"的校验
3. **凭证生命周期管理缺失** — 验证码/Token 发了就永久有效，用完不销毁
4. **凭证强度不够** — 把安全寄托在四位数字或可预测的 Token 上

---

## 三、攻击分类总览

```
找回 / 重置机制 攻击面
│
├── 一、信息泄露类
│   ├── 用户名/账号枚举
│   ├── 验证码在 HTTP 响应中返回
│   ├── 绑定信息部分泄露（脱敏手机号/邮箱可反推）
│   └── 错误信息过度暴露
│
├── 二、凭证可预测 / 可爆破类
│   ├── 短信/邮箱验证码太短且无次数限制
│   ├── 验证码有效期过长不销毁
│   ├── 重置 Token 可预测
│   ├── 密保问题答案空间太小
│   └── 重置链接可遍历
│
├── 三、身份绑定缺陷类 ← ★ 逻辑越权核心
│   ├── 凭证与用户未绑定
│   ├── 修改接收目标（手机/邮箱）
│   ├── 修改操作对象（user_id）
│   └── 凭证跨用户复用
│
├── 四、流程跳过类
│   ├── 跳过验证步骤直达重置页
│   ├── 修改绑定不需要验证旧凭据
│   └── 不校验原密码直接改
│
├── 五、凭证复用类
│   ├── 重置 Token 用完不失效
│   ├── 验证码多次有效
│   └── 旧重置链接不销毁
│
└── 六、资源滥用类
    ├── 短信/邮件轰炸
    └── 并发竞争绕过后端状态检查
```

---

## 四、六大类逐个展开

### 4.1 信息泄露类

#### 用户名/账号枚举

输入邮箱或手机号后，接口返回不同提示：

```
"该账号不存在"   vs   "验证码已发送至您的手机"
```

两种响应直接暴露了账号是否注册。和登录页的用户枚举同理，拿到存在账号再去爆破或社工。

#### 验证码在 HTTP 响应里返回

```
POST /reset/send_sms
Body: phone=13800001111

Response: {"code": 4729, "message": "验证码已发送"}
```

验证码直接写在响应 body 里，攻击者不需要看短信就知道验证码是什么。通常是因为前端做了"输入验证码后本地比对"的逻辑，但忘了去掉调试代码或者根本没意识到这是漏洞。

#### 绑定信息部分泄露

```
输入手机号 → 返回："验证码已发送至 138****1234"
输入邮箱   → 返回："验证码已发送至 adm***@example.com"
```

脱敏不彻底，配合常见手机号段/邮箱格式能反推出完整信息。

#### 错误信息过度暴露

```
/reset?token=xxx&step=2 → 报错：
"SQL error: Table 'users' not found"
"token format invalid, expected 32-char hex"
"JWT decode error: invalid signature"
```

报错里泄露了技术栈、Token格式、加密方式，攻击者可以针对性地构造攻击。

---

### 4.2 凭证可预测 / 可爆破类

#### 验证码太短且无次数限制

| 验证码长度 | 组合总数 | 无限制下爆破耗时（Burp Intruder 单线程） |
|-----------|---------|---------------------------------------|
| 4 位纯数字 | 10,000 | 秒级 |
| 6 位纯数字 | 1,000,000 | 数分钟（10线程） |
| 6 位数字+字母 | 约 21 亿 | 不可行 |

爆破操作：

```
1. 抓包 POST /reset/verify → phone=138xxxx&code=0000
2. 发到 Intruder，标记 code 为爆破位置
3. Payload type: Numbers, From 0 To 9999, Step 1
4. 看 Status Code / Response Length / 响应关键字判断成功
```

⚠️ 有些系统看似有次数限制（比如 5 次错误锁定），但重新发一条短信验证码就重置了计数——实质等于没有限制。

#### 验证码有效期过长

发了一个验证码，几个小时甚至几天都不失效，给爆破提供了充足时间窗口。

#### 重置 Token 可预测

邮箱收到的重置链接：
```
/reset?token=abc123
```

如果 token 的生成逻辑是以下之一，就可以预测：

| 生成方式 | 怎么破 |
|---------|--------|
| 时间戳 | 根据邮件发送时间推算出时间戳范围 |
| MD5(用户ID) | 用户ID已知或者可枚举，直接算 |
| 自增数字 | 直接遍历 token=001, 002, 003... |
| 未加盐的 MD5(用户名) | 用户名已知，自己算 |

#### 密保问题答案太弱

常见密保问题：
```
"你出生的城市是？"
"你母亲的名字是？"
"你最喜欢的颜色是？"
```

答案空间很小。城市名全国几百个，姓氏常见几百个，颜色几十种——都可以爆破。而且这些信息很多时候能从社交网络收集到。

#### 重置链接可遍历

```
/reset?token=000001 → 404
/reset?token=000002 → 200  ← 命中了一个还在有效期内的 Token
```

自增 Token 可以直接遍历，找到有效 Token 就进重置页面。

---

### 4.3 身份绑定缺陷类 ★ 核心

这是真正意义上的逻辑越权——功能本身正常工作，但凭证和操作目标没绑死。

#### 凭证与用户未绑定

```
POST /reset/verify
Body: phone=13800001111&code=4729  ← 系统发给 13800001111 的验证码

攻击者改成：
Body: phone=13912345678&code=4729  ← 用同一个验证码去验证另一个手机号
```

后端只检查了"t_user 表中是否存在 code=4729 且未过期的记录"，没检查"这条验证码是不是发给 phone=13912345678 的"。攻击者拿到任意一个有效验证码，就能验证任意手机号。

#### 修改接收目标

```
POST /reset/send_sms
Body: phone=13800001111  ← 目标是重置这个账号

攻击者改成：
Body: phone=13912345678  ← 验证码发到自己手机上
```

如果后端没有把目标账号和重置请求者的身份绑在一起，那么：
- 攻击者在找回页面输入**别人的账号**（比如 admin@xxx.com）
- 抓包把接收验证码的手机号改成**自己的**
- 验证码发到自己手机上 → 输入验证 → 成功重置别人的密码

#### 修改操作对象

```
POST /reset/new_password
Body: user_id=10&new_password=abc123  ← user_id 来自前端

攻击者改成：
Body: user_id=1&new_password=hacked   ← 直接重置了管理员(user_id=1)的密码
```

后端没有从 Session/Token 中取当前正在被重置的账号，而是直接用了前端传的 user_id。

#### 凭证跨用户复用

```
A 发起重置请求 → 收到验证码 1234
B 发起重置请求 → 收到验证码 5678

B 验证时提交：
Body: phone=B的号码&code=1234  ← 用的是发给 A 的验证码
→ 通过了？
```

验证码应该是一条记录（code + 目标用户 + 过期时间 + 是否已验证），后端查的是"目标用户 = B 且 code = 1234"，如果这一行存在就不该通过——但实际上可能后端只查了"code = 1234 且在有效期内"。

---

### 4.4 流程跳过类

#### 跳过验证步骤直接到重置页面

```
找回流程三步：
Step1: 输入账号 → /reset/step1
Step2: 输入验证码 → /reset/step2
Step3: 设置新密码 → /reset/step3
```

攻击者在 Step1 后，直接访问 Step3 的 URL：
```
/reset/step3?user_id=10
→ 设置新密码页面直接打开了，不需要验证码
```

后端没有校验"当前请求是否已经通过了 Step2"。

#### 修改绑定不需要验证旧凭据

```
修改手机号：
新手机号：139xxxxxxxx
→ 直接改成功了，不需要验证旧手机号
```

跳过旧凭据验证，一旦账号被盗就难以找回——攻击者改了绑定手机号，原主连找回密码都做不了。

#### 不校验原密码直接改密码

```
已登录状态下改密码：
POST /user/change_password
Body: new_password=abc123
→ 成功。没有任何验证当前密码的步骤。
```

攻击者拿到一个已登录的会话（比如 XSS 偷了 Cookie），直接就能改密码，不需要知道原密码。

---

### 4.5 凭证复用类

#### 重置 Token 用完不失效

```
用户点了邮箱里的重置链接 → 设置新密码 → 完成

攻击者再次使用同一个链接：
/reset?token=xxx
→ 还能进入重置页面 → 再次修改密码
```

Token 在密码重置成功后没有被标记为"已使用"。攻击者只要截获或猜到这个 Token，就能在用户重置后再次重置。

#### 验证码多次有效

```
POST /reset/verify
Body: phone=138xxxx&code=4729
→ 验证成功，进入设置密码页

再次发送：
POST /reset/verify
Body: phone=138xxxx&code=4729
→ 再次验证成功
```

验证成功后验证码没被销毁。如果用户重置完成后退出了，攻击者可以重放同一组参数再走一遍。

#### 旧重置链接不销毁

用户发起了**两次**重置密码请求，收到了两封邮件（Token_A 和 Token_B）。用户用 Token_B 重置成功，但 Token_A 仍然有效。Token_A 如果被截获就能再次重置。

---

### 4.6 资源滥用类

#### 短信/邮件轰炸

```
POST /reset/send_sms
Body: phone=13800001111

攻击者用 Intruder 反复发送，一秒几十次：
→ 对方手机被验证码短信轰炸骚扰
→ 平台短信费用被刷爆
```

没有针对"同一手机号/同一 IP/同一 Session"的发送频率限制。

#### 并发竞争

```
修改绑定手机号：
Step1: 给旧手机发验证码 → 验证旧手机号所有权
Step2: 输入新手机号 → 给新手机发验证码 → 验证新手机号所有权
Step3: 提交修改

并发攻击：
同时发两个"提交修改"请求 → 都绕过了 Step1 的验证
→ 因为后端查"Step1 状态"的时候两个请求同时到达，都读到了"已通过"
```

和支付场景里的并发提现是一回事——时间窗口内有并行请求，读到了同一份状态。

---

## 五、测试方法

测试找回/重置机制时的方法论：

### 5.1 先走一遍完整流程

```
1. 用正常账号走完整个重置流程，Burp 全程抓包
2. 标记每个请求的接口路径、参数、响应
3. 画出流程图：Step1 → Step2 → Step3 → ... 
```

### 5.2 逐个问题排查

| 分类 | 测试的问题 |
|------|-----------|
| 信息泄露 | 输入不存在/存在的账号，响应有什么不同？验证码/Token 有没有出现在别的地方？ |
| 凭证强度 | 验证码几位？有没有尝试次数限制？重发验证码计数会重置吗？Token 长什么样？ |
| 身份绑定 | 把验证码/手机号/user_id 换成别的值，还能走通吗？ |
| 流程完整性 | 跳过某一环节直接访问下一步 URL，能进去吗？ |
| 凭证回收 | 同一个验证码/Token 用两遍行不行？重置完成后旧 Token 还能用吗？ |
| 并发/频率 | 一秒钟发十条短信？同时两个重置请求会怎样？ |

### 5.3 用 Burp Suite 辅助

```
Repeater   → 改单个参数重放，观察结果
Intruder   → 爆破验证码 / 遍历 Token
Autorize   → 换 Cookie/用户身份重放请求，对比响应
```

---

## 六、防御思路

### 6.1 防信息泄露

- 不管账号存不存在，统一返回"如果该账号已注册，验证码已发送"（不区分存在/不存在）
- 验证码绝不在任何 HTTP 响应中返回
- 手机号/邮箱脱敏显示：`138****1234` 且脱敏位数要足够多

### 6.2 防凭证爆破

- 验证码至少 6 位，有效期 5-10 分钟，超过 3-5 次错误立即失效并需要重新获取
- 错误计数绑定到"验证码本身"，重发验证码也不重置计数
- Token 用 `UUID 随机数 + HMAC 签名`，不可预测
- 密保问题：允许用户自定义问题，或者把答案当"密码"处理（加盐哈希存储，不支持模糊匹配）

### 6.3 防身份绑定缺陷

```python
# ✅ 正确做法：凭证（code）和 用户身份（phone）和 操作目标（target_user）三者绑定

# 生成验证码时：
code_record = {
    "code": "4729",
    "receiver": "138xxxx",          # ← 发给谁
    "target_user_id": 1001,         # ← 要重置哪个账号
}

# 验证时：
def verify_code(target_user, receiver, code):
    record = get_code_record(code=code)
    if not record:
        return False  # 验证码不存在
    if record.receiver != receiver:
        return False  # 不是发给你的（别人在变更手机号码劫持漏洞）
    if record.target_user_id != target_user.id:
        return False  # 不是用来重置这个账号的
    if record.expired:
        return False  # 过期了
    return True
```

### 6.4 防流程跳过

- 后台用状态机管理重置流程（Session 中记录当前处于哪个步骤）
- 每个步骤的接口先检查"上一步是否已完成"
- 关键操作（改密码、改绑定）必须验证原凭据

### 6.5 防凭证复用

- 验证码/Token 验证成功或过期后立即从存储中删除
- 重置密码成功后，销毁该用户所有未使用的重置 Token

### 6.6 防资源滥用

- 同一手机号/邮箱/设备/Session/IP：1 分钟最多 1 条
- 图形验证码放在"发送验证码"按钮之前（防止脚本自动化）
- 并发敏感操作加锁（数据库行锁、Redis 分布式锁）

---

## 总结

这张表覆盖六大类攻击和对应的防御要点：

| 类别 | 核心问题 | 防御关键词 |
|------|---------|-----------|
| 信息泄露 | 接口返回了不该返回的信息 | 统一提示、不区分存在/不存在 |
| 凭证可爆破 | 验证码/Token 太弱又可猜 | 长度+有效期+次数限制+不可预测 |
| 身份绑定缺陷 | 凭证和用户没绑死 | 验证码绑定 receiver + target_user |
| 流程跳过 | 状态机缺失 | Session 记录步骤，每步校验上一步 |
| 凭证复用 | 用过的凭证不销毁 | 验证即删除，重置后销毁全部旧 Token |
| 资源滥用 | 无频率/并发限制 | 频率限制 + 图形验证码 + 并发锁 |

---

## 关联笔记

- [登录脆弱与认证缺陷](./2026-08-09-登录脆弱与认证缺陷.md) — 同一天学的，认证入口的攻击面，和找回机制共享"爆破"和"凭证绕过"的思路
- [越权漏洞详解](./2026-08-08-越权漏洞详解.md) — 身份绑定缺陷的本质就是越权（水平越权操作别人的账号）
- [支付篡改与业务逻辑漏洞](./2026-08-09-支付篡改与业务逻辑漏洞.md) — 并发竞争、流程跳过是共通的攻击手法

## 待实践

- [ ] 找一个有完整找回密码流程的靶场（DVWA / Pikachu / PortSwigger Lab），按上面的六大类逐个测试
- [ ] 用 Burp Intruder 爆破一次四位验证码，体会"秒出结果"意味着什么
- [ ] 尝试复现"修改接收目标"漏洞（发验证码时把 phone 改成自己的）

> ⚠️ 声明：以上技术仅用于授权靶场、CTF 竞赛和渗透测试授权目标。未经授权对他人系统进行账号重置和越权测试属于违法行为。
