# 2026-08-09 登录脆弱与认证缺陷

> 认证缺陷不只是弱密码，登录流程上的每一个环节都可能是突破口。

---

## 一、产生原因

以前觉得登录安全就是密码复不复杂，学完才发现，认证是一个多环节的流程，每个环节校验缺失都可能被绕过：

```
注册 → 登录 → 记住登录态 → 忘记密码 → 多因素验证
  │       │         │            │            │
  ▼       ▼         ▼            ▼            ▼
弱口令   爆破     Cookie篡改    重置流程缺陷   MFA跳过
```

本质上都是同一个问题：系统没有正确验证"你就是你声称的那个人"。

和越权的区别——认证缺陷是"进门"的问题，越权是"进门之后能干多少事"的问题。实战中通常是先绕过认证拿到身份，再用这个身份去测越权，两者是递进关系。

---

## 二、弱口令爆破

### 原理

很多系统的管理员或用户用的密码太简单，字典一跑就能跑出来。

### Burp Suite 实操步骤

1. 浏览器设代理 → 打开靶场登录页 → 随便输个 `admin/123` 提交
2. Burp Proxy → HTTP history 找到 `POST /login` 的请求
3. 右键 → Send to Intruder
4. 切到 Positions 标签，点 Clear §，选中要爆破的字段（一般是密码），点 Add §
5. Attack type 选择：
   - **Sniper**：一次爆一个位置，已知用户名只爆密码时用
   - **Cluster bomb**：两个位置同时遍历，用户名和密码都不确定时用（比如 10 个用户名 × 100 个密码 = 1000 次请求）
6. 切到 Payloads 标签，Payload type 选 Simple list → Load 字典文件或直接 Paste 常见弱口令
7. Resource Pool 新建资源池，线程 5-10（线上授权测试要更低）
8. 点 Start attack，看攻击窗口：
   - **Status Code**：登录成功通常是 302 重定向，失败是 200
   - **Response Length**：成功和失败的返回包长度通常不一样
   - 找到与众不同的那条 → 右键 Send to Repeater 确认是不是真的登录成功了

一个小技巧：先在浏览器故意输错一次密码，看失败响应长什么样（状态码、包长度），再去攻击结果里找和这个**不一样的**，就是成功的那条。

### 常见弱口令

```
admin/123456    admin/admin     root/root
admin/password  test/test       guest/guest
```

---

## 三、用户名枚举

### 原理

从登录失败的响应差异中判断用户名到底存不存在。拿到存在的用户名再去爆密码，比盲爆效率高很多。

### 判断依据

| 失败响应 | 说明 |
|----------|------|
| "用户名不存在" | 用户名不在系统里 |
| "密码错误" | 用户名存在，只是密码不对 |
| 响应时间不同 | 用户名存在时走密码校验逻辑，响应可能慢几十毫秒 |
| HTTP 状态码不同 | 存在/不存在返回不同状态码 |

---

## 四、验证码绕过

按照防御的薄弱程度从低到高排：

### 4.1 验证码不刷新

同一个验证码可以反复用。抓一次包拿到验证码，Intruder 里把验证码字段写死，只换密码字典跑——验证码永不过期。这是最低级的错误。

### 4.2 验证码前端校验

客户端 JS 判断验证码对不对。抓包后直接扔掉验证码字段重放，或者在浏览器控制台改 JS 让它永远返回 true。后端根本没验，前端校验只是摆设。

### 4.3 验证码写在 Cookie 里

```
Set-Cookie: captcha_code=ABCD   ← 验证码答案直接暴露在 Cookie 里
```

后端逻辑只是比对 Cookie 里的值和表单提交的值是否一致。构造请求：Cookie 写 `captcha_code=AAAA`，表单也填 `AAAA`，两者一致就通过——攻击者完全控制这两个值。

### 4.4 验证码可 OCR 识别

四位纯数字验证码，OCR 识别率很高，配合 Python 脚本自动识别后爆破。

---

## 五、Cookie / 记住我 篡改

### 原理

如果 Cookie 里存了身份信息且可以被篡改，那密码、验证码这些都不需要了——直接改 Cookie 就行。

### 常见情况

```
# 明文存角色
Cookie: role=user   → 改成 role=admin
Cookie: isAdmin=0   → 改成 isAdmin=1

# "记住我" 存了 Base64 编码的用户信息
Cookie: remember=eyJ1c2VySWQiOjEwLCJyb2xlIjoidXNlciJ9
# Base64 解码：{"userId":10,"role":"user"}
# 把 role 改成 admin，重新 Base64 编码塞回去

# JWT 密钥太弱
# 如果密钥是 secret123 这种，可以直接在 jwt.io 上伪造 Token
```

---

## 六、密码重置流程缺陷

不需要知道原密码，只要走通"重置密码"流程就能接管账号。

```
正常重置流程：输入用户名/邮箱 → 收验证码 → 验证 → 设置新密码
                                          ↑ 攻击面集中在这里
```

### 攻击点

| 攻击点 | 具体利用方式 |
|--------|-------------|
| 验证码太短且不过期 | 6 位数字 = 100 万种组合，Intruder 跑几分钟就能爆出来 |
| 验证码在响应里返回 | `{"code":123456}` 直接写在 HTTP 响应里，等于白验 |
| 修改重置目标 | POST 里 `phone=别人手机号` 改成自己的，重置的是别人账号但验证码发到我手机上 |
| 重置链接不失效 | `/reset?token=xxx` 用完还能用第二次、第三次 |
| 安全问题太弱 | "最喜欢的颜色是？" → 常见颜色就几十种，可以爆破 |

---

## 七、MFA 多因素认证跳过

MFA 是密码之后的额外防线，但前后逻辑如果没衔接好就可以绕过去。

实际测试中碰到过的绕过方式：

- **跳过验证页面**：输完密码不经过 `/mfa/verify`，直接 URL 跳到 `/dashboard`，后端可能没在 `/dashboard` 页面校验 MFA 状态
- **篡改 MFA 状态**：后端返回 `mfa_verified: false`，如果前端判断这个字段决定是否显示验证页面，抓包把响应改成 `true`
- **爆破 MFA 验证码**：6 位数字，没有尝试次数限制就能跑 Intruder

---

## 八、测试流程总结

拿到一个网站的登录功能，我目前的测试顺序：

```
1. 先试弱口令 → admin/admin, admin/123456 这些最常见的
2. 没验证码且没有失败次数限制 → Intruder 上字典爆破
3. 有验证码 → 测能不能绕过（抓包重放、看 Cookie、前端校验）
4. 看 Cookie 里有没有身份字段可改（role、isAdmin、userType 等）
5. 看忘记密码流程有没有缺陷
6. 清 Cookie 直接访问登录后的 URL → 看后端有没有拦住
```

---

## 九、和越权漏洞的关系

```
认证缺陷                         越权
  │                                │
  │ 破了认证拿到身份                 │
  ├─────────────────────────────→  │
  │                                │ 用这个身份访问不该访问的接口
  │                                │ 改 ID 看别人的数据
  ▼                                ▼
"你是谁"被绕过了               "你能干什么"也没守住
```

实战中是递进关系：先用认证缺陷手段进系统（爆破 / Cookie 篡改 / MFA 绕过），拿到合法 Cookie 后，再用这个 Cookie 去测水平越权和垂直越权。

---

## 关联笔记

- [越权漏洞详解](./2026-08-08-越权漏洞详解.md) — 认证缺陷的下一步，进去之后干什么
- [支付篡改与业务逻辑漏洞](./2026-08-09-支付篡改与业务逻辑漏洞.md) — 同天学的，都属于逻辑漏洞大类

## 待实践

- [ ] DVWA Brute Force 模块 Low→High，配合 Burp Intruder
- [ ] Pikachu 验证码绕过 / Cookie 篡改相关关卡
- [ ] 找一个有密码重置流程的靶场，把上面几个攻击点测一遍

> ⚠️ 声明：以上技术仅用于授权靶场、CTF 竞赛和渗透测试授权目标，未经授权对他人系统进行爆破和登录测试属于违法行为。
