# 2026-08-10 验证码安全与绕过技术

> 验证码本身也是逻辑漏洞的高发区——问题不在于"机器能不能识别图片"，而在于"生成、传输、校验、销毁这个流程上每一步都可能掉链子"。

---

## 一、是什么

验证码（CAPTCHA）的设计目标：区分人类和机器，防止自动化攻击。常见类型：

| 类型 | 例子 | 现在还用吗 |
|------|------|:---:|
| 文本验证码 | 扭曲的 4-6 位字母数字 | 老旧系统还在用 |
| 数学计算 | "3 + 5 = ?" | 部分站点的土法验证码 |
| 图片选择 | "点击所有包含红绿灯的图片" | Google reCAPTCHA |
| 滑块 | 拖动滑块拼合图片 | 国内主流，极验/网易易盾 |
| 短信/邮箱验证码 | 6 位数字 | 登录/找回密码场景广泛使用 |

---

## 二、验证码的工作流程

```
        ┌──────────────────────────────────────┐
        │          Step 1: 生成                 │
        │  服务端随机生成 → 存入 Session/缓存     │
        └────────────┬─────────────────────────┘
                     ▼
        ┌──────────────────────────────────────┐
        │          Step 2: 传输                 │
        │  图片发给前端 / 短信发到手机            │
        └────────────┬─────────────────────────┘
                     ▼
        ┌──────────────────────────────────────┐
        │          Step 3: 用户输入             │
        │  人类看图 → 输入 → 提交                │
        └────────────┬─────────────────────────┘
                     ▼
        ┌──────────────────────────────────────┐
        │          Step 4: 校验                 │
        │  服务端比对：用户输入 == 存储的答案？    │
        └────────────┬─────────────────────────┘
                     ▼
        ┌──────────────────────────────────────┐
        │          Step 5: 销毁                 │
        │  验证成功后标记已使用 / 直接删除         │
        └──────────────────────────────────────┘
```

**核心认知**：这五个步骤，每一步实现不到位都会产生漏洞。不是一定需要 AI 识别图片才算绕过。

---

## 三、六大类攻击面

### 3.1 生成阶段的漏洞

#### 验证码太简单，可预测

```
生成逻辑：随机四位数字 → 0000-9999
问题：组合空间只有 10000，没有尝试次数限制时直接爆破
```

更夸张的情况——验证码根本不是随机的：
```
/vcode.php 返回的验证码 = 当前分钟数 + 固定偏移
/vcode.php?t=1234 返回的验证码 = substr(md5(1234), 0, 4)
→ 完全可预测，不需要 OCR
```

#### 验证码答案在客户端可计算

```javascript
// 前端生成验证码，答案存在全局变量里
var captcha_answer = 'A7X2';
// 或者存在 Cookie、localStorage、隐藏表单字段里
```

打开浏览器控制台 → 输入 `captcha_answer` → 答案直接显示。

---

### 3.2 传输阶段的漏洞

#### 验证码在 HTTP 响应里返回

```
POST /send_sms
Body: phone=13800001111

Response: {"code": 4729, "message": "验证码已发送"}
```

短信确实发了，但验证码也同时写在响应 Body 里了。这是最经典的泄露方式——测试环境下加了个 log 忘删，就上线了。

#### 验证码在 Cookie 里

```
Set-Cookie: vcode=A7X2; Path=/
```

验证码的值直接由服务端写在 Cookie 里，前端 JS 读 Cookie 来做"本地比对"。后端校验时只比对"请求里的 vcode 字段"和"Cookie 里的 vcode 值"是否一致。

攻击：请求里 vcode 写 `AAAA`，Cookie 里也写 `vcode=AAAA` → 必然通过。

#### 验证码图片的 URL 泄露了答案

```
<img src="/captcha.php?code=4729">
```

URL 参数里直接带了验证码的值。可能是调试接口没关，也可能是开发为了方便直接把答案嵌在 URL 里。

#### 验证码在隐藏表单字段里

```html
<input type="hidden" name="real_captcha" value="A7X2">
<input type="text" name="user_input" placeholder="请输入验证码">
```

后端比对 `$_POST['user_input']` 和 `$_POST['real_captcha']`——两个值都来自客户端，攻击者完全可控。

---

### 3.3 校验阶段的漏洞

#### 验证码不刷新 / 可复用（最经典）

```
整个 Session 生命周期内，Session['vcode'] 不刷新：
  1. 浏览器打开一次登录页 → PHP 生成验证码 A7X2，写入 Session
  2. 用户用 A7X2 成功登录一次 → $_SESSION['vcode'] 还在
  3. Intruder 带同一个 PHPSESSID + 写死的 vcode=A7X2 → 畅跑
```

原因：生成验证码时写了 `$_SESSION['vcode'] = 'A7X2'`，但验证成功后没有 `unset($_SESSION['vcode'])`。

#### 前端校验 — 后端没验

```javascript
// 前端代码
if (user_input !== generated_captcha) {
    alert("验证码错误");
    return false;
}
// 阻止表单提交
```

但抓包修改请求绕过前端 JS 后，后端代码里根本没有读 vcode 参数，直接处理了登录逻辑。

#### 验证码空值绕过

```
POST /login
Body: username=admin&password=123456&vcode=

→ 把 vcode 字段删掉或留空，成功绕过
```

原因：后端代码可能是：

```php
if (!empty($_POST['vcode'])) {   // ← 只有传了才验
    if ($_POST['vcode'] != $_SESSION['vcode']) {
        die("验证码错误");
    }
}
// 没传 → 跳过校验 ↑
```

#### 验证码与用户身份未绑定

和你学过的"身份绑定缺陷"同一个道理。验证码生成时绑定的应该是"这个验证码发给谁的、用于什么操作"，但如果只存了 code 没存 target：

```
攻击者给自己请求验证码 → 收到 1234
攻击者在重置管理员的流程中输入 1234 → 通过
→ 因为后端只查了"code=1234 且在有效期内"，没查"这个 code 是发给谁的"
```

#### 尝试次数限制可绕过

常见的绕过姿势：

| 绕过方式 | 怎么操作 |
|---------|---------|
| 重新获取验证码重置计数 | 错了 4 次 → 点"重新发送验证码" → 计数清零 → 再试 |
| 换 Session 重置计数 | 清 Cookie 重开浏览器 → 新的 Session → 计数清零 |
| 换 IP 绕过 | 挂了代理 → 后端按 IP 限频没生效 |
| 用另一个账号做跳板 | A 账号试几次 → 换 B 账号试 → 同一手机号/验证码绑定的是目标用户 |

---

### 3.4 销毁阶段的漏洞

#### 验证成功后不销毁

验证码验证通过后仍然有效，可以：
- 同一个验证码用于多次验证（重复重置密码）
- 同一个验证码在多个接口复用（登录和修改手机号用同一个验证码校验逻辑）

#### 过期不销毁

验证码有效期设置太长或根本没有过期机制。即使设置了"5 分钟过期"，但过了 5 分钟只是前端提示"验证码已过期"，后端数据库/缓存里那条记录还在，抓包重放仍然通过。

---

### 3.5 模式识别 / OCR 绕过

当上面的逻辑漏洞都堵死了，最后的手段才是靠识别。

#### 工具

| 工具/库 | 适用场景 | 说明 |
|---------|---------|------|
| **ddddocr** | 文本验证码 | Python 库，一行代码识别四位字母数字，正确率看复杂度 |
| **Captcha-killer-modified** | Burp 联动 | 把验证码图片转发到本地 OCR 服务，结果自动填入 Intruder |
| **Tesseract OCR** | 通用 OCR | 对扭曲不大的文字效果还行，但不如专用模型 |
| **muggle_ocr** | 通用验证码 | 另一个 Python OCR 库，纯离线 |

#### 基本思路

```python
import ddddocr

ocr = ddddocr.DdddOcr()

# 从响应里拿到验证码图片的 bytes
captcha_bytes = requests.get("http://target/captcha.php", cookies=session).content
result = ocr.classification(captcha_bytes)  # → "A7X2"

# 然后带识别结果去发登录请求
```

#### 更复杂的情况

现在很多验证码做了干扰线、扭曲、粘连，ddddocr 的识别率会掉。应对方式：

- **预处理图片**：灰度化 → 二值化 → 去噪点 → 再去 OCR
- **多次取样取最优**：抓十张验证码图片，识别最简单的那张（有些验证码随机性大，偶尔生成特别简单的）
- **打码平台**：批量识别交给真人，一条几分钱

---

### 3.6 短信轰炸

虽然不属于"绕过验证码"，但是验证码系统的附带问题。同一个手机号在一分钟内被调了几十次短信发送接口，造成骚扰和费用损耗。

防御方式：同一手机号 60 秒内只发 1 条，同一 IP 做频率限制，加图形验证码挡在"发送短信"之前。

---

## 四、验证码攻击的实战优先级

拿到一个带验证码的登录/重置接口，攻击顺序：

```
1. 先测能不能绕过
   → 删掉 vcode 参数 / 留空 / 乱填 → 看会不会过
   → 看 Cookie 里有没有泄露验证码答案

2. 再测能不能复用
   → 手动输一次正确验证码 → 抓包 → 同一个 Session + 同一个 vcode → Intruder 跑密码

3. 再测能不能爆破
   → 验证码是 4 位数字且没次数限制 → Intruder 跑 0000-9999

4. 最后才考虑用 OCR
   → 识别率低时配合多次取样
```

优先顺序是：**绕过 > 复用 > 爆破 > OCR 识别 > 打码平台**。越往前成本越低、成功率越高。

---

## 五、防御方案

每一类问题的对应防御：

| 攻击面 | 防御措施 |
|--------|---------|
| 可预测/太简单 | 至少 6 位随机，加字母，20 秒有效期 |
| 响应泄露 | 验证码决不在任何 HTTP 响应/Cookie/HTML 隐藏字段中出现 |
| 前端校验 | 前端只是 UX，校验必须在后端 |
| 空值绕过 | 后端无条件校验 vcode 字段，空值视为验证失败 |
| 复用 | 验证成功后立即 `unset(SESSION['vcode'])` |
| 身份未绑定 | 校验时同时查 code + target_user + receiver + 操作类型 |
| 次数限制绕过 | 错误计数绑定到 target_user（不是 Session/IP），重发也不清零 |
| OCR 识别 | 扭曲+干扰线+粘连+背景噪点，增加机器识别难度 |
| 短信轰炸 | 图形验证码挡在发短信之前 + 同手机号频率限制 |

---

## 关联笔记

- [登录脆弱与认证缺陷](./2026-08-09-登录脆弱与认证缺陷.md) — 验证码绕过是爆破登录的前置步骤
- [找回与重置机制的逻辑越权](./2026-08-09-找回与重置机制的逻辑越权.md) — 短信验证码爆破、身份绑定缺陷
- [Token 安全与防护绕过](./2026-08-10-Token安全与防护绕过.md) — 同天学的，验证码和 Token 都是"凭证"，有很多共性问题

## 待实践

- [ ] Pikachu 验证码绕过（client）（server）两个关卡
- [ ] 装 ddddocr 跑一遍，对比纯数字、字母、混合验证码的识别率
- [ ] PortSwigger Authentication Lab 中涉及 brute-force protection bypass 的几个 Lab

> ⚠️ 声明：以上技术仅用于授权靶场、CTF 竞赛和渗透测试授权目标。
