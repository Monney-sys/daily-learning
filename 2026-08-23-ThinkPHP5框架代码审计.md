# 2026-08-23 代码审计④：ThinkPHP5 框架代码审计

> 状态：🧪 理论，TP5.0.23 老版本源码已下（`D:\php\audit-targets\`），待实操
> 代码审计板块第四课：框架审计落到**具体框架 ThinkPHP5**。课程核心：**三种写法分类 → 版本确认 → 版本 CVE → 内置过滤问题**。把第二课（框架 MVC 通用）的知识点在一个框架上落地。

---

## 一、是什么（我的理解）

### 1.1 一句话

TP5 审计 = **先找入口**（`public/index.php` → 路由 → 控制器）→ **按写法分三类** → 自写代码用第一课的追踪法（Source→Sink），框架层走「版本确认 → 查已知 CVE → 利用」。

### 1.2 三种写法（课程分类，决定审计策略）

| 写法 | 含义 | 审计策略 |
|------|------|---------|
| 自写写法 | 未引用框架内置过滤，全自己写 | **第一课的追踪法**，跟审无框架一样 |
| 不安全写法 | 部分用了框架过滤，部分裸写 | 追踪法 + 判断框架过滤覆盖范围，**找漏网之鱼** |
| 规则写法 | 完全按框架规范，引用内置过滤 | 业务层基本安全 → 查**框架版本 CVE** + 依赖组件漏洞 + 内置过滤本身 |

### 1.3 我的理解验证（用户原话 → 结论）

> "先找入口，然后分情况：自己写的代码 / 没有完全按照框架写的 / 完全按照框架写的。自己写的就能用之前的追踪方式；另外两种可以找到框架版本信息之类的，找存在的漏洞来利用。"

- ✅ 先找入口（index.php → 路由 → 控制器 = 攻击面清单）
- ✅ 自写代码用追踪法
- ✅ 另外两种查版本 CVE 利用
- ⚠️ 补充两点：
  1. **"不安全写法"也必须追踪**——业务代码部分绕过框架防护 = 洞就在那里，光查版本会漏
  2. **"规则写法"不只看版本 CVE**——框架内置过滤本身也可能有洞（过滤规则不全 / 可绕过），要查 `input()`/`I()` 的过滤实现

---

## 二、审计流程（课程步骤）

1. **搭环境 / 入口访问**：`public/index.php` → 看加载了什么、路由怎么分发
2. **追踪对应**（三个手段）：
   - **结合入口路由配合对应**：URL → 控制器方法（路由文件 = 攻击面清单）
   - **调试模式信息获取对应**：报错页泄露路径/版本/配置
   - **断点调项目执行试跟踪**：Xdebug 断点（08-18 已配好），危险函数行断下看调用栈
3. **版本查看（三个位置）**：
   - `/thinkphp/base.php`
   - `/ThinkPHP/ThinkPHP.php`
   - `/thinkphp/library/think/App.php`
4. **调试开关（config.php）**：
   - `app_debug = 'true'` → 报错页泄露堆栈/路径/配置
   - `app_trace = 'true'` → 页面底部 trace 调试条（SQL/参数/耗时全暴露）
   - 开发者遗留 `echo` / `var_dump` → 信息泄露，且调试代码常伴漏洞
5. **版本安全问题**：查 TP5 已知漏洞（见第三节速查）
6. **框架内置过滤问题**：TP 的 `input()` / `I()` 到底过滤了什么、拦不住什么 → 待查框架源码（上节课疑问 1 延续）

---

## 三、TP5 已知漏洞速查（🧪 待逐条验证，版本边界以官方公告为准）

| 漏洞 | 影响版本（社区口径） | 类型 | 一句话原理 |
|------|---------------------|------|-----------|
| 参数覆盖 RCE（invokefunction） | 5.0.x（≤5.0.23） | RCE | URL 参数覆盖路由/方法名，`call_user_func_array` 调 `system`（经典 payload：`?s=index/\think\app/invokefunction&function=call_user_func_array&vars[0]=system&vars[1][]=id`） |
| Request 类 RCE（_method 覆盖） | 5.0.x / 5.1.x（≤5.1.31） | RCE | `_method=__construct` 覆盖 filter 属性 → 参数进 `system` 等函数（2019-01 公开） |
| 反序列化 RCE | 5.0.x / 5.1.x | RCE | 需存在可控 `unserialize()` 触发点（cookie/参数），配合框架 POP 链 |
| SQL 注入 | 5.x 多处 | 注入 | `where` 表达式注入、`Db::query()`/`M()->query()` 原生拼接（绕过 ORM）、`order` 可控等 |

> 实操法：拿到源码先看 `base.php` / `App.php` 版本号 → 按版本搜对应公开 POC（GitHub/先知/漏洞库）→ 本地搭环境复现。**先对版本，这步经常直接出结果**（第二课就讲过）。

---

## 四、怎么防御（审计的镜像）

- 业务代码**禁止绕过框架防护的原生写法**：`Db::query()` 裸拼接、`request()->get()` 裸收参
- 收参统一走 `input()` / 验证器，参数化查询
- **框架勤升级**（TP5 老版本 RCE 全公开，升级到 5.1.x 最新 / 6.x / 8.x）
- 上线关 `app_debug` / `app_trace`，删掉调试 `echo`/`var_dump`
- 不反序列化不可信数据；`disable_functions` 兜底

---

## 五、总结 / 疑问

- 收获：框架审计落地 = **入口 → 写法分类 → （自写走追踪 / 框架层走版本 CVE）**；TP5 三处版本位置、两个调试开关、版本漏洞速查表
- 疑问：
  1. ❓ TP5 的 `input()` / `I()` 默认过滤规则具体是什么？哪些注入类型默认拦、哪些拦不住？（待查源码，上节课遗留）
  2. ❓ 5.0.23 和 5.1.31 这两个版本边界怎么来的？要下对应版本源码对比修复 diff
  3. ❓ 反序列化链审计：已知链的情况下，怎么快速在项目里找可利用的 `unserialize` 触发点？（正则特征：`unserialize(` / `cookie` 传入）
  4. ❓ 课程说"自动审计或搜索"——Metinfo/phpmydmin 这类无框架 CMS 用 Seay 一键扫就能定位，TP5 这类框架项目 Seay 扫出来的命中点要怎么结合框架判断？（第二课遗留）

## 关联笔记

- [2026-08-18(续)-框架MVC代码审计.md](./2026-08-18(续)-框架MVC代码审计.md) — 框架审计地图（通用方法论，本课是 TP5 落地）
- [2026-08-18-PHP无框架代码审计入门.md](./2026-08-18-PHP无框架代码审计入门.md) — 无框架追踪法（自写写法用这个）
- [2026-08-23-文件操作代码审计.md](./2026-08-23-文件操作代码审计.md) — 今天的文件操作课
- [2026-07-22-中间件安全与Web源码分析.md](./2026-07-22-中间件安全与Web源码分析.md) — CMS 源码分析基础

> ⚠️ 声明：仅限授权环境（靶场/CTF/授权渗透）中测试，所有 Payload 只用于验证漏洞存在。
