# 2026-09-02 应急响应①：总体框架与溯源思路（表现→收集→攻击→追查 + 案例2 WEB / 案例3 WIN）

> 状态：🧪 理论（课程截图，总框架课），待实操/后续课展开
> 同天（09-02）学习的第三块内容（前两块：域横向隧道技术⑦、CS&MSF 联动补充）——**应急响应是新主题模块**，本课只搭总体框架 + 两个溯源案例的思路，细节待后续课。
> 课程标题线索：#案例2-WEB攻击应急响应溯源-后门，日志 / #案例3-WIN系统攻击应急响应溯源-后门,日志,流量（案例从 2/3 出现，说明是系列课、前面还有案例1，截图没拍到）

## 一、一句话认知

**应急响应溯源** = 从"表现"（用户报障的异常症状）出发 → 先"收集"（摸清主机/资产环境）→ 推断"攻击"（可能从哪里被打、什么方式）→ 最后"追查"（据表现选对分析手段，还原攻击链）。贯穿始终的核心是**时间线**：找到"第一次被改动/首次写入"的时间点做基线——**时间之前 = 攻击分析（怎么进来的），之后 = 操作分析（进来干了什么）**。

## 二、总框架导图（课程原图结构）

```
表现  →  收集  →  攻击  →  追查
        （追查节点上方标注：据表现选择最佳方法）
```

### 1. 表现（症状在哪冒头，先看现象猜方向）

| 主分支 | 子节点（异常症状） |
|--------|------------------|
| 网站 | 篡改 / 丢失 / 乱码 |
| 文件 | 篡改 / 丢失 / 泄漏 |
| 系统 | 系统卡顿 / CPU爆满 / 服务宕机 |
| 流量 | 大量数据包 / 多个外部连接（原图写"对个外部连接"，疑笔误）/ 网速网络卡顿 |
| 第三方 | 服务异常 / 运行异常 / ⚠️ 第 3 子节点被截图截断 |

> ⚠️ 各主分支第 3 节点下方还有第 4 根延伸线（空白无文字）——原导图更大，截图不全，待完整图补全

### 2. 收集（先摸环境再判断，导图标注 win & linux & mac 三类系统通用）

```
对外服务 / 开放端口 / 系统版本 / 网络环境 / 漏洞情况 / 软件平台 / 口令整理 / 有无防护
```

→ 和渗透测试信息收集几乎同款（对照我 08-30 内网信息收集笔记）：应急时用来判断"这台机器暴露过什么、可能从哪被打"。

### 3. 攻击（按资产类型猜攻击面）

| 主分支 | 子节点 |
|--------|--------|
| WEB | 漏洞攻击 / 结合攻击 / 流量攻击 / ⚠️ 第 4 节点截断 |
| 第三方 | 数据库 / 远程软件 / 服务平台 / ⚠️ 第 4 节点截断 |
| 操作系统 | 权限提权 / 内网渗透 / 远程漏洞 / ⚠️ 第 4 节点截断 |

### 4. 追查（据表现选最佳方法，不是上来就翻日志）

```
日志分析 / 后门分析 / 流量分析 / 脚本软件分析 / 模拟渗透分析
```

> ⚠️ 右上、右下各有被截断的分支线，完整分支数 > 5，待完整导图

## 三、案例2：WEB 攻击应急响应溯源——后门，日志（网站首页被篡改）

**原文**：

```
故事回顾：某客户反应自己的网站首页出现被篡改，请求支援
分析：涉及攻击面 涉及的操作权限 涉及的攻击意图 涉及的攻击方式等
思路1：利用日志定位修改时间基数，将前时间进行攻击分析，后时间进行操作分析
思路2：利用后门Webshell查杀脚本或工具找到对应后门文件，定位第一次时间分析
```

**我的理解**：
- **分析四问先行**（攻击面 / 操作权限 / 攻击意图 / 攻击方式）= 先建立"对手画像"再动手，避免瞎翻
- **思路1 = 时间基线法**：从日志定位首页被篡改的时间点 → 往前查攻击入口（漏洞利用/文件上传），往后查攻击者操作（提权/内网/留后门）
- **思路2 = 后门反推法**：Webshell 查杀脚本/工具扫出后门文件 → 看后门文件**创建/首次写入时间** → 反查该时间点的访问日志，两条思路最终收敛到同一时间线

## 四、案例3：WIN 系统攻击应急响应溯源——后门，日志，流量（服务器卡顿）

**原文**：

```
故事回顾：某客户反应服务器异常出现卡顿等情况，请求支援
思路：利用监控工具分析可疑进程，利用杀毒软件分析可疑文件，利用接口工具抓流量
获取进行监控：PCHunter64
获取执行列表：UserAssistView
AppCompatCacheParser.exe --csv c:\temp -t
```

**工具理解**（标准知识，细节待对照视频）：
- **PCHunter64**：内核级进程/驱动查看工具，查隐藏进程、可疑进程的路径/模块/网络连接（比任务管理器看得深）
- **UserAssistView**：读注册表 UserAssist 键，列出用户运行过的 GUI 程序执行记录
- **AppCompatCacheParser.exe**（MANDIANT 出品）：解析程序兼容性缓存（AppCompatCache/AmCache），`--csv c:\temp` 导出 CSV、`-t` 处理时区 → 还原系统执行过的程序**时间线**
- 三个工具对应三条取证线：**进程视角**（现在谁在跑）+ **执行痕迹视角**（之前跑过什么）+ **文件/流量视角**（杀软查文件、抓包看外连）→ 交叉定位后门与攻击行为

## 五、框架方法论沉淀

1. 四阶段不一定是线性流程——**追查是"据表现选方法"**：症状偏"篡改"→ 优先日志/后门分析；偏"卡顿/异常外连"→ 优先进程/流量分析
2. **时间线是万能钥匙**：任何溯源先找"基线时间点"，向前向后二分攻击/操作（案例2 思路1 的精髓，案例3 的 AppCompatCache 也是在重建时间线）
3. **分析四问先于动手**：攻击面/权限/意图/方式，避免无目的翻日志
4. 应急复用的是**渗透知识库**：收集八项 ≈ 信息收集，"攻击"节点里的漏洞/提权/内网/Webshell 手法都是已学过的攻击面

## 疑问

1. ⚠️ 三张导图均有截图截断（表现图第三方第 3 子节点、攻击图三分支第 4 节点、追查图上下被截分支），完整导图待课程补全
2. ❓ 应急响应完整闭环：本课只到"追查"，**处置（隔离/清除/加固）与复盘报告**阶段没出现？案例1/4+ 是什么内容？
3. ❓ UserAssistView / AppCompatCacheParser 的读取原理与时间线字段？（AppCompatCache 的 -t 时区、CSV 输出字段）
4. ❓ PCHunter64 判定"可疑进程"的实战标准？（路径/签名/父子关系？内核级能看到什么任务管理器看不到的）

## 关联笔记

- [2026-08-30(续)-内网安全-基本认知与信息收集.md](./2026-08-30(续)-内网安全-基本认知与信息收集.md) — "收集"阶段八项与渗透信息收集同源，应急视角 = 复用渗透知识库
- 渗透知识库整体（Web 漏洞/提权/内网/Webshell 手法）是"攻击"节点判断攻击面的依据，后续案例课会反复调用

## 待实践

- 暂无独立应急靶场；后续案例课带环境时按四阶段走一遍完整溯源
- 本机低成本可试：UserAssistView / AppCompatCacheParser.exe 对自己机器跑一次，看看执行痕迹输出长什么样（先建立工具手感）

> ⚠️ 声明：仅限授权环境（靶场/CTF/授权渗透）中测试，所有 Payload 只用于验证漏洞存在。
