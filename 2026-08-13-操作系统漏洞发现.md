# 2026-08-13 操作系统漏洞发现（小迪安全 · 漏洞发现系列 ①）

> 今天进入新的大类：**漏洞发现**。第一课是**对操作系统的漏洞发现**——和前面学的 Web 漏洞（SQLi/XSS/RCE 都是应用层）不同，这课的目标是 OS 本身（Windows/Linux 内核、系统服务、补丁缺口）。老师给了一张思维导图，我把它整理成自己的笔记，并补充了一些图上没展开的细节。

---

## 一、是什么（我的理解）

### 1.1 一句话

**漏洞发现 = 在目标上找到"可以打的地方"**。操作系统漏洞发现 = 针对 OS 层的"扫描 → 定性 → 利用 → 修复"完整闭环。

### 1.2 类比（延续快递盒系列）

```
操作系统 = 一栋大楼（Windows / Linux 是不同户型）
漏洞      = 楼里的门窗缝隙、坏锁、年久失修的管道
扫描      = 拿仪器绕着楼扫一圈，标出可疑点
利用      = 顺着可疑点进去（进去的路径叫 exploit）
修复      = 把缝隙补上、坏锁换掉
```

### 1.3 和 Web 漏洞的区别

| 维度 | Web 漏洞（前面学的） | 操作系统漏洞（这课） |
|------|---------------------|----------------------|
| 目标 | 应用代码 / 业务逻辑 | OS 内核、系统服务、补丁缺口 |
| 发现方式 | 手工测参数、猜逻辑 | 工具扫描为主（版本指纹 ↔ 漏洞库比对） |
| 典型例子 | SQLi / XSS / RCE / 越权 | 永恒之蓝 MS17-010、脏牛、PrintNightmare |
| 利用门槛 | 看场景、看代码 | 多为已知 CVE，工具化程度高 |

---

## 二、怎么发现（扫描层）

导图上的扫描工具：**Goby、Nmap、Nessus、OpenVAS、Nexpose**

| 工具 | 定位 | 我的理解 |
|------|------|----------|
| Nmap | 端口 / 服务 / 版本扫描 | 先摸资产：开了什么端口、跑了什么服务、什么版本 |
| Goby | 资产发现 + 漏洞扫描一体化 | 红队常用，图形化，扫出资产直接挂漏洞库比对（导图里写的是 Goby，截图看起来像 "Coby"） |
| Nessus | 商业漏洞扫描器（Tenable） | 漏洞库全、报告专业，企业标配 |
| OpenVAS | 开源漏洞扫描器（Greenbone 维护） | Nessus 的开源替代 |
| Nexpose | 商业漏洞扫描器（Rapid7） | 和 Metasploit 同厂，扫完无缝对接利用 |

我的套路理解：**Nmap 先答"有什么"，Nessus/OpenVAS/Nexpose 再答"有什么洞"，Goby 一站式干两件事**。扫描器本质是"版本指纹 ↔ 漏洞库"比对——所以资产版本识得准不准，直接决定扫描结果靠不靠谱。

---

## 三、漏洞类型（定性层）

导图上的类型：**远程执行、权限提升、提权攻击、缓冲区溢出**

- **远程执行（RCE）**：攻击者直接在目标机器上执行命令。例：MS17-010 永恒之蓝（SMB 漏洞，可远程执行 + 横向传播）、CVE-2021-41773 Apache 路径穿越 → RCE
- **权限提升（提权）**：普通用户权限 → 管理员/root。例：脏牛 CVE-2016-5195（Linux 内核本地提权）、PrintNightmare（Windows 打印机服务提权）
- **提权攻击**：导图单独列的一项。我理解是和"权限提升"互补的视角——从攻击方看"怎么把权限升上去"（内核漏洞提权、令牌窃取、服务配置错误）
- **缓冲区溢出（Buffer Overflow）**：向程序塞超长数据覆盖内存，经典的 OS 漏洞类型（老系统多发，如 IIS/SMB 溢出）。现代系统靠 ASLR / DEP / 栈保护大幅压制，但仍是底层利用的核心手艺

---

## 四、怎么利用（利用层）

导图上的利用工具 / 平台：

**利用框架：**

- **Metasploit（MSF）**：渗透测试标配框架。流程 `search <漏洞关键词>` → `use exploit/...` → `set payload` → `run`
- **Searchsploit**：Exploit-DB 的本地命令行版，`searchsploit <关键词>` 秒查 exploit 和对应 CVE
- **企业安全产品**：导图里 Metasploit 分支下的节点。我理解指厂商/企业自研或商用的利用工具链（红队内部的自动化利用产品）

**漏洞库 / 平台（查"这个漏洞有没有现成利用"）：**

| 平台 | 定位 |
|------|------|
| CNVD（国家信息安全漏洞共享平台） | 国内官方漏洞库，漏洞公告 + 补丁信息 |
| Seebug（知道创宇） | 国内漏洞平台，含漏洞利用情报 |
| 1337day | 国外 0day / 漏洞利用发布站 |
| Exploit-DB | 全球最全的漏洞利用代码库（离线版 = Searchsploit） |
| Packetstorm Security | 老牌安全资料站，exploit / 工具 / 论文都有 |

**利用闭环**：扫描结果 → 定位 CVE / 漏洞编号 → Searchsploit / MSF 找现成 exploit → 打进去 →（需要时）提权。老系统 90% 的洞都能在 Exploit-DB 找到现成代码，这就是"漏洞发现"和"漏洞利用"衔接的地方。

---

## 五、怎么修复（修复层）

导图上的修复手段：**打补丁、关闭入口、防护应用**

1. **打补丁**：官方补丁第一时间跟进（Windows Update / yum / apt）。永恒之蓝就是补丁出了很久，还有一堆没打的机器中招
2. **关闭入口**：最小化攻击面——关掉不需要的端口 / 服务 / 共享（SMB 445、RDP 3389 直接暴露公网就是自杀）
3. **防护应用**：主机加固 + 安全软件兜底——杀软 / EDR、防火墙策略、基线核查、HIDS

---

## 六、导图上的三个问题（我的思考）

1. **系统漏洞发现的意义？** → 防御前置：不知道自己有什么洞，补丁 / 防护就是盲打；对攻击方，它是渗透测试的第一步，决定后续能不能打进去
2. **漏洞类型的危害情况？** → 危害分级大致：远程执行 > 提权（本地）> DoS / 信息泄露。远程执行 = 直接沦陷，提权 = 从低权限变管理员，最致命
3. **如何做好下一步？** → 形成闭环：发现 → 验证（确认不是误报）→ 利用 / 评估影响 → 修复 → 复测。不要停在"扫出一堆报告"就完事

---

## 七、我的总结 / 疑问

### 总结

- 操作系统漏洞发现 = **扫描工具 + 漏洞库比对**，比 Web 漏洞更依赖工具化，核心能力是"读扫描结果、判断误报、匹配利用"
- 链条：Nmap / Goby 摸资产 → Nessus / OpenVAS 定性 → MSF / Searchsploit 利用 → 补丁 / 加固修复
- 这课是"漏洞发现"大类的开篇，后续大概率还有 Web 漏洞发现、第三方组件（中间件 / 数据库）漏洞发现，方法论可以复用

### 疑问

- Goby 和 Nmap 的定位重叠在哪？什么时候只用 Nmap、什么时候上 Goby？（Goby 也有端口扫描，但资产测绘更强）
- Nessus / OpenVAS 扫出来的"高风险"误报率多高？怎么快速判断一个 CVE 能不能真实利用（版本对不对、利用条件满不满足）？
- 缓冲区溢出现在还需要手写 shellcode 吗？还是全部工具化（MSF 生成）了？

---

## 关联笔记

- [操作系统与第三方安全测试](./2026-07-23-操作系统与第三方安全测试.md) — 之前 OS/DB/中间件安全测试的旧笔记，这课是新体系下的深化
- [RCE 远程代码命令执行详解](./2026-08-05-RCE远程代码命令执行详解.md) — 远程执行的通用方法论
- [Java 安全代码审计](./2026-08-12-Java安全代码审计.md) — 审计视角：漏洞发现的白盒版

## 待实践

- [ ] 用 Nmap 扫本地 / 靶场主机，练"端口 → 服务 → 版本"指纹识别
- [ ] 装 OpenVAS（或直接 Goby）扫一次靶机，看扫描报告长什么样，学读报告
- [ ] `searchsploit` 查 MS17-010 / 脏牛，看 exploit 怎么写、对应 CVE 是什么
- [ ] MSF 里跑一次 `search` → `exploit` 的完整流程（本地靶机）

> ⚠️ 声明：以上技术仅用于 CTF 竞赛和授权靶场环境。
