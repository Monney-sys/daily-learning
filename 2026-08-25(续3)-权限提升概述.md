# 2026-08-25(续3) 权限提升①：提权概述（权限来源三类 + 提权方向）

> 状态：🧪 理论（课程导图），待实操
> 今天第二个板块——小迪安全《提权》第一课 概述。提权的大前提：**先搞清楚你现在手上有什么权限**。小迪把可提权的「起点权限」分成三类：**Webshell / 其他权限 / 服务器系统**，每类提权路径不同。

## 一、一句话认知

提权 = 你已经拿下一个**低权限入口**（webshell / 普通用户 / 某个服务账号），目标是爬到**最高权限**（administrator / root / system）。不同入口 → 不同提权路径，所以**先分类、再对症下药**。

> 放在整条渗透主线里看：信息收集 → 漏洞利用（拿入口）→ **提权** → 内网/后渗透。提权是入口到手之后的关键一环。

## 二、① Webshell（Web 层入口）

拿网站控制权的途径，分三大来源：

| 来源 | 子项 | 我的理解 |
|------|------|---------|
| **后台** | 功能点：文件上传 / 模板修改 / SQL执行 / 数据备份 / … | 能进后台就利用后台功能直接写 shell |
| | 思路点：已知程序（网上资料 / 代码审计）/ 未知程序 | 已知程序查历史漏洞 + 公开利用；未知程序靠代码审计挖洞 |
| **漏洞** | 单点漏洞：文件上传 / 文件包含 / RCE执行 / SQL注入 / … | 单个漏洞打通拿 shell |
| | 组合漏洞：各方面配合 | 多个漏洞打组合拳 |
| **第三方** | 编辑器 / 中间件平台 / phpmydmin / … | 第三方组件漏洞（编辑器、中间件、phpMyAdmin）拿 shell |

> ⚠️ 导图「模板修改」在截图里读作「极限修改」（疑为笔误/识别差异，待对照视频）；「phpmydmin」应为 phpMyAdmin。

## 三、② 其他权限（非 Web 的凭据/服务）

渗透过程除了 webshell，还常拿到这些「其他权限」：

| 类别 | 子项 | 说明 |
|------|------|------|
| **数据库** | mssql / mysql / oracle / … | 数据库凭据 → 数据库提权（mysql UDF、mssql xp_cmdshell …） |
| **服务类** | FTP / RDP / SSH / … | 服务弱口令 / 服务漏洞 → 服务器 / 系统权限 |
| **第三方接口** | 邮件 / 支付 / 空间商 / … | 第三方平台凭据 → 横向 / 渠道权限 |

## 四、③ 服务器系统（OS 层）

（导图底部被截断，只见根节点）操作系统层面提权 = Windows / Linux 的**内核、服务、配置、提权辅助**（SUID/计划任务/漏洞提权/内核提权…）。待对照视频补充。

## 五、我的理解 / 疑问

- 理解：提权概述把「起点权限」分成三类，本质是**渗透到一半时你手里有什么** —— 有 webshell（web 层）、有数据库/服务/第三方凭据（非 web 层）、或直接有服务器系统权限；提权目标统一指向最高权限。
- 疑问：
  1. ❓ 这三类权限在实际渗透里是怎么**顺序出现**的？（先 webshell → 数据库 → 系统？）
  2. ❓ 第三方接口（邮件/支付/空间商）提权具体指什么样？（拿到第三方平台后台？）
  3. ❓ phpmydmin（phpMyAdmin）拿到后一般怎么进一步拿站 / 提权？

## 关联笔记

- [2026-08-15-DC1靶机渗透实战.md](./2026-08-15-DC1靶机渗透实战.md) — 第一次实操提权（SUID find → euid=0），提权的实战例子
- [2026-07-27-高权限注入与文件操作.md](./2026-07-27-高权限注入与文件操作.md) — 高权限注入（SQL 提权基础）
- [2026-08-23-文件操作代码审计.md](./2026-08-23-文件操作代码审计.md) — Webshell 来源里的「文件上传/文件操作」的审计视角

> ⚠️ 声明：仅限授权环境（靶场/CTF/授权渗透）中测试，所有 Payload 只用于验证漏洞存在。
