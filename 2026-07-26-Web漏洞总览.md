# Web 安全漏洞总览

## 一、漏洞分类全景图

```
                            ┌── SQL 注入 ─────────── 数据库被恶意查询
                            ├── 文件上传 ─────────── 上传 Webshell 拿服务器权限
                            ├── XSS 跨站脚本 ─────── 浏览器端代码注入
           ┌── 注入类 ──────├── 文件包含 ─────────── 包含恶意文件执行代码
           │                ├── 反序列化 ─────────── 对象反序列化触发恶意逻辑
           │                ├── 代码执行 ─────────── 直接执行系统命令/代码
           │                └── 逻辑安全 ─────────── 业务逻辑缺陷（越权、支付篡改）
           │
           │                ┌── CSRF ────────────── 跨站请求伪造，借用户之手发请求
           │                ├── SSRF ────────────── 服务端请求伪造，拿服务器当跳板
           │                ├── 目录遍历 ─────────── 访问 Web 根目录外的文件
           ├── 请求伪造类 ──├── 文件读取 ─────────── 任意文件读取（/etc/passwd）
           │                ├── 文件下载 ─────────── 任意文件下载
           │                ├── 命令执行 ─────────── 执行操作系统命令
           │                └── 未授权访问 ───────── 无需认证直接访问敏感资源
           │
           └── 其他 ────────├── XXE 安全 ────────── XML 外部实体注入
                            └── 信息泄露 ────────── 敏感信息暴露（源码、配置、备份）
```

---

## 二、各漏洞详解

### 2.1 SQL 注入（SQL Injection）

| 项目 | 说明 |
|------|------|
| **原理** | 用户输入被拼接到 SQL 语句中执行，导致恶意查询 |
| **危害** | ⭐⭐⭐⭐⭐（最高）— 脱库、篡改数据、写文件拿服务器权限 |
| **类型** | 数字型、字符型、报错注入、布尔盲注、时间盲注、堆叠注入、宽字节注入 |
| **防御** | 参数化查询（预编译）、输入过滤、最小权限原则、WAF |

### 2.2 文件上传（File Upload）

| 项目 | 说明 |
|------|------|
| **原理** | 上传恶意脚本文件（如 PHP 一句话木马），服务端执行后拿到 Webshell |
| **危害** | ⭐⭐⭐⭐⭐ — 直接获取服务器控制权 |
| **绕过** | 前端 JS 校验绕过、MIME 类型伪造、后缀名绕过（`.php5`、`.phtml`、`.php.`）、内容头欺骗、条件竞争 |
| **防御** | 白名单校验后缀、文件内容检测、存储目录禁执行权限、重命名文件 |

### 2.3 XSS 跨站脚本（Cross-Site Scripting）

| 项目 | 说明 |
|------|------|
| **原理** | 将恶意 JS 代码注入网页，在受害者浏览器中执行 |
| **危害** | ⭐⭐⭐ — 盗取 Cookie、劫持会话、钓鱼、页面篡改 |
| **类型** | 反射型（一次性的）、存储型（持久化的）、DOM 型（纯前端） |
| **防御** | 输出编码（HTML 实体转义）、CSP 策略、HttpOnly Cookie |

### 2.4 文件包含（File Inclusion）

| 项目 | 说明 |
|------|------|
| **原理** | 应用动态包含文件时未校验路径，导致包含恶意文件 |
| **危害** | ⭐⭐⭐⭐ — 读取源码、包含上传的木马文件、远程包含执行代码 |
| **类型** | 本地文件包含（LFI）、远程文件包含（RFI） |
| **技巧** | 路径穿越、伪协议（`php://filter`、`php://input`）、日志包含、session 包含 |
| **防御** | 白名单限制被包含文件、禁用远程包含 |

### 2.5 反序列化（Deserialization）

| 项目 | 说明 |
|------|------|
| **原理** | 反序列化不可信数据时触发魔术方法（`__destruct`、`__wakeup`）执行恶意代码 |
| **危害** | ⭐⭐⭐⭐⭐ — 代码执行、权限提升 |
| **常见于** | PHP（`unserialize`）、Java（`ObjectInputStream`）、Python（`pickle`） |
| **防御** | 不反序列化不可信数据、使用安全的序列化方案 |

### 2.6 代码执行 / 命令执行（Code / Command Execution）

| 项目 | 说明 |
|------|------|
| **原理** | 用户输入被传入代码执行函数或系统命令执行函数 |
| **危害** | ⭐⭐⭐⭐⭐ — 完全控制服务器 |
| **PHP 危险函数** | `eval()`、`assert()`、`preg_replace(/e)`、`system()`、`exec()` |
| **防御** | 禁用危险函数、不将用户输入传入执行函数 |

---

### 2.7 CSRF（Cross-Site Request Forgery）

| 项目 | 说明 |
|------|------|
| **原理** | 诱导用户点击恶意链接，借用户已登录的身份向目标站发请求 |
| **危害** | ⭐⭐⭐ — 改密码、转账、发帖 |
| **防御** | CSRF Token、Referer 校验、SameSite Cookie |

### 2.8 SSRF（Server-Side Request Forgery）

| 项目 | 说明 |
|------|------|
| **原理** | 服务器端发起请求的功能被利用，访问内网资源 |
| **危害** | ⭐⭐⭐⭐ — 探测内网、读取云元数据（AWS/阿里云）、攻击内网服务 |
| **常见协议** | `http://`、`file://`、`gopher://`、`dict://` |
| **防御** | 白名单限制请求目标、禁用危险协议 |

### 2.9 XXE（XML External Entity）

| 项目 | 说明 |
|------|------|
| **原理** | XML 解析器处理外部实体声明时读取本地文件或发起请求 |
| **危害** | ⭐⭐⭐⭐ — 文件读取、SSRF、DoS |
| **防御** | 禁用外部实体解析（`libxml_disable_entity_loader(true)`） |

### 2.10 未授权访问（Unauthorized Access）

| 项目 | 说明 |
|------|------|
| **原理** | 缺少身份验证，直接访问后台/API/服务 |
| **危害** | ⭐⭐⭐⭐ — 数据泄露、服务被控 |
| **常见目标** | Redis、MongoDB、Docker API、Jenkins、Hadoop、未授权接口 |
| **防御** | 强制认证、IP 白名单、最小权限 |

### 2.11 逻辑安全（Business Logic Vulnerability）

| 项目 | 说明 |
|------|------|
| **原理** | 业务流程存在设计缺陷，被攻击者利用 |
| **危害** | ⭐⭐⭐ — 越权操作、支付金额篡改、验证码绕过、密码重置劫持 |
| **防御** | 服务端校验关键步骤、权限模型检查 |

---

## 三、危害等级速查表

| 等级 | 漏洞 |
|------|------|
| ⭐⭐⭐⭐⭐ 致命 | SQL 注入、文件上传、命令执行、反序列化、代码执行 |
| ⭐⭐⭐⭐ 高危 | 文件包含、SSRF、XXE、未授权访问 |
| ⭐⭐⭐ 中危 | XSS、CSRF、目录遍历、逻辑漏洞 |
| ⭐⭐ 低危 | 信息泄露（版本号、路径） |

---

## 四、学习路线建议

```
1. SQL 注入（入门首选，最经典）
2. 文件上传（拿下 Webshell，成就感强）
3. XSS（浏览器端攻击，花样多）
4. 命令执行（直捣服务器）
5. CSRF + SSRF（理解请求伪造）
6. 文件包含 + 反序列化（需要编程基础）
7. 逻辑漏洞（需要理解业务）
```
