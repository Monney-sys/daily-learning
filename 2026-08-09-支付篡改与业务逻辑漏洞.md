# 2026-08-09 支付篡改与业务逻辑漏洞

> 支付篡改不只是改金额那么简单——一次完整的购买流程里，几乎每个参数都可能被篡改。

---

## 一、产生原因

学完越权和认证缺陷之后，我发现支付篡改和它们有一个共同的根因：**后端信任了前端传来的数据**。

```
越权       → 后端信任了前端传来的 userId
支付篡改   → 后端信任了前端传来的 price、quantity、discount
认证缺陷   → 后端信任了前端传来的 role、isAdmin
```

核心理念是一样的：前端传什么，后端就用什么，没有在服务端做"这个值合不合理"的校验。说白了就是**客户端可控的数据参与了服务端的业务逻辑决策**。

另外还有一个原因：开发往往只关注"功能能不能跑通"，没站在攻击者视角想"流程中的每个参数如果被改了会怎样"。

---

## 二、一次购买流程中的攻击面

正常的购买流程大概是：

```
选择商品 → 确定数量 → 生成订单 → 支付 → 回调通知
   │           │           │         │        │
   ▼           ▼           ▼         ▼        ▼
 商品ID      数量上限     金额参数    支付金额   订单状态
```

每一步都有可以下手的地方。

### 2.1 选择商品阶段

**① 商品 ID 篡改**

最直接的手法——把商品 ID 换掉。比如一个商品原价 999，另一个商品原价 9.9，但后端只校验了"商品是否存在"没校验"价格对不对得上"。

```
POST /order/create
Body: productId=1001&quantity=1    ← 1001 是 999 元的商品

改成：
Body: productId=1002&quantity=1    ← 1002 是 9.9 元的商品
```

这其实也是一种 IDOR（不安全的直接对象引用），和越权漏洞本质相通。

**② 价格参数由前端传入**

有些系统的实现是：前端计算好总价，直接 POST 给后端。

```
POST /order/create
Body: productId=1001&price=999&quantity=1&total=999

改成：
Body: productId=1001&price=1&quantity=1&total=1    ← 1 块钱买 999 的商品
```

后端应该查数据库取价格，而不是用前端传来的价格。

### 2.2 确定数量阶段

**① 负数攻击**

把数量改成负数，逻辑上余额不降反升。

```
POST /order/pay
Body: orderId=5001&quantity=-1&amount=100

如果后端计算方式是 balance = balance - (price × quantity)：
balance = 100 - (100 × -1) = 100 - (-100) = 200  ← 余额反而多了
```

修复方式：后端必须校验 `quantity > 0`。

**② 小数攻击**

```
quantity=0.001   ← 超小数值，可能绕过最低金额限制
quantity=1.5     ← 非整数可能触发计算错误
```

有些系统只校验了"是不是数字"，没校验"是不是正整数"。

**③ 库存绕过**

商品库存只有 10 件，但后端没有校验 `quantity <= stock`：

```
POST /order/create
Body: productId=1001&quantity=9999     ← 买了 9999 件，远超库存
```

**④ 大数溢出**

```
quantity=9999999999999999999999999999
可能导致整数溢出，金额计算异常。
```

### 2.3 生成订单阶段

**① 金额篡改**

订单生成后，提交支付前，订单金额可能仍可被修改：

```
POST /order/confirm
Body: orderId=5001&totalAmount=100

改成：
Body: orderId=5001&totalAmount=1       ← 把 100 元改成 1 元
```

后端没有在支付环节和服务端已存订单做金额比对。

**② 运费篡改**

有些系统把运费作为单独参数传递：

```
POST /order/create
Body: productId=1001&quantity=1&shippingFee=20

改成：
Body: productId=1001&quantity=1&shippingFee=0    ← 免运费
Body: productId=1001&quantity=1&shippingFee=-10  ← 运费变成负数，抵扣商品价格
```

**③ 优惠券 / 折扣篡改**

```
POST /order/applyCoupon
Body: couponCode=SAVE10&discount=10

改成：
Body: couponCode=SAVE10&discount=999     ← 折扣金额被改大了
Body: couponCode=SAVE10&discountRate=0.01 ← 把 9 折改成 0.1 折
```

或者同一张优惠券被重复使用——后端没有标记"已使用"。

**④ 订单号遍历**

和越权漏洞一模一样：修改订单号看别人的订单。

```
GET /order/detail?id=5001     ← 自己的订单
GET /order/detail?id=5002     ← 别人的订单，收货地址、手机号全看到了
```

### 2.4 支付阶段

**① 支付金额与订单金额不一致**

支付网关（支付宝/微信）发起支付时，金额参数可能来自前端：

```
POST /pay/create
Body: orderId=5001&payAmount=100

改成：
Body: orderId=5001&payAmount=1    ← 向支付宝发起 1 元的支付请求
```

支付网关处理了 1 元 → 回调通知"已支付 1 元" → 后端直接标记订单"已支付" → 实际订单是 100 元，只付了 1 元。

**② 支付方式篡改**

```
POST /checkout
Body: paymentMethod=online_pay

改成：
Body: paymentMethod=cod   ← COD（货到付款），可能绕过预支付流程
```

### 2.5 回调通知阶段

**① 伪造支付成功回调**

如果回调接口没有签名校验，可以直接构造请求：

```
POST /pay/callback
Body: orderId=5001&status=success&amount=1

伪造：
POST /pay/callback
Body: orderId=5001&status=success&amount=1   ← 直接告诉后端"支付成功了"
```

后端信了 → 标记订单已支付 → 发货。实际一分钱没付。

**② 重复回调**

同一笔支付的回调请求被重放，触发两次发货：

```
POST /pay/callback
Body: orderId=5001&tradeNo=ALIPAY20240809001&status=success

同一个请求发两次 → 后端没有做回调去重 → 发货两次
```

---

## 三、全流程通用攻击手法

### 3.1 条件竞争（Race Condition）

这是支付场景里最经典的高危问题。

**场景**：账户余额 100 元，想买两件各 100 元的商品。

```
正常情况：买第一件 → 扣 100 → 余额 0 → 买第二件失败

并发攻击：同时发两个购买请求（用 Burp Intruder 或 Turbo Intruder 并发）
  → 两个请求同时读取余额（都是 100 元）
  → 两个请求都判断"余额足够"
  → 两个请求都扣款
  → 最终扣了 200 元但余额只有 100 元 💥
```

并发提现也是同样的道理：

```
余额 100 元 → 同时发两个 100 元提现请求
→ 读到两个 100 → 两次都通过 → 提走 200 元 💥
```

测试手法：用 Burp 的 Turbo Intruder 或者 Python 多线程同时发请求。

### 3.2 参数污染（HPP）

```
POST /pay
Body: amount=100&amount=1

不同后端处理方式不同：
- PHP 取最后一个值：amount=1
- Java 取第一个值：amount=100
- Node.js 可能变成数组：[100, 1]

如果 WAF 和业务后端解析方式不一样，WAF 检测 100（正常），业务端执行 1（篡改后的值）。
```

### 3.3 业务流程跳过

有些系统的购买流程是严格按步骤来的，但可以直接跳到后续步骤：

```
正常流程：Step1 选商品 → Step2 确认订单 → Step3 支付 → Step4 收货

跳过：Step1 → 直接访问 Step4 的 URL → ?orderId=5001&action=confirm
→ 可能跳过支付直接确认收货
```

---

## 四、测试思路

测试支付流程时，关注的核心问题很简单：**每个参数是不是后端自己算的？**

```
我应该测什么？
1. 价格参数 → 是不是前端传的？能不能改？
2. 数量 → 能不能负数/小数/超大？
3. 商品ID → 能不能换？后端有没有校验"当前用户能看到这个商品"？
4. 运费 → 能不能改成 0？
5. 优惠券/折扣 → 能不能篡改折扣力度？能不能重复用？
6. 支付金额 → 和订单金额是不是强绑定的？
7. 回调 → 有没有签名校验？能不能重放？
8. 并发 → 能不能同时发两个请求实现超扣/超提？
```

测试方法论：

```
1. 走一遍完整购买流程，用 Burp 记录所有请求
2. 逐个请求看参数 → 改值重放 → 看后端反应
3. 重点关注"从响应里返回了、又被前端传到下一个请求"的参数
   → 这种参数往往后端没做校验
4. 用两个浏览器（不同账号）同时操作 → 测并发问题
```

---

## 五、总结

| 环节 | 攻击点 | 一句话总结 |
|------|--------|-----------|
| 选择商品 | 商品ID、价格 | 后端应该从数据库查价格，不认前端传来的 |
| 确定数量 | 负数、小数、超大数、超库存 | 加 `quantity > 0 && quantity <= stock` |
| 生成订单 | 金额、运费、优惠券、订单号 | 金额和订单强绑定，优惠券用后标记已使用 |
| 支付 | 支付金额、支付方式 | 支付金额必须从服务端已存订单取 |
| 回调 | 伪造回调、重复回调 | 验签 + 回调去重 + 金额二次比对 |
| 全流程 | 条件竞争、参数污染、步骤跳过 | 并发加锁，流程有状态机控制 |

核心就一句话：**客户端传参不可信，涉及金额/数量/身份的决策必须在服务端做。**

---

## 关联笔记

- [越权漏洞详解](./2026-08-08-越权漏洞详解.md) — 商品ID篡改、订单号遍历本质都是IDOR，同一类问题
- [登录脆弱与认证缺陷](./2026-08-09-登录脆弱与认证缺陷.md) — 同天学的，都属于业务逻辑漏洞

## 待实践

- [ ] 用 DVWA / Pikachu 的支付/购买模块，把上面各阶段的参数都改一遍
- [ ] 尝试并发攻击，用 Burp Turbo Intruder 同时发两个购买请求
- [ ] 学一下支付宝/微信支付的回调验签机制，理解为什么回调签名这么重要

> ⚠️ 声明：以上技术仅用于授权靶场、CTF 竞赛和渗透测试授权目标，未经授权篡改订单和支付参数属于违法行为。
