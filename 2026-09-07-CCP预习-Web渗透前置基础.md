# 2026-09-07 CCP 预习：Web 渗透前置基础（网络/Web 架构/操作系统）

> 用途：CCP 集训预习速览（对应备考地图 9/8 课程：网络基础 + Linux/Windows 基础）
> 目标：**大致概念扫盲**，能跟上培训节奏；渗透视角串联，带着主线听课
> 关联：已学笔记（07-21 数据包与抓包实战 / 07-22 中间件安全 / 07-23 操作系统与第三方安全）

---

## 一、计算机网络基础

### 1. OSI 七层 vs TCP/IP 四层（背映射表，必考）

| OSI 七层 | TCP/IP 四层 | 代表协议/设备 | 一句话职责 |
|---|---|---|---|
| 应用层 | **应用层** | HTTP/HTTPS、DNS、FTP、SSH | 用户能感知的服务 |
| 表示层 | （并入应用层） | TLS/SSL 加密、编码 | 数据格式转换/加密 |
| 会话层 | （并入应用层） | Session 会话管理 | 建立/维持会话 |
| 传输层 | **传输层** | TCP、UDP | 端到端传输，**端口在这层** |
| 网络层 | **网络层** | IP、ICMP | 寻址和路由（跨网段） |
| 数据链路层 | **网络接口层** | 以太网、MAC、ARP | 同一网段内传帧 |
| 物理层 | （并入网络接口层） | 网线、交换机 | 比特流传输 |

**数据传输流程一句话**：发送方从应用层**逐层向下封装**（每层加头部）→ 物理介质传输 → 接收方**逐层向上解封装** → 剥到头拿到原始数据。

> 🎯 渗透视角：攻击多在**应用层协议**（HTTP）；"扫端口"= 探测**传输层**开了哪些服务。这两层是主线。

### 2. HTTP/HTTPS 协议（重点）

- HTTP 明文；**HTTPS = HTTP + TLS**，端口 443，防窃听/篡改。
- **GET**：参数放 URL（`?id=1`）；**POST**：参数放请求体（登录/提交表单）。
- **请求报文** = 请求行（方法+路径+版本）+ 请求头 + 空行 + 请求体

常用请求头：

| 请求头 | 作用 | 渗透相关 |
|---|---|---|
| Cookie | 携带会话凭证 | 登录态绕过、越权测试 |
| User-Agent | 客户端标识 | 改 UA 绕过简单 WAF |
| Referer | 来源页面 | CSRF/防盗链 |
| Content-Type | 请求体格式 | 上传绕过（改类型） |
| X-Forwarded-For | 真实 IP（可伪造） | IP 白名单绕过 |

常用响应头：

| 响应头 | 作用 |
|---|---|
| Set-Cookie | 服务端下发 Cookie（种会话） |
| Location | 重定向地址（配合 3xx） |
| Server | 服务端类型版本（信息收集！） |
| Content-Type | 返回内容类型 |

**状态码必背**：200 成功 · 301/302 重定向 · 400 请求错误 · 401 未认证 · 403 禁止访问 · 404 不存在 · 405 方法不允许 · 500 服务器内部错误 · 502/503 网关/服务不可用

**Cookie vs Session**：
- Cookie 存**客户端**（浏览器）；Session 存**服务端**（内存/数据库）。
- 流程：登录 → 服务端建 Session → `Set-Cookie` 下发 Session ID → 之后请求带 Cookie，服务端比对。
- 🎯 渗透：Session ID 被盗 = 会话劫持/账号冒充；"登录态"机制是越权、CSRF 等漏洞的上下文。

### 3. 端口、IP、DNS

- **端口**：IP 定位主机，端口定位**服务**。常见：22 SSH · 80/443 Web · 3306 MySQL · 3389 RDP · 445 SMB · 6379 Redis · 21 FTP。
  > 🎯 渗透第一步 = 端口扫描（nmap），端口 = 服务 = 突破口。
- **IP**：IPv4 = 网络位 + 主机位。私网段 10.x / 172.16-31.x / 192.168.x（内网渗透都在私网横向移动）。
- **DNS 解析流程**：域名 → 本地 hosts → 本地缓存 → 递归问上级 DNS → 拿到 IP。
  - 记录类型：A（IPv4）、CNAME（别名）、MX（邮件）、NS（域名服务器）。
  - 🎯 渗透：子域名枚举、改 hosts 绑定、DNS 域传送漏洞是考点。

---

## 二、Web 前端与后端基础

### 1. 前端三件套（目标是看懂，不是会写）

| 语言 | 干什么 | 渗透要懂的 |
|---|---|---|
| HTML | 页面**结构**（标签树/DOM） | 找表单 action/method、隐藏字段、注释泄露 |
| CSS | 页面**样式** | 基本不用管 |
| JavaScript | 页面**行为** | 读脚本逻辑 → 找接口/前端校验 → 看 XSS 触发点 |

- **浏览器-服务器交互**：浏览器发 HTTP 请求 → 服务器处理后返回 HTML/JSON → 浏览器渲染。前端校验**抓包即可绕过**——这是渗透核心思维。

**XSS 原理一句话**：网站把用户输入**原样当代码**输出到页面，输入 `<script>...</script>` 就被当脚本执行。
- 反射型：脚本在 URL 里，骗点击（一次性）
- 存储型：脚本存进数据库，任何人访问都中招（最危险，如评论区）
- DOM 型：不过服务器，纯前端 JS 拼 HTML 触发

### 2. 常见 Web 架构（知道谁在干活）

- **LAMP** = Linux + Apache + MySQL + PHP，经典全家桶。
- **Nginx + Tomcat**：Nginx 处理静态 + 反向代理转发动态请求给 Tomcat（Java），动静分离。
- 前后端分离：前端调 API（返回 JSON），不直接拼页面。
- 🎯 识别架构 = 知道打哪：Web 服务器 / 中间件 / 数据库 / 后端代码。

### 3. 数据库基础（MySQL，为 SQL 注入铺垫）

```sql
SELECT * FROM users WHERE username='admin' AND password='xxx';  -- 查询
INSERT INTO users (name) VALUES ('tom');     -- 插入
UPDATE users SET password='new' WHERE id=1;  -- 更新
DELETE FROM users WHERE id=1;                -- 删除
```
- 子句：WHERE（条件）、ORDER BY（排序）、LIMIT（限量）、JOIN（连表）
- 注释符：`--`、`#`、`/* */`
- `information_schema`：MySQL 自带"数据库的数据库"，存所有库名/表名/列名——SQL 注入拿数据全靠它
> 🎯 一句认知：**SQL 注入 = 把用户输入拼进 SQL 语句当代码执行**。记住"单引号、注释、information_schema"三关键词。

---

## 三、操作系统基础

### 1. Linux（渗透主战场）

- **目录**：`/` 根 · `/etc` 配置 · `/var` 日志（`/var/log`）· `/tmp` 临时目录（常传马）· `/home` 用户 · `/root` 管理员 · `/proc` 进程/内核虚拟信息
- **命令分组**：
  - 文件：ls cd cat cp mv rm find grep
  - 权限：chmod（改权限）chown（改属主）
  - 进程：ps aux（看进程）top kill
  - 网络：ifconfig/ip ss/netstat（看端口）
- **rwx 权限（必考，与提权挂钩）**：
  - 身份：属主 u / 属组 g / 其他人 o
  - 权限：读 r=4、写 w=2、执行 x=1
  - `chmod 755` = rwxr-xr-x（属主 7 + 属组 5 + 其他 5）
  - **SUID**（`-rwsr-xr-x` 的 s）：任何用户执行都以**属主身份**运行 → Linux 提权经典利用点
- **Shell 脚本入门**：`#!/bin/bash` 开头 → 变量 → if/for → `|` 管道 `>` 重定向。会读即可。
> 🎯 Linux 提权逻辑 = 低权限用户 → 找"借力点"（SUID、sudo 配置、内核漏洞、弱权限文件）→ root。

### 2. Windows（提权思路与 Linux 同构）

- **用户权限**：Administrator / 标准用户 / Guest；**UAC** 弹窗机制；内置组 Administrators（≈root）、Users。
- **进程管理**：任务管理器；`tasklist` 看进程、`taskkill /PID x` 杀进程、`netstat -ano` 看端口对应进程。
- **注册表**：Windows 配置数据库（五大根键）。渗透重点：`Run` 键 = **开机自启** → 后门持久化常写这里。
- **常见服务**：3389 RDP · 445 SMB · IIS（Windows Web 服务）· services.msc 管理开机服务 → 提权常利用服务权限/路径配置错误。
- **提权逻辑**：普通用户 → 找错配服务/可写目录/内核漏洞/明文密码文件 → **SYSTEM**（比 Administrator 还高）。

---

## 四、一句话串起来（听课脑中挂这张图）

> 浏览器输入网址 → **DNS** 域名解析成 **IP** → 数据从应用层逐层封装，经 **TCP 端口**（80/443）到服务器 → **Web 服务器**（Apache/Nginx）转发给**后端语言**（PHP/Java）→ 查 **MySQL** → 结果原路返回渲染成页面。
> 渗透 = 在这条链每一环找"**信任了不该信的输入**"：Web 层打 HTTP 参数（注入/XSS），系统层找权限漏洞（提权）。

---

## 五、预习自查清单（晚上课前过一遍）

- [ ] 能默背 OSI 七层 → TCP/IP 四层映射表
- [ ] 能说出 GET/POST 区别 + 5 个常用状态码含义
- [ ] 能讲清 Cookie 和 Session 的关系
- [ ] 能说出 5 个常见端口对应的服务
- [ ] 能用一句话解释 XSS 和 SQL 注入的成因
- [ ] 能说出 chmod 755 的含义 + SUID 是什么
- [ ] 能说出 Windows Run 键和持久化的关系

> 复习标记：本笔记为 🧪 预习性质（未实操验证项待课程后补实操）
