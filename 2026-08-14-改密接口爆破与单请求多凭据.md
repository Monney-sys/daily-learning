# 2026-08-14 PortSwigger 认证机制绕过实战二（Authentication 11/14 → 12/14）

> 状态：✅ 实操过（Lab 12 改密接口爆破）· 🧪 思路已验证待实操（Lab 13 EXPERT 单请求多凭据）

> 零碎 lab 笔记精简版：题目 / 要点 / 踩坑 / 后端缺陷代码还原。进度明细在 `PortSwigger-Lab-做题进度.md`。

## Lab 1: Password brute-force via password change（改密接口爆破，PRACTITIONER）

- **要点**：密码修改功能就是登录功能的"旁路"，防护经常被忽略。这关两个缺陷叠加：
  1. `username` 是**隐藏字段**、客户端可控 → 改成 carlos 就能测别人的当前密码
  2. **锁定逻辑缺陷**：当前密码错误时，只有两次新密码**一致**才触发账号锁定；两次新密码**不一致**时只报错、不锁定 → 可以无限爆破
- **怎么识别命中**：爆破时把两个新密码固定成不同值（如 `123` / `abc`），响应文案就是信号——错误当前密码返回 `Current password is incorrect`；**正确当前密码返回 `New passwords do not match`**（因为只有当前密码验证通过，才会走到"检查新密码一致性"这一步）。官方用 grep-match 标记这个文案。
- **踩坑**：我一开始直接抓改密包爆破，错一次账号就被锁死没法继续——因为表单默认两次新密码是一致的，正好踩中锁定条件。

### 后端缺陷代码（还原）

> 靶场源码不公开，以下是根据官方文档描述的行为还原的伪代码，用来理解校验缺陷：

```python
@app.route('/my-account/change-password', methods=['POST'])
def change_password():
    username = request.form.get('username')      # 隐藏字段，可篡改 → 测谁的当前密码都行
    current  = request.form.get('current-password')
    new1     = request.form.get('new-password-1')
    new2     = request.form.get('new-password-2')

    if not check_password(username, current):    # ① 先验当前密码
        if new1 == new2:                         # ← 缺陷：锁定条件与"两次新密码是否一致"绑定
            lock_account(username)               #    开发者以为"新密码一致"才说明是暴力尝试
        return "Current password is incorrect"   # ② 不一致 → 只报错不锁定 → 无限试错

    if new1 != new2:                             # ③ 当前密码正确才会走到这里
        return "New passwords do not match"      #    ← 这个文案 = "密码对了"的信号！

    update_password(username, new1)
    return "Password updated successfully"
```

**防御**：当前密码验证失败必须**无条件**计数/锁定（与两次新密码是否一致无关）；错误文案统一，别让两种报错的差异泄露"当前密码是否正确"。

## Lab 2: Broken brute-force protection, multiple credentials per request（单请求多凭据，EXPERT）

- **要点**：登录接口收 JSON，`password` 字段**类型未校验**——字符串、数组都收。服务端把 password 当"凭据集合"遍历验证，传数组 = 一次请求带一整本字典。
- **为什么能绕过锁定**：**防护按"请求"计数，业务按"密码"处理**——100 个密码塞进一个请求，内部试 100 次，对外只算 1 次失败，锁定永远不触发。
- **为什么换 IP / UA / Cookie 没用**：这关锁定按**账号**维度计数（carlos 失败 3 次就锁），不按 IP——Lab 5/6 的 X-Forwarded-For 那套根本不改变 carlos 的失败计数。
- **怎么打**：Burp 里把 JSON body 的 `"password": "xxx"` 改成 `"password": ["候选1","候选2",...]`（全部 100 个候选），一次请求 → 返回 **302** 即命中（响应已带 carlos 会话）→ 右键 Show response in browser 就是 carlos 了。
- **踩坑**：我开始盯着请求头（IP/UA/cookie）改，方向错了——攻击面不在"请求流"，在"请求体里能塞多少凭据"。

### 后端缺陷代码（还原）

```python
@app.route('/login', methods=['POST'])
def login():
    data = request.get_json()
    username = data['username']
    password = data['password']               # ← 缺陷①：不校验类型，字符串/数组都收

    if isinstance(password, str):             # 正常登录：字符串包成单元素
        password = [password]

    for pwd in password:                      # ← 缺陷②：一次请求内部遍历所有候选
        if verify(username, pwd):
            session['username'] = username    # 命中即登录
            return redirect('/my-account')

    fail_count[username] += 1                 # ← 缺陷③：失败只按"请求"计数
    if fail_count[username] >= 3:             #    数组里错 100 个也只算 1 次失败
        lock_account(username)
    return "Incorrect password"
```

**防御**：服务端**强制类型校验**（password 必须是 string，收到数组直接 400）；失败计数按"密码尝试次数"而非请求数；错误文案统一。

## 方法论沉淀（两关对比）

- **防护机制的计数粒度 = 攻击者的突破口**：
  - 按 IP 计数 → 伪造 XFF（Lab 5）
  - 按 IP + 成功登录重置计数 → 正确登录和爆破交替刷计数（Lab 6）
  - 按账号失败次数 → 让"失败"不算数：新密码不一致（Lab 12）/ 单请求多凭据（Lab 13）
- **改密码 / 重置密码 / 记住我 = 登录的旁路**，防护常被忽略，是爆破的好入口
- **响应文案差异永远是最好的命中信号**：`New passwords do not match` 判命中（Lab 12）、302 判命中（Lab 13）

## 待实践

- [ ] Lab 13 EXPERT 实操通关（思路已通：Repeater 里 password 改数组，一发入魂）
- [ ] 剩余 1 关：2FA bypass using a brute-force attack

## 关联笔记

- [PortSwigger 认证机制绕过实战](./2026-08-13-PortSwigger认证绕过实战.md) / [登录脆弱与认证缺陷](./2026-08-09-登录脆弱与认证缺陷.md) / [找回与重置机制的逻辑越权](./2026-08-09-找回与重置机制的逻辑越权.md)

> ⚠️ 声明：以上技术仅用于 CTF 竞赛和授权靶场环境。
