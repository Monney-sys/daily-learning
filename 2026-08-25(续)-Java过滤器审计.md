# 2026-08-25(续) Java 代码审计②：过滤器（四看法 + Filter vs Interceptor）

> 状态：🧪 理论（课程导图 + 对比表），待实操验证
> 第二课：**过滤器审计四看法** —— 看构成 → 看指向 → 看配置 → 看代码，四步走到「找绕过思路」。核心就是今天听课的感受：**找过滤器，看它拦了什么、哪里能绕过、哪里不完善**。

## 一、一句话认知

过滤器（Filter）是 Java Web 的第一道关卡。**审计过滤器 = 找它拦了什么、没拦什么、能不能绕过去**。四个「看」层层递进：先知道过滤器在哪（构成）→ 是谁写的（指向）→ 拦哪些请求（配置）→ 过滤逻辑怎么写的（代码），最后一步永远导向**绕过思路**。

## 二、过滤器审计四看法（课程核心）

### ① 看构成（过滤器在哪声明）

- **常规类**：web.xml —— `<filter>` / `<filter-mapping>` 声明
- **框架类**：struts.xml / spring-mvc.xml / hibernate.cfg.xml / mybatis-config.xml / applicationContext.xml …（框架自带过滤器）

### ② 看指向（内部还是外部）

- **内部**：自主编写的过滤器 —— 一般在**内部项目库**（自己写的代码，直接审计逻辑）
- **外部**：框架过滤器 —— 一般在**外部项目库**（jar 依赖里，如 Shiro 的过滤器，重点看配置 + 版本）

> 根据指向找到对应代码段：自主过滤器在项目里直接看实现；框架过滤器去 jar 里看实现 + 版本查 CVE。

### ③ 看配置（拦什么请求、什么规则）

- **触发请求**：哪些 URL/路径被过滤？（`/*` 全拦？还是只拦 `/admin/*`？**没被 mapping 覆盖的路径 = 绕过点**）
- **触发规则**：过滤规则怎么写的？（黑名单？白名单？匹配到了才拦？）

> 根据请求和规则想绕过思路 —— 比如只拦 `.jsp` 不拦 `.action`、只检查 POST 不检查 GET、路径黑名单漏了大小写/编码变体…

### ④ 看代码（过滤逻辑本身）

代码可能是**自主过滤代码**或**框架过滤代码**，逐行找绕过：
- 黑名单过滤 → 找没列进来的关键字 / 编码绕过
- 白名单校验 → 找**校验与使用不一致**（08-23 文件操作课学过：校验的是 A，用的是 B）
- 过滤后是否替换/拼接 → 找二次组合成新 payload 的可能

## 三、Filter vs Interceptor（必背对比）

| 维度 | Filter 过滤器 | Interceptor 拦截器 |
|------|--------------|-------------------|
| 实现机制 | 基于**函数回调** | 基于 **Java 反射** |
| 依赖 | 依赖 **Servlet 容器** | 不依赖 Servlet 容器（Spring/框架层） |
| 作用范围 | 几乎**所有请求** | 只能对 **action 请求**（Controller 方法）起作用 |
| 上下文访问 | 不能访问 Action 上下文 / 值栈 | 可以访问 **Action 上下文、值栈里的对象** |
| 执行顺序 | **先 Filter，后 Interceptor** | |

> 审计含义：Filter 是容器级第一道闸（web.xml），Interceptor 是框架级第二道闸（如 struts.xml 的 interceptor stack）。**两级都可能被绕过，且绕过点不一样** —— Filter 看 URL 匹配，Interceptor 看 action 匹配。

## 四、我的总结 / 疑问

- 收获：四看法 = 过滤器审计的标准套路，最后都收敛到「绕过思路」；Filter/Interceptor 五个差异点 = 两级防线各自的绕过面
- 疑问：
  1. ❓ 框架类过滤器（struts.xml / spring-mvc.xml 里配置的）具体长什么样？怎么和 web.xml 的常规过滤器区分？
  2. ❓ 「内部 / 外部项目库」在 IDEA 里怎么区分？jar 里的框架过滤器源码怎么看（反编译？）
  3. ❓ 黑名单过滤的编码绕过，Java 里具体有哪些变体？（URL 编码 / Unicode / 大小写 / 双写？）
  4. ❓ Filter/Interceptor「先 F 后 I」的执行顺序有没有例外？（比如注解配置的拦截器？）

## 关联笔记

- [2026-08-25-Java代码审计-整体技术.md](./2026-08-25-Java代码审计-整体技术.md) — 第一课整体地图（本课是「找拦截器」那一步的展开）
- [2026-08-23-ThinkPHP5框架代码审计.md](./2026-08-23-ThinkPHP5框架代码审计.md) — 「不安全写法 = 漏网之鱼」和过滤器绕过一个思路
- [2026-08-18(续)-框架MVC代码审计.md](./2026-08-18(续)-框架MVC代码审计.md) — 绕过框架防护的高危写法

> ⚠️ 声明：仅限授权环境（靶场/CTF/授权渗透）中测试，所有 Payload 只用于验证漏洞存在。
