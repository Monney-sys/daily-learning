# 2026-07-31 编辑器与 CMS 文件上传漏洞

> 编辑器是文件上传漏洞的高发区——很多站点上线后编辑器目录忘了删，或用了有公开漏洞的旧版本。

---

## 一、编辑器漏洞

### 1.1 FCKeditor

| 项目 | 内容 |
|------|------|
| 默认路径 | `/fckeditor/editor/filemanager/connectors/php/upload.php` |
| 版本探测 | `/fckeditor/_whatsnew.html` |
| 管理页面 | `/fckeditor/editor/filemanager/browser/default/browser.html` |

**常见漏洞**：
- **目录遍历**：`CurrentFolder` 参数可指定上传到任意目录
- **文件重命名**：上传 `1.asp;.jpg`，IIS 6.0 截断为 `.asp`
- **二次上传创建文件夹**：先传 `.asp` 文件夹，再传文件进去

**检测清单**：
```
/fckeditor/editor/filemanager/connectors/asp/connector.asp
/fckeditor/editor/filemanager/connectors/php/connector.php
/fckeditor/editor/filemanager/connectors/jsp/connector.jsp
```

### 1.2 eWebEditor

| 项目 | 内容 |
|------|------|
| 默认路径 | `/ewebeditor/`、`/admin/ewebeditor/` |
| 上传页面 | `/ewebeditor/admin_uploadfile.asp` |
| 样式管理 | `/ewebeditor/admin_style.asp` |

**常见漏洞**：
- **数据库下载**：`/ewebeditor/db/ewebeditor.mdb` 直接下载，拿到管理员密码哈希
- **样式绕过**：登录后台后修改样式配置，允许上传 `.asp` / `.asa`
- **后台弱口令**：默认 `admin` / `admin` 或 `admin888`
- **未授权样式修改**：部分版本可直接通过 URL 参数修改允许上传的后缀

### 1.3 CKEditor

| 项目 | 内容 |
|------|------|
| 默认路径 | `/ckeditor/`、`/ckfinder/` |
| 上传接口 | `/ckfinder/core/connector/php/connector.php` |

**常见漏洞**：
- **CKFinder 未授权上传**：旧版本未关闭调试模式，无认证即可上传
- **文件重命名**：上传 `shell.asp;.jpg`

### 1.4 KindEditor（CVE-2017-1002024）

| 项目 | 内容 |
|------|------|
| 影响版本 | ≤ 4.1.11 |
| 默认路径 | `/kindeditor/` |
| 上传接口 | `/kindeditor/php/upload_json.php?dir=file` |

**漏洞原理**：`dir=file` 参数允许上传任意类型文件，且未做认证和扩展名限制。

**复现步骤**：
1. 访问 `/kindeditor/php/upload_json.php?dir=file`
2. 构造 multipart POST 请求，上传 `shell.php`
3. 返回 JSON 包含文件路径
4. 访问 webshell 执行命令

```bash
# curl 复现
curl -F "imgFile=@shell.php" "http://target/kindeditor/php/upload_json.php?dir=file"
```

### 1.5 UEditor

| 项目 | 内容 |
|------|------|
| 默认路径 | `/ueditor/` |
| 上传接口 | `/ueditor/php/controller.php?action=uploadimage` |
| 远程拉取 | `/ueditor/jsp/controller.jsp?action=catchimage` |

**常见漏洞**：
- **SSRF**：`catchimage` 可拉取远程图片，用于探测内网端口
- **配置泄露**：`/ueditor/php/config.json` 可能暴露上传路径和白名单配置

### 编辑器速查表

| 编辑器 | 特征路径 | 常见版本 |
|--------|---------|---------|
| FCKeditor | `/fckeditor/editor/` | 2.x |
| CKEditor | `/ckeditor/samples/` | 3.x/4.x |
| eWebEditor | `/ewebeditor/admin_login.asp` | 3.x ~ 11.x |
| KindEditor | `/kindeditor/demo.php` | 3.x ~ 4.x |
| UEditor | `/ueditor/dialogs/` | 1.x |

---

## 二、CMS 文件上传漏洞

### 2.1 WordPress

| 漏洞点 | 说明 |
|--------|------|
| 主题/插件上传 | 后台上传恶意 ZIP 主题包，解压后 getshell |
| 媒体库 | 默认不解析 PHP，需配合其他漏洞 |
| REST API | CVE-2015-1000012 旧版本未授权上传 |
| File Upload 插件 | CVE-2024-11613/11635，见近年 CVE 笔记 |

**测试路径**：`/wp-admin/async-upload.php`、`/wp-content/uploads/`

### 2.2 DedeCMS（织梦）

| 漏洞点 | 说明 |
|--------|------|
| 会员头像上传 | `/member/edit_face.php` → 上传图片马 + 配合文件包含 |
| 后台文件管理器 | `/dede/file_manage_control.php` → 获取后台后直接写文件 |
| 会员投稿附件 | 黑名单不完整，可能绕过 |

**常见版本**：v5.7（使用最广泛的版本）

### 2.3 PHPCMS

| 漏洞点 | 说明 |
|--------|------|
| 会员中心头像 | `/index.php?m=member&c=index&a=upload_avatar` → MIME 绕过 |
| 编辑器上传 | 集成了 KindEditor，受其漏洞影响 |

### 2.4 EmpireCMS（帝国）

| 漏洞点 | 说明 |
|--------|------|
| 后台附件上传 | 默认允许 `.php` 文件上传 |
| 数据库备份还原 | 用 webshell 写入 `.php` 后缀的备份文件名 |

### 2.5 ThinkCMF

| 漏洞点 | 说明 |
|--------|------|
| 模板上传 | 后台上传含 `.php` 文件的 ZIP 模板包 |

---

## 三、通用测试清单

### 3.1 识别 CMS / 框架

```
/robots.txt       → 可能暴露 CMS 路径
/CHANGELOG.txt    → Drupal 版本泄露
/readme.html      → WordPress 版本
Meta Generator    → <meta name="generator" content="DedeCMS 5.7">
```

### 3.2 编辑器扫描路径

```
/fckeditor/editor/filemanager/browser/default/browser.html
/ckeditor/samples/
/kindeditor/demo.php
/kindeditor/php/upload_json.php
/ueditor/dialogs/image/image.html
/ewebeditor/admin_login.asp
/ewebeditor/db/ewebeditor.mdb
```

### 3.3 测试流程

```
1. 指纹识别 → 确定 CMS/编辑器 类型和版本
2. 搜索 CVE → CVE Details / Exploit-DB / GitHub PoC
3. 找上传点 → 按 CMS 默认路径逐个测试
4. 上传测试 → 先传合法文件，观察返回路径和文件名规则
5. 选择绕过 → 根据后端校验类型选择对应手法
```

---

> 关联笔记：[文件上传漏洞体系总览](./2026-07-31-file-upload-vulnerability.md) | [解析漏洞](./2026-07-31-file-upload-parser-vulnerability.md) | [近年文件上传 CVE](./2026-07-31-file-upload-recent-cve.md)

> ⚠️ 声明：以上技术仅用于授权靶场和渗透测试授权目标。
