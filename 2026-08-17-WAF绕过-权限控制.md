# 2026-08-17 WAF绕过：权限控制阶段（④）

> 状态：🧪 理论未验证（本地三层 WAF 环境还没搭，这部分同样是纯理论，没实操）

> 今天学 WAF 绕过系列第四部分：**权限控制阶段**（第三部分「漏洞利用阶段」明天再学）。这一部分讲的是**拿到/维持权限阶段**（Webshell 落地、用管理工具连接）遇到 WAF 怎么办 —— 核心是**脚本特征和工具指纹**的绕过：从用现成工具（菜刀/蚁剑/冰蝎），到用三个代码方法（变量覆盖/加密混淆/异或生成）+ 指纹变异改造，最后走到**自己写脚本**（自写轮子）。

---

## 一、是什么（我的理解）

### 1.1 一句话

权限控制阶段 = 已经拿下（或正在维持）Webshell 权限的阶段。这个阶段 WAF 拦的不再是"请求频率"，而是**脚本文件本身**和**管理工具的指纹** —— 所以绕过思路从"降速、改包"变成"改代码、改指纹"，终点是自写轮子。

### 1.2 为什么这个阶段会撞 WAF

- **脚本落地时**：Webshell 文件里有危险函数名（eval/assert/执行函数等）、加密串等特征，上传/落地瞬间就可能被查
- **工具连接时**：菜刀/蚁剑/冰蝎等管理工具的流量有固定指纹（默认 UA、请求格式、默认参数名、固定加密密钥），WAF 的工具特征库一认一个准
- 权限控制阶段是"人和 WAF 缠斗"最长的阶段：权限要持续可用、工具要反复连接，特征暴露面大

---

## 二、导图总览：四个维度

| 维度 | 内容 | 一句话理解 |
|------|------|-----------|
| **脚本** | ASP / PHP / JSP / ASPX / PY、WAR 等 | Webshell 的载体语言，按目标环境选型 |
| **工具** | 菜刀 / 蚁剑 / 冰蝎 | 现成的 Webshell 管理工具，好用但指纹全被收录 |
| **代码** | 加密混淆 / 变量覆盖 / 异或生成 / 指纹变异 / 自写轮子 | 让脚本"失去特征"的手段，从改代码到不依赖工具 |
| **行为** | 检测 / 常规安全脚本工具使用 | 操作层面的验证：用常规工具检测自己的脚本/流量有没有被识别 |

**一条主线**：使用工具 → 工具特征被 WAF 检测 → 三个代码方法（变量覆盖/加密混淆/异或生成）消除明文特征 → 指纹变异换掉工具默认特征 → **自写轮子**（特征完全未知，最彻底）。

---

## 三、脚本维度：载体语言

Webshell 不是一套代码走天下，要按目标环境的语言选：

| 语言 | 常见环境 |
|------|---------|
| ASP / ASPX | Windows + IIS 系 |
| PHP | Linux + Apache/Nginx（最常见） |
| JSP | Java 系中间件（Tomcat/WebLogic 等） |
| PY、WAR 等 | Python 应用 / Java 打包部署 |

**我的理解**：每个语言的危险函数、写法都不一样，WAF 是分语言建特征库的 —— 所以"脚本"维度解决的是**在什么环境用什么载体**，后面的代码改造都得在对应语言里做。

---

## 四、工具维度：菜刀 / 蚁剑 / 冰蝎

三个主流 Webshell 管理工具，特点都是**上手快、功能全**，但**指纹已经被 WAF 收录**：

- 固定特征包括：默认 User-Agent、固定请求头、固定参数名/格式、连接密码特征、甚至固定加密密钥
- 用工具默认配置连 Webshell，流量特征基本等于"明牌"，WAF 规则一匹配就拦

**我的理解**：工具维度点出问题根源 —— 不是 WAF 多厉害，是**工具太有名、特征太固定**。所以后面的代码维度全是围绕"让工具流量失去已知特征"展开。

---

## 五、代码维度：三个核心方法 + 指纹变异 + 自写轮子

| 方法 | 干什么 | 我的理解 |
|------|--------|---------|
| **变量覆盖** | 把特征关键字拆散到变量里，运行时再组合出来 | 静态扫描（WAF 特征匹配看的是明文）找不到危险函数名；执行到那一步才动态还原 |
| **加密混淆** | 真实代码先加密/编码，落地的是一段"乱码"，运行时解密再执行 | WAF 特征库匹配的是明文特征，密文不命中；解密逻辑只存在于运行时 |
| **异或生成** | 用异或运算在运行时"算"出特征字符/字符串 | 文件里不出现明文关键字，正则/特征码匹配不到，但运算结果能还原出真正的函数名 |
| **指纹变异** | 改掉工具的默认特征（UA/请求头/参数名/密钥） | 相当于"换衣服"：工具还是那个工具，但 WAF 认不出来了 |
| **自写轮子** | 自己写连接/交互脚本，不用现成工具 | 特征完全未知，WAF 没有可匹配的库 —— 最彻底，但开发维护成本最高 |

**我的理解**：三个核心方法（变量覆盖/加密混淆/异或生成）本质都是同一件事 —— **让明文特征消失**：静态看不像攻击，运行时才还原。指纹变异和自写轮子则是跳出"改脚本"、上升到"改工具/弃工具"。

---

## 六、行为维度：检测 / 常规安全脚本工具使用

导图最后一个分支是**行为**：检测 + 常规安全脚本工具使用。

**我的推测**：这是操作层面的闭环 —— 代码改完、指纹变完，要用**常规安全脚本工具去"检测"**自己的脚本/流量是否还会被 WAF 识别（被拦就继续改）。也就是"行为"上的验证环节，对应前面学的"本地搭环境复现"思路。

> ❓ 待确认：这分支也可能指 WAF 的"行为检测"（识别你在用常规安全脚本工具），如果是的话"自写轮子"就是为了躲行为检测。上课时留意老师怎么讲的。

---

## 七、我的总结

- **拦截对象变了**：权限控制阶段 WAF 拦的是脚本特征 + 工具指纹，不是请求频率 —— 所以绕过手段和前两阶段（降速/改包）完全不同
- **三个代码方法是核心**：变量覆盖/加密混淆/异或生成，本质都是"让明文特征消失，运行时才还原"
- **演进路线**：用工具（省事但特征已知）→ 代码改造（变量覆盖/加密混淆/异或生成）→ 指纹变异（换衣服）→ 自写轮子（新面孔，特征未知）
- **成本与隐蔽性同涨**：越往后越难被检测，但开发维护成本也越高
- 和前面呼应：整个 WAF 绕过系列就围绕一个核心 —— **让 WAF 认不出你**，不同阶段只是"认不出的点"不同（数据包/频率/脚本/工具指纹）

---

## 关联笔记

- [WAF绕过：信息收集与漏洞发现阶段](./2026-08-15-WAF绕过-信息收集与漏洞发现.md) — 本系列前两阶段：数据包拦截改特征、速度拦截降频率（代理池+延时最稳）
- [文件上传WAF绕过与防御](./2026-08-02-文件上传WAF绕过与防御.md) — Webshell 落地的入口场景，15 种上传绕过手法，和本课"脚本落地被查"直接相关
- [SQL注入WAF绕过](./2026-07-30-SQL注入WAF绕过.md) — 注入场景的 WAF 绕过（双写/大小写/内联注释），同样是"让 payload 不像攻击"

## 待实践

- [ ] 三层 WAF 本地环境搭好后，实测菜刀/蚁剑/冰蝎默认连接流量长什么样，抓包对比改 UA/参数前后的拦截差异
- [ ] 明天补第三部分：漏洞利用阶段的 WAF 绕过

> ⚠️ 声明：以上技术仅用于 CTF 竞赛和授权靶场环境。
