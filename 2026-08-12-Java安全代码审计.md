# 2026-08-12 Java 安全代码审计（Java 安全板块第二关）

> 今天学 Java 安全里的"通用漏洞代码审计"。**我没学过 Java**，所以这关的难点不是漏洞本身（前面的 SQLi/XSS/RCE 都学过了），而是看懂 Java 代码。我的策略：先背熟高危 API，再去 WebGoat 里看每关的原始代码，边审边学。这篇标记为**需要大量练习**。

---

## 一、是什么（我的理解）

### 1.1 一句话

代码审计 = 从源码里找漏洞。Java 版 = 认识 Java 的**高危 API** + 顺着**数据流**（用户输入 → 危险函数）找触发点。

### 1.2 为什么"不会 Java"也能学

漏洞的本质和语言无关：SQL 注入还是拼接、XSS 还是没转义、命令执行还是参数进 shell。Java 只是换了层语法外壳，危险 API 就那么十几个 —— **先认脸，再看数据流**。

### 1.3 类比（延续快递盒系列）

```
代码审计 = 在仓库里查"进货 → 上架"流程里的漏洞
Java 代码 = 仓库的货架摆放规则（先学会看货架）
高危 API  = 贴着"危险品"标签的货架（看到就警觉）
数据流    = 货从哪进来、经不经过安检（校验）到哪去
WebGoat   = 每个货架都配了"正常版 vs 出事故版"对照
```

---

## 二、Java 代码审计的地基

### 2.1 高危 API 速查表（看到就警觉）

| 漏洞类型 | Java 里的危险写法 | 安全写法 |
|---------|------------------|---------|
| SQL 注入 | `Statement.executeQuery("SELECT ... WHERE name='" + name + "'")`；MyBatis `${name}` | `PreparedStatement` 预编译；MyBatis 用 `#{}` |
| 命令执行 | `Runtime.getRuntime().exec(cmd)`、`ProcessBuilder` | 参数拼接用户可控就是洞 |
| 反序列化 | `ObjectInputStream.readObject()`；fastjson `JSON.parseObject(str)` 带 `@type`；`XMLDecoder` | 过滤危险类/关 autoType |
| 文件操作 | `new File(userInput)`、`Files.copy(...)`、没有 `getCanonicalPath()` 校验 | 路径白名单 + canonical 校验 |
| SSRF | `URL.openConnection()`、HttpClient/OkHttp 请求用户可控 URL | URL 白名单/禁止内网段 |
| XSS | `response.getWriter().println(param)`；Thymeleaf `th:utext`（vs `th:text`） | 输出编码/默认转义 |
| 文件上传 | `MultipartFile` + 黑名单后缀 + 路径直接拼接 | 白名单 + 随机文件名 |
| 越权/IDOR | 只查登录不查归属、`id` 参数直接查库、缺 `@PreAuthorize` | 归属校验 + 权限注解 |
| 认证缺陷 | JWT 密钥硬编码、密码 MD5/明文存储、Cookie 可篡改 | 延续昨天 JWT 笔记 |

### 2.2 读懂 Java 代码的最小知识集（没学过 Java，先记这些）

- **结构**：`public class` / `public static void main`；方法 `返回值 方法名(参数)`；`import` 引包
- **Spring 注解（审计地图）**：
  - `@RestController` / `@Controller` = 这是处理 HTTP 的类
  - `@GetMapping("/xxx")` / `@PostMapping("/xxx")` = **路径入口（审计从这进）**
  - `@RequestParam` / `@PathVariable` / `@RequestBody` = **用户可控参数（审计源头）**
  - `@Autowired` = 注入依赖（Service/Mapper）
- **分层**：Controller（接参数）→ Service（业务）→ Mapper/DAO（查库）→ 返回前端
- **依赖**：`pom.xml`（Maven）里看版本 —— 老版本 = 已知 CVE（fastjson、log4j 这类）
- 我的策略：**不先系统学语法，直接啃 WebGoat 源码，遇到不懂的 API 再查** —— 边审边学效率最高

---

## 三、怎么利用（WebGoat 源码审计实操）

### 3.1 在 WebGoat 里看原代码的方法

- WebGoat 每关的 Java 源码在 lesson 包里（`org.owasp.webgoat.lessons.*`），IDE 打开工程或去 GitHub 仓库直接看
- 对照流程：**页面功能 → 抓包看请求路径 → 源码里搜对应的 `@PostMapping("/路径")` → 找到处理方法 → 跟参数和数据流**

### 3.2 审计五步法（我的套路）

```
① 入口：找所有 @RequestMapping / @GetMapping / @PostMapping —— 先知道哪些接口能调
② 源头：方法参数哪些用户可控（@RequestParam / @RequestBody / 路径变量）
③ 数据流：可控参数怎么走到危险 API（直接拼接？还是先校验？）
④ 校验：有没有过滤 / 白名单 / 权限注解（@PreAuthorize）
⑤ 对照修复版：WebGoat 每关有修复代码，看差异 = 漏洞根因
```

### 3.3 逐关计划（WebGoat 源码对照，待实践）

| 关卡 | 我要在源码里找什么 |
|------|-------------------|
| SQL 注入 | `Statement` 拼接 vs `PreparedStatement`，MyBatis `${}` vs `#{}` |
| XSS | 输出点有没有转义（`th:utext`？直接 println？） |
| 反序列化 | `readObject` 入口在哪、对象类型怎么被控制 |
| JWT | 密钥写在哪（硬编码？弱密钥？） |
| 文件上传 / 命令执行 | 后缀校验、`exec` 参数拼接 |

（具体代码细节我还没逐关看完，先把方法论记下来，**看一关补一关**。）

---

## 四、怎么防御（审计视角的防御清单）

| 层面 | 措施 |
|------|------|
| 查询 | 参数化查询/预编译：JDBC `PreparedStatement`、MyBatis `#{}`，禁用 `${}` |
| 输入 | 统一校验：白名单优先（文件类型/URL/上传），长度、类型限制 |
| 权限 | 接口级 `@PreAuthorize` + 业务级归属校验（查到的数据属于当前用户吗） |
| 输出 | 模板默认转义（Thymeleaf `th:text`），用户输入禁止进 `th:utext` |
| 依赖 | 版本管理 + SCA 扫描（OWASP Dependency-Check），盯 fastjson/log4j 高危库 |
| 密钥 | JWT 密钥/数据库密码进配置中心或 KMS，不硬编码（延续 JWT 笔记） |

---

## 五、我的总结 / 疑问

### 总结

- **代码审计 = 找"用户输入 → 危险函数"的路径**，Java 只是外壳，高危 API 就十几个，先背熟再谈看代码
- 不会 Java 反而是优势：逼着自己只抓"数据流"主线，不被语法细节带偏；WebGoat 的"漏洞版 vs 修复版"对照是最快的学法
- 这关**需要大量练习**：练到"看到高危 API 就条件反射"的程度为止

### 疑问

- MyBatis `${}` 和 `#{}` 的具体区别？（`${}` 直接拼 SQL，`#{}` 预编译占位符 —— 待写代码验证）
- 为什么 `PreparedStatement` 能防注入而 `Statement` 不能？（预编译后参数不参与 SQL 解析）
- Java 项目里"前端可控"的边界到底在哪？（`@RequestParam` vs `@RequestBody` vs Header/Cookie，哪些真可控）

---

## 关联笔记

- [JWT 安全与攻击](./2026-08-12-JWT安全与攻击.md) — 同板块第一关，认证类漏洞的 Java 侧写法
- [Java 反序列化入门](./2026-08-11-Java反序列化入门.md) — `readObject` 入口的审计点
- [Java 反序列化实战-WebGoat与选链](./2026-08-11-Java反序列化实战-WebGoat与选链.md) — WebGoat 看源码/构造 payload 的经验
- [SQL 注入基础](./2026-07-26-SQL注入基础.md) — SQLi 原理通用，Java 里换写法
- [RCE 远程代码命令执行详解](./2026-08-05-RCE远程代码命令执行详解.md) — `exec` 审计点的通用方法论

## 待实践

- [ ] 系统补 Java 基础（面向对象/集合/IO/网络），每天 30 分钟，跟着下午视频走
- [ ] WebGoat 逐关看源码：SQL 注入 → XSS → 反序列化 → JWT → 上传，每关记录"漏洞代码 vs 修复代码"
- [ ] 本地把 WebGoat 源码工程跑起来（IDEA），打断点跟一次完整数据流
- [ ] 找一个开源 Java 项目（mall 商城 / 若依 RuoYi 这类）做一次小范围审计练手

> ⚠️ 声明：以上技术仅用于 CTF 竞赛和授权靶场环境。
