# 2026-08-23 代码审计③：文件操作类漏洞审计（上传 / 删除 / 读 / 写）

> 状态：🧪 理论 + 课程案例（Metinfo 变量覆盖、phpmydmin 反序列化），待下源码实操
> 代码审计板块第三课：聚焦**文件操作功能**的审计。这课把之前学的污点分析落到文件函数上，顺带补了两个无框架实战案例（已追加到第一课笔记末尾）。

---

## 一、是什么（我的理解）

### 1.1 一句话

**文件操作类漏洞 = 文件函数（Sink）的参数被用户输入（Source）控制，中间过滤缺失或可绕** → 结果：上传马（getshell）/ 删任意文件 / 读任意文件 / 写任意文件。

### 1.2 审计三问（所有文件操作通用）

1. **危险函数的参数可控吗？**—— 往上回溯，参数最终来自用户输入还是写死？
2. **中间有过滤吗？**—— 白名单还是黑名单？（白名单难绕，黑名单能绕）
3. **校验用的是哪个值，实际使用的是哪个值，一致吗？**—— `校验 $ext` 但 `拼接 $filename`（原始值）= 校验形同虚设，这是最经典的洞。

> 核心认知：审计不是"找到赋值处去修改代码"，而是**确认变量可控性 + 找过滤绕过**。写死的值 = 不可控 = 这条线走不通。

---

## 二、四类文件操作审计清单

| 功能 | 危险函数（Sink） | 可控点 | 漏洞结果 |
|------|-----------------|--------|---------|
| 上传 | `move_uploaded_file` / `copy` | 目标路径、文件名、文件内容 | 传 webshell → getshell |
| 删除 | `unlink` / `rmdir` | 路径参数 | 任意文件删除 |
| 读取 | `file_get_contents` / `readfile` / `fopen` / `include` | 路径参数 | 任意文件读 / LFI |
| 写入 | `file_put_contents` / `fwrite` | 路径 + 内容 | 任意文件写 → 写 shell |

### 2.1 上传（重点中的重点）

- 目标路径可控？**文件名可控**？（`$_FILES['file']['name']` 是用户输入）
- 扩展名校验：**白名单 or 黑名单**？黑名单可绕过（见下）
- **校验值 vs 使用值**：校验 `$ext`，拼接 `$filename` → 不一致 → 绕过
- 文件内容检没检？只查 Content-Type（客户端头，可改）/ 查文件头 magic bytes（jpg 头 + php 内容）
- 经典绕过手法：大小写 `.pHp`、双写 `.pphphp`、空格/点结尾（Windows 截断）、`%00` 截断（PHP<5.3.4）、`.htaccess` / `.user.ini` 上传、中间件解析特性（Apache 多后缀、Nginx `fix_pathinfo`、IIS 分号）
- 路径穿越：文件名里带 `../../` → 传到 web 根外 / 任意目录

### 2.2 删除

- `unlink($path)` 的 `$path` 直接拼用户输入 → **任意文件删除**
- 穿越过滤：`../` 被过滤？能绕吗（编码 `%2e%2e%2f`、双写 `....//`、绝对路径）？
- 利用场景：删安装锁文件 → 重装 getshell；删配置文件 → 报错泄露信息；删备份 → 破坏数据

### 2.3 读取

- 路径可控 → 任意文件读（源码 / 数据库配置 / 密码文件 / 日志）
- `include` 可控 → LFI → `php://filter` 链 RCE（之前文件包含课已学）
- 过滤绕过：`../` 双写、编码、绝对路径、伪协议

### 2.4 写入

- **路径 + 内容都可控 = 直接写 shell**（最严重）
- 内容可控、路径不可控：写日志文件 → 日志投毒 → 配合 LFI 包含执行
- 写入内容有过滤（禁 `<?php`）？可绕：短标签 `<?=`、大小写、编码、`.htaccess` 配解析

---

## 三、方法论（与第一课衔接）

1. **功能点定位**：上传/删除/下载/编辑页面 → 找到对应的处理函数（控制器方法）
2. **反向**：Seay 一键扫 / grep 危险函数（`move_uploaded_file` / `unlink` / `file_put_contents`…）→ 逐个回溯参数
3. **正向**：从可控输入（`$_FILES` / `$_GET` / `$_POST`）往下追，看能流到哪些文件函数
4. **关键检查**：过滤函数的位置、过滤的变量、使用的变量是否同一个（三问第 3 条）
5. **落地验证**：本地搭环境（XAMPP），构造请求看文件是否落地 / 被删 / 被读

---

## 四、课程案例速记（无框架两个例子，详见第一课笔记补充）

| 案例 | 类型 | 一句话本质 |
|------|------|-----------|
| Metinfo | 变量覆盖 | 变量覆盖本身不是洞，**被覆盖的变量流进文件操作函数**才是洞（自动审计/搜索 `extract()` / `parse_str()` / `$$` 特征） |
| phpmydmin | 反序列化 | `unserialize()` 入口可控 → 触发魔术方法链 → RCE（重点找：可控反序列化点 + POP 链） |

---

## 五、怎么防御（审计的镜像）

- 文件操作路径用 **ID 映射 / 白名单**，不直接拼用户输入
- 上传：**扩展名白名单**（不是黑名单）+ 内容校验 + **随机文件名**（用户不可控文件名）+ 存储目录禁执行（Nginx/Apache 配置）
- 删除/读取：`realpath()` + 目录白名单校验，拒绝 `../`
- 集中收参走框架过滤入口，禁止裸拼路径
- 框架/依赖勤升级；`disable_functions` 兜底（危险文件函数禁用）

---

## 六、总结 / 疑问

- 收获：文件操作类审计 = **污点分析在文件函数上的落地**；四大类函数一张表、审计三问、一个关键（校验与使用不一致）
- 疑问：
  1. ❓ Metinfo 变量覆盖具体是怎么覆盖到文件操作参数的？（源码已下 `D:\php\audit-targets`，走一遍）
  2. ❓ phpmydmin 反序列化的入口和利用链具体是什么？（课程素材不在公开源，待从课程网盘拿）
  3. ❓ 上传「校验 $ext / 拼接 $filename」的绕过，除了经典那几种还有实战变体吗？（如双扩展名 + 解析特性组合）

## 关联笔记

- [2026-08-18-PHP无框架代码审计入门.md](./2026-08-18-PHP无框架代码审计入门.md) — 无框架审计两主线 + 危险函数速查（本课案例已补到末尾）
- [2026-08-18(续)-框架MVC代码审计.md](./2026-08-18(续)-框架MVC代码审计.md) — 框架审计地图
- [2026-08-23-ThinkPHP5框架代码审计.md](./2026-08-23-ThinkPHP5框架代码审计.md) — 今天的框架课
- [2026-07-31-文件上传漏洞.md](./2026-07-31-文件上传漏洞.md) — 漏洞侧的完整体系（黑盒视角，呼应）

> ⚠️ 声明：仅限授权环境（靶场/CTF/授权渗透）中测试，所有 Payload 只用于验证漏洞存在。
