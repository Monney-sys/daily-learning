# 网站信息搜集方法论

## 一、站点搭建分析

拿到一个目标网站，第一步是搞清楚它的"底子"——用了什么架构、怎么部署的。

### 1.1 网站类型判断

- **CMS 识别**：WordPress / DedeCMS / 帝国CMS / Discuz 等，利用特有路径（`/wp-admin`、`/plus`、`/api/uc.php`）或页面特征（favicon hash、meta generator）判断
- **框架识别**：ThinkPHP / Struts2 / Spring Boot / Laravel / Django，看响应头（`X-Powered-By`）、错误页面特征、路由格式
- **开发语言**：PHP / JSP / ASPX / Python，通过 URL 后缀（`.php`、`.jsp`、`.aspx`、`.do`、`.action`）、Cookie 名（`JSESSIONID`、`PHPSESSID`、`ASP.NET_SessionId`）判断
- **中间件识别**：Nginx / Apache / IIS / Tomcat，看 `Server` 响应头、404 页面、文件解析特性

### 1.2 搭建习惯分析

| 类型 | 说明 | 搜集思路 |
|------|------|----------|
| **目录型站点** | 主站下一个目录就是另一个系统，如 `xxx.com/blog` | 目录扫描（dirsearch / gobuster）发现隐藏目录，可能对应不同系统 |
| **端口型站点** | 不同端口跑不同服务，如 `:8080`、`:8443`、`:9090` | 全端口扫描（nmap / masscan），每个端口都可能是一个独立站点 |
| **子域名站点** | 不同子域名对应不同系统，如 `admin.xxx.com`、`api.xxx.com` | 子域名爆破、证书透明度（crt.sh）、DNS 域传送漏洞 |
| **类似域名站点** | 注册相近域名，如 `xxx-inc.com`、`xxx.cn`、`xxx.net` | 域名变体生成，WHOIS 反查注册邮箱关联的其他域名 |
| **旁注 / C段站点** | 同一服务器或同一网段下的其他站点 | 同 IP 反查（旁注）—查看同一服务器上还有哪些站；C 段扫描—同一网段的服务器可能有相似配置 |
| **搭建软件特征** | 如 phpStudy、宝塔面板、LNMP 一键包 | 特定端口（宝塔 8888、phpMyAdmin 等）、默认页面、默认账号密码 |

### 1.3 补充：站点指纹信息

- **HTTP 响应头分析**：`Server`、`X-Powered-By`、`Set-Cookie`、`X-AspNet-Version` 等直接暴露技术栈
- **SSL/TLS 证书信息**：证书 SAN 列表可能暴露其他子域名
- **JS/CSS 源码泄露**：前端代码中的 API 接口、注释、路径信息
- **robots.txt / sitemap.xml**：可能暴露不想被搜索引擎收录但实际存在的路径
- **错误页面信息泄露**：触发 404/500 错误，观察是否暴露中间件版本、绝对路径
- **网页爬虫**：爬取全站 JS 文件，提取 API 接口、隐藏路径、云存储地址

---

## 二、子域名搜集

子域名是信息搜集的核心环节，往往一个不起眼的子域名就是突破口。

### 2.1 搜集渠道

| 方法 | 工具/平台 | 说明 |
|------|-----------|------|
| **证书透明度（CT）** | crt.sh、Censys | 查询 TLS 证书签发记录，发现历史子域名 |
| **DNS 枚举爆破** | Layer、Subfinder、ksubdomain、OneForAll | 字典爆破子域名，速度快慢取决于字典质量 |
| **DNS 域传送漏洞** | dig / nslookup | `dig axfr @dns-server domain.com`，极少见但一旦存在直接全泄露 |
| **搜索引擎** | Google / Bing（site: 语法） | 搜索引擎收录的子域名 |
| **第三方平台** | SecurityTrails、VirusTotal、DNSdumpster | 聚合 DNS 数据，免费额度通常够用 |
| **GitHub 搜索** | 代码中可能包含内网域名、测试域名 | 监控代码泄露 |

### 2.2 子域名验证

- DNS 解析验证：确认子域名是否解析到有效 IP
- HTTP 探活：多线程请求，判断是否 200/403/301
- 截图留存：用 EyeWitness / gowitness 对存活域名截图

---

## 三、DNS / 备案 / 证书

### 3.1 WHOIS 与备案

- **WHOIS 查询**：域名注册人、注册邮箱、注册时间、DNS 服务器
- **WHOIS 反查**：通过注册邮箱反查该人名下所有域名，发现关联资产
- **ICP 备案查询**（国内特有）：beian.miit.gov.cn，通过备案号查主体、通过主体查所有域名
- **企业信息关联**：天眼查 / 企查查，通过备案主体查企业股权关系，找到子公司、关联公司域名

### 3.2 DNS 记录

- **A/AAAA 记录**：域名到 IPv4/IPv6
- **CNAME 记录**：域名别名，可判断是否使用了 CDN
- **MX 记录**：邮件服务器，可判断邮件网关品牌（如腾讯企业邮、网易）
- **TXT 记录**：SPF/DKIM 信息，可能泄露邮件域名、第三方服务商
- **NS 记录**：DNS 服务商信息

### 3.3 证书信息

- **证书透明度日志**：免费且全面，不接触目标即可获取
- **证书 SAN 字段**：一张证书可能覆盖多个域名
- **证书过期/自签名**：运维疏忽可能泄露内网架构

---

## 四、CDN 识别与绕过

### 4.1 判断是否使用 CDN

- **多地 ping**：不同地区解析到不同 IP，大概率有 CDN（工具：ping.pe、17ce.com）
- **DNS 历史记录**：查看历史解析 IP 是否变更
- **ASN 归属**：同一域名解析到多个不同 ASN 的 IP

### 4.2 CDN 绕过与真实 IP 获取

| 方法 | 说明 |
|------|------|
| **子域名** | 部分子域名可能没挂 CDN，直接暴露真实 IP |
| **邮件服务** | 发送邮件触发目标服务器发信，查看邮件头中的原始 IP |
| **国外节点** | 部分 CDN 仅加速国内，国外解析可能指向真实 IP |
| **历史 DNS** | SecurityTrails / DNSdb 查询历史 A 记录 |
| **SSL 证书** | 对 IP 段进行 SSL 扫描，匹配证书 CN/SAN 字段 |
| **网站漏洞** | phpinfo、错误日志、上传接口返回等泄露真实 IP |
| **全网扫描** | 基于 HTTP 响应特征（title、favicon hash、特定 header）对 IP 段扫描匹配 |

---

## 五、端口扫描与服务发现

### 5.1 端口扫描

- **全端口扫描**：masscan（速度快）或 naabu（Go 实现，轻量）
- **服务版本探测**：nmap -sV，识别端口上运行的具体服务及版本
- **操作系统指纹**：nmap -O，判断目标服务器操作系统

### 5.2 常见高危端口

```
21   FTP（匿名登录、弱口令）
22   SSH（弱口令爆破）
3306 MySQL（弱口令、未授权）
6379 Redis（未授权访问）
27017 MongoDB（未授权访问）
1433 MSSQL（弱口令）
1521 Oracle（弱口令）
5432 PostgreSQL（弱口令）
11211 Memcached（未授权）
2181 Zookeeper（未授权）
2375 Docker Remote API（未授权，可直接控制容器）
8080/8443  Web 管理后台
8899 宝塔面板
```

---

## 六、WAF 防护分析

### 6.1 什么是 WAF

WAF（Web Application Firewall）是部署在 Web 应用前的安全防护系统，通过分析 HTTP/HTTPS 流量来拦截 SQL 注入、XSS、命令注入等攻击。

### 6.2 如何快速识别 WAF

- **工具识别**：wafw00f（支持 150+ 种 WAF 指纹）
- **手动判断**：发送恶意 Payload（如 `/index.php?id=1' OR '1'='1`），观察拦截页面特征
- **响应头匹配**：部分 WAF 会在 Cookie（如 `__cfduid` = Cloudflare）或 Header 中打标记
- **HTTP 状态码**：WAF 拦截后可能返回特定状态码（403、406、501 等）

### 6.3 识别 WAF 的意义

- **制定绕过策略**：不同 WAF 的绕过手法不同（分块传输、协议层绕过、编码绕过）
- **评估目标难度**：有 WAF 的目标需要更多的绕过技巧和耐心
- **规避封禁**：了解 WAF 的拦截规则后可控制扫描频率和 Payload 特征，避免 IP 被封

---

## 七、黑暗引擎 / 网络空间搜索引擎

这些搜索引擎通过全网扫描建立设备指纹库，可快速获取目标资产概况。

| 平台 | 特点 |
|------|------|
| **FOFA**（fofa.info）| 国内最强，支持丰富的搜索语法，国内资产覆盖率高 |
| **Shodan**（shodan.io）| 全球范围，偏 IoT/工控设备搜索 |
| **ZoomEye**（zoomeye.org）| 国内，知道创宇出品，Web 资产覆盖好 |
| **Hunter**（hunter.qianxin.com）| 奇安信出品，国内资产较准 |
| **Censys**（censys.io）| 证书和域名搜索能力强 |

### 常用搜索语法（以 FOFA 为例）

```
domain="xxx.com"                    # 子域名
cert="xxx.com"                      # 证书匹配
title="后台管理"                      # 标题关键字
body="技术支持"                       # 页面内容关键字
header="Server: nginx"              # 响应头匹配
ip="1.1.1.1/24"                     # C 段搜索
protocol="redis" && country="CN"    # 全国 Redis 服务
```

---

## 八、GitHub 监控

### 8.1 价值

- 发现目标企业的**源代码泄露**（配置文件、数据库密码、API Key）
- 获取最新 **exp 或 PoC** 仓库，第一时间复现漏洞
- 搜索目标域名/内网地址，发现员工无意上传的**内部文档**

### 8.2 监控方式

- **GitHub Code Search**：`"target.com" password`、`"target.com" api_key`
- **CVE PoC 监控**：`CVE-2026 poc`、`RCE exploit`（自建脚本或现成工具）
- **自动化**：定时运行脚本，结果推送到本地或微信通知

---

## 九、微信公众号 / 小程序接口搜集

### 9.1 为什么关注

- 公众号/小程序后台 API 可能与企业内网互通
- 接口鉴权不严可直接越权获取数据
- 部分企业内部系统通过微信入口暴露

### 9.2 搜集方式

- **微信公众平台**：搜索企业名称，发现关联公众号和小程序
- **小程序抓包**：Charles / Fiddler 抓取 HTTPS 请求，提取 API 域名
- **小程序反编译**：安卓端小程序 wxapkg 包可反编译，获取源码中的 API 接口、密钥
- **公众号接口测试**：获取 access_token，测试消息推送、用户管理等接口权限

---

## 十、APP 资产搜集

移动端 App 往往有独立的 API 后端，可能绕过 Web 端的防护。

### 10.1 APK 逆向

- **抓包**：Burp Suite 设置代理 + 安装 CA 证书，捕获 App 所有 API 请求
- **反编译**：jadx / GDA 反编译 APK，提取硬编码的 URL、Key、Secret
- **脱壳**：加固的 App 需先脱壳（Frida / xposed 模块）

### 10.2 常见发现

- 内网 API 地址（`http://10.x.x.x`、`http://192.168.x.x`）
- 硬编码的 AccessKey / Token
- 云存储地址（OSS / S3 Bucket）
- 未文档化的 API 接口

---

## 十一、信息搜集流程总结

```
目标网站
│
├── 1. 网站指纹
│   ├── CMS/框架/中间件/语言识别
│   ├── 响应头分析 & 错误页面
│   └── robots.txt / sitemap.xml / JS 源码
│
├── 2. 域名资产
│   ├── 子域名爆破 + 证书透明度
│   ├── WHOIS / 备案反查关联域名
│   └── 类似域名 / 旁注 / C 段
│
├── 3. 网络资产
│   ├── CDN 判断 & 真实 IP 获取
│   ├── 全端口扫描 & 高危服务发现
│   └── 黑暗引擎（FOFA/Shodan/ZoomEye）搜索
│
├── 4. 安全防护
│   ├── WAF 识别 & 绕过策略
│   └── 蜜罐识别（避免陷入）
│
├── 5. 外围资产
│   ├── 微信公众号 / 小程序 API
│   ├── App 抓包 & 反编译
│   └── GitHub 源代码 & 配置泄露
│
└── 6. 输出
    ├── 资产清单（域名/IP/端口/服务）
    ├── 脆弱点标记（未授权/弱口令/信息泄露）
    └── 下一步渗透测试切入点
```
