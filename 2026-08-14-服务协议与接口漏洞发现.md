# 2026-08-14 服务协议与接口漏洞发现（小迪安全 · 漏洞发现系列 ④）

> 状态：🧪 理论未验证（老师只讲了思路框架，没逐个演示服务利用）

> 漏洞发现系列第四课：**对服务协议和 API 接口的漏洞发现**。和前几课最大的不同 —— 老师基本没讲具体漏洞怎么打，核心讲的是**资产收集（攻击面扩展）**：拿到一个网站，先找它的**端口、域名、子域名**，因为**网站越多攻击机会越多**；资产收全了，再对照"服务协议字典"识别服务、查已知漏洞。这张图的分支是：服务协议（分类字典）、端口服务流程、API 接口、其他补充。

---

## 一、是什么（我的理解）

### 1.1 一句话

服务协议与接口漏洞发现 = **先把目标的攻击面扩展**（端口 / 域名 / 子域名，入口越多越好）→ 对每个资产**识别服务类型**（Web/数据库/文件共享/远程访问…）→ 按**服务+版本查漏洞库** → 找 EXP 利用。

### 1.2 系列对比（延续快递盒系列）

```
① 操作系统 = 进小区看"楼型"（Windows/Linux），查这栋楼通病
② Web 应用 = 进小区看"装修风格"（PHP/Java/Python 框架），查框架毛病
③ APP 应用 = 先抓包看 APP"跟外面怎么说话"，按协议分流测
④ 服务协议 = 先数这小区**有多少栋楼、多少扇门**（端口/域名/子域名），
             门越多越好进 —— 网站越多，攻击机会越多
```

| 维度 | ③ APP 应用 | ④ 服务协议与接口 |
|------|-----------|-----------------|
| 先摸什么 | 抓包看协议 | 端口 / 域名 / 子域名（资产收集） |
| 核心目标 | 识别 APP 流量协议 | **扩展攻击面**，找更多入口 |
| 主要工具 | Burp / Wireshark / jadx | Nmap / Masscan / 子域名爆破 |
| 兜底手段 | 逆向 APK | 服务漏洞库比对 + API 接口测试 |

**核心心法：网站越多，攻击机会越多。** 一个目标不要只看一个网站 —— 同 IP 的其他端口、其他域名、子域名，全是最新入口，总有一个防御薄弱。

---

## 二、资产收集（攻击面扩展：先找端口、域名、子域名）

拿到一个目标网站后，向三个方向扩展：

| 方向 | 找什么 | 为什么 |
|------|--------|--------|
| **端口** | 同 IP 开放的其他端口 / 服务（8080、7001、6379…） | 每个端口可能挂着一个服务，多一个入口 |
| **域名** | 同 IP 的其他站点（旁站）、相关域名 | 旁站可能比主站好打，先打薄弱的 |
| **子域名** | 主域下的子域（后台、测试站、老站） | 子站防御往往比主站差，还可能有测试环境裸奔 |

**我的理解**：这一步是整堂课的重心 —— 漏洞发现不是拿到目标就直接打，而是先"把目标变多"。入口越多，命中薄弱点的概率越大。

---

## 三、服务协议分类（端口 → 服务 → 漏洞 的对照字典）

扫到端口后，识别它是什么服务，按大类去查对应漏洞库。导图上的分类清单：

| 分类 | 服务 | 典型漏洞方向 |
|------|------|------------|
| WEB 服务类 | Weblogic / Websphere / Glassfish / Jetty / Apache / IIS / Resin / Nginx | 中间件反序列化、解析漏洞、未授权、弱口令 |
| 数据库类 | Mysql / Mssql / Oracle / Redis / Postgresql / Sybase / Memcache / ElasticSearch / DB2 / Hadoop / Zookeeper | 未授权访问、弱口令、注入、提权 |
| 大数据类 | Hadoop / Zookeeper | 组件未授权访问（如 Hadoop YARN RCE） |
| 文件共享 | FTP / NFS / Samba / LDAP | 匿名访问、未授权、口令爆破 |
| 远程访问 | SSH / RDP / Telnet / VNC / PcAnywhere | 弱口令、未授权、RDP 漏洞 |
| 邮件服务 | SMTP / POP3 / IMAP / DNS | 用户枚举、开放中继、投递伪造 |
| 其他服务 | DHCP / SNMP / Rlogin / Rsync / Zabbix / RMI / Docker / Radius / Openldap | 默认口令、未授权、组件漏洞 |

**用法**：Nmap 扫到 6379 → 对照字典是 Redis → 查 Redis 已知漏洞（未授权访问）→ 找 EXP 试。**清单的作用是"扫到端口能对上号"，不是背每个服务的利用细节**（老师没逐个讲利用，就是这个意思）。

---

## 四、端口服务测试流程（闭环）

导图给的固定流程：

1. **探针检测**：Nmap / Nessus / Masscan —— 扫出开放端口、服务类型、版本
2. **利用测试**：单个 EXP / 单个脚本或工具 —— 按服务+版本找对应利用
3. **安全修复**：打好补丁 / 版本升级 / 部署 WAF —— 测试完的修复闭环

（和系列①系统层的"扫描 → 定性 → 利用 → 修复"是同一个闭环，换到服务层复用。）

---

## 五、API 接口漏洞发现

对接口的四个面：

- **探针面（怎么找到接口）**：应用猜测（试常见路径 /api、/v2/api-docs）、引擎查找（搜索引擎/目录扫描找接口文档）、逻辑越权
- **应用面（业务功能）**：用户管理 / 支付管理 / 短信管理 / 订单管理 —— 每类功能模块都是逻辑漏洞候选（越权、金额篡改、短信轰炸…）
- **安全面（测什么）**：输入控制（注入/XSS）、接口安全（鉴权）、信息泄露
- **利用面（用什么打）**：SoapUI PRO / WS-Attacker / Burp Suite / 单个 EXP

---

## 六、其他补充：资产收集的三视角

导图最后一张补充图，其实是把"资产收集"讲透 —— 同一个目标可以从三个视角找 Web 入口：

| 视角 | 内容 | 我的理解 |
|------|------|---------|
| **端口WEB** | xx.com、xx.com:8080、IP、IP/xx、IP/xx/xx | 一个 IP 多个端口都可能是 Web 服务，每个都要看 |
| **IP-WEB** | 注册人信息（whois） | 通过 IP 查归属、注册人，关联出更多资产 |
| **域名WEB** | 域名登记、特有信息 | 域名 whois、备案信息，反查关联域名/子域名 |

**核心**：信息收集要从"一个网站"扩展成"一张资产网"——端口维度、IP 维度、域名维度三条线交叉，入口自然就多了。

---

## 七、我的总结 / 疑问

### 总结

- 这课讲的是**服务/接口层的漏洞发现方法论**，核心动作是**资产收集（攻击面扩展）**：端口、域名、子域名，网站越多攻击机会越多
- 流程闭环：**资产收集 → 探针检测（Nmap/Masscan）→ 服务分类对照字典 → 漏洞库查 EXP → 修复**
- API 接口按四面试：**探针面找接口 → 应用面看功能 → 安全面测漏洞 → 利用面打**
- 和系列①的关系：①是系统层，④是服务层，方法论完全复用

### 疑问

- 子域名收集具体用什么工具 / 流程？（subfinder / oneforall / 证书透明日志 crt.sh？待实践确认）
- "旁站"查询（同 IP 其他站点）用什么工具？在线平台（如 sun 数据库类站点）还是自己扫？
- 老师说的"引擎查找"是指 Google 语法（site:xxx）还是专门的接口搜索引擎？
- 端口WEB / IP-WEB / 域名WEB 这三条线的**完整工具链**是什么？老师课上只给了维度没给工具？

---

## 关联笔记

- [操作系统漏洞发现](./2026-08-13-操作系统漏洞发现.md) — 系列①，"扫描→定性→利用→修复"闭环的出处
- [Web 应用漏洞发现](./2026-08-13-Web应用漏洞发现.md) — 系列②，服务指纹识别思路通用
- [APP 应用漏洞发现](./2026-08-14-APP应用漏洞发现.md) — 系列③，今天上午学的，同一天连着看
- [操作系统与第三方安全测试](./2026-07-23-操作系统与第三方安全测试.md) — OS/DB/第三方组件、CDN 绕过，资产收集的旧基础

## 待实践

- [ ] 拿一个授权目标（或自己的服务器）练资产收集：Nmap 扫端口 + 子域名爆破 + 旁站查询，画一张资产清单
- [ ] 用 Masscan 练快速端口扫描，对比 Nmap 的速度和结果
- [ ] 对着服务分类字典，把常见端口（21/22/23/3306/6379/7001/8080…）对应的服务记熟
- [ ] 找一个开放 API 的目标，按探针面→应用面→安全面→利用面过一遍流程

> ⚠️ 声明：以上技术仅用于 CTF 竞赛和授权靶场环境。
