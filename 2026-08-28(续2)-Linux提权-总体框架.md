# 2026-08-28(续2) Linux 提权①：总体框架（针对环境 / 信息收集 / 提权方法）

> 状态：🧪 理论（课程导图），待实操
> 小迪安全《提权》Linux 部分开课（Windows 部分见 08-26 + 08-28(续)）。课程导图三大分支：**针对环境**（web / 本地）→ **信息收集**（8 项清单）→ **提权方法**（SUID / 内核漏洞 / 环境变量 / 计划任务 / 第三方服务）。本篇先把框架立起来，后续每课往里填肉。

## 一、一句话认知

Linux 提权 = **先明确"针对环境"（web 还是本地）→ 按 8 项清单做"信息收集" → 根据收集结果从 5 类"提权方法"里选打法**。

> 关键认知：**信息收集是提权的核心**。提权方法就那几板斧（SUID/内核/环境变量/计划任务/第三方服务），能不能打、打哪个，全靠收集阶段发现了什么。类比：方法 = 工具箱里的工具，信息收集 = 判断该用哪把。
>
> 放在整条渗透线里：信息收集 → 漏洞利用（拿入口）→ **提权** → 内网/后渗透。Linux 提权是 08-25「提权概述」③ 服务器系统(OS)分支的 Linux 落地（Windows 落地是 08-26/08-28(续)）。

## 二、① 针对环境（web / 本地）

| 环境 | 入口长什么样 | 特点 |
|------|------------|------|
| **web** | webshell（PHP 一句话等），进程身份通常是 `www-data`（Apache/nginx） | 权限低，通常只能读写 web 目录，需要先想办法执行命令/写文件 |
| **本地** | 直接有 shell（SSH / 反弹 shell），普通用户身份 | 权限比 www-data 高一点，能做更多信息收集 |

- 和 08-25 提权概述的"起点权限三类"呼应：web 环境 ≈ ① Webshell 入口，本地环境 ≈ ③ 服务器系统入口
- 不同环境决定信息收集能做什么、提权路径怎么选（比如 web 环境可能先从低权限 www-data 提到普通用户，再提 root）

## 三、② 信息收集（8 项清单）——提权的核心

> 导图 8 项，下面每项补了典型命令（标准知识，待对照课程视频确认老师的完整命令清单）：

| # | 收集项 | 收集什么 | 典型命令 |
|---|--------|---------|---------|
| 1 | **内核信息** | 内核版本、发行版版本 → 查对应内核漏洞 | `uname -a` / `cat /etc/issue` / `lsb_release -a` |
| 2 | **用户群组** | 当前用户、UID/GID、sudo 组成员、passwd 里的可登录用户 | `id` / `whoami` / `cat /etc/passwd` / `sudo -l` |
| 3 | **环境信息** | 网络配置、开放端口、挂载、PATH | `ifconfig` / `netstat -antlp` / `mount` / `echo $PATH` |
| 4 | **进程服务** | 以 root 跑的进程、对外服务 → 找可利用的第三方服务 | `ps aux`（看 root 的）/ `netstat -antlp` / `systemctl` |
| 5 | **计划任务** | root 的计划任务、可写的 cron 脚本 | `crontab -l` / `cat /etc/crontab` / `ls /etc/cron.d` 等 |
| 6 | **有无 ssh 私钥** | 找 `.ssh/id_rsa` 等私钥 → 免密登录 root/横向 | `find / -name "id_rsa" -o -name "*.pem"` |
| 7 | **有无明文密码** | 配置文件密码、命令历史、备份文件里的凭据 | `cat ~/.bash_history` / 网站配置 / 数据库配置 |
| 8 | **可读写执行目录** | 找可写目录 → 放脚本等计划任务/服务执行（提权落地位置） | `find / -writable -type d` / `ls -la /tmp` 等 |

- 8 项里**信息价值排序**（我的理解）：内核信息（决定能不能打内核洞）＞ 计划任务/可写目录（决定能不能借刀）＞ ssh 私钥/明文密码（最省事，直接登录）＞ 进程服务（第三方服务提权入口）

## 四、③ 提权方法（5 大类）

| # | 方法 | 原理一句话 | 主要依赖的收集项 |
|---|------|-----------|-----------------|
| 1 | **SUID** | 找属主 root 且设了 SUID 位的程序——普通用户执行它时以 root 身份运行，利用其功能/漏洞提权（DC-1 实操过 `find -exec` 拿 euid=0） | 可读写执行目录、进程服务 |
| 2 | **内核漏洞** | 内核版本老 → 查对应 CVE/EXP（脏牛 DirtyCow 等经典）→ 编译/执行 EXP 直接拿 root | 内核信息 |
| 3 | **环境变量** | PATH 劫持：root 的脚本/程序调用了某个命令，攻击者把同名恶意脚本放到 PATH 更靠前的可写目录 → root 执行时中招 ❓ | 环境信息、计划任务 |
| 4 | **计划任务** | root 的计划任务执行了可写脚本 → 替换/劫持脚本，等 cron 触发时以 root 执行 | 计划任务、可读写执行目录 |
| 5 | **第三方服务** | 以 root 运行的服务/软件（数据库、中间件等）有漏洞或配置不当 → 借它的权限执行命令（今天学的数据库提权就是这一类！） | 进程服务 |

- **方法优先级（我的理解）**：先看有没有现成凭据（ssh 私钥/明文密码，最省事）→ 再看 SUID/计划任务/环境变量（配置类，不用等补丁）→ 最后才内核漏洞（要编译 EXP、要看版本匹配，动静最大）

## 五、总结：框架闭环

```
针对环境（web/本地）→ 信息收集 8 项 → 提权方法 5 类
     ↑                                      ↓
     └──────── 收集到什么决定用哪招 ◄────────┘
```

- **Linux vs Windows 提权对照**（复习用）：Windows 侧重"服务配置/令牌"（08-28(续)），Linux 侧重"文件权限位/计划任务/内核"——但**信息收集先行**的思路两边一样
- 已实操过的：SUID（DC-1）；已学过的：第三方服务（数据库提权 = 第三方服务的一种）

## 疑问

1. ❓ 环境变量（PATH 劫持）提权的完整利用链？（root 的什么脚本/程序会被劫持？怎么构造？）
2. ❓ 8 项信息收集有没有现成的自动化脚本/工具？（LinEnum / linux-exploit-suggester 这类？课程会教吗？）
3. ❓ 第三方服务提权具体覆盖哪些？（数据库/中间件/容器 docker？边界在哪？）
4. ❓ 内核漏洞提权里 EXP 怎么选？（版本匹配 + 编译环境，课程案例是哪个洞？）

## 关联笔记

- [2026-08-25(续3)-权限提升概述.md](./2026-08-25(续3)-权限提升概述.md) — 提权概述：本课是 ③ 服务器系统(OS) 分支的 Linux 落地
- [2026-08-28-数据库提权-五大数据库.md](./2026-08-28-数据库提权-五大数据库.md) — 「第三方服务」提权的实例（MySQL/MSSQL/Redis 等）
- [2026-08-28(续)-Windows提权-Potato-DLL劫持-引号路径-服务权限.md](./2026-08-28(续)-Windows提权-Potato-DLL劫持-引号路径-服务权限.md) — Windows 侧对照（服务配置类 vs Linux 文件权限类）
- [2026-08-26-Windows提权-补丁筛选与at-sc-ps提权.md](./2026-08-26-Windows提权-补丁筛选与at-sc-ps提权.md) — Windows 补丁/溢出提权（与 Linux 内核漏洞提权对应）
- [2026-08-15-DC1靶机渗透实战.md](./2026-08-15-DC1靶机渗透实战.md) — SUID 提权首次实操（find -exec → euid=0）

## 待实践

- 把 8 项信息收集整理成自己的速查命令清单（学一课补一课）
- 找台 Linux 靶机跑一遍完整流程：信息收集 → 判断提权方法
- 看课程视频补：环境变量提权细节、内核漏洞案例（对应疑问 1/4）

> ⚠️ 声明：仅限授权环境（靶场/CTF/授权渗透）中测试，所有 Payload 只用于验证漏洞存在。
